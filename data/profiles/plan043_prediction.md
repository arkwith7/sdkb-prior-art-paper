# PLAN-043 예측값 — CR-013 재지정판 (결과 보기 전 산출)

생성: `scripts/plan043_predict.py` · 입력 `concept_links.parquet`(행 129,922) · `ir_corpus_v09.parquet`(행 41,031)

## 1. 적용기 층 — 검증기준 ③④⑤

| # | 기준 | 전 | **후(예측)** | 델타 |
|---|---|---:|---:|---:|
| ③ | `material:hf_acid` 문서 | 1,522 | **412** | -1,110 |
| ④ | `material:hfO2` 문서 | 391 | **298** | -93 |
| ⑤ | 고유 (doc,concept) 쌍 | 106,496 | **105,347** | -1,149 |
| — | `material:dielectric` 문서 | 6,766 | **6,820** | 54 |
| — | 적용기 링크 행 | 129,922 | **128,677** | -1,245 |

### 1.1 ⑤ 의 분해 — 세 항이 전부다

- 단독 `hf` 로만 `hf_acid` 를 얻던 문서 **−1,110** (표면형 `hf` 문서 1,245 중 135 는 `불산`·`hydrofluoric acid` 로 유지)
- `high k` 로만 `hfO2` 를 얻던 문서 **−93** (`high k` 문서 111 중 18 는 `hfo2`·`hafnium oxide` 로 유지)
- `high k` 로 `dielectric` 을 **새로 얻는** 문서 **+54** (57 는 이미 보유)
- 합 **-1,149** → **105,347**

### 1.2 무효가 된 구 예측의 재현 (방법 동일성 확인)

`high k` 를 **재지정 없이 제거**하면 106,496 → **105,293** (-1,203). CR-013 §6 이 적은 105,293 과 일치하므로, 달라진 것은 모의 적용의 방법이 아니라 **상류가 고른 ⓑ 의 형태**다.

## 2. 코퍼스 union 층 — 그래프 ∪ 적용기 (`concepts` 열)

코퍼스의 `concepts` 는 A-Box 그래프 링크와 적용기 링크의 **합집합**이다(`concept_link.apply_to_corpus:132`). 그래서 ⑤ 와 다른 수이며, 상류 A-Box 정리(회신 §3 · 링크 34 → 15)가 여기에만 나타난다.

| 개념 | union 문서 전 | **후(예측)** | 그래프 링크 문서 |
|---|---:|---:|---:|
| `hf_acid` | 1,522 | **422** | 34 → 15 |
| `hfO2` | 392 | **307** | 23 (불변) |
| `dielectric` | 6,766 | **6,820** | 0 (불변) |

**코퍼스 union (doc,concept) 쌍 154,264 → 예측 153,133** (-1,131).

> `hf_acid` 의 union 이 ③ 의 적용기 값보다 큰 이유는 상류가 남긴 A-Box 링크 15 건 중 10 건이 적용기 사후 집합 밖에 있기 때문이다. 어느 쪽도 오류가 아니다 — 잣대가 둘이다.

## 3. 이 파일의 지위

**결과를 보기 전에 적은 예측이다.** 재조립이 이 값과 어긋나면 상류가 회신문 이외의 것도 바꾼 것이므로 그 자리에서 멈추고 원인을 묻는다(CR-013 §6).
