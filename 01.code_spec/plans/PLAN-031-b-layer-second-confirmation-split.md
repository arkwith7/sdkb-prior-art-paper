# PLAN-031 — B층 제2 확증분할 수집 사전등록 (C2 · H3·H4·H5 재확증)

> **상태: 🔒 동결 (2026-07-31 승인 · 2026-07-29 작성 · 2026-07-30·31 갱신).**
> **이 커밋이 사전등록 증거이며, 결과 커밋과 분리한다.** 승인 기록은 §8.
> 동결 후 변경된 항목은 §3 IPC 목록(15→21종 · 실측 근거)과 §5 B2′ 확정(`BAAI/bge-m3`)이며,
> **둘 다 결과를 보기 전 · 수집 착수 전 변경이다.**
> **갱신분:** §3.1 신설 — 이전 판에 빠져 있던 **거절근거 라벨 수집 경로**를 채웠다(CR-004 수집으로
> 의견제출통지서 998건 확보 · 작동 오퍼레이션 확인 · 표 칸 한정 파싱 규칙 동결). §8.1 신설 —
> 호출 예산의 파라메트릭 추정과 파일럿 500건. **이 두 항목이 동결의 마지막 구멍이었다.**
> 수집 규칙을 **결과를 보기 전에** 동결하는 것이
> 이 문서의 유일한 목적이다. **수집 후에 규칙을 정하면 질의를 고를 여지가 생겨 사전등록이 무의미해진다.**
> 승인 후 `[동결 시 기입]`을 채운 커밋이 사전등록 증거가 되며, **사전등록 커밋과 결과 커밋을 분리**한다.
>
> 상위: [PLAN-029](PLAN-029-post-remediation-reexperiment.md) §3 B층 · 근거 [upstream/DEFECT-LEDGER.md](../../upstream/DEFECT-LEDGER.md)
> 흡수: [PLAN-027](../archive/PLAN-027-strong-dense-baseline.md) 강한 밀집 기준선(B2′) 설계

---

## 1. 요구정의 (CLAUDE §2-1)

| | |
|---|---|
| **목적** | 자원 교정 후 H3·H4·H5를 **오염되지 않은 표본**에서 재평가한다. 기존 test 198은 이미 1회 개봉·결과 공표됐고 자원까지 바뀌므로 확증에 쓸 수 없다(PLAN-029 §1) |
| **주장** | C2 (선행기술 검색에서 온톨로지 보강의 실효) |
| **입력** | KIPRIS Plus API — 수집 도구는 `~/Dev/paper_data/scripts/expand_dataset_via_api.py`(기존). 호출 1회(`getBibliographyDetailInfoSearch`)로 IPC·claim1·초록·**심사관 인용**(`priorArtDocumentsInfoArray`)을 함께 얻는다. **거절근거 라벨은 의견제출통지서에서 별도 수집**(`IntermediateDocumentOPService.pdfInfoV2` · §3.1) |
| **출력** | 거절특허 **200건**의 질의 + qrel(봉인) + 후보 코퍼스 증분 + **거절근거 라벨(T2 층화용)** |
| **성공기준** | 아래 §3 포함기준을 만족하는 200건 확보 · 기존 1,000건과 **패밀리 중복 0** · 누출 검사 통과 · **거절근거 조항 확보율 ≥ 95 %**(A층 실측 95.5 %) |
| **비목표** | 기존 test 재사용, 설계 분야 확대(§5), 새 확증 가설 추가 |

---

## 2. 봉인 기준을 "미래"가 아니라 "미사용"으로 둔다 (2026-07-29 실측 근거)

KIPRIS 실측(PLAN-029 §3.0): 출원일 2021-07-22 이후 풀은 크지만(C23C16 5,332 · H10B 7,437 ·
H01J37 6,199 · G11C 4,039) **거절 비율이 0.4~3.0%**다(표본 500건 · 공개 84~90%로 대부분 심사 중).
**미래 구간만으로 200건은 채워지지 않는다.**

확증 표본의 요건은 **결과를 본 적이 없는 질의**이지 미래 출원이 아니다. 시점 타당성은
후보 마스킹(F10 · 공개일 < 질의 출원일)이 질의마다 이미 보장한다. 따라서 출원 시기를 넓히되
**미사용·서로소**를 봉인 요건으로 삼는다.

---

## 3. 포함·배제 기준 (동결 대상 — 결과를 본 뒤 바꾸지 않는다)

**포함(전부 만족):**
1. `register_status` = 거절 (또는 `examination_status` ∈ {거절결정(일반), 거절결정(재심사), 원결정유지(심사전치)})
2. 심사관 인용(`priorArtDocumentsInfoArray`) **≥ 1건**
3. 독립항 텍스트 확보(`claim1` 비어 있지 않음)
4. IPC 주분류가 반도체 범위 — **동결 목록 21종**(2026-07-31 승인 · 기존 1,000건 주분류 상위 20
   + `H10K`):
   `H01L` · `C23C` · `H01J` · `H10D` · `H10P` · `G11C` · `H10B` · `C09G` · `B23K` · `C04B` ·
   `C07F` · `B22F` · `G02F` · `H10W` · `C09K` · `H10F` · `C22C` · `G03F` · `B82Y` · `B81B` · `H10K`
   **도출 근거(실측).** 기존 1,000건의 주분류(첫 IPC) 분포에서 상위 20종 = 누적 **89.4 %**.
   ~~구 15종 목록~~은 **82.5 %만 덮어** B층을 A층보다 좁게 만들었다 — 신규 6종
   (`C09G` 16·`B23K` 16·`C07F` 14·`B22F` 13·`B82Y` 7·`B81B` 7)을 추가했다.
   `H10K`(A층 4건)는 2023년 IPC 개편 반영분이므로 상위 20 밖이어도 유지한다.
   꼬리 49종(n ≤ 5, 잔여 10.6 %)은 넣지 않는다 — 검색식이 비대해지고 "반도체 범위"의 경계가
   흐려진다. **B층이 A층보다 약간 좁다는 사실은 §4 보고 의무로 공개한다.**
5. ~~출원일 **2005-01-01 ~ 2025-12-31**~~ → **§9 로 개정됨(2026-08-01): 출원일 2018-01-01 ~ 2020-12-31.**
   구 문면은 지우지 않는다 — 파일럿이 실제로 집행한 규칙이기 때문이다(§1-3).
   구 근거: 실측 확인상 A층 1,000건 중 2005년 미만은 **9건**뿐(2004년 7·1998년 1·1997년 1)이라
   하한을 2005로 두어도 A층 대비 손실이 없었다. **개정 사유는 손실이 아니라 건초더미다** — §9.1.

**배제(하나라도 해당하면 제외):**
1. 기존 1,000건과 **동일 패밀리**(**DOCDB simple family** — `collect/bq_family_ir` 교집합 ≠ ∅)
   — **2026-07-31 필드 정정.** 원문은 `family_pub_numbers` 또는 `simple_family_id`였으나
   전자는 우선권·PCT 출원번호이고(46.6 %가 결측) 후자는 메타에 열 자체가 없다(D-17).
   **규칙은 불변이고 그 규칙을 담은 필드만 바꾼다** — 느슨해지는 방향이 아니라 엄격해지는
   방향이므로 §1-2에 저촉되지 않는다. 근거 PLAN-032 §2.2 · 승인 기록 §8 항목 7.
   DOCDB 판정 불능(fallback-self 4.2 %) 건은 **보수적으로 배제**한다(§8 항목 8).
2. 기존 1,000건에 이미 있는 출원번호
3. 심사관 인용이 전부 비특허문헌(NPL)인 건
4. 청구항 전문 결측

**표집 순서(결정성 보장):** 검색 결과를 `(출원일, 출원번호)` 오름차순으로 정렬한 뒤 **앞에서부터**
기준을 적용해 200건에 도달하면 중단한다. 무작위 표집·사후 선별을 하지 않는다.
검색식·수집 스테이지는 기존과 동일한 `semiconductor_commercial` 플랜을 쓰고, 실행 로그를
`data/MANIFEST.md`에 남긴다.

**정지 규칙:** 200건에 미달하면 **기준을 완화하지 않는다.** 확보된 N으로 진행하고
검정력을 재계산해 보고한다(§4).

---

## 3.1 거절근거 라벨 수집 — 이전 판의 구멍과 그것이 메워진 방식 (2026-07-30)

이전 판의 §3에는 **거절근거 라벨을 어떻게 얻는가가 없었다.** §5.1의 층화(신규성 포함 / 진보성 전용 /
기재불비)는 그 라벨 없이는 신규 200건에 적용할 수 없으므로, T2 하위집단 축이 A층에만 존재하고
B층에는 없는 상태였다. **CR-004 수집으로 그 구멍이 메워졌다 — 자원과 방법 양쪽에서.**

| | 상태 |
|---|---|
| **A층(기존 1,000건)** | 의견제출통지서 **998/1,000** · 거절결정서 538/539 확보 완료. 무라벨 600 → **1**. 조항 확보 출원 954/999(95.5 %) (대장 §1.9) |
| **작동하는 오퍼레이션** | `IntermediateDocumentOPService.pdfInfoV2`(출원번호 직접 조회). **검색 `advancedSearchInfo` 는 두 서비스 모두 0건을 반환한다** — 2026-05에 475건이 0으로 나온 원인이 이것이었다 |
| **파싱** | 텍스트층 존재로 **OCR 불필요**. 본문 평균 7,000자 |
| **B층에 필요한 추가 작업** | **채택된 200건에 대해 같은 호출 1회씩** — 즉 200콜. 새 방법이 아니라 검증된 절차의 반복이다 |

**동결 규칙(결과를 본 뒤 바꾸지 않는다).** 조항 라벨은 통지서 `[심사결과]` 표의
**`관련 법조항` 칸에서만** 읽는다. 본문 전체 정규식은 절차 안내문구를 거절근거로 세는
오검출을 만든다(제63조 999 · 제47조 645 · 제2조 236은 전부 오검출 — 표 칸 기준 제47조는 3건).
`특허법 시행령 제6조제2호`는 제45조에 부수하므로 별도 근거로 세지 않는다(법령명 필터 필수).
파서는 CR-004R 4단계 산출물(`scripts/reextract_claim_judgments.py`)을 그대로 쓴다 —
**B층을 위해 파서를 새로 짜지 않는다**(결정성·A/B 라벨 정의 동일성).

**층 정의(동결).** T2 축 = ① 신규성 포함(제29조1항 있음) / 진보성 전용(제29조2항만) ·
② 기재불비 있음(제42조 계열 있음) / 없음. A층 실측 분포는 신규성 포함 194 · 진보성 전용 727 ·
기재불비 있음 398 · 없음 601이며, **n ≥ 20 집단만 보고**한다.
라벨 미확보 건은 배제하지 않고 **`ground_unknown` 집단으로 별도 보고**한다(표본 손실 방지).

---

## 4. 평가 프로토콜 (기존 동결값 승계 — 재선택 없음)

| 항목 | 값 |
|---|---|
| 주 지표 | **family-level Recall@100** (fold-then-cut · 정답 ≥1 질의만 매크로 평균) |
| 보조 | nDCG@20(이진 이득) · MRR@500 · R@50/500 · Success@100 · bpref |
| **H3 판정** | R@100 **및** nDCG@20 **둘 다** 개선해야 지지 — **원래 조항을 그대로 유지한다** |
| 통계 | 질의단위 paired bootstrap 10,000회 · seed 고정 · 양측 |
| 다중비교 | 절제 가족(A1–A8)에 Holm(m=8). 주 비교(P 대 B3)는 보정 없이 보고 |
| 후보 | F10 마스크(시점유효·자기/동일패밀리 배제) 후 상위 1,000 절단 |
| 개봉 | **최종 1회.** 가중치·임계는 **기존 dev에서만** 선택하고 신규 분할로 재선택하지 않는다 |
| 검정력 | 기존 test 198에서 P1 Δ+0.0534 · 95%CI 폭 ≈ 0.078. 200건이면 유사 정밀도 기대(추정) — 확보 N으로 재계산해 **개봉 전** 보고 |

**보고 의무:** 신규 분할의 출원연도·IPC·정답 언어 분포를 **기존 test와 대조해 사전 공개**한다.
분포가 다르므로 **기존 test 결과와 직접 비교하지 않는다** — B층은 자체 완결적 확증이다.

---

## 5. 시스템 정의 (PLAN-029 §3.2 승계 — 가설은 늘리지 않는다)

확증 가설은 H1–H5 다섯 개 그대로다. 아래는 **H3의 시스템 정의 변경**이다.

- **개념 입력**: 그래프 기존 링크 → **매핑기 산출**(CR-007 사전·규칙 기반, 적용기는 하류 구현)
- **질의 측 번역**: 질의만 단일 언어로 번역해 대조(문서 4만 건 번역은 하지 않는다)
- **후보 생성 합집합**: 온톨로지팔을 재랭크가 아니라 **후보 생성**에도 투입(텍스트팔 ∪ 온톨로지팔)
- **강한 밀집 기준선 B2′**(PLAN-027 흡수 · **2026-07-31 동결**): **`BAAI/bge-m3`**.
  온톨로지 효과와 약한 밀집 성분을 분리하기 위함이다.
  **기준을 재진술한 이유(실측 제약).** PLAN-027의 "특허 특화 공개 인코더"는 이 벤치마크에서
  **충족 불가능한 조건**이다 — 질의·문서가 한국어 장문(질의 중앙값 527자·문서 2,255자)인데
  공개된 특허 특화 인코더는 영어 전용·512토큰이고, 한국어 특허 인코더(KIPI 계열)는 문장
  임베딩 학습본이 아니며 배포가 신청 절차를 탄다. 따라서 기준을 **"한국어·장문(≥2,000자)을
  자르지 않고 처리하는 공개 인코더"**로 바꾼다. bge-m3는 다국어·최대 8,192토큰이라 문서를
  절단 없이 넣을 수 있다. 실행 환경은 로컬 GPU(RTX 5090 · torch 2.13+cu130 CUDA 가용).
  **재현성 조건: 개봉 전에 모델 revision을 고정하고 가중치 sha256을 `data/MANIFEST.md`에 남긴다.**
  **✅ 기입 완료(2026-07-31 · 개봉 전 · 수집 착수 전).** revision(commit) =
  `5617a9f61b028005a4858fdac845db406aefb181` · `pytorch_model.bin` sha256 =
  `b5e0ce34…aad38`(전량 8파일 해시는 `data/MANIFEST.md` 2026-07-31 항). 실측 사양:
  max_seq_length **8192** · CLS 풀링 · 1024차원 · xlm-roberta. 이후 모델을 바꾸지 않는다 —
  바꾸면 재선택이다. (모델 *동결 기록*이며 추론 실행은 아직 없다)
- 후보 생성 변경은 **전 시스템에 동일 적용**하고, 구 아키텍처 결과와 직접 비교하지 않는다

---

## 5.1 새 거절근거 층화의 탐색적 결과 (2026-07-30) — **사전등록 설계의 근거**

의견제출통지서 수집으로 무라벨이 600 → 1건이 됐다(대장 §1.9). 그 라벨로 **기존 test 198**을
층화해 P1 대 B3 차이를 계산했다. **탐색적이며 확증이 아니다** — 순위(P1)가 test 결과를 본 뒤
'부차 구성'으로 재배치된 산출물이라, 층화가 새것이어도 순위는 새것이 아니다.
전체 Δ가 공표값 **+0.0534와 일치**해 파이프라인 정합은 확인됐다.
(산출: `scratchpad/explore_ground.py` — 원고에 실을 경우 `src/` 승격 필요, §1-7)

| 축 | 집단 | n | B3 | P1 | Δ | 95% CI | p |
|---|---|---:|---:|---:|---:|---|---:|
| 제29조 | 진보성 전용 | 136 | 0.4133 | 0.4718 | **+0.0585** | [+0.0156, +0.1038] | .011 |
| 제29조 | 신규성+진보성 | 58 | 0.4351 | 0.4802 | +0.0451 | [−0.0368, +0.1250] | .272 |
| 제29조 | 제29조 없음 ⚠ | 4 | 1.0000 | 1.0000 | 0.0000 | — | — |
| 제42조 | 기재불비 없음 | 109 | 0.4148 | 0.4800 | **+0.0652** | [+0.0143, +0.1188] | .013 |
| 제42조 | 기재불비 있음 | 89 | 0.4521 | 0.4909 | +0.0389 | [−0.0205, +0.0965] | .195 |

> **⚠ 이 표의 집단 배정은 재산출이 필요하다 (2026-07-30 추가).** 층화에 쓴 조항 라벨은
> **본문 전체 정규식** 산출이고, CR-004R 2단계 분석에서 그것이 **절차 안내문구를 거절근거로
> 세는 오검출**을 포함함이 확인됐다(제47조 645 → 3 등). 표 칸 기준으로 다시 라벨링하면
> 일부 질의의 집단이 바뀔 수 있다. **아래 세 결론(회귀 없음 · 방향 예측 미지지 · 검정력 부족)은
> 유지될 가능성이 높지만, 원고나 사전등록에 인용하기 전 재산출한다.** 이 대비는 어차피
> 확증 목록에서 제외돼 있으므로 사전등록의 유효성에는 영향이 없다.

**세 가지를 읽는다.**
1. **어느 집단에서도 회귀가 없다**(전 집단 Δ ≥ 0). T2 안전성 서술에 유리하나 탐색적 지위를 유지한다.
2. **§7.7의 방향 예측이 지지되지 않는다.** "신규성은 단일 문헌 대응이라 한정요소 신호가 더 유용할
   것"이라는 예상과 달리 **진보성 전용 집단의 이득이 더 크다**(+0.0585 대 +0.0451). 다만 두 CI가
   크게 겹쳐 **구분 불가**가 정직한 결론이다.
3. **따라서 B층에서 이 대비를 확증하려면 검정력이 부족하다.** 관측 차이 ≈ 0.013에 CI 폭 ≈ 0.16이므로
   200건으로는 검출되지 않는다. → **이 대비는 B층에서도 탐색적으로 사전등록**하고, 확증 목록에
   넣지 않는다. 대신 **하위집단 안전성(T2)의 층으로는 사용**한다(회귀 감시는 검정력 요구가 낮다).

## 6. 누출 통제 (§1-4 · 예외 없음)

1. 질의 인용 간선(`hasPriorArtExaminer`·`hasPriorArt`·`overPriorArt`) 마스킹
2. 시점 유효 — 후보 공개일 < 질의 출원일
3. 패밀리 분리 — 질의 자신·동일 패밀리 배제
4. 정답 파생 피처(`NoveltyScore` 등) 배제
5. **매핑 사전은 도메인 어휘에서만** 구축 — 정답 문서를 사전 입력에서 배제한 사실을 provenance에 기록
6. 수집·조립 후 `make leakage` **재통과 필수**(금지 간선 0)

---

## 7. 데이터 프로파일 의무 (§4)

수집 직후 코드가 산출: 구조(컬럼·dtype·키·원천) · 형태(행×열·중복·결측률·기간·**언어분포**·dedup 전후) ·
기술통계(연도별·IPC 분포·정답 수 분포) · 사용 목적. 산출 `data/profiles/ir_split_b.md` ·
같은 커밋에 `data/MANIFEST.md` 기록.

---

## 8. 승인 기록 — **전 항목 승인 · 동결 (2026-07-31)**

| # | 항목 | 결정 |
|---|---|---|
| 1 | §3 포함·배제 기준과 IPC 목록 | **승인** — 단 IPC 목록을 **15종 → 21종**으로 확장(구 목록은 A층의 82.5 %만 덮었다 · 실측). 출원일 하한 2005는 유지(A층 손실 9건) |
| 2 | §3 표집 순서(출원일·출원번호 오름차순 · 무작위 아님) | **승인 · 원안 그대로** |
| 3 | §4 H3 판정 조항(R@100 **및** nDCG@20) | **승인 · 원안 유지.** 기준을 느슨하게 고쳐 "지지"를 만드는 것이 §1-2가 금하는 바로 그것이다 — 자원 교정이 효과를 내면 nDCG도 움직여야 한다는 것이 교정의 가설이다 |
| 4 | §5 B2′ 모델 | **`BAAI/bge-m3`로 동결.** "특허 특화" 조건은 한국어에서 충족 불가이므로 "한국어·장문 대응 공개 인코더"로 재진술(§5) |
| 5 | API 예산 | **파일럿 500콜 선행 승인.** §3 표집 순서를 그대로 따르므로 본수집의 앞부분이며 버려지지 않는다. r 실측 후 예산 확정 |
| 6 | §3.1 거절근거 라벨 동결 규칙 | **승인 · 원안 그대로**(표 칸 한정 파싱 · 시행령 필터 · 층 정의 · `ground_unknown` 별도 보고) |
| 7 | §3 배제 1의 **패밀리 필드 정정** (2026-07-31 추가) | **승인 — DOCDB `bq_family_ir`로 정정.** 사유: `family_pub_numbers`는 패밀리 식별자가 아니다(우선권·PCT 출원번호 · `has_family=False` 466/1,000). 규칙 불변·필드만 교체이며 **기준을 엄격하게** 만든다. 결함은 `upstream/DEFECT-LEDGER.md` **D-17**로 등재 |
| 8 | fallback-self 4.2 %(DOCDB 패밀리 판정 불능) 처리 (2026-07-31 추가) | **승인 — 보수적 배제.** 판정 불능 건은 후보에서 제외한다. r(통과율)이 소폭 낮아지는 대가로 A/B 패밀리 누출 위험을 0으로 만든다. **r 을 본 뒤 이 결정을 되돌리지 않는다** |
| 9 | §8.1 **`r` 의 재정의** (2026-08-01 추가 · **개봉 전 · 수집 착수 전**) | **승인 — `r` = 채택 / 소비된 서지상세 콜.** 검색 응답만으로 포함 1·4·5·배제 1·2를 판정할 수 있음이 실측됐으므로(PLAN-032 §2.5(3)) 서지상세 콜은 인용≥1·claim1에만 쓴다. **§8.1 산식 `200/r + 200` 은 그대로 유효하고 분모의 모집단만 바뀐다.** 함께 `r_free`(검색 무료 판정 통과율)·`r_family`(패밀리 판정 통과율)를 병기하고, 호출은 search·detail·notice·audit **4계정으로 분리**한다. `registerStatus` 결측 건을 서지상세로 확정하는 콜은 **분모에 포함**하고, `registerStatus` 감사 50콜(PLAN-032 §6.1 결정 E)은 **분모에서 제외**한다 — 채택을 만들 수 없는 콜이기 때문이며, 이 포함·제외는 **결과를 보기 전에** 고정한다. 근거·설계 PLAN-032 §5.7·§6.1 |

**이 커밋이 사전등록 증거다.** 이후 §3·§3.1·§4·§5의 동결 항목은 **결과를 본 뒤 바꾸지 않는다.**
남은 `[동결 시 기입]`은 **실행 시점에만** 채운다 — B2′ revision·sha256(§5) · 파일럿 r 실측치(§8.1) ·
확보 N과 재계산 검정력(§4). 이들은 값의 *기록*이지 규칙의 *변경*이 아니다.

**동결되지 않은 것(변경 가능).** §5의 개념 입력(CR-007 완료 여부에 따라 매핑기 또는 현행 그래프
링크 — §8.1 미결 의존) · §5.1 탐색적 층화 결과(재산출 예정 · 확증 목록 밖).

### 8.1 호출 예산 — 파라메트릭 추정 (**추정**이며 파일럿으로 확정한다)

검색 응답의 `registerStatus` 는 **비어 있는 경우가 많아** 거절 여부를 서지상세로 확정해야 한다
(`expand_dataset_via_api.py:115` 주석). 따라서 총 호출은 대략:

> **총콜 ≈ 200/r (서지상세 스크리닝·채택분 포함) + 200 (의견제출통지서 pdfInfoV2)**
> — r = 서지상세 1콜당 포함기준 통과율(거절 ∧ 인용≥1 ∧ claim1 ∧ IPC ∧ 패밀리 서로소)

| r (추정) | 총콜 | free 150/일 | paid 600/일 |
|---:|---:|---:|---:|
| 3 % (최근 구간 관측치 · PLAN-029 §3.0) | ≈ 6,900 | 46일 | 12일 |
| 10 % | ≈ 2,200 | 15일 | 4일 |
| 20 % (심사 완료 구간 가정) | ≈ 1,200 | 8일 | 2일 |

r 은 구간을 2005–2025로 넓히면 크게 올라간다(최근 구간의 0.4~3.0 %는 **심사 미완** 때문이지
거절이 드물어서가 아니다). **파일럿 500콜로 r 을 실측한 뒤 예산을 확정한다** —
파일럿 자체는 §3의 표집 순서를 그대로 따르므로 **버려지는 호출이 아니다**(그대로 본수집의 앞부분).
파일럿은 qrel 을 열지 않으며, 수집 즉시 봉인 규율이 적용된다.

**미결 의존:** CR-007(개념 매핑 사전)이 없으면 §5의 개념 입력 교체를 실행할 수 없다.
CR-007 미완 시 **개념 입력은 현행 그래프 링크로 두고** 그 사실을 사전등록에 명시한다.
**상태(2026-07-31): CR-007 1·2·3단계 완료(결정 4건 확정) · 상류 4단계 착수 가능 · 상류 미착수.**
수집(§3·§3.1)은 CR-007과 **독립**이므로 먼저 진행할 수 있다 — 매핑기는 코퍼스 조립 단계에서
들어오고, 질의·qrel·라벨 수집은 그 앞이다.

---

## 9. 사전등록 개정 §3 표집 창 — 🔒 **동결** (2026-08-01 승인 · 같은 날 작성)

> **이 커밋이 개정된 사전등록 증거이며, 결과 커밋과 분리한다.** 개정은 **봉인 개봉 전 ·
> 재수집 착수 전**에 이뤄졌고, 개정 시점에 `data/processed/ir/qrel_b_sealed.parquet`은
> 열려 있지 않았다(§9.5 에 폐기 전 sha256 을 남긴다).
>
> **승인 기록:** 2026-08-01 사용자 응답 "개정안대로 진행" — 선택지 3안(질의 재표집 /
> 시대 코퍼스 증분 수집 / B층 용도 변경) 가운데 **질의 재표집**을 택했다.
> 개정 후 `src/sdkb_paper/config.py` 의 `B_LAYER_DATE_FROM`·`B_LAYER_DATE_TO` 가 이 절의
> 전사(轉寫)다 — 코드에서 고치면 사전등록 위반이라는 §3의 규율이 그대로 적용된다.

### 9.1 왜 고치는가 — 규칙대로 뽑았더니 건초더미가 사라졌다

PLAN-032 파일럿은 §3의 "출원일 오름차순 앞에서부터"를 그대로 집행해 200건을 채웠고, 그 결과가
**전부 2005-01-03 ~ 2005-02-28 출원**이다(§8.4). 규칙 위반은 없다. 문제는 **후보 모집단 쪽**이다.

| 실측 | 값 | 산출 |
|---|---:|---|
| 후보 코퍼스 전량 | 40,552 | `data/processed/ir/ir_corpus_v09.parquet` |
| 공개일이 **2005-01-03 이전**인 문서 | **2** | 〃 |
| 공개일 결측 | 4,492 | 〃 (대부분 2005년 이전 출원분) |
| 대조 — A층 test 질의(출원 2021–2025)가 마스크 후 마주하는 후보 | **19,039 ~ 23,000** | 〃 |

§4.4 시점 유효성 마스크(공개일 < 질의 출원일)를 걸면 **연대가 확인된 방해문서가 2건**이다.
건초더미가 없는 상태의 Recall@100은 A층의 Recall@100과 **같은 양이 아니다.** 이대로 재면
H3의 재확증이 아니라 다른 물음에 답하게 된다.

### 9.2 무엇을 고치고 무엇을 고치지 않는가

**고치는 것은 §3의 표집 창 하나뿐이다.**

| | 기존 (파일럿 집행분) | 개정안 |
|---|---|---|
| 표집 순서 | 출원일 오름차순 앞에서부터 | **출원일 2018-01-01 ~ 2020-12-31 창 안에서** 출원일 오름차순 앞에서부터 |
| 그 외 포함기준 | 동결 21종 IPC · 거절 상태 · 심사관 인용 ≥1 · NPL 전용 배제 · A층 패밀리 배제 | **불변** |
| 평가 프로토콜(§4) | family-level R@100 주지표 · H3 판정 조항 · 부트스트랩 10,000 · Holm(m=8) | **불변** |
| 시스템 정의(§5) | B2′ = `BAAI/bge-m3` (revision 고정) | **불변** |
| 봉인 | 최종 1회 개봉 | **불변** |

**고치지 않는 이유를 명시한다.** 임계·지표·판정 조항을 함께 손대면 개정이 아니라 재설계가 되고,
그 순간 "결과가 안 나와서 조건을 바꿨다"와 구분되지 않는다(CLAUDE.md §1-2·§1-3). 이 개정은
**결과를 보기 전이고 봉인을 열기 전이며 바꾸는 것은 질의 모집단 하나**다.

### 9.3 왜 2018–2020 인가

| 질의 출원연도 | 마스크 후 후보 수 |
|---|---:|
| 2016 | 9,685 |
| 2018 | 13,801 |
| 2020 | 17,209 |
| (A층 test 2021–2025) | 19,039 ~ 23,000 |

- **건초더미가 A층 수준에 근접한다** — 13,801 ~ 17,209 는 A층 test 의 60–75 % 다.
- **심사가 끝나 거절 표본이 존재한다** — §2 가 확인한 "2021-07-22 이후는 거절률 0.4~3.0 %"
  문제를 피한다.
- **A층 test 연도(2021–2025)를 건드리지 않는다.** A층 dev(2018 27 · 2019 36 · 2020 52 = 115질의)와는
  시기가 겹치므로, 기존의 **A층 출원번호·패밀리 959 배제 규칙이 실제로 일하게 된다**(파일럿에서는
  2005년이라 한 건도 걸리지 않아 `r_family` = 1.0000 이었다). 배제 발생 건수는 §5.4 원장에 남긴다.

### 9.4 함께 메워야 할 설계 구멍 — **후보 코퍼스 증분**

§1 출력란에 "후보 코퍼스 증분"이 적혀 있으나 **본 계획서에 그 설계 절이 없다.** 이것을 열어 둔 채
재수집하면 다음이 일어난다 — B층 질의의 심사관 인용 문헌이 후보 코퍼스에 없으면 **회수 상한이
구조적으로 1 미만으로 깎이고**, 그 상한은 시스템마다 같으므로 Δ 는 살아남지만 절대값은 해석 불가가 된다.

따라서 개정안은 다음을 **명시적 사전등록 항목으로 추가**한다.

1. **정답 도달성을 개봉 전에 보고한다.** 봉인 qrel 의 *식별자 집합*만 코퍼스와 대조해
   `정답 중 후보 코퍼스에 존재하는 비율`을 산출한다 — 이는 **정답의 내용을 읽지 않으므로 개봉이 아니다.**
   A층 대응값은 노드 도달성 95.3 %(2,211/2,321)다.
2. 도달성이 A층 대비 크게 낮으면, 부족분을 **KIPRIS 서지상세로 수집해 코퍼스에 증분 병합**한다.
   병합 범위·필드·중복 규칙은 착수 전에 이 절에 적고, 병합 후 **누출 검사(`make leakage`)를 다시 돌린다.**
3. 증분은 **정답 문헌만 넣지 않는다** — 정답만 추가하면 정답이 코퍼스의 유일한 신규 문서가 되어
   시점·출처 편향이 생긴다. 동결 21종 IPC·동일 공개연도 구간에서 **인용되지 않은 문서를 함께** 넣고
   그 비율을 보고한다.

### 9.5 폐기·재생성 대상

| 산출물 | 처리 |
|---|---|
| `data/processed/ir/b_layer/` 스크리닝 원장·채택 200건 | **폐기 후 재생성**(2005년 표본은 개정 창 밖) |
| `data/processed/ir/qrel_b_sealed.parquet` | **미개봉 상태로 폐기 후 재생성.** 열지 않았음을 해시로 증명하고 `data/MANIFEST.md`에 남긴다 |
| 파일럿 실측 `r`·`r_free`·`r_family`·감사 위음성률 | **판정 기록으로 보존**(2005년 표본에서의 관측). 새 창의 `r` 는 다시 잰다 — 연도가 다르면 거절률이 다르기 때문이다 |
| PLAN-032 §8 실행 기록 | **보존**(소급 수정 금지) |
| BigQuery `kr_family_map.parquet` 4,878,954행 | 재사용(KR 전량이므로 창과 무관) |

### 9.6 예산과 착수 조건

- 파일럿 실측 `r` = 0.5970 은 2005년 표본의 값이다. 새 창에서 다시 재되, 같은 수준이면
  **detail ≈ 335콜 · search ≈ 25콜**로 200건에 닿는다(파일럿과 동급). 캡은 §1 동결값을 그대로 쓴다.
- 착수 조건 셋: ① 이 절의 승인 ② 개정 커밋과 결과 커밋의 분리 ③ 봉인 폐기·재생성 절차를
  MANIFEST 에 기록. **셋이 갖춰지기 전에는 수집 드라이버를 돌리지 않는다.**

---

## 10. 개정 후 재수집 실행 기록 (2026-08-01 · **200건 도달 · 봉인 미개봉**)

### 10.0 결론 먼저 — 창 개정은 목표를 달성했고, **§9.4 가 새 차단을 잡았다**

개정한 창(출원 2018-01-01 ~ 2020-12-31)에서 200건을 다시 모았고 **건초더미가 살아났다.**
그러나 §9.4 가 사전등록 항목으로 넣은 **정답 도달성 검사가 실패**했다 — B층 질의의 심사관
인용 문헌이 후보 코퍼스에 거의 없다. **이 상태로 개봉하면 회수 상한이 구조적으로 깎인다.**
§9.4 를 넣지 않았다면 개봉 후에야 알았을 것이고, 그때는 되돌릴 수 없었다.

### 10.1 수집 결과 (`sdkb_paper.collect.b_layer` · 정지 사유 `target_reached`)

| 지표 | 새 창 (2018) | 구 창 (2005 · 파일럿) |
|---|---|---|
| 채택 | **200** | 200 |
| 출원일 범위 | **2018-01-02 ~ 2018-02-16** | 2005-01-03 ~ 2005-02-28 |
| `r` | **0.9524** [0.9146, 0.9739] (200/210) | 0.5970 (200/335) |
| `r_free` | 0.0816 [0.0717, 0.0928] (210/2,573) | 0.0783 (335/4,281) |
| `r_family` | 1.0000 [0.9820, 1.0000] (210/210) | 1.0000 (335/335) |
| 감사 위음성률 | 0.0000 [0.0000, 0.0713] (0/50) | 0.0000 (0/50) |
| 호출 | search 22 · detail 210 · audit 50 · `quota_hit=false` | search 25 · detail 335 · audit 50 |

**`r` 가 0.597 → 0.952 로 올랐다.** 사유 코드가 이유를 말한다 — `no_examiner_citation` 이
113 → **9**로 줄었다. 최근 출원일수록 심사관 인용이 서지에 일관되게 실린다.

**§9.3 이 예고한 대로 A층 배제 규칙이 실제로 일했다** — `dup_application_a_layer` **5건**.
구 창(2005)에서는 한 건도 걸리지 않아 규칙이 작동하는지 확인되지 않았었다.

### 10.2 건초더미 복구 — 개정의 목적은 달성됐다

| | 구 창 | 새 창 |
|---|---:|---:|
| 마스크(공개일 < 질의 출원일) 후 후보 | **2** | **13,801 ~ 14,053** |

채택분이 다시 창 앞머리(2018-01~02)에 몰린 것은 "오름차순 앞에서부터"의 귀결로 구 창과 같다.
**차이는 그 시점에 건초더미가 있느냐**이고, 그것이 개정이 고치려던 것이다.

### 10.3 🛑 §9.4 도달성 검사 실패 — **개봉 전 차단**

봉인 규율을 지켜 **집계값만** 산출했다(질의별 정답·인용 목록은 출력하지 않았다. 읽은 것은
인용 식별자 열의 집합 통계뿐이며, 이는 §9.4 ①이 명시적으로 허용한 범위다).

| 항목 | 값 |
|---|---|
| 봉인 qrel 형태 | 538행 / 질의 200 · 고유 인용 식별자 **514** |
| 후보 코퍼스 적중 | **6 → 도달성 0.0117** |
| 대조 — A층 노드 도달성 | 0.953 (2,211/2,321) |

**원인은 둘이고, 하나는 측정 문제, 하나는 실재 문제다.**

1. **키공간 불일치(측정 문제).** qrel 의 KR 인용은 **공개번호**(`KR10-2018-0012345 A` 형태)인데,
   후보 코퍼스의 KR 문서 37,518건은 **출원번호**로 키가 잡혀 있다(`publication_number` 는 A층
   질의 1,000건에만 채워져 있다). 즉 **KR 235건은 지금 구조로는 적중 여부를 잴 수 없다.**
2. **실재 부재(진짜 문제).** 고유 인용 514건 중 **외국 문헌이 268건(52 %)** 이다(JP 195 · US 52 ·
   WO 29 · CN 6 · EP 2). 코퍼스의 공개번호형 식별자 3,025건은 **A층의 인용 선행기술**이고,
   B층 인용은 그중 3건만 걸린다. **KR 235건이 전부 해소된다고 가정해도 도달성 상한은 0.457** 이다.

**따라서 §9.4 ②가 발동한다 — 후보 코퍼스 증분 없이는 개봉하지 않는다.**

### 10.4 착수 전 필요한 설계 — 🛑 **승인 대기**

증분 수집은 데이터 수집·변형이므로 CLAUDE §2 의 예외가 없다. 아래를 설계로 확정한 뒤 착수한다.

| # | 항목 | 쟁점 |
|---|---|---|
| 1 | **KR 공개번호 → 출원번호 해소** | BigQuery `patents-public-data` 로 매핑(dry-run 실측 기준 쿼리당 5.22 GB · **$0.03**). 이것이 먼저다 — 해소 전에는 KR 235건의 도달성을 *측정조차* 못 한다 |
| 2 | **외국 문헌 268건 확보 경로** | KIPRIS 는 KR 중심이다. A층의 외국 인용은 어떤 경로로 G0 에 들어왔는지 확인한 뒤 같은 경로를 쓴다. 경로가 없으면 EPO OPS/PatentsView 가 후보 — **신규 외부 의존이라 별도 승인 대상** |
| 3 | **동반 미인용 문서** | §9.4 ③ — 정답만 넣으면 정답이 코퍼스의 유일한 신규 문서가 되어 시점·출처 편향이 생긴다. 동결 21종 IPC·동일 공개연도 구간에서 함께 넣고 비율을 보고한다. **규모(몇 건)가 설계 결정이다** |
| 4 | **증분 후 재검사** | `make leakage` 재실행(금지 간선 0) · 코퍼스 서명 갱신 · `data/profiles/` 재생성 |
| 5 | **도달성 목표선** | A층 0.953 과 같은 수준을 요구할지, 낮은 값을 명시하고 갈지. **개봉 전에 정한다** — 개봉 후 정하면 사후 조정이다 |

**지금은 여기서 멈춘다.** 봉인은 열리지 않았고, §6.2–6.4 재작성은 여전히 대기다.

---

## 10.5 사전 조사 — A층 외국 인용은 어떻게 들어왔나 (2026-08-01 · **읽기 전용 · API 호출 0**)

§10.4 ②의 선결 질문에 답한다. **경로는 이미 존재하고 작동한 적이 있다.** 새로 만들 것이 아니라
**있는 것을 B층에 다시 돌리는 문제**다.

### 10.5.1 유입 경로 — 이 저장소가 아니라 상류가 넣었다

```
인용 식별자 (prior_art_edges) ──> 소스별 라우팅 수집 ──> cited_enriched/*
   └ scripts/collect_cited_biblio_claims.py (paper_data)
                       │
                       v
   sdkb/scripts/build_abox_prior_art.py ──> ontology/sdkb-abox-prior-art.ttl (19 MB)
                       │
                       v            (사람이 make vendor)
   data/external/sdkb/ ──> corpus/assemble.py ──> ir_corpus_v09.parquet (source=`g0_cited`)
```

**이 저장소는 외국 문헌을 한 건도 직접 수집하지 않았다.** 코퍼스의 `g0_cited` 3,034건
(ko 1,728 · en 1,189 · ja 117)은 전량 **상류 SDKB의 `sdkb-abox-prior-art.ttl` 을 vendor 한 것**이다.
그러므로 B층에 같은 것을 하려면 **CR 로 상류에 요청하는 것이 §0.1 의 정해진 길**이고,
이 저장소가 직접 모아 코퍼스에 넣는 것은 "우회 패치"에 해당한다.

### 10.5.2 소스 라우팅 (`collect_cited_biblio_claims.py` 실측)

| 관할 | 원천 | A층 실측 수율 | 이 저장소의 의존 상태 |
|---|---|---|---|
| KR | KIPRIS Plus `biblio_detail` (청구항·서지·IPC/CPC 네이티브) | `kipris.jsonl` 12 MB | **이미 있음** |
| JP·WO·CN·EP·GB | **BigQuery Google Patents** (1쿼리 일괄) | 940건 · 해소 **0.888** · **청구항 0.000** | **이미 있음**(`collect/bq_family` 등) |
| US | **PatentsView Search API** (BQ 는 응용공개 자릿수 문제로 미스) | 471건 · 해소 **1.000** · 청구항 0.996 | **이 저장소에는 없음** — 상류·paper_data 에는 있음 |
| 잔여 | EPO OPS (서지+초록 · 청구항 없음) | 폴백 | 없음 |

**신규 외부 의존은 US 축 하나뿐이다**(PatentsView). JP 축은 BigQuery라 이미 쓰는 의존이고
비용도 패밀리 지도와 같은 수준이다. §10.4 ②가 우려한 "신규 외부 의존이면 별도 승인"은
**US 52건에만 걸린다.**

### 10.5.3 A층과 B층의 인용 구성 — 같은 종류의 문제다

| | A층 (examiner · 고유 2,291) | B층 (고유 514) |
|---|---|---|
| KR | 1,273 (55.6 %) | 235 (45.7 %) |
| JP | 596 | 195 |
| US | 333 | 52 |
| WO·CN·EP·GB | 71 | 37 |
| **외국 합계** | **1,018 (44.4 %)** | **268 (52.1 %)** |

B층이 외국 비중이 조금 더 높지만 **같은 자릿수**다. A층에서 성립한 경로가 B층에서
성립하지 못할 구조적 이유는 없다.

### 10.5.4 미리 알아야 할 상한 — JP 축은 청구항이 오지 않는다

BigQuery 경로의 **청구항 확보율이 0.000** 이다(940건 전량). 그래서 코퍼스의 외국 문서는
본문이 얇다 — 실측:

| `g0_cited` | n | title | abstract | claims_full | `text_main` 중앙길이 | concepts 있음 |
|---|---:|---:|---:|---:|---:|---:|
| ko | 1,728 | 1.000 | 0.999 | 1.000 | **2,064** | 0.817 |
| en | 1,189 | 1.000 | 0.992 | 0.394 | 1,106 | 0.696 |
| ja | 117 | 1.000 | **0.573** | **0.000** | **164** | **0.000** |

**이것이 §6.2f 교차언어 진단의 물리적 원인이다** — 일본어 정답은 초록조차 절반뿐이고 개념 링크가
하나도 없다. B층의 JP 195건도 같은 상태로 들어올 것이므로, **증분 후에도 JP 정답의 회수는
구조적으로 낮을 것**을 미리 적어 둔다(개봉 후에 말하면 변명이 된다).

### 10.5.5 이 조사가 §10.4 에 주는 답

| # | 항목 | 조사 결과 |
|---|---|---|
| ① | KR 공개번호→출원번호 해소 | **BigQuery 없이 될 수 있다.** 상류 KR 경로는 KIPRIS 서지조회이고 인용 식별자를 그대로 넣는다. 즉 "코퍼스 키와 맞추는" 문제가 아니라 **문서를 새로 실체화하는** 문제다 |
| ② | 외국 268건 경로 | **존재·검증됨.** JP/WO/CN/EP = BigQuery(기존 의존) · US 52 = PatentsView(**유일한 신규 의존**) |
| ③ | 동반 미인용 문서 | 여전히 미정 — **규모가 설계 결정**이다. 다만 A층도 `g0_cited` 가 전량 인용 문헌이라 동반 미인용이 없었다는 점을 함께 본다(A층 방해문서는 G1/G2 3.6만 건이 맡았다) |
| ④ | 증분 후 재검사 | 변동 없음 |
| ⑤ | 도달성 목표선 | A층 실측이 기준선을 준다 — 노드 도달성 0.953. **단, 도달했다고 다 같은 문서가 아니다**(JP 는 초록 절반·청구항 0). 목표선을 "도달성" 하나로 잡으면 얇은 문서가 채운 0.95 를 성공으로 읽게 된다. **도달성과 본문 확보율을 함께 못 박을 것을 제안한다** |

### 10.5.6 그래서 다음 행동은 CR 이다 — 🛑 승인 대기

§0.1 대로면 이 저장소가 할 일은 **수집이 아니라 CR 제출**이다. 등재 요건 셋을 이미 갖췄다 —
**증거**(§10.3 도달성 0.0117 · 본 절 실측) · **수정 제안**(B층 514 인용에 대해
`collect_cited_biblio_claims.py` → `build_abox_prior_art.py` 재실행) · **검증기준**(재 vendor 후
B층 도달성이 A층 0.953 수준에 이르고, 관할별 본문 확보율이 A층과 같은 수준일 것).

**막는 것 하나:** 상류가 B층 인용 식별자를 받아야 하는데 그것은 **봉인 qrel 의 내용**이다.
A층에서는 인용이 곧 공개 자원이라 문제가 없었지만, B층은 봉인 중이다. 누출 통제상 **문서의
존재**는 허용되고(§4.5.1 이 막는 것은 *간선*이지 문서가 아니다) A층도 같은 구조였지만,
**"봉인을 상류에 넘긴다"는 절차를 사전등록에 명시하지 않고 하면 안 된다.** 이것이 승인 대상이다.
