# PLAN-033 — v2.0 투고본 재구성 · **1단계(재실험 무관분) 실행 계획**

> **상태: 축 확정(사용자 승인 2026-08-01) · 1단계 착수.**
> 대상 정본: [paper/논문_v0_9_SDKB_통합초안.md](../../paper/논문_v0_9_SDKB_통합초안.md) (CLAUDE.md 정본 회귀 조항).
> 상위: [PLAN-029](../archive/PLAN-029-post-remediation-reexperiment.md) (재실험 로드맵) ·
> [PLAN-031](../archive/PLAN-031-b-layer-second-confirmation-split.md) 🔒(B층 사전등록) ·
> [PLAN-032](../archive/PLAN-032-b-layer-pilot-collection.md)(B층 수집 · 200건 확보 완료).
>
> **이 문서는 사전등록을 바꾸지 않는다.** 가설의 이름(H1–H5)·임계(ε=0.02·δ=0.05·τ)·분할 경계·
> 판정 결과는 전부 불변이다. 이 문서가 정하는 것은 **투고본에서 무엇을 어느 지위로 싣는가**뿐이다.
> 사전등록 문서와 충돌하는 항목이 발견되면 이 문서가 진다.

---

## 0. 결론 먼저 — 지금 하는 것은 "분량 재배분"이지 "결과 재해석"이 아니다

현재 정본은 **104,497자**(공백 제외)로 AEI 투고 분량의 약 2.3배다. 목표는 **45,700자(43.7 %·약 22쪽)**.
줄이는 방법은 결과를 지우는 것이 아니라 **축을 둘로 좁히고, 축 밖 결과의 지위를 강등**하는 것이다.

- **주장 축**: C1(공유 T-Box 자원) + C2(선행기술 검색 유용성) + 태스크 결합.
- **확증 가설 2개**: **H3**(하이브리드 효과) · **H5**(특이성 — 음성 대조군). RQ도 2개.
- **라벨은 바꾸지 않는다.** H3/H5는 PLAN-031이 "H3·H5 재확증"으로 사전등록한 이름이다. 여기서
  H1/H2로 번호를 다시 매기면 **사전등록 기록과의 추적성이 끊긴다.** 개수만 줄이고 이름은 그대로 둔다.
- **H1·H2·H4는 삭제가 아니라 지위 강등**이다 — 판정은 그대로 두고 싣는 자리와 분량만 바꾼다(§3.2).

**1단계(이 문서의 실행 범위)는 B층 재실험과 무관한 절감분**이다(§5). §6.2–6.4의 새 결과 반영은
2단계이고, **지금 막혀 있다**(§6).

---

## 1. 요구정의 (CLAUDE §2-1)

| | |
|---|---|
| **목적** | v0.9 정본을 v2.0 투고본 골격으로 재구성한다 — 축 2개·확증가설 2개로 좁히고, 축 밖 결과를 강등하며, 분량을 목표 배분(§4)에 맞춘다 |
| **주장** | C1 + C2 (+ 태스크 결합). C3는 §4 설계와 §9 한계로 축소 |
| **입력** | 정본 원고 · 사전등록 문서(PLAN-018/029/031/032) · [RECONCILIATION-v09.md](../RECONCILIATION-v09.md) §1 라벨 사전 · [CANONICAL-INDEX.md](../CANONICAL-INDEX.md) §1 서명 수치 |
| **출력** | ⓐ 재구성된 정본 원고 ⓑ 절별 분량 실측 보고(§2 규약) ⓒ 강등 항목의 이동 대장(§3.2) ⓓ supplementary 로 내보낼 항목 목록(§4.3) |
| **성공기준** | ① 1단계 완료 시 총량 **≤ 79,000자**(현재 대비 약 25 % 절감 · §5 합계) ② **수치가 하나도 바뀌지 않는다** — 강등 전후 diff 에서 숫자 토큰 변경 0 ③ 강등된 H1·H2·H4의 판정 문장이 원고 어딘가에 **여전히 존재**한다(삭제 아님을 검증) ④ §10 이 3단 구조로 재작성되고 데이터셋 URL 오기가 정정된다 ⑤ 자원 "pin" 과 평가 "freeze" 의 용어가 분리된다 |
| **비목표** | 재실험 결과 반영(2단계) · 영문 번역 · 판정의 재해석 · 임계·분할·가설 이름 변경 · 새 실험 |

---

## 2. 분석 — 분량 실측 (측정 규약 동결)

**측정 규약: 공백 제외 문자수**(`re.sub(r'\s','',text)` 의 길이). 마크다운 기호·표 파이프 포함.
이 규약으로 정본 총량은 **104,497자**이고, 아래 표는 그 분해다(2026-08-01 실측).

> **작업 언어: 한글 정본 유지(2026-08-01 결정).** 영문화는 **실험 결과로 신규성·진보성의 논리
> 기조가 확정된 뒤 원고 전체를 한 번에** 수행한다. 따라서 지금 필요 없는 것은 국문 초록이 아니라
> **영문 Abstract** 다 — 부분 영문이 남아 있으면 기조가 바뀔 때마다 두 벌을 고쳐야 한다.
> 페이지 요구량은 **한글본에서 맞춘다**(한글본이 목표 분량에 들어가면 영문본도 들어간다는 판단).

| 절 | 현재 | 목표 | 무엇을 하는가 |
|---|---:|---:|---|
| Abstract (영문) | 5,680 | **0** | 삭제. 전체 영문화 시점에 새로 쓴다 |
| 국문 초록 · 약어표 | 4,505 | 2,600 | 초록은 축 2개(H3·H5)에 맞춰 재작성 · 약어표는 **실제 사용 항목만** |
| §1 서론 | 5,745 | 3,500 | 기여 3→2(자원·검색) · 가설 5→2 |
| §2 관련연구 | 7,154 | 4,000 | 게이트 진화검증 계열 축소(축이 검색으로 이동) |
| §3 SDKB | 7,156 | 4,500 | §3.1 세 태스크 뷰 4,150 → 1,500(표 1개+문단) |
| §4 방법 | 6,481 | 4,000 | §4.10 결함주입 1,556 → 400 · §4.9 T-gate 893 → 500 |
| §5 평가설계 | 2,817 | 2,200 | — |
| §6 결과 | 26,135 | 9,000 | §6.5 10,921 → 1,200 · §6.6 1,909 → 500 · §6.2–6.4 는 **B층 결과로 재작성(2단계)** |
| §7 예상발견 | 7,761 | **0** | §8로 흡수(핵심 2,000자만) |
| §8 토의 | 3,327 | 4,500 | §7 흡수분 포함 |
| §9 한계 | 3,943 | 2,200 | H2 미검정·특이성 미검정을 여기로 |
| §10 가용성 | 965 | 1,200 | 3단 구조로 전면 재작성(§4.3) |
| §11 결론 | 1,553 | 1,200 | — |
| 참고문헌 | 7,546 | 6,000 | 게이트 계열 인용 정리 |
| 부록 A–H | 11,340 | 800 | **B(주장–증거 매트릭스)만 축약 잔존**, 나머지는 저장소 문서/supplementary |
| **합계** | **104,497** | **45,700** | 43.7 % · 약 22쪽 |

> 표의 "현재" 값은 실행된 측정의 출력이다(CLAUDE §1-1). 재측정 명령은 §7에 있다.
> 잔여(제목·AI 고지 등 미기재 항목)는 목표 합계에 흡수한다.

---

## 3. 설계 — 축과 강등 (결과를 본 뒤 바꾸지 않는다)

### 3.1 확정된 축

- **RQ2 검색 유용성 → H3(하이브리드 효과)** = C2 의 핵심증명.
- **RQ3 계층 특이성 → H5(음성 대조군)** = C2·C3 경계, 그리고 **태스크 결합**의 근거.
- 이 둘이 **확증 가설의 전부**다. 나머지는 탐색적·설계 서술·한계로 배치된다.
- **번호 재부여 금지.** "H3·H5" 라는 이름은 PLAN-031 이 사전등록에 쓴 이름이므로 유지한다.
  §1.4 는 "확증 가설은 둘이며, 사전등록 라벨을 그대로 유지한다(H3·H5)"를 명시한다.

### 3.2 강등 대장 — 삭제가 아니라 지위 강등

| 가설 | 현재 지위 | 새 지위 | 이동처 |
|---|---|---|---|
| **H1** (게이트 판별력) | 확증 (4회차 재판정 · §6.5 10,921자) | **설계 + 홀드아웃 확증 1문단** | §4(설계) · §6.5(1,200자) · 4회차 과정 전량 → supplementary |
| **H2** (승인 안전성) | 확증 (실은 **미검정**) | **운용 사례** | §6 에서 사례 서술 · **T-Box 델타가 존재한 적 없다는 구조적 이유(D-12) → §9 한계 1문단** |
| **H4** (계층 기여) | 확증 (기각) | **ablation 표의 두 행** | §6.4 표의 A4/A5 행으로 흡수 |

**강등의 규율(중요).** 이것은 §1-2·§1-3 위반이 아니다 — 위반은 *판정을 바꾸는 것*이고, 여기서
바꾸는 것은 *싣는 자리와 분량*뿐이다. 따라서:

1. **기각은 기각으로 남는다.** H4 는 ablation 표에서도 "기각"으로 표기된다.
2. **H2 는 "확증"이라고 쓰지 않는다.** 현재 원고 §1.5-2 의 "안전성은 동결 테스트셋에서 지지되었고(H2)"
   는 **오기**다(CLAUDE.md §0 경계표 · STATUS §0: H2 는 미검정). 이 문장은 "게이트가 승인한 델타의
   운용 사례"로 정정하고, 미검정 사유를 §9 로 보낸다. **이는 판정의 완화가 아니라 과주장의 철회다.**
3. **삭제 검증**: 강등된 세 가설의 판정 문장이 원고에 남아 있는지 §7 의 체크로 확인한다.

### 3.3 용어 분리 — pin ≠ freeze

지금은 자원과 평가가 둘 다 "동결"이라 **데이터셋이 유물로 읽힌다.** 분리한다.

| 대상 | 용어 | 뜻 |
|---|---|---|
| SDKB 자원 (릴리스·스냅샷) | **릴리스 버전 고정(pin)** | 본 논문이 쓴 것은 릴리스 vX 의 커밋 SHA + sha256 스냅샷. **데이터셋은 이후에도 버전을 올려 개선된다** |
| 평가 (임계·분할·프로토콜) | **사전등록 동결(preregistration freeze)** | 개봉 전 고정 · 사후 변경 금지 |

원고 전역에서 자원 문맥의 "동결"을 "고정(pin)"으로 바꾼다. **평가 문맥의 "동결"은 건드리지 않는다.**

---

## 4. §10 재작성의 뼈대 — 오기 정정 + 3단

현재 §10 은 데이터셋 저장소 URL 을 **`github.com/arkwith7/sdkb-prior-art-paper`(= 논문 저장소)** 로
적고 있다. **오기다.** 데이터셋은 별도 저장소다. 3단으로 나눠 다시 쓴다.

### 4.1 데이터셋 (공개 · 지속 개선)
`github.com/arkwith7/semiconductor-knowledge-base` · **CDLA-Permissive-2.0** · `CITATION.cff` ·
버전과 CHANGELOG. **"본 논문이 사용한 것은 릴리스 vX 의 커밋 SHA + sha256 으로 고정(pin)된
스냅샷이며, 데이터셋 자체는 이후에도 버전을 올려 개선된다"** 를 명시한다(§3.3 의 pin).

### 4.2 논문 부속 supplementary (공개 · 재배포 가능)
질의 식별자 1,000+200 · qrel 인용 식별자 · 분할 경계 · **동결 임계 전량**(ε=0.02 · δ=0.05 · τ ·
overlap Q1=0.0079) · 게이트 규칙 명세 · 결과 표 원본 · 재구축 절차. §3.2 에서 강등된 H1 4회차
재판정 과정, §7 예상발견 전문, 부록 A·C–H 도 여기로 간다.

### 4.3 비공개
KIPRIS 원문(학술이용 · 비재배포) · 회사 기밀 계층 · 평가 하네스 저장소. **사유를 명시**하고,
재현은 §4.2 의 식별자 + 절차로 가능함을 밝힌다.

---

## 5. 1단계 작업 목록 (재실험 무관 · 약 25 % 절감)

| # | 작업 | 절감(자) |
|---|---|---:|
| 1 | 영문 Abstract 삭제 (전체 영문화 시점에 재작성) | 5,680 |
| 2 | 부록 A·C–H 제거, B 축약 잔존 | 10,540 |
| 3 | §7 → §8 흡수 (핵심 2,000자만 남김) | 6,588 |
| 4 | §6.5 압축 (10,921 → 1,200) · §6.6 (1,909 → 500) | 11,130 |
| 5 | §3.1 축소 (4,150 → 1,500 · 표 1개 + 문단) | 2,650 |
| 6 | §10 3단 재작성 + URL 오기 정정 | −235 (증가) |
| 7 | 용어 분리 (pin ↔ freeze) | 0 |
| 8 | §1 가설·기여 정리 (5→2 · 3→2) | 2,245 |
| | **누적 절감** | **≈ 35,500** |

1단계 종료 시 예상 총량 **≈ 69,000자** — 성공기준 ①(≤79,000)을 넘어선다. 남은 절감(§2·§4·§9·
참고문헌 · 국문 초록 재작성)은 2단계와 함께 처리한다.

**순서**: 1 → 2 → 3 → 4 → 5 → 8 → 6 → 7. 큰 덩어리를 먼저 떼어내야 §1·§10 의 문면이 확정된다.

---

## 6. 2단계는 막혀 있다 — B층 코퍼스 시점 문제 (미결)

§6.2–6.4 를 B층 결과로 재작성하려면 **B층에서 H3 를 A층과 같은 의미로 잴 수 있어야** 한다.
현재는 그렇지 않다.

- PLAN-032 파일럿이 채택한 **B층 200건이 전부 2005-01-03 ~ 2005-02-28 출원**이다
  (PLAN-032 §8.4 · `data/profiles/ir_split_b_pilot.md` §3.3).
- §4.4 시점 유효성 마스크(공개일 < cutoff)를 적용하면 후보 풀이 **4,494건**으로 줄고,
  그중 **4,492 가 공개일 결측**이라 **연대가 확인된 방해문서는 2건**뿐이다.
- 즉 건초더미가 사라진다 — **이 상태의 R@100 은 A층의 R@100 과 같은 양이 아니다.**

**2단계 착수 전에 이 결정이 선행돼야 한다.** 1단계는 이것과 무관하게 진행한다.

---

## 7. 검증 (CLAUDE §2-5)

```bash
# 절별 분량 재측정 (§2 규약 = 공백 제외 문자수)
python3 - <<'PY'
import re
lines=open('paper/논문_v0_9_SDKB_통합초안.md').read().split('\n')
nw=lambda s: len(re.sub(r'\s','',s))
marks=[(i,l) for i,l in enumerate(lines) if re.match(r'^# ',l) or l.startswith('## 국문 초록')
       or l.startswith('## Abstract') or l.startswith('## 약어표')]
for k,(i,l) in enumerate(marks):
    j=marks[k+1][0] if k+1<len(marks) else len(lines)
    print(f"{nw(chr(10).join(lines[i:j])):7d}  {l[:50]}")
print('TOTAL', nw('\n'.join(lines)))
PY
```

성공기준 대응 검증:

| 기준 | 검증 방법 |
|---|---|
| ① 총량 ≤ 79,000 | 위 스크립트의 `TOTAL` |
| ② **수치 불변** | `git diff` 에서 숫자 토큰만 추출해 삭제분 외 **변경 0** 확인 |
| ③ 강등 ≠ 삭제 | `grep` 로 H1 홀드아웃 판정(`12/45`·`p = .0001`) · H2 미검정 사유 · H4 기각 표기가 원고에 잔존함을 확인 |
| ④ §10 | `semiconductor-knowledge-base` URL 존재 · 3단 소제목 존재 |
| ⑤ 용어 | 자원 문맥 "동결" 잔존 0 (평가 문맥은 제외) |

`make lint && make test` 는 원고 편집으로 영향받지 않지만, 부록 E(Makefile 발췌) 제거가
문서 참조를 깨지 않는지 [CANONICAL-INDEX.md](../CANONICAL-INDEX.md) 링크 감사를 함께 돌린다.

---

## 8. 이 계획이 넘지 않는 선

1. **판정을 소급 수정하지 않는다.** 기각은 기각, 미검정은 미검정으로 남는다(§3.2 규율).
2. **사전등록 라벨·임계·분할을 바꾸지 않는다.** H3/H5 이름 유지가 그 집행이다.
3. **수치를 다시 만들지 않는다.** 1단계는 편집이며, 새 수치가 필요하면 2단계로 넘긴다.
4. **B층 결과를 미리 쓰지 않는다.** 봉인 qrel 은 §6 의 결정이 날 때까지 열리지 않는다.

---

## 9. 1단계 실행 기록 (2026-08-01 · **완료**)

### 9.0 결론 먼저 — 목표를 넘겨 절감했다

**104,497 → 61,231자**(공백 제외 · 41.4 % 절감). 성공기준 ①(≤79,000)을 크게 넘어섰고, 최종 목표
45,700 까지 남은 것은 15,531자다 — 그 대부분은 2단계 대상(§6.2–6.4)과 §2·§4·참고문헌이다.

| 절 | 시작 | 목표 | 현재 | 비고 |
|---|---:|---:|---:|---|
| 국문 초록 | 2,764 | — | **1,898** | 유지·재작성(축 2개 반영). 영문 Abstract 5,680 **삭제** |
| 약어표 | 1,741 | — | 1,514 | 미사용 6항목 제거(AEI·FTerm·KSIA·MAP·RDF·SPARQL) |
| §1 서론 | 5,745 | 3,500 | 5,052 | §1.4·§1.5·§1.6 재작성(가설 5→2 · 기여 3→2) |
| §2 관련연구 | 7,154 | 4,000 | 7,154 | **미착수**(2단계) |
| §3 SDKB | 7,156 | 4,500 | 4,871 | §3.1 4,150 → 1,528(표 1개+문단) · mermaid 그림 1 제거 |
| §4 방법 | 6,481 | 4,000 | 6,479 | **미착수**(2단계) |
| §5 평가설계 | 2,817 | 2,200 | 2,813 | — |
| §6 결과 | 26,135 | 9,000 | **13,901** | §6.5 10,921→1,268 · §6.6 1,909→668 · §6.3 압축. §6.2–6.4 는 2단계 |
| §7→§8 흡수 | 7,761+3,327 | 4,500 | **3,472** | §7 소멸 → 신 §7 논의(주장 C·E·F·H 를 §7.2·§7.3 으로 흡수) |
| §9→§8 한계 | 3,943 | 2,200 | 2,321 | **H2 미검정의 구조적 이유(D-12)를 §8.1 로** |
| §10→§9 가용성 | 965 | 1,200 | 1,345 | 3단 재작성 + URL 오기 정정 |
| §11→§10 결론 | 1,553 | 1,200 | 1,286 | 축 2개로 재작성 |
| 부록 A–H | 11,340 | 800 | **767** | B 축약만 잔존 · 나머지는 supplementary |
| **총계** | **104,497** | 45,700 | **61,231** | |

### 9.1 삭제하지 않았다 — 이동했다

| 산출물 | 내용 |
|---|---|
| `paper/supplementary/S1-appendices-v09.md` | 잘라낸 부록 A·C–H **전문**(부록 B 잘라내기 전 전문 포함) |
| `paper/supplementary/S2-fault-injection-v09.md` | §6.5–§6.6 **전문**(H1→H1′→H1″→H1‴ 4회차 재판정) + §S2.1 구 §6.3 가설 판정표 전문 |

두 파일 모두 **한 글자도 고치지 않았다.** 재판정의 순서를 숨기면 p-해킹과 구분되지 않으므로,
그 순서를 드러내는 것이 이 기록의 존재 이유다.

### 9.2 §1.5 "H2 지지" 과주장을 철회했다 (완화 아님)

정본 §1.5-2 와 §6.3 은 "게이트가 승인한 델타의 **안전성은 동결 테스트셋에서 지지**되었고(H2)"라고
적고 있었다. 이는 CLAUDE.md §0 경계표·STATUS §0 과 충돌한다 — H2 는 **미검정**이고, P1 대 B3 는
서로 다른 두 시스템의 비교라 H2 의 증거가 아니다. §6.3 표의 H2 행을 "미검정"으로 고치고, 구조적
사유(D-12 · T-Box 델타가 존재한 적 없음 · 클래스 103 · 술어 97/81 · 델타 0)를 **§8.1 로 옮겼다.**
이것은 판정의 완화가 아니라 **과주장의 철회**이며, 원 판정(P1 대 B3 의 Accept(ΔG)=1)은 "게이트
실행 사례"로 수치 그대로 남는다.

### 9.3 검증 결과

| 기준 | 결과 |
|---|---|
| ① 총량 ≤ 79,000 | **61,231** ✅ |
| ② 수치 불변 | 신규 원고에만 있는 수치 토큰 = 파일 경로·버전 문자열 + **클래스 103·술어 97/81** 셋뿐. 셋은 D-12 실측치(CLAUDE.md §0 · DEFECT-LEDGER D-12)를 §8.1 에 인용한 것이며 **새로 만든 수치가 아니다.** 제거된 토큰 102개는 전부 supplementary 로 이동 ✅ |
| ③ 강등 ≠ 삭제 | `12/45`(4회) · `p=.0001` · `0/27` · H4 "기각" · H2 "미검정" 전부 원고에 잔존 ✅ |
| ④ §9 | `semiconductor-knowledge-base` URL 존재 · §9.1/9.2/9.3 3단 ✅ |
| ⑤ 용어 분리 | 자원 문맥 "동결" 잔존 **0** — §3.7 에 pin↔freeze 구분 명시 ✅ |
| 참조 무결성 | `§N.M` 참조 전량이 실재 헤더와 일치(깨진 참조 0) · 그림 번호 2–5 → 1–4 재부여 ✅ |
| `make lint && make test` | **처음엔 실패했다** — `test_appendix_b_consistency.py` 가 부록 B 를
`# 부록 B. 논문 주장–증거 매트릭스` ~ `# 부록 C.` 구간으로 찾는데 축약으로 제목이 바뀌고 부록 C 가
사라져 픽스처가 죽었고, §6.3 의 강등 3행이 굵게 표기되지 않아 판정 파싱에서도 빠졌다. 셋 다 고쳤다 —
①§6.3 강등 행의 라벨을 `**H1**` 형식으로 통일 ②부록 B 의 `(**H3**)` 를 `(H3)` 로(테스트 정규식이
괄호 안 굵게를 읽지 못한다) ③테스트의 끝 앵커를 제거해 **부록 B 가 문서의 마지막 절**임을 반영.
이 테스트는 "같은 사실이 두 곳에 살면서 한쪽만 갱신되는" 표류를 막는 장치이므로 우회하지 않고
새 구조에 맞게 계약을 다시 적었다. 재실행 전량 통과 |

### 9.4 남은 정합 — **사용자 결정 필요**

`CLAUDE.md` §0 은 여전히 "확증 가설은 다섯 개다(H1–H5)" · "기여는 4개"라고 적고 있다. 원고가
2개·2개로 바뀌었으므로 **작업 규약과 원고가 어긋난다.** CLAUDE.md 는 프로젝트 헌장이므로 이
문서가 임의로 고치지 않고 사용자 판단을 받는다. `01.code_spec/STATUS.md` 의 §7.x 참조는
**과거 진행 기록**이므로 소급 수정하지 않는다.

### 9.5 2단계는 여전히 막혀 있다

§6 의 차단 사유는 그대로다 — B층 200건이 전부 2005-01~02 출원이라 시점유효 마스크 적용 시
연대가 확인된 방해문서가 2건뿐이다. 이 결정이 나기 전에는 §6.2–6.4 를 건드리지 않는다.

---

## 10. 문체 교정 — 쉬운 한국어 (2026-08-01 · 사용자 지시)

### 10.1 원칙 — 용어를 없애지 않고 순서를 바꾼다

"비열등성" 같은 말은 **통계에서 정의가 고정된 용어**라 지우면 심사자가 어떤 검정을 했는지 못 읽는다.
그래서 규칙은 *용어 삭제*가 아니라 **"뜻을 먼저 쓰고 용어를 괄호에 넣는다"** 이다. 규약은 CLAUDE.md
§8.1에 등재했다(이 저장소의 항구 규칙).

| 바꾼다 | 바꾸지 않는다 |
|---|---|
| 한자어 뭉치(비열등성·비회귀·정박·위양성·도달성·훼손·간극·소거·천장) | 수치·통계량·지표명(Recall@100·nDCG@20·McNemar·95% CI) |
| 긴 접속 문장 → 짧은 문장 | 가설 라벨(H3·H5)·게이트 이름(L0–L3·T1–T3)·CQ 번호 |
| 약어를 문장 안에서 풀기 | 클래스·술어 IRI(`Process`·`hasSubprocess` 등) |

### 10.2 수행 범위

- **국문 초록 전면 재작성** — "지식집약 공학 태스크는 완결되지 않는다" 같은 문장을 걷어내고,
  세 갈래가 무엇을 하는 일인지부터 설명하는 순서로 바꿨다. 2,314자.
- **신설 「이 논문을 읽는 법 — 자주 나오는 말」**(671자) — 되풀이되는 14개 용어의 대응표를
  약어표 뒤에 두었다. 비열등성/비회귀/절제/음성 대조군/도달성/위양성/델타/봉인·사전등록 등.
- **§1.1–1.3 전면 재작성** — §1.2 제목을 "과제 의미 회귀" → **"검사를 다 통과했는데 성능이
  나빠지는 일"**, §1.3을 "왜 단일 태스크 게이트로는 부족한가" → **"한 갈래만 보는 검사로는
  부족하다"** 로 바꾸고 본문도 그에 맞췄다. 용어는 첫 등장 시 괄호 병기.
- **§4.9 승인 규칙** — 수식만 있던 자리에 **"각 항이 묻는 것"을 말로 푼 표**를 넣었다.
  "식이 곱이므로 하나라도 0이면 승인은 0"을 먼저 말한다. T1은 "더 좋아졌는가가 아니라
  나빠지지 않았는가"임을 명시하고 비열등성 검정은 그 뒤에 괄호로 붙였다.
- **§4.10 결함 주입 · §5.4 절제** — 도입·마무리 문단을 풀어 썼다. "잘 잡는 검사는 아무거나
  거부해도 만들 수 있다"처럼 **왜 그 지표를 함께 보는지**를 문장으로 설명한다.
- **§5.1–5.2 지표·통계** — 주 지표를 "상위 100건 안에 얼마나 들어왔는가"로 먼저 정의하고,
  정밀도를 주 지표로 쓰지 않는 이유(부분 정답이라 "관련 없음"을 단정할 수 없다)를 풀어 썼다.
  paired bootstrap·McNemar·Holm 도 "왜 짝을 짓는가"를 한 줄로 붙였다.
- **§6.5 · §7.3 · §8.1 · §10** — 판정 문단의 한자어를 풀었다("층 정의의 포개짐" → "두 검사가
  보는 범위가 서로 포개져 있었다").

### 10.3 검증

- 참조 무결성 재확인 — 깨진 `§N.M` **0**.
- **수치·라벨 불변** — 치환 목록에 수치·지표명·가설 라벨을 넣지 않았고, 치환 후 부록 B ↔ §6.3
  정합 테스트(4건) 통과.
- 분량 61,244 → **63,045자**(+1,801). 쉬운 말은 짧지 않다 — 한자어 한 덩어리를 풀면 글자 수는
  늘어난다. 목표 45,700 대비 여유는 2단계(§6.2–6.4)와 §2·§4·참고문헌에서 확보한다.
- `make lint` 통과 · `make test` 전량 통과.

### 10.4 남은 범위

§2(관련연구)·§3·§6.1–6.4 본문은 이번 패스에서 **손대지 않았다.** §6.2–6.4는 2단계에서 B층
결과로 다시 쓸 자리이므로 지금 문체만 고치면 두 번 일하게 된다. §2·§3은 다음 패스 대상이다.

---

## 11. 2단계 부분 실행 — B층 무관분 (2026-08-01)

### 11.0 결론 먼저

**2단계 중 B층 결과에 의존하지 않는 부분만 실행했다.** §6.2–6.4 는 손대지 않았다(§9.5 차단 유효).
분량 **63,045 → 57,193자**(−5,852 · 9.3 %). 목표 45,700 까지 남은 11,493자의 대부분이
§6(13,956 → 9,000)이며 이는 B층 결정 이후의 몫이다.

| 절 | 목표 | 이전 | 현재 | 판정 |
|---|---:|---:|---:|---|
| §2 관련연구 | 4,000 | 7,155 | **4,676** | 목표 대비 +676 |
| §3 SDKB | — | 4,875 | 4,800 | 문체 교정(분량 목표 아님) |
| §4 방법 | 4,000 | 6,960 | **5,466** | 목표 대비 +1,466 |
| 참고문헌 | 6,000 | 7,546 | **5,742** | 목표 초과 달성 |
| §6 결과 | 9,000 | 13,956 | 13,956 | **미착수 — B층 결정 대기** |
| **총계** | 45,700 | 63,045 | **57,193** | |

**§2·§4 가 목표에 못 미친 이유를 적어 둔다.** 남은 분량의 대부분이 **압축되지 않는 형식 요소**다 —
§4 는 mermaid 흐름도 1개 + 수식 3개 + 표 2개가 약 1,900자를 차지하고, §2 는 관련연구 위치표
(§2.6)가 약 640자다. 이들을 더 줄이려면 방법의 재현 가능성이나 심사자가 보는 위치 근거가
깎인다고 판단해 **더 줄이지 않았다.** 필요하면 §2.6 표 삭제(−640)와 §4.1 흐름도 삭제(−330)가
남은 수단이다.

### 11.1 §2 관련연구 — 7,155 → 4,676

- **교차언어(CLIR) 단락 축약이 가장 큰 절감이다.** 이 축은 v1.1 에서 **탐색적 진단으로 강등**됐는데
  본문 분량은 확증 축과 같았다. 공백 진술(언어중립 개념 IRI = 제3 통로)과 §6.2f 포인터만 남기고
  통로별 문헌 열거를 줄였다. 인용 8건이 본문에서 빠졌다.
- **§2.3 의 L0–T3 표를 삭제했다 — §4.9 표와 중복이기 때문이다.** 층 이름과 각 층이 보는 것은
  산문 한 문단으로 남기고, "각 항이 묻는 것과 어긋났을 때의 처리"는 §4.9 표를 가리킨다.
- §2.2 · §2.4 · §2.5 는 문장 단위 압축(내용 삭제 없음). §2.6 위치표는 "대표 평가"·"장점" 두 열을
  덜어 3열로 줄였다.

### 11.2 §4 방법 — 6,960 → 5,466 · **잘라낸 것은 supplementary S3 로 옮겼다**

§4 에서 잘라낸 것의 다수는 **설계했으나 실행하지 않은 옵션**이다. 실행하지 않았다는 사실 자체가
보고 대상이므로(CLAUDE.md §1-1·§8) 삭제하지 않고 신설
[`paper/supplementary/S3-unexecuted-design-v09.md`](../../paper/supplementary/S3-unexecuted-design-v09.md)
에 **한 글자도 고치지 않고** 옮겼다. 본문 §4.2·§4.8·§4.10 이 S3 를 가리킨다.

| S3 절 | 옮긴 것 | 본문에 남긴 것 |
|---|---|---|
| S3.1 | §4.2 질의 표현 3종 설계 | "4종을 준비했으나 **표현 사이의 비교는 실행하지 않았다**" |
| S3.2 | §4.3 5-fold rolling-origin 보조 분석 | as-built 분할(600/200/200 · 경계 2개 · 패밀리 959) |
| S3.3 | §4.5.2–4.5.3 전문 | 두 모드를 각 한 문장 |
| S3.4 | §4.6 밀집 기준선 선택(설계 시점) | **as-built = Titan Embed v2** 와 그 이유 |
| S3.5 | §4.8 보조지표 4종 | "설계했으나 **산출하지 않았다**" |
| S3.6 | §4.10 결함 12종 × 예상 탐지층 대응표 | 12종 구성과 **두 교차 결함**을 산문으로 |

**§4.6 은 오기 정정이기도 하다.** 본문이 "최소 후보는 PatentSBERTa 또는 PaECTER"라는 **설계 시점
문장을 그대로 달고 있었는데** 실제 as-built 는 Titan Embed v2 다. as-built 로 고치고 설계 시점
기술은 S3.4 에 보존했다.

### 11.3 §3 문체 교정 (PLAN-033 §10.4 잔여)

§3.2–§3.7 을 CLAUDE.md §8.1 규약대로 고쳤다 — "표현 가능성과 실제 인스턴스 완전성은 다르다" →
"**표현할 수 있다는 것과 실제로 채워져 있다는 것은 다르다**", "도달성 사다리" 에 괄호 풀이 추가,
"과대평가/과잉 일반화" → "부풀리게 된다 / 전체로 넘겨 짚는다". **수치·분모 규율 문장
(2,534 · 2,321 · 2,211 · 584)은 굵게 강조만 하고 문면을 바꾸지 않았다.**

### 11.4 참고문헌 — 7,546 → 5,742

본문에서 인용이 사라진 항목 **8건**(Alcácer et al. 2009 · Choi 2009 · Fujii et al. 2004 ·
Keet & Khan 2024 · Leveling et al. 2011 · Magdy & Jones 2011 · Sharma et al. 2019 ·
Zhou et al. 2013)을 제거했다. 대부분 CLIR 축약의 귀결이다. 제거 후 **인용–항목 양방향 감사**를
돌려 미인용 항목 0 · 항목 없는 인용 0 을 확인했다(USPTO·W3C 는 정규식 오탐).

### 11.5 검증

| 기준 | 결과 |
|---|---|
| 참조 무결성 | `§N.M` 참조 전량이 실재 헤더와 일치 — **깨진 참조 0**(헤더 69) |
| **수치 불변** | 새 원고에만 있는 수치 토큰 = **`527` · `2,255` 둘뿐**. 둘은 CLAUDE.md §0 동결 사실(질의 독립항 중앙값 527자 · 문서 2,255자)을 §4.2·§4.6 에 인용한 것이며 **새로 만든 수치가 아니다** |
| 판정 불변 | H1–H5 판정 문장·§6 전량 무수정. §4 는 설계 절이라 판정을 담지 않는다 |
| 인용 무결성 | 미인용 항목 0 · 항목 없는 인용 0 |
| `make lint` | 통과 |
| 부록 B ↔ §6.3 정합 테스트 | 4건 통과 |

### 11.6 여전히 막혀 있는 것

§6.2–6.4 는 §9.5 그대로 **B층 코퍼스 시점 결정 대기**다. 실측을 하나 더 붙여 둔다 —
후보 코퍼스 40,552건 중 **공개일이 2005-01-03 이전인 문서는 2건**이고, 공개일 결측 4,492건의
대부분이 2005년 이전 출원분이다. 대조로, A층 test 질의(출원 2021–2025)가 마스크 후 마주하는
후보는 **약 23,000건** 규모다. 건초더미 크기가 네 자릿수 이상 다르다.
