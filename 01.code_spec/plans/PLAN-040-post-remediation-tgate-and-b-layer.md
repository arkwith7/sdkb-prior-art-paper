# PLAN-040 — 사전등록: 상류 교정 4건 반영 스냅샷의 T-gate 재적용 + B층 확증분할 H3 재확증

> **상태: 조건 동결 (§2·§3·§5·§6) · 서명 미기입 (§4.2).**
> [CLAUDE.md §2.1](../../CLAUDE.md)이 규정한 **유일한 정지 게이트**이며, 이 커밋 이후 §7 실행은
> 중간 승인 없이 끝까지 돌린다. **여기 적힌 동결 항목은 결과를 본 뒤 바꾸지 않는다.**
>
> **왜 조건과 서명을 나눠 커밋하는가.** 상류에 US 문헌 2건 재시도를 요청해 둔 상태라
> (`upstream/HANDOFF-QUEUE.md` §1.8 ②) 스냅샷 sha256 이 아직 확정되지 않았다. **판정 기준을
> 먼저 얼리고 서명을 나중에 채우면**, 어느 회신이 오든 기준은 이미 고정돼 있다. 반대로 하면
> 사후 사전등록이다(§1-3).
>
> **선행:** PLAN-035(H2 1회차 · 기각) · PLAN-031(B층 수집 완료 200건 · **봉인 미개봉**) ·
> 상류 CR-004R `84ea514` · CR-008·CR-009 `39855bb`.

---

## 0. 한 문장

**상류 교정 4건이 한꺼번에 들어온 스냅샷 O″ 에 대해, ⓐ 승인 게이트(T1·T2·T3)를 규정대로
집행하고, ⓑ 그 위에서 아직 한 번도 개봉하지 않은 B층 분할로 H3 를 재확증한다.**

---

## 1. 무엇이 들어오는가 — 자원 4건

| 자산 | 출처 | 델타 유형([CLAUDE.md §0](../../CLAUDE.md)) | 검색 파이프라인 도달 경로 |
|---|---|---|---|
| `ontology/sdkb-abox-prior-art.ttl` | CR-008 `39855bb` | **③ A-Box 코퍼스 델타** — `CitedPatent` 3,034 → 3,513 | `assemble.py:77` 이 `CitedPatent` 를 문서 노드로 받는다 → **후보 코퍼스가 커진다** |
| `mappings/concept_mapping.json` (스키마 1.1) | CR-009 `39855bb` | ② 개념층 (추가만 · `entries` 무변경) | `concept_meta` 는 **현행 적용기가 읽지 않는다** — §3.3 |
| `ontology/sdkb-abox-claim-features.ttl` | CR-004R `84ea514` | ① T-Box 사용 + ② | `RejectionReason` 2,749건 → **하위집단 층화 재료**(검색 신호 아님) |
| `mappings/abox_term_aliases.json` | CR-007(D-16) | ② 개념층 | 적용기 입력 — 스냅샷만으로 개념링크 재현 |

**`vendor.py` 코드 수정은 2건이다** — `abox_term_aliases.json`·`sdkb-abox-claim-features.ttl`
추가. 나머지 둘은 이미 `VENDOR_FILES` 에 있고 내용만 갱신된다(`vendor.py:44`·`:53`).
**이 수정은 §2.1 이 금지하는 "로직 변경"이 아니다** — 검색 설정·가중치·토큰화·Dense 모델·분할
경계 중 어느 것도 건드리지 않고, **스냅샷에 담을 파일 목록만** 넓힌다(PLAN-034 의 전례).

> **집행 정정 (2026-08-05 · 사용자 승인).** 위 두 번째 항목의 **방식만** 바뀌었다 —
> `sdkb-abox-claim-features.ttl` 은 **888MB** 이고 이미 `central_axis.py` 가 sha256 으로 얼려 두는
> 별도 경로가 있어, `VENDOR_FILES` 에 넣으면 매 vendor 가 888MB 를 복사하며 이중화된다.
> 그래서 `rejection_basis.csv` 와 **같은 파생 패턴**을 쓴다 — `_derive_rejection_reasons()` 가
> `ont:RejectionReason` 만 라인 스캔으로 뽑아 `rejection_reasons.csv` 로 얼리고(원문 0열),
> **원본 TTL 의 sha256 을 PROVENANCE 에 남긴다.** 실측 2,749건 · 출원 994 · 미연결 0 · 3.5초 ·
> 재실행 sha256 동일 · 단위테스트 4건(`test_baseline_integration.py`).
> **들어오는 자원도, 판정 조건(§2·§3·§5·§6)도 바뀌지 않는다** — 같은 데이터를 어떤 그릇에
> 담아 얼리느냐만 바뀌었다. `abox_term_aliases.json` 은 예정대로 `VENDOR_FILES` 에 추가됐다.

---

## 2. 두 판독 — 하나의 스냅샷, 두 개의 질문

| | 판독 A · T-gate 재적용 | 판독 B · B층 H3 재확증 |
|---|---|---|
| 질문 | 이 델타를 **릴리스로 승인해도 되는가** | 검색 이득이 **새 표본에서 재현되는가** |
| 주장 | C3(H2 3회차) | C2(H3) |
| 표본 | **A층 test 198**(이미 개봉됨) | **B층 200**(미개봉 · 최종 1회) |
| 지위 | 사전등록된 승인 규칙의 **집행** | **확증** |
| 근거 | §2.1 — 개봉된 표본 위에서 허용되는 것은 승인 규칙 적용과 탐색적 재측정뿐 | §2.1 — 재확증은 신규 확증분할로만 |

**둘을 한 사전등록에 담는 이유는 스냅샷이 하나이기 때문이다.** 판독 A 가 승인을 거부해도
판독 B 는 실행한다 — B 는 "이 자원을 릴리스할까"가 아니라 "이 자원 위에서 검색 이득이
재현되는가"를 묻기 때문이다. **두 결과는 서로를 취소하지 않는다.**

---

## 3. 판독 A — T-gate 재적용 (H2 3회차)

### 3.1 두 팔 — 문서집합을 상수로 묶는다

**③ A-Box 코퍼스 델타가 섞이면 T1 비교는 성립하지 않는다**([CLAUDE.md §0](../../CLAUDE.md)
델타 유형표). CR-008 은 문서를 479건 늘리므로, 그대로 두면 이번 비교는 **자격이 없다.**
그래서 **B층 문서를 양 팔에 동일하게 넣어 상수로 만든다.**

| 팔 | 코퍼스(문서집합) | 개념·구조 자산 |
|---|---|---|
| **O′** | O″ 코퍼스와 **바이트 동일** | `2839afb` 자산 (스키마 1.0 `concept_mapping` · claim-features 없음) |
| **O″** | 같음 | `39855bb` + `84ea514` 자산 |

- **O′ 팔은 상류를 체크아웃해 만들지 않는다**(§0.1). 현재 디스크의 `data/external/sdkb/` 가
  이미 O′ 이고(PLAN-035 이후 상태), `make vendor` 를 돌리는 순간 그것이 O″ 로 덮인다 —
  **vendor 전에 O′ 스냅샷을 별도 경로로 복사해 둔다.** 원고 §6.2 확증 표는 여전히 **O 팔**의
  산출이므로 `make tables` 를 무심코 돌리지 않는다(메모리 `disk-resource-is-oprime-manuscript-is-o-arm`).
- **변인은 ①·② 델타뿐**이므로 `classify_delta` = `tbox`/`concept` ∈ `H2_ELIGIBLE_DELTA_TYPES`.
- **적격심사(E1–E7)는 PLAN-035 의 것을 그대로 승계한다.** 추가 1항 — **E8: 두 팔의
  `ir_corpus.parquet` 문서 id 집합이 동일**(문서 델타가 팔 사이로 새지 않았음의 증명).

### 3.2 판정식 — 재선택 없음

`Accept = (L0=L1=L2=L3=pass) ∧ (LB₉₅(ΔR₁₀₀) > −ε) ∧ (max_s drop_s < δ) ∧ (∀f∈{em,tf,core}: pass_f(new) ≥ pass_f(old))`

ε = **0.02** · δ = **0.05** · 주지표 **family-level Recall@100** · 비교 시스템 **P1** · 분할
**test** · 하위집단 축 **거절근거 · 공정군 · 언어(KR/외국)** · CQ 스위트 **pa·em·tf·core**.
**전부 기존 동결값이며 이 사전등록에서 새로 고르지 않는다.**

### 3.3 미리 자인하는 것 — **이번 T1 은 공허할 수 있다**

`concept_meta`(df)는 **자산에 있을 뿐 하류가 읽지 않는다.** 특이도 가중을 실제로 걸려면
점수식을 고쳐야 하고, 그것은 재측정이 아니라 **새 방법이므로 §2 전체를 탄다.**
`RejectionReason` 역시 검색 신호가 아니라 층화 재료다. 따라서 **개념링크가 변하지 않으면
ΔR₁₀₀ 은 0 에 가깝고, T1 은 D-19 때처럼 "통과했으나 아무것도 검정하지 않은" 상태가 된다.**

**그래서 실행 전에 도달 통로를 먼저 잰다(E9).** 두 팔의 `ir_corpus.parquet` **개념링크 열의
sha256 을 비교**해서 동일하면, 판정은 `Accept=1` 이 아니라 **"미검정(vacuous)"으로 기록한다.**
공허한 통과를 승인으로 적지 않는다 — D-19 에서 이미 한 번 치른 값이다.

---

### 3.4 실행 결과 (2026-08-05) — **T1·T2 는 미검정 · T3 는 통과**

**§3.3 이 자인한 상황이 그대로 일어났다. 사전등록이 예측했으므로 오판으로 가지 않는다.**

| 조건 | 결과 |
|---|---|
| L0–L3 | ✅ PASS (새 G₀ 115,095 위에서 재실행) |
| 누출 감사 | ✅ PASS — 금지간선 잔여 0 · run 7개 · L-1·L-2·L-2b·L-3·L-4 전부 통과 |
| **T1 비열등성** | **미검정** — 아래 |
| **T2 하위집단** | **미검정** — 같은 이유 |
| **T3 교차 태스크 CQ** | ✅ **PASS (실질 검정)** — em 1.000 · tf 1.000 · core 1.000 · 하락 **0** |
| **Accept** | **산출하지 않는다** — T1 이 미검정이면 승인식이 정의되지 않는다 |

**왜 미검정인가 — 두 팔이 실질적으로 같은 자원이다(E9 발동).**
O′(`2839afb`)와 O″(`39855bb`)의 `concept_mapping.json` 을 대조한 결과:

| 항목 | 대조 |
|---|---|
| `patent-text.entries` | `d5939365ce45` **==** `d5939365ce45` |
| `patent-text.blocked` | `4f0c9eb3570e` **==** `4f0c9eb3570e` |
| `expert-tag.entries` | `2cd824e2b0fa` **==** `2cd824e2b0fa` |
| 차이 | **`concept_meta` 추가뿐**(스키마 1.0 → 1.1) |

그리고 **`concept_meta` 를 읽는 코드가 저장소에 0건이다**(`grep -rn "concept_meta\|df_abox\|df_denominator\|is_superordinate" src/` → 없음).
따라서 두 팔은 **바이트 동일한 코퍼스**를 만들고 ΔR₁₀₀ = 0 이 **정의상 확정**이다.
§3.3 대로 `Accept=1` 이 아니라 **"미검정"으로 기록한다.**

**T3 는 다르다 — 실질 검정이다.** CR-004R 이 T-Box 술어 5개를 실제로 넣었고(§4.2 다섯째 자산),
그것이 다른 태스크의 CQ 를 뒷걸음치게 했는지는 자원의 성질이지 run 의 성질이 아니다.
하락 0 은 상류 회신(§1.1b #6)과 **독립적으로 일치한다.**

> **⚠ `make gate SPLIT=test` 의 콘솔 출력 `Accept(ΔG)=1` 을 인용하지 않는다.**
> 그 실행은 **`mode=system`** 이었다 — `sys_P1_test.txt` 대 `sys_B3_rrf_test.txt`, 즉 **서로 다른 두
> 시스템의 비교**이고 [CLAUDE.md §0](../../CLAUDE.md) 경계표가 *"P1 대 B3 비교는 H2 의 증거가
> 아니다"* 라고 명시한 바로 그 형태다. 산출물 자체도 정직하게 라벨돼 있다 —
> `data/processed/tgate_report.json` 의 `h2_eligible: false` · `verdict: "system_comparison"`.
> **게다가 그 run 들은 2026-08-01 자로 구 코퍼스(40,552행) 위에서 만들어진 것이다**
> (현 코퍼스 41,031행 · sha `db8132c3…`). 시스템 비교로서도 낡았다.
> **판독 A 의 결론은 위 표이며, 그 콘솔 값이 아니다.**

**run 을 새 코퍼스로 재산출하지 않았다 — 의도적이다.** T1·T2 가 위 이유로 이미 미검정이므로
재산출은 확증 증거를 만들지 않고, `make tables` 는 **원고 §6.2 확증 표(O 팔 산출)를 덮어쓴다**
(§6 비목표 3 · 메모리 `disk-resource-is-oprime-manuscript-is-o-arm`). 탐색적 재측정(B층 479
방해문서 추가가 A층 test 성능에 미치는 영향)이 필요하면 **별도 지시 아래** 돌린다.

---

## 4. 판독 B — B층 확증분할 H3 재확증

### 4.1 승계 — PLAN-031 §4·§5·§6 을 그대로 쓴다

주지표 family-level R@100 · **H3 판정은 R@100 과 nDCG@20 둘 다 개선해야 지지** ·
질의단위 paired bootstrap 10,000회 양측 · 절제 가족 Holm(m=8) · 후보 F10 마스크 후 상위 1,000 ·
**개봉 최종 1회** · 가중치·임계는 **기존 dev 에서만** 선택하고 신규 분할로 재선택하지 않는다.
**신규 분할의 출원연도·IPC·정답 언어 분포를 기존 test 와 대조해 개봉 전 공개**하고,
**기존 test 결과와 직접 비교하지 않는다**(B층은 자체 완결적 확증이다).

### 4.2 개봉 전 차단 게이트 — 통과 못하면 **개봉하지 않는다**

| | 게이트 | 합격선 | 값 |
|---|---|---|---|
| G1 | **B층 정답 도달성**(분모 **503** · NPL 11 별도 · 총계 514 병기) | **≥ 0.95** | `[vendor·조립 후 기입]` |
| G2 | 관할별 본문 확보율 | 합계로 뭉치지 않고 관할별로 보고 | `[기입]` |
| G3 | `make leakage` — 금지 간선 | **0** | `[기입]` |
| G4 | 스냅샷 서명 sha256 | 사전등록에 기입 후 실행 | ✅ **기입 완료(2026-08-05)** — 아래 표 · **움직인 것은 4건이 아니라 5건이다** |
| G5 | 봉인 qrel 해시 **불변** | B층 `qrel_b_sealed.parquet` = `127a138f1c1651676ea81b9ecf50aa53e0172ca4ee7ff0c5b8f26e9d171db4c3`(538행·질의 200) · A층 `qrel_test_sealed.parquet` = `984f8ef3…fda2c` | 대조 |

**G4 서명표 (2026-08-05 · `make vendor` 실행 후 기입 · 상류 `39855bb` · dirty=false · 20/20 검증).**

| 자산 | 스냅샷 파일 | sha256 |
|---|---|---|
| CR-008 B층 A-Box | `sdkb-abox-prior-art.ttl` | `1abbf5e54f66c8dc49e203244b5c3925369839524a5fd22768abf3aae137c3fd` |
| CR-009 개념 df | `concept_mapping.json` | `b2da08e7c261465e4d50b0228f05fdcc2edc6b30b26643c90e1cffa2ab9cf09a` |
| CR-007 별칭 사전 | `abox_term_aliases.json` | `f54ff7ea434612ccb91a3c33f1e4480de0be4d0a04cb30e3a9fa682e990ed179` |
| CR-004R 거절근거 조항 (파생) | `rejection_reasons.csv` | `c0cecac96e8e47958363439213efb8bd578704431d9bd4acfa83ce7f6abbbd21` |
| **CR-004R T-Box 술어 델타** | **`sdkb-patent.ttl`** | `ebe1c301e5585047900575e577989c26e58b90716f8bc38e78851c40010470e8` |
| CR-004R 원본 (vendor 하지 않음 · 서명만) | `ontology/sdkb-abox-claim-features.ttl` | `9f75836fe240997226dabc171db129f4f98a521b68cc32e43884d9c1beb05fb6` (888,298,833 B) |

**나머지 15개 파일은 바이트 불변이다** — `sdkb-abox-patents.ttl`·`sdkb-core*.ttl`·거버넌스 4종·
`rejection_basis.csv` 등. 상류 재빌드(`owl convert abox abox-patents abox-vendors compliance`)를
거쳤는데도 불변이라는 것이 **재빌드가 자원을 조용히 흔들지 않았다는 증거**다.

> **예상하지 못한 다섯째 — `sdkb-patent.ttl` 이 움직였고, 그것은 T-Box 술어 델타다.**
> §1 표는 자원 4건을 적었으나 실제로 서명이 바뀐 것은 **5건**이다. 다섯째는 CR-004R 이
> `84ea514` 로 넣은 **술어 5개**이며, O′ 대조 실측은 다음과 같다 —
> `owl:ObjectProperty` **31 → 32**(`reasonGround`) · `owl:DatatypeProperty` **22 → 26**
> (`groundClause`·`noticeDate`·`noticeRound`·`noticeType`) · `owl:Class` **22 불변** ·
> `RejectionType` 개체 언급 9 → 17.
> **이것은 델타 유형 ①(T-Box)이고 H2 자격이 있다**(CLAUDE.md §0 델타 유형표). 연구 통틀어
> **두 번째** T-Box 술어 델타다(첫 번째는 `2839afb` 의 `skos:broader` 선언).
> **판정 조건은 바뀌지 않는다** — 자격이 있다는 것과 검정됐다는 것은 다르며, 이번에도
> §3.3 의 자인(개념링크가 그대로면 "미검정"으로 기록)이 그대로 적용된다. 다만 이 델타는
> **T3(교차 태스크 CQ 비회귀)의 실질 대상**이다 — 상류가 T3 하락 0 을 회신했고(§1.1b),
> 하류 `make gate` 가 그것을 독립적으로 재확인한다.

**대조군 O′ 의 서명도 얼려 두었다** — `make vendor` 가 덮기 전에 복사·기록했다(§7 (0-b) 완료 ·
[PLAN-040-oprime-snapshot-signatures.json](PLAN-040-oprime-snapshot-signatures.json) · 상류 `2839afb` · 18파일).

### 4.3 실행 결과 (2026-08-05) — **G1 은 통과했고, 그럼에도 개봉하지 않았다**

| | 게이트 | 합격선 | 실측 | 판정 |
|---|---|---|---|---|
| G1 | B층 정답 도달성 (분모 503) | ≥ 0.95 | **482/503 = 0.9583** | ✅ 통과 |
| G2 | 관할별 본문 확보율 | 뭉치지 않고 보고 | 아래 표 | ⚠ **A층과 비대칭** |
| G3 | `make leakage` 금지 간선 | 0 | **0** (run 7개 · L-1·L-2·L-2b·L-3·L-4 전부 PASS) | ✅ 통과 |
| G4 | 스냅샷 서명 | 사전등록 기입 후 실행 | §4.2 표 | ✅ 기입 완료 |
| G5 | 봉인 qrel 해시 불변 | 대조 | `qrel_test_sealed` `984f8ef3…` 일치 · B층 `127a138f…` **미개봉** | ✅ |

**G1 은 상류 값(0.9583)과 정확히 일치한다.** 분모 503 도 일치하고 미도달 21건의 내역
(JP 19 · US 2)도 같다. **PLAN-031 §10.3 이 지적한 KR 키공간 불일치는 해소됐다** — KR 도달성 1.0000.

**G2 — 도달성은 채워졌는데 본문이 비대칭이다.**

| 층 | KR 청구항 | US 청구항 | JP 청구항 | 초록 |
|---|---|---|---|---|
| A층 인용 | **1.0000** (n=1,718) | **0.9958** (n=471) | 0.0000 (n=720) | 0.9979 |
| B층 인용 | **0.0085** (n=235) | **0.0000** (n=49) | 0.0000 (n=167) | 0.9710 |

원인은 **발행 형식**이다 — 상류가 청구항을 `ont:claimText` 단일 문자열로 넣었고, 코퍼스는
사이드카 경로(`hasClaim`→`hasFeature`→`featureText`)로만 청구항을 만든다. B층 479 노드는
그 형식으로 분해되지 않았다. **대장 D-26 · CR-011 로 발행했다**(HANDOFF-QUEUE §1.9).

**결정(2026-08-05 · 사용자 판단) — 개봉하지 않는다.** 규칙상 G1 만 합격선을 가지므로 개봉할
수 있었으나, PLAN-031 §10.4 ⑤ 가 개봉 전에 경고한 상황이 정확히 이것이다. G1 이 말하는 것은
"문서가 있다"이지 "A층과 비교 가능한 문서다"가 아니다. **개봉은 1회뿐이므로 지금 열면
"청구항 없는 B층"이 확증 분할의 영구 조건이 되고, 나중에 청구항이 들어와도 다시 확증할
표본이 없다.** CR-011 반영 후 **같은 사전등록 조건으로** 개봉한다 — 조건은 재선택하지 않는다.

**G1 이 실패하면 §9.4 가 다시 발동한다** — 개봉하지 않고 원인을 보고한다. 상류 실측
482/503 = 0.9583 은 **상류 그래프에서의 값**이고, 하류 후보 코퍼스에서 다시 재는 것이 G1 이다.
**두 값이 다를 수 있는 자리가 있다** — PLAN-031 §10.3 이 지적한 **KR 키공간 불일치**(qrel 은
공개번호 · 코퍼스 KR 문서는 출원번호)는 CR-008 이 해소한 문제가 아니다. 그것이 남아 있으면
G1 은 상류 값보다 낮게 나온다. **낮게 나오면 낮은 대로 보고하고 멈춘다.**

---

## 5. 확증과 탐색의 구분 (§2.1 요구 항목)

| 지위 | 항목 |
|---|---|
| **확증** | 판독 B 의 **H3**(B층 200 · R@100 ∧ nDCG@20) |
| **집행(확증 아님)** | 판독 A 의 T1·T2·T3 판정과 `Accept` 값 |
| **탐색** | 거절근거 축 층화(검정력 부족 · PLAN-031 §5.1) · 관할별·언어별 회수 · B층 df 분포 · **CR-004R `RejectionReason` 로 재산출한 층화**(본문 정규식 → 표 칸 기준 교체) |
| **대장 회신용 참고** | B층 도달성 · 관할별 본문 확보율 · 상위 30개념 df 순위 Spearman ρ(CR-009 회신) |

---

## 6. 이 사전등록이 하지 않는 것 (비목표)

1. **df 가중을 넣지 않는다.** 점수식 변경은 새 방법이며 별도 사전등록(§2 전체)이다.
2. **`w_h`·τ·α·RRF·토큰화·Dense 모델을 건드리지 않는다.** 바뀌는 것은 자원과 코퍼스 증분뿐이다.
3. **A층 test 로 H3·H4·H5 를 재확증하지 않는다.** 이미 개봉된 표본이다(§2.1 안전장치).
4. **청구항 단위 조항 발행을 기다리지 않는다.** 출원 단위로 확정했다(HANDOFF-QUEUE §1.8 ①).
5. **C2′(RQ5) 전달 실험을 이 사전등록에 넣지 않는다.** 검색 층 결과가 확정된 뒤다.

---

## 7. 실행 순서 — 중간 승인 없이 관통한다

```
(0) ✅ 상류 회신 접수(2026-08-05): US 2건 재시도 **실패** → 0.9608 미달 확정 · 노드 0 추가 ·
    concept_mapping 재발행 조건 미발동(HANDOFF-QUEUE §1.6a)
(0-b) ✅ 현 data/external/sdkb/ (= O′) 를 별도 경로로 복사 · 서명 기록  ← 덮이기 전에
(1) ✅ vendor.py: VENDOR_FILES += abox_term_aliases.json ·
    claim-features 는 _derive_rejection_reasons() 파생으로(§1 집행 정정)
(2) ✅ make vendor        → §4.2 G4 서명 기입 → **커밋(= 사전등록 완성)**
    상류 39855bb · dirty=false · 20/20 검증 · 서명 이동 5건(나머지 15건 바이트 불변)
(3) make corpus          → 문서 증분 프로파일(§4 의무) · B층 도달성 G1 · 두 팔 코퍼스 E8
(4) make index → make leakage(G3)
(5) make retrieve → eval → gate   ← 판독 A 의 T1·T2·T3
(6) B층 개봉 1회 → eval → ablation ← 판독 B 의 H3
(7) make figures
(8) 결과 보고 → 그 다음에 C0(대장·CR 회신 배치 처리)
```

**(2) 의 커밋 전에는 (3) 이후를 돌리지 않는다.** 순서를 뒤집으면 사후 사전등록이다.
**(5) 와 (6) 사이에 판정 기준을 고치지 않는다.**

---

## 8. 보고 의무 (§4 · §8)

- 코퍼스 증분 프로파일: 문서 40,552 → `[기입]` · 언어분포 · 중복·결측 · **B층 노드의 본문 보유율**
- 두 팔 서명 대조표(E1–E9) — **하나라도 실패하면 실행 중단하고 보고**
- 판정 결과는 **가설과 반대여도 그대로** 적는다. `Accept=0` 이면 0 으로, H3 미지지면 미지지로.
- 기각·무효과를 적을 때는 **그것이 상류에 요구하는 수정**을 함께 적는다(C0).

---

## 9. 승인 기록

| 항목 | 상태 |
|---|---|
| §1.8 판단 3건(청구항 단위 발행 · US 0.9608 · `requests`) | ✅ 승인 2026-08-04 |
| 본 사전등록의 범위(판독 A + B) | ✅ 승인 2026-08-04 |
| §2·§3·§5·§6 조건 동결 | ✅ 커밋 `c39aa9a`(2026-08-04) |
| §1 집행 정정(claim-features 를 파생으로) | ✅ 승인 2026-08-05 — **판정 조건 무변경** |
| §4.2 G4 서명 기입 | ✅ **이 커밋 — 사전등록 완성.** 이후 `make corpus` 부터 무정지 관통 |
