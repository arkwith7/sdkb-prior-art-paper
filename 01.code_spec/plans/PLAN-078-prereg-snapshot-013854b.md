# PLAN-078-prereg — 사전등록: 스냅샷 `48971f8` → `013854b` 반영 (B층 인용문헌 복원)

> **지위: 동결 — 이 문서를 커밋한 뒤에 §5 를 실행한다.** 근거 규약은
> [CLAUDE.md §2.1](../../CLAUDE.md)(자원 버전이 바뀌면 정지 게이트 1개)과 **§1-3**
> (사전등록을 사후에 고치지 않는다)이다.
>
> **이 문서는 기존 판정과 수치를 하나도 바꾸지 않는다** — 원고 §6 의 값 · PLAN-035 의 기각 ·
> PLAN-047 의 T4 판정은 소급 수정되지 않는다.
>
> **선행:** [OPEN-ITEMS O-8](OPEN-ITEMS.md) ① 종결(2026-08-24 · 원인 = 재생성 누락) ·
> 상류 복원 커밋 `013854b`(2026-08-25) · [DEFECT-LEDGER D-52](../../upstream/DEFECT-LEDGER.md).

---

## 0. 결론 먼저 — 무엇을 하고 무엇을 하지 않는가

**한다.** 상류 복원분을 벤더하고 **G₀ 를 재조립**한다. 그 결과 자원 서명이 어떻게 움직였는지를
**관측으로 보고**한다.

**하지 않는다 — 결과를 보기 전에 박는다.**

| # | 하지 않는 것 | 이유 |
|---|---|---|
| ① | **검색·평가 파이프라인 실행**(`corpus`·`index`·`retrieve`·`eval`·`gate`) | 이 사전등록은 자원 반영까지다. 측정은 별도 사전등록에서 선다 |
| ② | **가설 검정 · 판정** | 검정할 가설을 세우지 않는다. 확증 0 · 탐색 0 |
| ③ | **CR-001B ⑤ · CR-020 ⑥ 의 판정** | 그 판정은 파이프라인 실행을 요구하므로 ① 에 걸린다 |
| ④ | **동결 상수·발행 서명의 갱신** | 사용자 결정 사항이다(2026-08-24 보류 결정의 연장) |
| ⑤ | **봉인 개봉** | 이 작업에 봉인 qrel 접근은 없다 |

**왜 여기서 자르는가.** 자원이 두 번 움직였다 — CR-020 이 개념 축을 바꾸고(의도), 이번 복원이
B층 문헌을 되돌렸다(누락 교정). 한 실행에서 재면 두 델타가 한 팔에 섞여 가를 수 없다(DP4).
**측정 사전등록은 자원이 멈춘 뒤에 선다.**

---

## 1. 동결 항목 — 결과를 본 뒤 바꾸지 않는다

| 항목 | 값 |
|---|---|
| 상류 커밋 | `013854b052d9672fe7190f6d0201294f7e029d8f` (2026-08-25 · `source_dirty=false`) |
| 직전 스냅샷 | `48971f8d36fdd07c3db72c3f15a638f9bc0383ac` |
| 바뀐 파일 | **`sdkb-abox-prior-art.ttl` 하나** — `51873ceb…` → `7f0a4df6…` |
| 그 밖의 21 파일 | **sha256 불변** |
| ε · δ | 0.02 · 0.05 (불변 · 이 실행에서 쓰이지 않는다) |
| 주지표 | family-level Recall@100 (불변 · 이 실행에서 산출하지 않는다) |
| 분할 경계 · 봉인 qrel | 불변 · **접근 0** |

---

## 2. 자원 델타의 성격 — 유형 ②·③ 혼합

[CLAUDE.md §0](../../CLAUDE.md) 델타 유형표 기준으로 **③ A-Box 코퍼스 델타**(문헌 479 추가)와
**② 개념층 델타**(그 문헌에 붙는 개념 링크)가 함께 들어온다. **① T-Box 델타는 없다** —
상류 커밋이 T-Box 를 건드리지 않았고 테스트가 그것을 단정했다. 그러므로 **H2 자격은 이 델타에
없다**(유형 ③ 이 섞여 있어 "문서집합 불변" 조건이 깨진다).

---

## 3. 되돌아오는 것과 되돌아오지 않는 것

| 관측 | 복원 전 | 복원 후 | 성격 |
|---|---:|---:|---|
| `CitedPatent` | 3,034 | **3,513** | 되돌아온다 — CR-008 분 |
| `claimText` | 2,197 | **2,479** | 되돌아온다 |
| `concernsSkill` (prior-art) | 262 | **296** | **되돌아오지 않는다** — CR-020 의 의도된 축 교정(구 1,486) |

**`concernsSkill` 을 복원 대상으로 삼지 않는다** — 그것을 되돌리면 CR-020 이 무효가 된다.

---

## 4. 예상하지 않는다

**G₀ 재조립 결과의 기대치를 적지 않는다.** 직전 동결값 119,251 은 CR-020 이전 자원의 값이고,
현 자원은 CR-020 의 개념 축 교정을 포함하므로 **119,251 로 돌아갈 이유가 없다.** 기대치를 적으면
그것이 결과를 읽는 자가 된다(§1-2). **관측하고 적는다.**

---

## 5. 실행 순서 — 중간 승인 없이

```
make vendor        # 완료 (2026-08-24T21:59 UTC · PROVENANCE source_commit = 013854b)
make baseline      # G₀ 재조립
pytest -q          # 서명 검사 4건이 어떻게 움직이는지 관측 (녹색으로 만들지 않는다)
```

**`make baseline` 이후의 어떤 명령도 이 사전등록에 포함되지 않는다.**

---

## 6. 완료 조건

- `PROVENANCE.json` `source_commit` = `013854b…` · `source_dirty=false` (**충족**)
- 벤더 파일 22 중 sha256 이 바뀐 것이 **정확히 1개** (**충족**)
- G₀ 재조립 완료 · 서명을 **관측값으로** 보고
- 동결 상수 갱신 **0** · 판정 변경 **0** · 원고 변경 **0** · 봉인 접근 **0**

---

## 7. 실행 기록 (2026-08-25) — **위 §0–§6 은 동결이므로 고치지 않는다**

**완주했다.** `make vendor` → `make baseline` → `pytest -q` 를 중간 승인 없이 실행했다.

| 관측 | 동결값 | CR-020 직후 | **복원 후** |
|---|---:|---:|---:|
| G₀ 트리플 | 119,251 | 110,880 | **120,147** |
| `CitedPatent` | 3,513 | 3,034 | **3,513** |
| `claimText` | 2,479 | 2,197 | **2,479** |
| CQ01 커버 공정 | 20 | 23 | **23** |
| CQ18 `patents_by_skill` | 10행 | 4행 | **4행** |

**§6 완료 조건 대조 — 하나가 어긋났고, 그것을 여기 적는다.**

| 조건 | 결과 |
|---|---|
| `source_commit` = `013854b…` · `source_dirty=false` | **충족** |
| sha256 이 바뀐 벤더 파일 정확히 1개 | **충족** (`sdkb-abox-prior-art.ttl`) |
| G₀ 재조립 · 서명을 관측값으로 보고 | **충족** (위 표) |
| 동결 상수 갱신 0 · 판정 변경 0 · 원고 변경 0 | **충족** |
| **봉인 접근 0** | **어긋났다 — 1행** |

**어긋난 자리의 내용.** `pytest -q` 가 `analysis.effort` 경로를 태우면서
`sdkb_paper.analysis.metrics:load_qrel_for_split` 이 **A층** `qrel_test_sealed.parquet`
(sha `984f8ef3…`)를 재판독했고, 원장에 한 줄이 남았다(39행 → 40행). **판독 B 봉인
(`127a138f…`)에는 접근이 없다.** A층 test 분할은 이미 개봉된 표본이며(§2.1 안전장치),
이 접근으로 산출된 수치는 없고 판정도 없다.

**그럼에도 "0" 이라 적은 것은 부정확했다.** 원인은 예측이 아니라 배선을 보지 않은 것이다 —
PLAN-076 이 A층 적재까지 원장에 남게 배선했으므로(커밋 `cdfbf3d`), **전체 테스트 실행은
원장을 건드린다.** 다음 사전등록에서는 이 조건을 *"판독 B 봉인 접근 0"* 으로 적는다.
**이 문서의 §6 은 고치지 않는다**(§1-3).
