# PLAN-087(제안 · v2) — 사례·그림 중심의 읽기 쉬운 본문: 관통 예시 도입, 본문·보충자료 분리, 그림 프로그램

| | |
|---|---|
| 대상 | `paper/submission/manuscript.md` 전 장 (중심은 §3–§5 · §1·§2·§6 은 그림·제목만) · 영문 파생본은 후행 |
| 입력 | 첨부 질의응답 · 사용자 방향 확정(2026-09-01): ① 논리·주장은 사례로 ② 본문 수치는 핵심 최소 · 나머지 전부 보충자료 ③ 장·절 제목 + 그림만으로 전체가 파악되는 구성 · 저장소 재검증(`mini_graph.ttl` · `queries/cq` · S2 · S7 · `scripts/skim_outline.py` 실측) |
| 바꾸지 않는 것 | **판정·수치·기여 전부**(`verdicts.yaml` 판정 열 무변경) · 사전등록 문구 · 보충자료 S1–S9 의 내용(자리만 늘어남) |
| 바꾸는 것 | (a) 설명 매체: 추상 규칙 → 예시 인스턴스 위의 규칙 · (b) 본문의 수치 밀도: 판정에 직접 걸린 수치만 · (c) 시각 골격: §3–§5 모든 절에 그림 또는 예시 박스 1개 · 캡션 첫 문장은 주장 · (d) 절 제목: 식별자 선행 → 주장형 |
| 선행 관계 | PLAN-086(층간 정합·감량) **이후** — 단, 본 계획 §3 의 표 이관 목록은 PLAN-086 트랙 B 의 입력으로 미리 넘긴다 |
| 상태 | 제안 · §10 의 결정 여덟을 받은 뒤 착수 |

> **결론 먼저.** 읽기 어려움의 원천은 셋이다 — **순서**(규칙이 인스턴스보다 앞선다), **밀도**(판정에 걸리지 않는 수치가 본문에 산다), **골격**(제목과 그림만으로는 논문이 읽히지 않는다 — §3–§5 의 20개 절 중 **15개에 그림도 예시도 없다**, `skim_outline.py` 실측). 처방도 셋이다. ① 거절특허 하나를 주인공으로 하는 **예시 그래프**를 §3 첫머리에 놓고 §5 판정까지 관통시킨다(v1 그대로 · 실행 확인). ② 본문에는 **판정 문장이 직접 참조하는 수치만** 남기고 표 9개 중 5개를 보충자료로 내린다 — 각 표가 떠난 자리에는 판정 한 문장과 포인터만 남는다. ③ 절마다 그림 하나(예시 그래프 위에 그린 것)를 두고 캡션 첫 문장을 주장으로 쓰며, 절 제목을 주장형으로 바꾸어 **제목 + 캡션만 이어 읽어도 논문이 되는 스킴 경로**를 만든다(부록 C 에 골격 시안 · 기계 검사 `make skim`).

---

## 0. 첨부 질의응답에 대한 평가 — 무엇을 받고 무엇을 고치는가

질의응답의 골격(**사례 도입 → 온톨로지 매핑 → 오류 시나리오 주입 → 게이트 판정**)은 그대로 채택한다. 다만 저장소 실물과 논문 규약(CLAUDE.md §0·§0.8·§1-2)에 맞추어 다섯 곳을 조정한다.

| 질의응답의 제안 | 판단 | 조정 이유 |
|---|---|---|
| ALD 장비 관련 **가상 미국 특허** US1029XXXX | **교체** → 저장소 픽스처의 합성 거절특허 `KR 10-2013-0000004` "플라즈마 식각 종점 검출 방법" | 새 가상 사례는 T-Box 어휘와 어긋날 위험이 있고 검증 불가. 픽스처는 어휘·IRI·라벨이 실물 SDKB 와 동일하고(주석 명시) CI 에서 매 커밋 실행된다 |
| 증착·식각을 "반도체 챔버 공정"으로 통합하는 **가상 델타** | **채택하되 축소판으로 명시** → 상위 공정 Etch 를 하위 공정 Plasma Etch 로 흡수(S2 F11 동의어 오병합의 축소판) | 실제 결함군 라벨(F11)과 대응시켜야 §5.4 의 실측(F11→CQ18)과 연결된다. 실행 결과 L3 통과·T3 단독 실패 패턴이 실제로 재현된다(§2.3) |
| T1 실패 사례로 **"정답이 100위 밖으로 밀려나는" 가상 수치** | **가상 수치 금지** → 실측 EP3 델타(`d578bf3→2839afb`)를 예시 특허 위에서 설명하고, 개별 질의 순위는 실측 추적표(`scripts/trace_query.py`)로 낸다 | §0.8 판정 문구 사전과 서명 검사(sig-check)가 수치를 관리한다. 가상 수치가 본문에 들어가면 실측과 구분되지 않는다 |
| "교차 태스크 감시망이 적발하여 **최종 승인 거부**" 하는 극적 결말 | **실제 판정 그대로** — EP3 의 거부 근거는 T1 이고 T3 는 통과하였다(§5.2). T3 단독 검출은 EP2(홀드아웃 결함) 의 몫이다 | 실제 서사는 더 강하다: "온톨로지 쪽 지표가 전부 좋아지고 형식 검증도 통과했는데 검색 성능 조건 하나가 막았다". 사실과 다른 결말을 예시로 쓰면 판정 문구 규약 위반이다 |
| "전체 198개 질의·45개 홀드아웃에서도 일관" 이라는 통계 확장 문장 | **문구는 verdicts.yaml 의 것만** — "일관" 은 판정 문구가 아니다 | §0.8 · PLAN-086 ③(주장 크기의 과잉) |

---

## 1. 진단 — 세 원천 (v2 · 실측 보강)

원고 §3–§5(국문 40,652자 · 본문의 61%)를 다시 읽고, 이번에는 `scripts/skim_outline.py`(첨부)로 **제목과 그림만 남긴 원고**를 뽑아 대조하였다.

**① 순서 — 규칙이 인스턴스보다 앞선다.** §3.0 은 첫 다섯 문단에서 신조어 일곱(평가 에피소드·주입 결함·실제 델타·홀드아웃·통제된 자원 교체·자격 있는 델타·사전 적격심사)을 연속 정의하고, 어느 것에도 "예컨대 이런 트리플" 이 없다. §3.5 는 승인식을 먼저 놓고 항을 해설한다. 청구항 하나·인용 하나·전문가 하나가 트리플로 등장하는 곳이 본문 어디에도 없다.

**② 밀도 — 판정에 걸리지 않는 수치가 본문에 산다.** 본문 표 9개 가운데 판정 문장이 직접 참조하는 것은 넷(표 2 에피소드 지위 · 표 4 EP3 · 표 5 의 판정 3행 × 2패널 · 표 8 결정 안정성)이다. 표 1(DSR 단계)·표 3(CQ 계수)·표 6(하위집단·절제 17행 중 5행)·표 7(Brick 10판정)·표 9(결손 여섯)는 근거의 **명세**이지 주장의 **근거**가 아니며, 산문에도 계수(105,588 · 924,814 · 2,534/2,321/2,211/584 · 40,552→4,034 · 124초/12.5초/1.6초 …)가 판정과 무관하게 흩어져 있다. 독자는 어느 수가 결론을 지탱하는지 가려낼 수 없다.

**③ 골격 — 제목과 그림이 논문을 말하지 않는다.** `skim_outline.py --check` 실측(현행 원고):

| 검사 | 결과 |
|---|---|
| K1 · §3–§5 의 ## 절에 그림 또는 예시 박스가 있는가 | **20개 절 중 15개 없음** — §3 서두·3.2·3.3·3.4·3.6·4.1–4.6 전부·5.1·5.2·5.4·5.5 |
| K2 · 그림 캡션 첫 문장이 주장인가 | 그림 1 "연구 개요도." · 그림 2 "층간 지표 불일치." · 그림 7 "시스템 × 지표." — **라벨이지 주장이 아님** |
| K3 · 절 제목이 식별자로 시작하는가 | §5.1–5.5 다섯 절 전부 "EPn ·" 로 시작 — 제목이 결과를 말하지 않음 |

같은 도구로 뽑은 스킴 경로(제목 + 캡션)를 읽으면 §4 는 "실무 절차와 실험 구성의 대응" 한 줄, §5 는 "평가 에피소드와 승인식의 대응 / 시스템 × 지표" 두 줄이 전부다. **논문의 중심 발견(형식 검증을 다 통과한 실제 변경이 검색 성능 조건에서 막혔다)이 제목·캡션 어디에도 없다.**

**④ 반복 — 판정 문단이 동결 범위를 되풀이한다.** §5.2 열두 문단 중 넷이 무엇을 동결했는가를 다른 낱말로 다시 말한다 — PLAN-086 감량 대상이며, 본 계획은 그 자리를 예시·그림 예산으로 쓴다.

> **독자 모델.** 기술경영 박사과정 독자는 특허 실무(거절이유·인용·청구항)와 R&D 관리에 익숙하고, RDF 트리플·SHACL·Recall@K·비열등성 검정에는 낯설다. 예시의 출발점은 **거절이유통지서 한 장**이고, 그림의 출발점은 **그 통지서가 그래프가 되는 장면**이어야 한다. 통계 세부를 기대하는 심사자는 보충자료 대응표(§3.3)로 안내한다.

---

## 2. 관통 예시 설계 — 예시 그래프 \(G_{ex}\)

### 2.1 주인공과 출처

| 항목 | 내용 | 출처 |
|---|---|---|
| 주인공 거절특허 | `pat:1020130000004` **"플라즈마 식각 종점 검출 방법"** · KR · 출원 2013-05-10 · 출원인 삼성전자 · IPC H01L 21/3065 | `data/samples/mini_graph.ttl` |
| 거절근거 · 심사관 인용 | `rejectedFor ont:Rejection_Inventiveness`(진보성) · `hasPriorArtExaminer <patent/us_US7000001>` "Prior art: plasma etch endpoint detection" | 같은 파일 |
| 실현 공정 | `realizesProcess <subprocess/plasma_etch>` (Plasma Etch ⊂ Etch) | 같은 파일 |
| 전문가 매칭 뷰 접점 | plasma_etch `requiresSkill` Endpoint Detection ← `hasSkill` 전문가 M01 · 문제 PROB_M01 "식각 챔버 파티클 증가" · 결함모드 Micro-trenching · 장비 Lam Kiyo(Etcher · Lam Research) | 같은 파일 |
| 기술예측 뷰 접점 | 같은 공정 축 위의 연도별 출원(CQ01·CQ04) · 장비 클래스 Etcher 의 국가핵심기술 지정(CQ24) | 같은 파일 |
| 지위 | **합성 A-Box, 실물 T-Box.** 공정·장비·역량 IRI 와 라벨은 SDKB A-Box 발췌(합성 아님)이고 특허 5건만 합성이다. 169 트리플 | 파일 머리말 주석 |

**왜 실물 거절특허를 쓰지 않는가.** 두 이유를 각주로 밝힌다. (a) KIPRIS Plus 자료의 공개 범위(메타데이터 공개 방침 · §6.5)와 청구항 원문의 재배포 제약. (b) 실물은 인용 3,034건·정답 2,321건 위에 있어 **한 특허의 이웃만 그려도 지면을 넘는다**. 합성 그래프는 세 뷰의 최소 완비 복제(어떤 CQ 도 공허하게 통과하지 않음 · SPEC-003)이므로 설명에 필요한 것만 있다. 실물 수치가 필요한 자리(§5.2)는 예시가 아니라 **실측 추적표**로 채운다(§2.4).

### 2.2 본문 삽입용 발췌 — "거절이유통지서 한 장 → 트리플 아홉 줄"

§3 첫머리(표 1 앞)에 들어갈 **예시 1** 박스의 초안이다. 트리플은 픽스처에서 그대로 옮겼고, 라벨은 산문으로 풀었다.

> **예시 1 · 예시 그래프.** 이하 세 장은 거절특허 하나를 따라 읽는다. 2013년 출원된 "플라즈마 식각 종점 검출 방법"(합성 사례 · 출원번호 10-2013-0000004)은 심사관이 인용한 미국 선행문헌 하나 때문에 진보성 위반으로 거절되었다. 거절이유통지서의 이 사실은 자원에서 다음 트리플로 표현된다.
>
> ```turtle
> pat:1020130000004  a  ont:Patent, ont:RejectedPatent ;
>     skos:prefLabel        "플라즈마 식각 종점 검출 방법"@ko ;
>     ont:filingDate        "2013-05-10"^^xsd:date ;
>     ont:realizesProcess   <…/subprocess/plasma_etch> ;          # 어느 공정의 발명인가
>     ont:rejectedFor       ont:Rejection_Inventiveness ;          # 무엇으로 거절되었는가
>     ont:hasPriorArtExaminer <…/patent/us_US7000001> .            # 심사관이 무엇을 들었는가
> <…/subprocess/plasma_etch>  ont:requiresSkill  <…/skill/endpoint_detection> .   # 이 공정에 필요한 역량
> <…/expert/EXP_M01>          ont:hasSkill       <…/skill/endpoint_detection> .   # 그 역량을 가진 사람
> ```
>
> 위쪽 여섯 줄은 선행기술조사 뷰가 읽는 사실이고, 아래 두 줄은 전문가 매칭 뷰가 읽는 사실이다. 두 뷰를 잇는 것은 `plasma_etch` 라는 **공정 노드 하나**이다. 이 노드가 바뀌면 두 뷰의 답이 함께 바뀐다 — 3장이 설명하는 구조와 4·5장이 검사하는 위험이 모두 이 한 노드 위에 있다.

이 발췌는 손으로 관리하지 않는다. `scripts/gen_example_excerpt.py`(§8 ①)가 픽스처에서 지정 트리플을 추출하여 `paper/tables/example_1_graph.md` 를 생성하고, 원고는 그것을 조립 단계에서 포함한다 — 그래야 픽스처가 바뀌어도 예시가 낡지 않는다(서명 검사와 같은 원리).

### 2.3 예시 델타 두 개와 실행 결과 — "예시가 실행된다"

첨부 스크립트 `scripts/example_delta_demo.py` 는 예시 그래프에 CQ 31개 전량을 실행하고, 델타를 적용한 뒤 다시 실행하여 §3.5 의 v2 판정(존재검사 ∧ 극성별 분포검사 · τ = 0.05)을 그대로 적용한다. 실행 결과(2026-09-01 · rdflib 7.6):

**Δ_ex-A · 동의어 오병합(F11 축소판)** — 그림 7 의 첫 칸 — "회수율을 올리려는 관리자가 상위 공정 Etch 와 하위 공정 Plasma Etch 를 하나의 개념으로 합친다"(9 트리플 변경 · 169→168).

| 관찰 층 | 결과 | 읽는 법 |
|---|---|---|
| **L1 (SHACL · delta shape)** | **conforms = True** — 실측(`make example-gate` · 2026-09-02) | 예상대로다: "합치기"는 구조 제약을 위반하지 않는다 |
| **L2 (HermiT 추론기)** | **consistent = True** — 실측(같은 실행) | 논리 층도 침묵한다. T-Box 에 서로소·카디널리티 공리가 사실상 없기 때문이며, 이는 §5.4 가 이미 보고한 조건과 같다 |
| **L3 (선행기술조사 CQ 5개)** | **5/5 통과** — 거절근거·후보 질의는 그대로이고, 개념 기준 후보 CQ16 1→2행 · 수출통제 노출 CQ26 3→6행으로 **응답이 늘어난다** | 주 태스크 쪽에서 보면 개선이다 |
| **T3 (다른 태스크·공유 코어 CQ 23개)** | **22/23 통과 · CQ21(공정 계층 포트폴리오) 2→1행 회귀 → T3 실패** · 전문가 매칭 CQ11 은 1→4행으로 부풀지만 극성이 `up` 이어서 통과 | 한 뷰의 개선이 다른 뷰의 계층 질문을 무너뜨린다. CQ11 의 부풀림을 못 잡는 것은 §5.4 가 보고한 극성 검사의 한계 그대로다 |

**Δ_ex-B · 전문가 사례–결함모드 재배치(F14 축소판)** — 사례 CASE_M01 의 결함모드 링크 하나를 다른 노드로 옮긴다(1 트리플 · 간선 수 보존).

| 관찰 층 | 결과 |
|---|---|
| **L1 (SHACL · delta shape)** | **conforms = True** — 실측(`make example-gate` · 2026-09-02) |
| **L2 (HermiT 추론기)** | **consistent = True** — 실측(같은 실행). 타입 서명이 어긋나는데도 통과한다: T-Box 에 서로소 공리가 없어 논리 층이 이 어긋남을 볼 수 없다(§5.4 의 관측과 같은 이유). **예상이 아니라 실측이다** |
| **L3 (선행기술조사 CQ 5개)** | **5/5 통과** |
| **T3** | **CQ28(특허–결함모드–전문가) 1→0행 → 존재검사 실패** — §5.4 실측 F14→CQ28 과 **같은 CQ 가 걸린다** |

> **`cq_runner` 를 단독으로 돌리면 델타 A 의 회귀가 보이지 않는다 (2026-09-02 실측).** 그 CLI 는
> 기준선을 받지 않아 **v1(존재검사)만** 적용하고, CQ21 은 2→1 이어도 `1 ≥ 1` 이므로 통과로 읽힌다
> (실측: 델타 A 28/28 통과 · 델타 B 27/28 — CQ28 이 0행이라 존재검사에서 걸린다). v2 의 분포검사는
> 기준선을 요구하며 저장소에서는 `t3_cross_task_cq --rule v2` 가 **세대 라벨**로 수행하는데, 그
> 아티팩트(`data/cq_generations/`)는 git 추적 대상이자 표 6.6 의 입력이므로 예시용 세대를 만들지 않는다.
>
> 그래서 `make example-gate` 는 CQ 층을 `example_delta_demo.py --engine repo` 로 돌린다 — **행 수는
> `cq_runner.run_cqs`(oxigraph)가, 판정은 `CQResult.judge(base_rows)`(v2)가** 낸다. 규칙이 두 곳에
> 살지 않으므로 저장소의 판정식이 바뀌면 예시도 함께 바뀐다. 두 경로(rdflib · 저장소 엔진)의 행 수는
> `make example-both` 가 대조하며, 2026-09-02 실측에서 세 델타 전량 일치하였다(사이드카 CQ 3개 제외).

두 델타는 §5.4 의 실측 결함군(F11·F14)과 회귀 CQ 를 공유하므로, 본문에서 "예시에서 이렇게 걸린 것이 홀드아웃 45건에서는 12건 T3 단독 검출로 나타났다" 로 **예시 → 실측**을 잇는 문장이 성립한다. 전량 표는 `make example-delta` 가 `paper/tables/example_delta_*.md` 로 생성한다.

**Δ_real · 실제 EP3 델타(§5.2)** — 예시가 아니라 실물이다. 상류 교정 `d578bf3→2839afb`(개념 사전 도입 · 문서당 개념 1.545→3.779 · `skos:broader` 11→18). 본문에서는 이것을 예시 특허 위에 **정성적으로** 얹는다: "예시 특허의 청구항 문장 '플라즈마 식각 챔버 내 종점을 검출하는…' 은 교정 전에는 개념 하나(plasma_etch)에, 교정 후에는 사전이 인식한 표면형을 통해 개념 셋 안팎에 연결된다. 자원 쪽에서 보면 개선이다. 그런데 표 4 가 보이듯…". 수치는 표 4 의 것만 쓴다.

### 2.4 실측 단일 질의 추적표 — 표 4 의 평균을 한 질의에서 읽기

표 4 의 −0.0293 은 198 질의의 평균이다. 독자는 "한 질의에서 무슨 일이 있었는가" 를 보아야 평균의 뜻을 안다. 첨부 `scripts/trace_query.py` 는 O·O′ 두 실행의 순위 파일(TREC run)과 봉인 qrel 을 읽어, 지정 규칙(기본 `drop` — 부차 구성 P1 에서 순위가 가장 밀린 질의)으로 질의 하나를 고르고 시스템별 정답 문헌의 순위와 그 질의의 R@100 을 표로 낸다(패밀리 단위 · §3.3). 산출 예(합성 시험 자료):

| 시스템 | 조건 | 정답 문헌의 순위 | R@100 (이 질의) |
|---|---|---|---:|
| B3 텍스트 | O | F130: 157위 · F32: 251위 | 0.00 |
| P1 | O | F130: 285위 · F32: **5위** | 0.50 |
| P1 | O′ | F130: 285위 · F32: **250위** | 0.00 |

**지위.** 탐색적 기술이며 판정을 만들지 않는다. 본문 §5.2 에는 표 하나와 두 문장(그리고 그림 9 하단 띠)만 두고, 선택 규칙·질의 식별자·다른 규칙(`gain`)의 표는 S9 로 보낸다. 질의 식별자는 봉인 qrel 의 키이므로 공개 가능 범위를 §6.5 규칙에 따라 확인한다(가명 `Q-test-###` 권고).

---

---

## 3. 본문·보충자료 분리 — "판정이 참조하는 수치만 본문에"

### 3.1 잔류 규칙 (기계 검사 가능하게 쓴다)

본문에 남는 수치는 다음 넷 중 하나에 해당해야 한다. 그 외는 전부 보충자료로 내리고, 본문에는 **판정 한 문장 + 포인터**만 남긴다.

| 잔류 조건 | 예 |
|---|---|
| R1 · `verdicts.yaml` 의 판정 문장이 직접 참조하는 값 | ΔR@100 −0.0293 · CI [−0.0542, −0.0053] · T3 단독 12/45 · 위양성 0/27 · McNemar p=.0001 |
| R2 · 승인식의 임계와 그 임계가 걸린 실측값 | ε=0.02 · δ=0.05 · τ=0.05 · 하위집단 최대 하락 0.0401 |
| R3 · 주장 범위를 한정하는 계수 하나 | "자격 있는 실제 델타 1건" · "확증 분할 198질의" · "홀드아웃 결함 45건" |
| R4 · 예시 그래프의 값 (합성 · 이해용) | CQ21 2→1행 · 예시 특허 R@100 = 1 또는 0 |

### 3.2 표 아홉의 처분

| 표 | 지금 | 처분 | 본문 잔류 문장(초안) |
|---|---|---|---|
| 표 1 DSR 단계 | §3 서두 | **그림 1(개요도)에 흡수** — 개요도의 아래 띠를 DSR 단계 순환으로 | (그림 캡션이 대신) |
| 표 2 에피소드 | §3 서두 | **잔류 · 열 축소** — "묻는 것 / 판정 / 지위 / 결과" 넷만. 판정 방식 열의 장문은 S6 | — |
| 표 3 CQ 31 구분 | §3.4 | **S1 이관** | "게이트가 관찰하는 역량질문은 28개이고, 선행기술조사 5개는 기능 층(L3)이, 다른 태스크·공유 코어 23개는 교차 태스크 조건(T3)이 관찰한다(전량 S1)." |
| 표 4 EP3 | §5.2 | **잔류 · 그림 8 과 병치** | — |
| 표 5 2패널 | §5.3.1 | **잔류 · 행 축소** — 판정 구성 3개 × 2패널 = 6행 · 탐색적 기준선 2종(B★·B10) 4행은 S9 · 승/패/동 열 S9 | "탐색적 기준선 둘의 값은 S9 에 있으며 판정에 들어가지 않는다." |
| 표 6 하위집단·절제 | §5.3.2 | **S9 이관** · 본문은 판정 두 문장 | "계층 특이성 점검은 첫 분할에서 기각되었고(전문가 매칭 전용 계층 제거가 Holm 보정 후 유일하게 유의 · +0.0316) 두 번째 분할에서 재현되지 않았다(0.0000). 이득은 예상과 반대로 어휘가 이미 겹치는 질의에 집중되었다(하위집단 전량 S9)." |
| 표 7 Brick 10판정 | §5.5 | **S8 이관** · 본문은 그림 10 의 한 칸 + 문장 | "인접 릴리스 다섯 쌍 열 판정 가운데 부분 승인은 둘이고, 통과한 쌍은 사전등록이 음성 대조군으로 지목한 v1.4.1→v1.4.2 이다(전량 S8)." |
| 표 8 결정 안정성 | §6.3 | **잔류** — 교훈의 근거 (R1·R2) | — |
| 표 9 결손 여섯 | §6.4 | **S3 이관** · 본문은 결손 여섯의 **이름만** 한 문장 | "결손 여섯 — 마진 미보정 · 하위집단 구간 미추정 · 자격 있는 델타 1건 · 전문가 관련성 미판정 · T4 판정 1회 · 이식 1건 — 의 해소 명세는 전부 새 사전등록의 대상이다(S3)." |

산문의 계수도 같은 규칙으로 거른다. §3.2 의 트리플·특허 계수는 관측 사실 인용 블록 하나로 압축하고 나머지는 S5 로, §3.3 의 분모 넷은 "분모가 넷이므로 혼용하지 않는다" 와 등급 정의만 남기고 값은 S5 로, §3.6·§5.5 의 실행시간·메모리는 S8 로 보낸다.

### 3.3 주장–근거–자리 대응표 (본문 §4.5 끝 · 표 1장 신설)

통계 세부를 본문에서 찾을 심사자를 위해, **본문이 하는 주장 → 판정 문구 → 실측 수치가 있는 자리**를 한 표로 둔다. S6(사전등록 대응표)의 요약판이며 본문의 유일한 "찾아가기" 표다.

| 주장(본문) | 판정 문구(verdicts.yaml) | 근거의 자리 |
|---|---|---|
| 형식 검증을 다 통과한 실제 변경이 T1 에서 거부되었다 | Accept = 0 · T1 불충족 | 표 4 · S7 · S9 |
| T3 은 주 태스크 감시가 통과시키는 교차 태스크 회귀를 검출한다 | 세 조건 충족 (12/45 · p=.0001 · 0/27) | §5.4 · S2 |
| 검색 유용성 점검 | 분할 A 부분 지지(주 지표 한정) · 분할 B 미지지 | 표 5 · S9 |
| 계층 특이성 점검 | 분할 A 기각 · 분할 B 재현되지 않음 | S9 |
| 전달 점검(T4) | 비열등 미달 · 판정 1회 | S5 부록 A |
| 이식 판정 | 절차 이전 · 결함 명세 미이전 · 정밀 검증 미달 | S8 |

---

## 4. 그림 프로그램 — "제목 + 캡션만 이어 읽어도 논문이 된다"

### 4.1 원칙 넷

| | 원칙 | 검사 |
|---|---|---|
| F1 | §3–§5 의 모든 ## 절에 **그림 또는 예시 박스 1개 이상** | `skim_outline.py --check` K1 |
| F2 | 캡션의 **첫 문장은 주장**("무엇이 보이는가"), 둘째 문장부터 읽는 법 | K2 (길이 하한) · 사람이 문장 종류 확인 |
| F3 | §3–§5 의 그림은 **예시 그래프 위에** 그린다 — 노드 라벨은 예시 라벨(플라즈마 식각 · 전문가 M01 · 종점 검출 …), 실측 수치는 그림 8·9·10 에만 | `gen_figures.py` 의 라벨 소스를 `mini_graph.ttl` 로 |
| F4 | 그림 하나에 **메시지 하나** — 현행 그림 3(뷰 + 통로 + CQ 계수 세 메시지)·그림 5(대응 + 미대응 둘 + 전제 띠)는 나눈다 | 캡션 첫 문장이 한 문장으로 쓰이는가 |

### 4.2 그림 목록 (현행 7 → 11 · 번호는 등장 순서 · V6)

| 새 번호 | 자리 | 내용 | 원천 | 캡션 첫 문장(초안 · 주장형) |
|---|---|---|---|---|
| **그림 1** | §1 | **그래픽 초록** — 왼쪽: 거절이유통지서 한 장 → 트리플 아홉 줄(예시 1) · 가운데: `plasma_etch` 노드 하나에 걸린 세 뷰 · 오른쪽: 변경 → 게이트 → 판정(실제 델타는 T1 에서 정지). 아래 띠: DSR 단계(표 1 흡수) | 신규(`concept_overview.svg` 개정) | "거절특허 한 건의 사실이 세 태스크가 공유하는 노드에 모이고, 그 노드의 변경은 형식 검증이 아니라 태스크 조건에서 걸린다." |
| 그림 2 | §2.3 | 세 층(자원·검색·생성)의 지표와 어긋남 셋 — **각 층에 실측 예시값 한 개**(자원 1.5→3.8 / 검색 −0.029 / 생성: T4 미달) | 현행 개정 | "자원 층의 지표가 2.4배로 좋아지는 동안 한 층 아래의 검색 지표는 내려갔다." |
| **그림 3** | §3.1 | 예시 인스턴스 위의 세 경로 — `plasma_etch` 에서 ↑특허·인용(선행기술조사) · →역량→전문가 M01(전문가 매칭) · ↓연도별 출원(기술예측) | 현행 `concept_tbox_views` 를 예시 라벨판으로 | "세 태스크 뷰는 별개의 온톨로지가 아니라 같은 공정 노드를 서로 다른 술어로 지나는 세 질의 경로다." |
| **그림 4** | §3.2–3.3 | 거절특허 축의 술어와 등급 — 예시 특허의 청구항 1 · 심사관 인용 · 거절근거를 잇는 트리플에 **등급 1/등급 2 · 관찰 수준**을 표시 | 신규 | "정답의 등급은 법적 관련성이 아니라 그래프에서 관측된 증거의 세밀도다." |
| **그림 5** | §3.4–3.5 | 게이트 흐름 — 세 열: (a) 예시 델타 A 가 지나는 층 (L0–L3 통과 · T3 에서 정지) · (b) 예시 델타 B (T3 정지) · (c) 실제 델타 (T1 정지) | 현행 `concept_gate_flow` 에 열 추가 | "형식 검증 네 층을 통과한 변경도 태스크 조건 한 항에서 멈추며, 어느 항에서 멈추는가가 변경의 종류를 말해 준다." |
| 그림 6 | §4.1–4.2 | 예시 특허를 질의로 넣는 장면 — 청구항 1 텍스트 → 시점 필터(출원일 이전만) → 패밀리 제외 → **`hasPriorArtExaminer` 간선 제거**(누출 차단) → 채점표(qrel) | 현행 `concept_experiment_flow` 를 예시 중심으로 재구성 · 미대응 둘은 §6.2 로 | "질의 특허 자신의 인용 간선을 색인에서 지운 뒤에 남는 신호 셋만으로 후보를 정렬한다." |
| **그림 7** | §4.4 | 결함 주입 네 종의 전·후 미니 그래프 — 동의어 오병합(예시 델타 A) · 공유 계층 역전 · 허브 집중 · 사례 재배치(예시 델타 B) · 각각 어느 CQ 가 걸리는지 화살표 | 신규 (`example_delta_demo.py` 출력 기반) | "교차 결함은 검색에는 무해하거나 유리하면서 다른 태스크의 질의 행 수를 바꾸는 변경이다." |
| 그림 8 | §5 서두 | 에피소드 × 승인식 행렬 (현행 그림 6) | 현행 | "다섯 에피소드는 승인식의 서로 다른 항을 검증하였고, 실제 델타는 T1 에서, 홀드아웃 결함은 T3 에서 걸렸다." |
| **그림 9** | §5.2 | **층간 불일치의 실측** — 좌: 자원 층 막대(개념/문서 1.545→3.779 · 어휘 141→199) 상승 · 우: 검색 층 ΔR@100 점추정과 95% CI 가 −ε 선을 가로지름 · 하단 띠: 단일 질의 추적(정답 순위 이동) | 신규 (표 4 + `trace_query.py`) | "온톨로지를 고치는 사람이 보는 지표는 전부 올랐고, 조사자가 보는 지표는 신뢰구간째로 마진 아래로 내려갔다." |
| 그림 10 | §5.3 | 시스템 × 지표 (현행 그림 7 · 탐색적 기준선 제거) | 현행 축소 | "온톨로지 재순위화는 검토 깊이 100 안의 회수는 늘리지만 상위 20위의 정렬은 개선하지 않는다." |
| **그림 11** | §5.4–5.5 | **검출 지도** — 행: 홀드아웃 결함군 5종(+Brick 3종) · 열: L1·L2·L3·T3 · 칸: 검출 건수 · T3 단독 12 강조 · 표 7·표 8 의 τ 민감도는 옆 띠 | 신규 (S2·S8 데이터) | "형식 층이 침묵한 결함 45건 중 12건을 교차 태스크 조건이 단독으로 잡았고, 어느 태스크의 어느 질문이 깨졌는지까지 지목한다." |

§6 에는 그림을 두지 않는다 — 교훈 셋은 그림 9·11 을 다시 가리키는 것으로 충분하다(같은 그림을 두 번 그리지 않는다).

### 4.3 스킴 경로의 목표 상태

부록 C 가 **제목 + 캡션 첫 문장**만 이어 놓은 골격 시안이다. 검사 기준: 부록 C 만 읽은 사람이 (a) 무엇을 만들었고 (b) 무엇을 발견했고 (c) 그 발견의 범위가 어디까지인지 말할 수 있어야 한다. `make skim` 이 현행 원고에서 같은 골격을 뽑아 `paper/SKIM.md` 로 남기고, PR 마다 diff 를 검토한다.

---

## 5. 장별 재구성 — 예시 · 표 처분 · 그림을 한 표에

원칙 셋(v1 §3 유지). (i) 절의 첫 문단은 예시, 둘째 문단부터 규칙. (ii) 예시 박스는 `> **예시 N ·**` 연번. (iii) 예시 문장은 행 수로만 말하고 판정 낱말은 실측에만.

### 5.1 §3 산출물

| 자리 | 예시 | 그림·표 | 삭제·이관 |
|---|---|---|---|
| §3 서두 | **예시 1** 예시 그래프(v1 §2.2 박스) | 표 1 → 그림 1 흡수 · 표 2 열 축소 | ART/RQ/DSR 대응 세 문단 → 한 문단 |
| §3.0 델타 유형 | 유형별 예시 한 줄(T-Box/개념층/A-Box) | — | 적격심사 일곱 항 해설 → 두 문장 + S1 |
| §3.1 공유 T-Box | 예시 1 재참조 | **그림 3** 예시 라벨판 | 집합식 · 대안 비교 세 문단 → 두 문장 |
| §3.2–3.3 계보·등급 | "예시 특허의 인용은 등급 1" | **그림 4** 신규 | 술어 열거 · 계수 열거 · 분모 넷 값 → S5 |
| §3.4 게이트 절차 | **예시 2** 델타 A 가 네 층을 지나는 장면(형식 층 결과는 `make gate` 실행 후 기입) | 표 3 → S1 | 구조 층 두 겹·논리 층 사영 문단 → 각 두 문장 |
| §3.5 승인식 | 첫 문단 "예시 델타 A 는 T3 항이 0 · 실제 델타는 T1 항이 0" → 둘째 문단 수식 | **그림 5** 세 열 | T2 보수성 문단 → S5 |
| §3.5.1 T4 | — | — | 유지(짧다) |
| §3.6 CI 운영 | — | 그림 5 참조 | 비가시 세 상태 → S1 · 실행시간 → S8 |

### 5.2 §4 평가 설계

| 자리 | 예시 | 그림·표 | 삭제·이관 |
|---|---|---|---|
| §4 서두 | 절마다 예시 그래프의 지위(시험 대상/필터 입력/특징) | — | 유지 |
| §4.1–4.2 | **예시 3** 정답 비참조: 예시 1 의 `hasPriorArtExaminer` 줄을 지운다 | **그림 6** | 분할 계수 열거 → 캡션 |
| §4.3 | "묻는 질문 → 구성" 표(B3/B5/P0/P1) · 예시 청구항 1 을 각 구성이 받는 입력 | (표 · 잔류) | 대조군 역할 문단 → S5 |
| §4.4 | 예시 델타 A·B 를 결함군에 대응 | **그림 7** | 108 인스턴스 개정 경위 → S2 |
| §4.5 | "묻는 질문 → 지표 → 예시값" 표 · ε 의 직관 환산(≈ 198 질의 중 넷) | **주장–근거–자리 표**(§3.3) | 정밀도/bpref 미사용 근거 · 3-gram 경계 → S5/S9 |
| §4.6 | — | — | 유지 · 계수는 S8 |

### 5.3 §5 평가 결과

| 자리 | 예시 | 그림·표 | 삭제·이관 |
|---|---|---|---|
| §5 서두 | "각 절은 예시 특허에서 시작하여 전체로 넓힌다" | **그림 8** | 유지 |
| §5.1 | "예시 그래프의 사이드카 셋은 0행 — 청구항 한정요소가 없다. 실물은 28 중 27" | — (짧은 절 · 예시 문장으로 K1 충족) | 언어별 보유율 → S5 · §5.3.3 참조만 |
| **§5.2** | 둘째 문단 = 실제 델타를 예시 특허 위에 정성 서술 · 셋째 = 단일 질의 추적표 | **그림 9** + 표 4 | 동결 반복 넷 → 하나 (PLAN-086 트랙 B) |
| §5.3 | "표 5 의 각 값은 §4.5 예시가 정의한 질의별 0/1 의 평균" | 표 5 축소 · **그림 10** · 표 6 → S9 | §5.3.2 하위집단 서술 → 판정 두 문장 · §5.3.3 → 세 문장 |
| §5.4 | "예시 델타 B 가 걸린 CQ28 이 홀드아웃에서도 F14 의 회귀 지점" | **그림 11** | 식별자 나열 문장 · 홀드아웃 구성 두 축 상세 → S2 |
| §5.5 | "예시 델타 B 의 방향 문제와 같은 종류(`hasPart`/`isPartOf`)" | 그림 11 의 Brick 행 · 표 7 → S8 | 실행시간 · 관찰면 정의 상세 → S8 |

### 5.4 분량 예산 (국문 자수 · 표는 자수에 포함)

| | 삽입 | 삭제·이관 |
|---|---|---|
| §3 | 예시 1·2 ≈ 1,400 · 그림 캡션 3 ≈ 600 | 표 1·3 −1,300 · 정의·대안·열거 압축 −2,400 · 비가시·실행시간 −700 |
| §4 | 예시 3 ≈ 400 · 표 2장(구성·지표) ≈ 700 · 캡션 2 ≈ 400 | 대조군·통계 근거·경계 −1,100 · 108 경위 −400 |
| §5 | Δ_real 서술·추적표 ≈ 900 · 캡션 3 ≈ 600 | 표 6·7 −2,300 · 표 5 축소 −600 · 동결 반복 −1,400 · 하위집단·교차언어 서술 −900 · 식별자·상세 −700 |
| §6 | — | 표 9 −1,000 |
| **합계** | **≈ +5,000** | **≈ −12,800** |

순감 약 7,800자(현행 66,984 → ≈ 59,000 · PLAN-086 권고 0.624 = 71,400 을 넉넉히 하회). 그림은 4개 늘지만 표는 5개 줄어 **시각 객체 수는 16 → 15** 로 같은 규모다.

---

## 6. 절 서술 템플릿과 제목 재작성

### 6.1 절 첫 문단의 다섯 자리 (STYLE V4 확장)

§3–§5 의 모든 ## 절은 첫 문단을 다음 순서로 쓴다. 자리가 비면 비운 채 두고 순서는 바꾸지 않는다.

1. **결론 한 문장** (이 절이 말하려는 것 · 판정이 있으면 판정 문구 그대로)
2. **예시 한 문장** (예시 그래프에서 그것이 어떻게 보이는가)
3. **그림 지시** ("그림 N 이 이 장면이다")
4. **범위 한 문장** (이 절의 값이 지지하는 주장의 경계)
5. **포인터** (전량이 있는 보충자료)

§5.2 에 적용한 시안:

> 형식 검증 네 층을 전부 통과하고 자원 지표를 전부 향상시킨 실제 변경 하나를 검색 성능 조건 T1 이 거부하였다(Accept = 0). 예시 특허의 청구항 문장은 이 변경으로 개념 하나가 아니라 셋 안팎에 연결되었으나, 그 특허를 질의로 넣었을 때 정답 문헌이 더 위로 올라온다는 보장은 없었고 198 질의의 평균은 내려갔다. 그림 9 가 이 장면이다 — 왼쪽의 자원 지표는 전부 오르고 오른쪽의 검색 지표는 신뢰구간째로 마진 아래에 있다. 이 진술의 범위는 자격을 갖춘 실제 변경 1건이며 차단된 사고의 빈도를 말하지 않는다. 동결 범위·적격심사·재현 확인의 전문은 S7·S9 에 있다.

### 6.2 제목 재작성 — 식별자 선행에서 주장형으로

| 현행 | 제안 |
|---|---|
| 3. 산출물 — SDKB 데이터셋과 T-gate | 3. 산출물 — 거절특허 한 건에서 시작하는 예시로 읽는 데이터셋과 게이트 |
| 3.1 공유 T-Box와 세 태스크 뷰 | 3.1 세 태스크는 같은 공정 노드를 서로 다른 술어로 지난다 |
| 3.4 검증 게이트의 전체 절차 | 3.4 변경은 형식 층 넷을 지나 태스크 조건 셋에 이른다 |
| 3.5 T-gate 승인 규칙 | 3.5 승인은 네 형식 층과 세 태스크 조건의 곱이다 |
| 4.2 후보 모집단과 누출 차단 | 4.2 질의 특허 자신의 인용은 색인에서 지운다 |
| 4.4 결함 주입과 절제 조건 | 4.4 게이트가 잡아야 하는 것: 검색에 무해하면서 다른 태스크를 깨는 변경 |
| 5.1 EP1 · 표현 감사 — 세 태스크 어휘의 자원 내 존재 | 5.1 세 태스크의 어휘는 자원에 실재한다 (EP1) |
| 5.2 EP3 · 통제된 자원 교체 — 게이트의 실제 거부 사례 | 5.2 형식 검증을 다 통과한 실제 변경을 검색 성능 조건이 막았다 (EP3) |
| 5.3 EP4 · 층간 지표 불일치의 실측과 검색 이득의 경계 | 5.3 검색 이득은 깊은 회수에만 있고 상위 정렬에는 없다 (EP4) |
| 5.4 EP2 · 게이트 판별력 — 홀드아웃 산출물 평가 | 5.4 교차 태스크 조건은 형식 층이 놓친 결함을 단독으로 잡는다 (EP2) |
| 5.5 EP5 · 제2 공학 온톨로지로의 이식 판정 | 5.5 절차는 이식되었고 결함 명세는 이식되지 않았다 (EP5) |

에피소드 식별자는 괄호로 뒤에 둔다 — 사전등록 대응(S6)과 그림 8 의 행 라벨이 EP 를 쓰므로 버리지 않는다. 제목의 판정 낱말은 `verdicts.yaml` 문구 범위 안에서만 고른다(§0.8).

---

## 7. 예시·그림 외의 이해 용이성 장치 (v1 유지 · 하나 추가)

① 세 층의 이름(자원·검색·생성)을 §1 에서 한 번 주고 그림 2 로 고정. ② 지표는 "묻는 질문 → 지표 → 예시값" 표 하나. ③ 승인식은 "예 → 말 → 식" 순서. **④ 신설 — 장 첫머리의 한 문단 요약.** §3·§4·§5 의 첫 문단을 "이 장의 그림 N–M 을 차례로 보면…" 으로 시작하여 스킴 경로와 본문을 잇는다.

---

## 8. 구현 계획 (저장소)

| # | 산출물 | 내용 | 검사 |
|---|---|---|---|
| ① | `scripts/gen_example_excerpt.py` | 픽스처 → `paper/tables/example_1_graph.md` (예시 1 Turtle 발췌) | `tables-stability-check` 편입 |
| ② | `scripts/example_delta_demo.py` (첨부 · 실행 확인) | Δ_ex-A/B 전후 CQ 행 수·v2 판정 표 · `--dump` 로 변형 픽스처 저장 → **기존 `make gate` 로 L0–L3 실행**하여 예시 2 의 형식 층 칸을 채움 | `make example-delta` · 회귀 CQ 집합 `{CQ21}`·`{CQ28}` 단언 테스트 |
| ③ | `scripts/trace_query.py` (첨부 · 합성 자료 확인) | O/O′ 순위 + 봉인 qrel → 단일 질의 추적표 (그림 9 하단 띠 · §5.2 표) | 로컬 실행 · 열람 원장 기록 1건 |
| ④ | **`scripts/skim_outline.py` (첨부 · 현행 원고 실측)** | 제목·캡션·예시 첫 문장 → `paper/SKIM.md` · K1–K3 | **`make skim` 신설** · CI 에서 `--check`(K1 위반 시 실패) |
| ⑤ | `scripts/style_check.py` | 예시 박스 연번(V6 준용) · 예시 박스 안 `VERDICT_LITERALS` 경고 · **본문 수치 잔류 규칙(§3.1) 보조 검사**: 표·수식·캡션 밖 산문의 4자리 이상 정수·소수 토큰 수를 절별로 계수하여 상한 초과 시 경고(임계는 재구성 후 실측으로 정함) | `make style-check` |
| ⑥ | `paper/glossary-terms.yaml` | 관통 예시 · 예시 그래프 · 스킴 경로 3항 | `glossary-check` |
| ⑦ | `scripts/gen_figures.py` | 그림 1·3·5·6 개정 + 그림 4·7·9·11 신규 · 라벨 소스 `mini_graph.ttl` · 수치 소스는 기존 `figures/data/` | 그림 번호 재배열 후 `style-check`(V6) |
| ⑧ | 보충자료 | 표 3→S1 · 표 6→S9 · 표 7→S8 · 표 9→S3 · 산문 계수 → S5/S8 · 각 이관에 "본문 §x 에서 이동" 머리말 | `check_supplementary_en` · S0 색인 갱신 |
| ⑨ | `verdicts.yaml` · `check_verdicts.py` | 변경 없음 · 실행 확인 | `make verdicts` |
| ⑩ | 영문 파생본 | 국문 확정 후 (PLAN-086 D 층 절차) | 단어수 상한 17,500 |

**순서.** ④·⑤·⑥(검사기·용어) → ①·②·③(생성물) → ⑧(표 이관 · PLAN-086 트랙 B 와 합류) → §3 → §4 → §5 → §1·§2·§6 제목·그림 → ⑦ → ⑩. 장마다 `make style-check glossary-check sig-check verdicts submission-check skim`.

**DoD.** (a) K1 위반 0 · K2·K3 경고 0 · (b) 예시 1·2·3 연번 존재 · 세 박스의 수치 전부 생성물 출처 · (c) 본문 표 4개(2·4·5·8) + 구성표·지표표·대응표 3개 · (d) 국문 자수 ≤ 60,000(목표) · (e) 검사기 전부 통과 · `verdicts.yaml` diff 없음 · (f) 부록 C 기준 스킴 테스트를 저자 외 1인이 수행하여 (a)(b)(c) 세 질문에 답함.

---

## 9. 위험

| 위험 | 완화 |
|---|---|
| 합성 예시를 실측으로 오독 | 박스 첫 문장 "합성 사례" · 각주 두 이유 · 실측 수치는 박스 밖 표·그림 8–11 에만 |
| 예시 델타 결과와 실측 판정의 혼동 | 예시는 행 수로만 · 판정 낱말은 실측에만 · style_check 경고 |
| 표를 내린 뒤 심사자가 근거를 못 찾음 | §3.3 주장–근거–자리 대응표 · 잔류 문장에 포인터 필수 · S0 색인 |
| 그림이 늘어 제작 부담 증가 | 4개 신규 중 3개(7·9·11)는 이미 있는 데이터(S2·표 4·S8)의 시각화 · `gen_figures.py` 파라미터화 |
| 주장형 제목이 과잉 주장으로 읽힘 | 제목의 판정 낱말은 `verdicts.yaml` 범위 · 범위 한정("1건" · "홀드아웃") 은 첫 문단 4번 자리에서 |
| 픽스처 변경으로 예시 서사 붕괴 | ② 단언 테스트 |
| CQ11 부풀림 미검출이 약점으로 읽힘 | 그대로 쓴다 — §5.4 가 보고한 극성 검사 한계의 예시이며 정직성에 유리 |
| PLAN-086 과 동시 편집 충돌 | 표 이관 목록(§3.2)을 PLAN-086 트랙 B 입력으로 먼저 넘기고, 본 계획은 그 종료 후 착수 |

---

## 10. 결정 요청 (여덟)

1. **예시 사례** — 픽스처 합성 거절특허(권고) vs 실물 익명화.
2. **예시 델타 A** — Etch↔Plasma Etch 병합(권고 · 실행 확인) vs 증착·식각 통합(픽스처 확장 필요).
3. **단일 질의 추적표** — §5.2 본문 표 + 그림 9 띠(권고) vs S9 전량.
4. **표 처분** — §3.2 의 처분안 승인 여부(특히 표 6·표 7 이관 · 표 5 축소).
5. **그림 프로그램** — 11개 안(권고) vs 현행 7개 + 예시 박스만 · 그래픽 초록(그림 1) 신설 여부.
6. **제목 재작성** — 주장형 제목 채택 여부 · EP 식별자를 괄호 후치로.
7. **본문 자수 목표** — ≤ 60,000(권고) vs PLAN-086 권고선 유지.
8. **착수 시점** — PLAN-086 종료 후(권고) · 표 이관 목록만 선행 인계.

---

## 부록 A. `example_delta_demo.py` 실행 결과 전량 (Δ_ex-A · 2026-09-01)

| CQ | 스위트 | 관찰 층 | 극성 | 행 수(전) | 행 수(후) | v2 판정 |
|---|---|---|---|---:|---:|---|
| CQ01 | core | T3 | up | 3 | 5 | 통과 |
| CQ02 | tf | T3 | up | 2 | 3 | 통과 |
| CQ03 | tf | T3 | down | 1 | 1 | 통과 |
| CQ04 | tf | T3 | up | 6 | 18 | 통과 |
| CQ05 | tf | T3 | up | 5 | 5 | 통과 |
| CQ06 | tf | T3 | down | 2 | 1 | 통과 |
| CQ07 | core | T3 | up | 1 | 2 | 통과 |
| CQ08 | core | T3 | up | 2 | 2 | 통과 |
| CQ09 | pa | L3 | up | 1 | 1 | 통과 |
| CQ10 | pa | L3 | up | 1 | 1 | 통과 |
| CQ11 | em | T3 | up | 1 | 4 | 통과 (부풀림 · 극성상 미검출) |
| CQ12 | em | T3 | up | 1 | 2 | 통과 |
| CQ13 | core | T3 | up | 1 | 1 | 통과 |
| CQ14 | core | T3 | up | 1 | 1 | 통과 |
| CQ15 | core | T3 | up | 1 | 2 | 통과 |
| CQ16 | pa | L3 | up | 1 | 2 | 통과 |
| CQ17 | em | T3 | up | 1 | 1 | 통과 |
| CQ18 | em | T3 | up | 1 | 1 | 통과 |
| CQ19 | core | T3 | up | 2 | 4 | 통과 |
| CQ20 | em | T3 | up | 2 | 2 | 통과 |
| **CQ21** | **core** | **T3** | up | **2** | **1** | **실패 · 분포검사 실패(회귀)** |
| CQ22 | core | T3 | up | 2 | 2 | 통과 |
| CQ23 | core | T3 | up | 4 | 4 | 통과 |
| CQ24 | core | T3 | up | 2 | 2 | 통과 |
| CQ25 | core | T3 | up | 2 | 2 | 통과 |
| CQ26 | pa | L3 | up | 3 | 6 | 통과 |
| CQ27 | pa | L3 | up | 1 | 1 | 통과 |
| CQ28 | em | T3 | up | 1 | 1 | 통과 |
| CQ29–31 | pa | 사이드카(분모 제외) | up | 0 | 0 | 픽스처에 청구항 한정요소 없음 |

**요약** — L3(pa): 통과 · **T3(em·tf·core): 실패**(회귀 CQ: core 1 · em 0 · tf 0). Δ_ex-B 는 CQ28 만 1→0 (em 1) · 나머지 전부 불변.

## 부록 B. 예시 그래프에서 세 뷰의 대표 CQ 가 내는 답 (기준 상태)

| 뷰 | CQ | 질문(풀어 쓴 것) | 답(행) |
|---|---|---|---|
| 선행기술조사 | CQ09 | 어느 거절특허가 어떤 근거로 어떤 선행기술 때문에 거절되었는가 | 플라즈마 식각 종점 검출 방법 · 진보성 · 인용 1 · 심사관 인용 1 |
| 전문가 매칭 | CQ11 | 공정이 요구하는 역량을 실제로 가진 전문가는 누구인가 | Plasma Etch · Endpoint Detection · 전문가 M01 |
| 전문가 매칭 | CQ28 | 결함모드를 다룬 특허와 그 결함모드를 다뤄 본 전문가 | Micro-trenching · 식각 챔버 플라즈마 균일도 제어 장치 · 전문가 M01 |
| 공유 코어 | CQ21 | 상위 공정별 하위 공정과 특허 수 | Etch→Plasma Etch 3건 · Lithography→EUV Lithography 1건 |

---

## 부록 C. 스킴 골격 시안 — 제목 + 캡션 첫 문장만 (검사 기준: 이것만 읽고 논문을 말할 수 있는가)

```
1. 서론
  [그림 1] 거절특허 한 건의 사실이 세 태스크가 공유하는 노드에 모이고, 그 노드의 변경은
           형식 검증이 아니라 태스크 조건에서 걸린다.
2. 배경과 연구 공백
  2.3 자원 지표가 태스크 성능을 대표하기 위한 조건
  [그림 2] 자원 층의 지표가 2.4배로 좋아지는 동안 한 층 아래의 검색 지표는 내려갔다.
3. 산출물 — 거절특허 한 건에서 시작하는 예시로 읽는 데이터셋과 게이트
  [예시 1 · 예시 그래프] 2013년 출원 "플라즈마 식각 종점 검출 방법"은 미국 선행문헌 하나로
           진보성 거절되었다 — 그 사실이 트리플 아홉 줄이다.
  3.1 세 태스크는 같은 공정 노드를 서로 다른 술어로 지난다
  [그림 3] 세 태스크 뷰는 별개의 온톨로지가 아니라 같은 공정 노드를 지나는 세 질의 경로다.
  3.2–3.3 거절특허 축과 약한 정답의 등급
  [그림 4] 정답의 등급은 법적 관련성이 아니라 그래프에서 관측된 증거의 세밀도다.
  3.4 변경은 형식 층 넷을 지나 태스크 조건 셋에 이른다
  [예시 2 · 델타 A] Etch 와 Plasma Etch 를 합치면 형식 층과 선행기술조사 질의는 그대로이고
           공정 계층 질의의 행이 2에서 1로 줄어 교차 태스크 조건에 걸린다.
  3.5 승인은 네 형식 층과 세 태스크 조건의 곱이다
  [그림 5] 형식 검증 네 층을 통과한 변경도 태스크 조건 한 항에서 멈추며, 어느 항에서
           멈추는가가 변경의 종류를 말해 준다.
4. 평가 설계
  4.1–4.2 질의 특허 자신의 인용은 색인에서 지운다
  [예시 3 · 정답 비참조] 예시 1 의 hasPriorArtExaminer 줄이 색인에서 지워지는 바로 그 줄이다.
  [그림 6] 질의 특허 자신의 인용 간선을 지운 뒤 남는 신호 셋만으로 후보를 정렬한다.
  4.4 게이트가 잡아야 하는 것: 검색에 무해하면서 다른 태스크를 깨는 변경
  [그림 7] 교차 결함은 검색에는 무해하거나 유리하면서 다른 태스크의 질의 행 수를 바꾸는 변경이다.
  4.5 무엇을 어떤 지표로 묻는가 — 주장·판정·근거의 자리
5. 평가 결과
  [그림 8] 다섯 에피소드는 승인식의 서로 다른 항을 검증하였고, 실제 델타는 T1 에서,
           홀드아웃 결함은 T3 에서 걸렸다.
  5.1 세 태스크의 어휘는 자원에 실재한다 (EP1)
  5.2 형식 검증을 다 통과한 실제 변경을 검색 성능 조건이 막았다 (EP3)
  [그림 9] 온톨로지를 고치는 사람이 보는 지표는 전부 올랐고, 조사자가 보는 지표는
           신뢰구간째로 마진 아래로 내려갔다.
  5.3 검색 이득은 깊은 회수에만 있고 상위 정렬에는 없다 (EP4)
  [그림 10] 온톨로지 재순위화는 검토 깊이 100 안의 회수는 늘리지만 상위 20위의 정렬은
           개선하지 않는다.
  5.4 교차 태스크 조건은 형식 층이 놓친 결함을 단독으로 잡는다 (EP2)
  [그림 11] 형식 층이 침묵한 결함 45건 중 12건을 교차 태스크 조건이 단독으로 잡았고,
           어느 태스크의 어느 질문이 깨졌는지까지 지목한다.
  5.5 절차는 이식되었고 결함 명세는 이식되지 않았다 (EP5)
6. 논의와 교훈
  6.1 층간 지표 불일치 — 그림 9 가 보인 것
  6.3 이전 가능한 설계지식 — 교훈 셋과 후속 가설 둘 (표 8)
7. 결론
```

이 골격에서 "무엇을 만들었나(그림 1·3·5) · 무엇을 발견했나(그림 9·11) · 범위는 어디까지인가(5.5 제목 · 표 8)" 가 읽히면 목표 상태다. 현행 원고의 같은 골격(`skim_outline.py` 출력)은 §4 한 줄 · §5 두 줄이며 중심 발견이 등장하지 않는다.

---

