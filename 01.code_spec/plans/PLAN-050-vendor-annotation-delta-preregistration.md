# PLAN-050 · 사전등록 — 주석 델타 스냅샷 교체와 CR-017 투영 편입

> **상태: 🔒 동결 대상.** 이 문서를 **커밋한 뒤** 파이프라인을 돌린다(CLAUDE.md §2.1 —
> 정지점은 이 하나뿐이고, 이후는 무정지 연속 실행이다).
> **결과를 본 뒤 이 문서를 고치지 않는다**(§1-3). 고쳐야 할 사실이 나오면 새 문서를 쓴다.

## 0. 왜 도는가 — 한 문장

상류 워킹트리의 스냅샷 대상 21개 중 **6개의 sha256이 바뀌었고**(전부 주석 갱신) **CR-017 투영
1개가 새로 생겼다.** §2.1의 발동 조건은 "T-Box 변경"이 아니라 **파일 sha256 변경**이므로 발동한다.

**예상은 "수치 불변"이다. 그리고 그 예상이 맞는 것이 이 실행의 산출물이다.**
D-23은 *"자원 지표가 2.4배 좋아졌는데 검색이 나빠진"* 사례였다. 이번은 그 반대편 —
*"자원 서명이 바뀌었는데 태스크가 안 움직이는"* 사례이며, 둘이 함께 있어야 **DP1(층별 검증)이
'자원 지표는 아무것도 예측하지 못한다'가 아니라 '자원 지표와 태스크 지표는 별개 층이다'** 로
읽힌다. 예상이 빗나가면 그것은 더 중요한 결과다 — **주석이 태스크를 움직였다는 뜻**이기 때문이다.

## 1. 무엇이 바뀌는가 — 사전 실측 (교체 전에 잰 값)

**상류 커밋 `0a7ff1537850264a06f9c31af0305efa86b3beb9`** (`~/Dev/sdkb`).

### 1.1 변경 6 — 전부 주석계, 서명 불변

| 파일 | 줄 델타 | 공리계 | 주석계 | 스냅샷 sha → 상류 sha |
|---|---|---:|---:|---|
| `sdkb-patent.ttl` | +36 −18 | 1* | 54 | `ebe1c301e558…` → `0a3173891462…` |
| `sdkb-commercialization.ttl` | +18 −9 | 1* | 27 | `763a4b0ed038…` → `9eaa371472df…` |
| `sdkb-foresight.ttl` | +16 −8 | 0 | 24 | `1a85936074e6…` → `ba0386e9f906…` |
| `sdkb-core.ttl` | +3 −2 | 0 | 4 | `256346fcb83b…` → `b33f0eb77581…` |
| `sdkb-rbv.ttl` | +10 −5 | 0 | 15 | `c2a260792d47…` → `09f8bfaf0507…` |
| `sdkb-governance-kr.ttl` | +4 −2 | 0 | 6 | `193136804bc7…` → `800b471717a4…` |

\* **공리계 2줄은 둘 다 `rdfs:comment`다.** 정규식이 주석 *문장 안의* `rdfs:subPropertyOf`·
`rdfs:domain` 이라는 단어를 잡은 것이며, 실물은 출원인 자인 선행기술과 심사관 인용의 구분 설명 및
*"`rdfs:domain`을 선언하지 않는다"* 는 설명문이다.

**서명은 6개 전부 불변이다** — `owl:Class`·`owl:ObjectProperty`·`owl:DatatypeProperty` 수가
하나도 움직이지 않았다. 즉 **D-12 표의 유형 ①(T-Box 델타)이 아니며, T1·T2·T3에 원리적으로
비가시**다. 그럼에도 §2.1을 정식으로 타는 이유는 §2.1이 트리거를 sha256으로 잡았기 때문이고,
여기서 예외를 만들면 **자격 있는 델타를 놓치지 않으려고 만든 장치를 스스로 무력화**한다.

### 1.2 신규 1 — CR-017 투영

`mappings/claim_features.parquet` (`2b21465cdad52a37…` · 5,610,647 B) 와 그 메타
`claim_feature_release_meta.json`. 상류 `0a7ff15` 에서 발행됐고 성공기준 ①②③④는 이미 통과했다.
**이 실행에서 이것을 읽는 코드는 아직 없다** — 편입은 스냅샷까지이며, 소비(개념 통로를 쓰는 팔)는
**새 방법이므로 §2 전체를 타는 별건**이다.

### 1.3 교체 후 스냅샷 sha256 (전량 · 이 값으로 동결한다)

| 파일 | sha256 | bytes |
|---|---|---:|
| `sdkb-core.ttl` | `b33f0eb775818d2e…` | 31,561 |
| `sdkb-patent.ttl` | `0a3173891462a193…` | 30,039 |
| `sdkb-foresight.ttl` | `ba0386e9f9061b07…` | 7,851 |
| `sdkb-core-data.ttl` | `f366a76456911360…` | 155,790 |
| `sdkb-abox-patents.ttl` | `974899fa414f7444…` | 3,437,493 |
| `sdkb-abox-b-layer-queries.ttl` | `5581e9b508fcd2c1…` | 566,831 |
| `sdkb-abox-experts-problems.ttl` | `4251b65a7032363a…` | 460,290 |
| `sdkb-abox-vendors.ttl` | `2bed199d9afc8734…` | 153,484 |
| `sdkb-abox-prior-art.ttl` | `e96d987358206f75…` | 21,675,315 |
| `sdkb-commercialization.ttl` | `9eaa371472df858c…` | 7,591 |
| `sdkb-rbv.ttl` | `09f8bfaf05073a5d…` | 6,597 |
| `sdkb-governance.ttl` | `5da48ee6a916c6cf…` | 4,659 |
| `sdkb-governance-kr.ttl` | `800b471717a4949d…` | 5,739 |
| `sdkb-governance-us-instances.ttl` | `ae2b670973a60b66…` | 7,216 |
| `sdkb-governance-kr-instances.ttl` | `8bf1086788172a35…` | 11,597 |
| `concept_mapping.json` | `cdf5fa5dc1dcc2b4…` | 417,875 |
| `abox_term_aliases.json` | `9c8bbeb2067beab4…` | 14,420 |
| `semiconductor_v0_3.json` | `9260a395a071067c…` | 400,917 |
| `schema_report.json` | `42d4bcd1c55109cf…` | 2,085 |
| **`claim_features.parquet` (신규)** | `2b21465cdad52a37…` | 5,610,647 |

## 2. 동결 사실 — 하나도 바꾸지 않는다

| | 값 |
|---|---|
| ε (비열등 마진) | **0.02** (`config.T_EPSILON`) |
| δ (하위집단 마진) | **0.05** (`config.T_DELTA`) |
| 주지표 | **family-level Recall@100** |
| 분할 | train 600 · dev 200 · test 200 · test_b 200 (`split.parquet` `f93c18d28857f630…`) |
| 봉인 qrel (A층 test) | `qrel_test_sealed.parquet` **`984f8ef3dfd3befc…`** |
| 봉인 qrel (B층) | `qrel_b_sealed.parquet` **`127a138f1c165167…`** |
| 심사관 qrel | `qrel_examiner.parquet` **`10ab67f21cc1328d…`** |
| 교체 전 코퍼스 | `ir_corpus_v09.parquet` **`83eef760ed0a8be2…`** |
| 개념 축 · 링크 | `197adc101cb60f13…` · `94395e3ea2656eb6…` |
| 검색 설정·가중치·토큰화·Dense 모델 | **전부 동결** — 하나라도 손대면 §2.1이 아니라 §2 전체다 |

## 3. 판정식 — 사전등록된 승인 규칙의 적용

`Accept = (L0=L1=L2=L3=pass) ∧ (LB₉₅(ΔR₁₀₀) > −ε) ∧ (max_s drop_s < δ) ∧ (∀f∈{em,tf,core}: pass_f(new) ≥ pass_f(old))`

**T4는 이 실행의 승인식에 들어가지 않는다**(CLAUDE.md §5 — 편입은 별도 승인 사항이며 요청된 바 없다).

## 4. 확증과 탐색의 구분 — 이 실행은 확증이 아니다

**test 분할은 이미 개봉됐다.** 그러므로 §2.1 말미의 안전장치가 그대로 적용된다.

| 무엇 | 지위 |
|---|---|
| **T1·T2·T3 판정** | **사전등록된 승인 규칙의 적용** — 이 실행의 정식 산출물 |
| ΔR₁₀₀·nDCG·하위집단 재측정값 | **탐색적** — 대장 회신과 불변 확인용 |
| H3·H4·H5 | **재확증하지 않는다.** 재확증은 신규 확증분할로만 한다 |
| CR-017 성공기준 ⑤-1·⑤-2·⑤-3 | **이 실행의 범위 밖** — 읽는 코드가 없다 |

**"수치가 같았다"를 우월성의 증거로 쓰지 않는다.** 이 실행이 답하는 질문은 하나다 —
*주석만 바뀐 자원 교체가 승인 규칙을 통과하는가, 그리고 태스크 수치를 움직이는가.*

## 5. 사전 예상 (결과를 보기 전에 적는다)

1. **코퍼스 sha256 불변** — 주석은 코퍼스 조립에 들어가지 않는다. 투영은 읽는 코드가 없다.
2. **ΔR₁₀₀ = 0.0000 · T1 통과 · T2 통과 · T3 통과 · Accept = 1.**
3. **L0–L3 통과** — 서명이 불변이므로 SHACL·HermiT·CQ가 움직일 이유가 없다.

**빗나가면 그대로 보고한다.** 특히 (1)이 빗나가면 *"주석이 코퍼스에 새어 들어가는 경로가 있다"*
는 뜻이고, 그것은 이 실행보다 중요한 발견이므로 즉시 멈추고 원인을 적는다.

## 6. 실행 절차 — 무정지

```
make vendor → corpus → index → leakage → retrieve → eval → gate → figures
```

`leakage`와 `gate`를 빼지 않는다 — 전자는 §1-4이자 T1 판정의 전제이고, 후자의 T1·T2 출력이
증거 그 자체다(§2.1).

## 7. 결과 기입란 — 실행 후에만 채운다

| | 값 |
|---|---|
| 교체 후 코퍼스 sha256 | `[실험 후 기입]` |
| 누출 감사 | `[실험 후 기입]` |
| ΔR₁₀₀ (P1, family) · 95% CI | `[실험 후 기입]` |
| T1 · T2 · T3 · Accept | `[실험 후 기입]` |
| L0–L3 | `[실험 후 기입]` |
| 예상 대비 | `[실험 후 기입]` |
