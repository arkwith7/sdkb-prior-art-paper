# PLAN-062 — 외부 검토 2차 반영 · 정의 정밀화와 주장 단위 정렬 (§2.3 파생본 절차)

## Context

투고 직전 외부 검토가 다섯(1–5)과 소수정 다섯을 지적했다. **다섯 전부 확인 결과 사실이며, 새 실험이나
수치 재산출을 요구하는 것은 하나도 없다.** 지적의 성격은 셋으로 갈린다.

| 성격 | 해당 | 실측 확인 |
|---|---|---|
| **본문이 구현보다 덜 말한다** (누락) | 1(T3–τ) · 3(bootstrap) | 코드는 검토자가 추정한 그대로다 |
| **주장 단위가 통제 설계보다 넓다** (과장) | 2(자원 번들) · 5(자연적 평가) | §5.3 산문은 이미 정확하고 표·라벨만 뒤처졌다 |
| **논리 형식이 약하다** | 4(반증 조건) | DP2·DP3 조건문이 "사례 부재 = 반증" 형태다 |

검토자의 최종 판단 — *"정의와 주장 범위를 정리하면 제출 가능한 원고"* — 이 실측과 일치한다.
**본 계획의 산출물은 정의 문장·표 셀 문구·조건문이며, 판정·수치·게이트 결과는 하나도 움직이지 않는다.**

### 실측으로 확인한 근거 (검토 항목별)

- **1.** `PassRate_f`는 v2다. `src/sdkb_paper/config.py:230-232` (`CQ_RULE_VERSION="v2"` · `CQ_TAU=0.05` ·
  `CQ_TAU_GRID=(0.0,0.05,0.10)`) · 판정식 `src/sdkb_paper/validate/cq_runner.py:75-101`.
  본문 §3.5 승인식([stage3_source.md:466](../../paper/manuscript/stage3_source.md#L466))은 `PassRate` 를
  **정의 없이** 쓰고, τ는 [:489-490](../../paper/manuscript/stage3_source.md#L489) 에서 "CQ 분포 검사 임계"
  로만 선언된다. 둘을 잇는 문장이 본문에 없다.
- **3.** 검토자의 4분해가 코드와 정확히 일치한다 — `src/sdkb_paper/analysis/bootstrap.py:93-107`.
  `delta=diff.mean()`(원표본) · `win/loss/tie`= 원표본 부호 계수 · CI = 10,000회 재표집의
  **percentile** · `p_two = 2*min(Pr(boot≤0), Pr(boot≥0))`. seed = `config.SEED = 20260726` 고정.
  표 8 캡션([:870](../../paper/manuscript/stage3_source.md#L870))은 넷 전부를 bootstrap 산출로 적고 있고,
  §4.5([:694-698](../../paper/manuscript/stage3_source.md#L694))에는 p식·seed·percentile 명시가 전혀 없다.
- **2.** §5.3 산문([:827](../../paper/manuscript/stage3_source.md#L827))은 이미 "자원 번들"로 정확하다.
  뒤처진 것은 DP4 셀(`scripts/build_submission_stage3.py:288-297`)의 *"자원만 교체해야 **온톨로지의
  효과**가 판별된다"* 와 **CLAUDE.md §0.7 DP4 행**이다.
- **4.** [:1173-1176](../../paper/manuscript/stage3_source.md#L1173) — DP2 "사례가 한 번도 없으면 반증" ·
  DP3 "성능을 바꾸지 않으면 반증". 유한 표본의 사례 부재는 반증이 아니다.
- **5.** 표 2 [:326](../../paper/manuscript/stage3_source.md#L326) `| 자연적 평가 | …(Accept = 0) |`.
  EP3는 실제 델타를 썼으나 **통제 실험**이며 운영 환경 평가가 아니다.

### 사용자 결정 (2026-08-19)

- 검토 4번은 **라벨 개명이 아니라 조건 재정식화**로 간다 → CLAUDE.md §0.6 조건 ④·§0.7 말미 불변.
- **규약을 먼저 고치고 원고를 만진다**(§2.2 요건 5).

---

## 비목적 (하나라도 어기면 §1-2 위반)

새 실험 0 · 수치 재산출 0 · 봉인 개봉 0 · `paper/verdicts.yaml` 수정 0 · 임계 이동 0
(ε·δ·τ·ε_T4·η 불변) · **판정 변경 0**(DP 등급 포함 — PLAN-060이 이미 재등급했고 본 계획은
등급을 건드리지 않는다) · 한계·경쟁 설명의 삭제 0(이관은 링크로 도달 가능).

## §2.2 요건 점검 — 본 계획은 기조 변경이 **아니다**

확증 점검 개수·지위 불변 · 기여 목록 3 불변 · 장 구성 불변 · C-표 불변 · 논지 문장 불변.
따라서 **§2.3 파생본 절차**(정지 게이트 1개 = 이 계획의 승인)로 진행한다. 다만 요건 5(역방향
점검)만은 발동하며, 그 처리가 아래 0단계다.

---

## 작업

편집 대상은 셋뿐이다. **`paper/submission/manuscript.md` 는 절대 직접 편집하지 않는다**(재조립 대상).
`paper/supplementary/` 는 감사 기록이므로 소급 수정하지 않는다.

| 무엇을 | 어디서 |
|---|---|
| 산문 문장·제목·표 2 | [paper/manuscript/stage3_source.md](../../paper/manuscript/stage3_source.md) |
| 복사되는 표의 셀 문구 | `CELL_FIXES` in [scripts/build_submission_stage3.py:233-389](../scripts/build_submission_stage3.py#L233) |
| 생성 표의 조건명 | [src/sdkb_paper/analysis/decision_stability.py](../src/sdkb_paper/analysis/decision_stability.py) → 재생성 |

### 0단계 · 규약 선행 개정 (요건 5)

`CLAUDE.md` §0.7 DP4 행 한 줄:

> **통제된 자원 교체** — 문서집합·모델·가중치를 고정하고 **자원 번들**(온톨로지·표면형 사전·
> 개념 매핑)만 교체해야 **온톨로지 중심 자원 변경의 효과**가 판별된다

근거·설계 시점·등급(*경험적 지지*)·수치는 **불변**. 같은 커밋에 본 계획을 등재한다.

### A · 검토 1 — T3와 τ를 승인식에 잇는다

**A1.** §3.5 승인식 직후([:469](../../paper/manuscript/stage3_source.md#L469) 앞뒤)에 정의 두 문장 +
축약 수식을 넣는다. 문구 뼈대:

> \(PassRate_f\)는 스위트 \(f\)의 역량질문 가운데 v2 판정을 통과한 비율이다. v2 판정은 존재검사와
> 사전 선언된 극성별 행 수 분포검사의 논리곱이며, 분포 회귀는 기준 행 수 대비 변화가 동결 임계
> \(\tau\)를 넘을 때 성립한다.
>
> \[ pass_{v2}(i) = [\,rows_i \ge expect\text{-}min_i\,] \wedge \neg regress_i,\quad
> regress_i = \begin{cases} rows_i < (1-\tau)\,base_i & (\text{up})\\ rows_i > (1+\tau)\,base_i & (\text{down})\end{cases} \]

- 극성 `up`/`down`만 싣는다 — 실측상 CQ 31개의 극성은 `up` 29 · `down` 2이고 `flat` 사용처가 0이다
  (`grep '^# monotone:' queries/cq/*.rq`). 코드는 `flat`도 정의하지만 본문에 미사용 분기를 실을
  이유가 없고, S2 보충자료의 2분기 서술과도 일치한다.
- τ 선언 문단([:489-493](../../paper/manuscript/stage3_source.md#L489))에서 "CQ 분포 검사 임계"를 이
  정의와 연결하는 한 구절로 다듬는다(문장 신설 없이).

**A2.** 표 10 조건명 구체화 — `T3 · 교차 태스크 검출` → `T3 · 교차 태스크 검출 (EP2 판독)`.
**생성기에서 고친다**(`decision_stability.py` 라벨 문자열 → `make tables-stability` 재생성).
수치·행 수·판정 열은 불변. 산문에서 손으로 옮겨 적으면 §1-1 위반이다.

### B · 검토 2 — 인과효과의 대상을 자원 번들로 통일

| 자리 | 현재 | 교정 |
|---|---|---|
| `CELL_FIXES` DP4 (`build_submission_stage3.py:288-297`) | 자원만 교체해야 **온톨로지의 효과**가 판별된다 | 자원 **번들**만 교체해야 **온톨로지 중심 자원 변경의 효과**가 판별된다 |
| 반증 조건 DP4 ([:1175-1176](../../paper/manuscript/stage3_source.md#L1175)) | "자원 효과가 동일하게 분리되면" | "자원 번들의 효과가 동일하게 분리되면" |
| 초록 · §5.3 · §6.4 · 결론 | 혼재 | "자원 번들" 단일화 · 효과를 "온톨로지" 단독에 귀속하지 않는다 |

DP4 셀은 기존 entry에 **사유 문자열이 이미 달려 있으므로** 수치 검사에 걸리지 않는다(문구만 변경).
초록은 [:13](../../paper/manuscript/stage3_source.md#L13)에서 이미 "자원 번들"이므로 §2·표 7 캡션
([:260](../../paper/manuscript/stage3_source.md#L260)·[:286](../../paper/manuscript/stage3_source.md#L286)·
[:837](../../paper/manuscript/stage3_source.md#L837))만 정렬한다.

### C · 검토 3 — bootstrap 산출 방법 정정

**C1.** 표 8 캡션([:870](../../paper/manuscript/stage3_source.md#L870)) — 산문 소스이므로 직접 편집:

> Δ와 승/패/동은 질의 단위 짝지은 원표본 요약이고, 95% 신뢰구간과 양측 *p*값은 질의 단위 paired
> bootstrap 10,000회로 산출하였다.

같은 오기가 있는지 표 9 캡션([:965-966](../../paper/manuscript/stage3_source.md#L965))도 함께 점검한다
(현재 "질의 단위 paired bootstrap 10,000회"만 있어 Δ 귀속 주장이 없다 → 무수정 가능성 높음).

**C2.** §4.5([:694-698](../../paper/manuscript/stage3_source.md#L694))에 두 문장 추가:

> 신뢰구간은 재표집 분포의 2.5·97.5 백분위이며, 양측 *p*값은
> \(2\min\{\Pr(\Delta^{*}\le 0),\ \Pr(\Delta^{*}\ge 0)\}\)로 산출한다. 재표집 시드는 평가셋 개봉
> 이전에 코드에 고정하였다(`SEED = 20260726`).

percentile 방식임을 명시한다 — BCa 여부는 현재 원고 어디에도 없고, 방법론 심사자가 반드시 묻는다.

### D · 검토 4 — 반증 조건 재정식화 (라벨 유지)

[:1173-1176](../../paper/manuscript/stage3_source.md#L1173) 네 문장 중 **둘만** 교체한다.

| | 현재 (사례 부재 = 반증) | 교정 (관측 가능한 반증) |
|---|---|---|
| DP2 | 형식 검증을 통과한 델타가 하류 성능을 훼손한 사례가 한 번도 없으면 | 여러 사전등록의 독립 델타에서 형식 검증층만으로 하류 비회귀가 높은 정밀도로 예측되고, 태스크 조건이 승인 결정을 한 번도 바꾸지 않으면 |
| DP3 | 무관하게 설계한 계층의 제거가 주 태스크 성능을 바꾸지 않으면 | 사전등록된 결함 집합에서 주 태스크 성능 검사의 검출률이 T3와 동등하여 T3 단독 검출이 0이 되면 |

DP1·DP4·DP5·DP6의 조건문은 이미 관측 가능하므로 **유지**한다.
아울러 검토자의 부수 지적대로 **DP3 근거의 본체가 홀드아웃 12/45임을 문장에서 먼저** 놓고 A8을
보조 진단으로 뒤에 둔다 — `CELL_FIXES` DP3 entry(`build_submission_stage3.py:308-317`)는 이미
*"근거는 결함주입 T3 단독 검출이 진다"* 로 그 순서이므로 산문 쪽만 맞춘다.

### E · 검토 5 — DSR 평가유형 명칭

표 2 [:326](../../paper/manuscript/stage3_source.md#L326):

> `| 실제 델타 기반 통제 평가 | 실제 자원 델타에 대한 게이트의 거부(Accept = 0) | §5.3 |`

"자연적 평가"를 쓰지 않는다. §3 서두 DSR 문단에 *"운영 환경에서의 자연적 평가는 수행하지 않았다"*
를 한 구절로 남겨 결손을 감춘 것이 아님을 보인다(§6.5 결손 표와 중복되지 않는 선에서).

### F · 소수정 다섯

| | 자리 | 교정 |
|---|---|---|
| F1 | `CELL_FIXES` DP3 | "검색을 −0.0316 악화" → "검색 성능이 0.0316 저하되었다(제거 손실 +0.0316)" — **수치 토큰이 바뀌므로 사유 문자열 필수**(부호 표기 통일 · 표 9 캡션의 제거 손실 정의와 일치) |
| F2 | 초록 [:17](../../paper/manuscript/stage3_source.md#L17) | "강한 다국어 기준선" → "다국어 융합 기준선". **결손 ⑦ 행([:1291](../../paper/manuscript/stage3_source.md#L1291))의 제목은 결손의 이름이므로 유지** |
| F3 | 결론 [:1352-1353](../../paper/manuscript/stage3_source.md#L1352) | "판별력은 … 45건에서 **확증되었다**" → "… 홀드아웃 산출물 평가에서 **지지되었다**" (§5.2 지위 문장·표 3 EP2 행과 일치) |
| F4 | §3.5.1 [:514](../../paper/manuscript/stage3_source.md#L514) | "편입의 발효 조건 둘은 충족되었다" → "정식 평가를 위한 선행조건은 충족되었으나, 승인식 채택에는 반복검증이 추가로 필요하다" |
| F5 | §3.5.1 전체 (690자) | 한 문단 + T4 정의 인용문으로 축약 · 세부 수식과 판독 조건은 S5 부록 A 링크로 이관 |

---

## 분량 — 이 계획의 진짜 제약

**착수 시점 여유 39자**(본문 69,788 / 상한 69,827 · 표 12/13 · 그림 8/8).

**실행 결과 (2026-08-19).** 검토가 요구한 정의·조건문 추가는 실측 **+863자**였고(추정 +600 초과),
그만큼을 **중복 서술의 압축으로 전액 회수**하였다. 최종 본문 **69,823자 / 상한 69,827자**(여유 4자) ·
표 12 · 그림 8 · 검사 전량 통과. **회수처는 전부 같은 사실의 2·3중 반복이다.**

| 회수처 | 내용 |
|---|---|
| §3.5.1 | T4 세부 임계·해석을 S5 부록 A로 이관(F5) — ε_T4·η 는 §6.6 동결 임계 목록에 잔존 |
| §6.4 | 등급 3단계 설명과 설계 시점 문단의 중복 · DP6 의 0.0005 재진술(표 10·§6.5 에 있음) |
| §6.5 | 승인 안전성의 계보 재설명(§3.2) · 전달 판독의 문장 중복 · 하위집단 유보 규칙(§4.5) · 관할 일반화(결손 ⑤) |
| §6.7 | 결론 "설계지식" 문단이 같은 장 "평가에서 확보한 것"의 둘째 발견을 재진술 |
| §5.2 | τ=0.10 민감도 서술이 표 10·결손 ③ 과 3중 |

**삭제한 사실은 없다** — 회수는 전부 같은 문서 안에서 이미 다른 자리가 말하고 있는 문장이거나
S5 로 도달 가능한 전문이다. 상한 `LENGTH_TARGET_RATIO = 0.61` 은 **건드리지 않았다.**

- **표·그림은 늘리지 않는다** — 그림은 이미 상한 8/8이고, A1은 수식이라 표로 세지 않는다.
- 상한 자체(`LENGTH_TARGET_RATIO = 0.61`)는 **투고처 규정이 아니라 우리 목표**이므로 최후 수단으로
  완화 가능하나, **별도 사용자 승인 없이 건드리지 않는다**.

---

## 검증

```
make submission-stage3          # tables-stability --check 선행 · CELL_FIXES 수치 검사
make verdicts && make style-check && make submission-check && make sig-check
make lint && make test
uv run python scripts/build_submission_stage3.py --check
```

체크포인트:

1. **판정·수치 diff 0.** `git diff paper/submission/manuscript.md` 에서 숫자 토큰의 변화는 **F1
   한 곳뿐**이어야 하고, 그 사유는 빌드 로그에 남는다.
2. **`CELL_FIXES` 신규·수정 entry는 앵커 1회 매치**여야 한다(불만족 시 rc 2).
3. **D9(존재하지 않는 절 참조)** — F5의 §3.5.1 축약이 절을 지우지 않는지 확인한다. 지우면
   본문의 `§3.5.1` 참조가 죽는다.
4. **style-check** — 신설 문장은 90자 이내(S3) · `~하였다/~한다` 종결(T1) · 주장 문장 볼드 금지
   (T5: `**…한다**` 형태는 즉시 실패) · 문두 `그래서/그런데/하지만` 금지(T3) · 표 셀은 어휘
   규칙(T2/T7/V5)만 적용된다.
5. **V6 연번** — 표 2 행 라벨 변경은 캡션 번호를 건드리지 않는다. 표를 신설하지 않으므로 연번 불변.
6. **D6 실측을 전후로 보고**한다(현재 69,788 / 상한 69,827).

## 순서

`0 규약 개정 → F5 축약(여유 확보) → A → C → D → B → E → F1–F4 → 검증 → 커밋`

## 남는 판단 (실행 중 사용자에게 물을 것)

- **S2 보충자료의 `pass_v2` 수식에 `flat` 분기가 빠져 있다.** 코드에는 있고 사용처는 0이다.
  감사 기록이므로 소급 수정하지 않는 것이 기본이나, 정오표 한 줄을 붙일지는 별도 판단이다.
- **A8 부호 표기(제거 손실 = 양수)를 §0.8 문구 사전에 행으로 넣을지.** 넣으면 재발이 막히지만,
  `verdicts.yaml` 까지 확장하면 §1-2 동급 변경이 되므로 이번 범위에서는 §0.8 사본에만 적는 것을
  기본으로 둔다.
