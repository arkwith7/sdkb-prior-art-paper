# desc: 특허가 하나도 매핑되지 않은 공정 단계(커버리지 공백)를 찾을 수 있는가?
# expect-min: 0
PREFIX sdkb: <https://w3id.org/sdkb#>
SELECT ?step
WHERE {
  ?step a sdkb:ProcessStep .
  FILTER NOT EXISTS { ?p a sdkb:Patent ; sdkb:appliesToProcessStep ?step . }
}
