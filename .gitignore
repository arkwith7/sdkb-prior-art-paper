# secrets
.env
*.key
starry-runner-310008-c753058e2590.json
arkwith-translat-1545471718983-5ad8f06da2c9.json
# data — 원문/대용량은 절대 커밋하지 않음 (MANIFEST.md와 samples/만 예외)
data/raw/
data/interim/
data/processed/*
!data/**/.gitkeep
!data/processed/README.md
*.parquet
*.sqlite

# 특허 전문(abstract/claim) 을 담은 스냅샷 — KIPRIS 학술 이용·비재배포 조건 (CLAUDE.md §1.4 · 논문 §8).
# 얼린 스냅샷이라도 이 두 파일은 커밋하지 않는다. PROVENANCE.json 의 sha256·집계만 커밋하고,
# 파일 자체는 `make vendor` 로 로컬 재생성한다. 개념 링크·서지 메타데이터는 재수집 가능.
data/external/sdkb/sdkb-abox-patents.ttl
data/external/sdkb/sdkb-abox-prior-art.ttl

# 게이트 리포트 (make cq 가 그래프별로 매번 재생성)
paper/figures/cq_report*.md

# Windows/WSL 부산물 (NTFS alternate data stream)
*:Zone.Identifier

# 에디터 로컬 워크스페이스 정보 (개인 설정 — 추적 제외)
*.code-workspace

# python
__pycache__/
*.egg-info/
.venv/
.ruff_cache/
.pytest_cache/
.ipynb_checkpoints/
dist/

# Protégé/OWLAPI 가 온톨로지를 열 때 폴더에 자동 생성 (편집 도구 부산물 — 얼린 스냅샷 오염 방지)
catalog-v*.xml

# --- W4 결함주입 격리 구역 (PLAN-020) — 오염 산출물은 절대 커밋하지 않는다 ---
data/quarantine/
data/pristine_backup/
data/PRISTINE.json

# S-시리즈(구 패러다임) 재생성물 — `make s1`·`s2`·`robustness`·`by-applicant`·`ksia-strata`·
# `figures` 가 여기에 쓴다. **동결본은 paper/archive/{tables,figures}/ 이고 그쪽이 인용 대상**이다.
# 재생성물을 커밋하면 "동결 기록"과 "현행 코드 출력"이 다시 뒤섞인다 — 커밋하지 않는다.
paper/archive/regenerated/
