# CR-007 · 텍스트→개념 매핑 규칙·사전을 상류 자산으로 정비 (D-14)

> 제출처: `~/Dev/sdkb` · 양식: 상류 CLAUDE.md §2 **1단계 요구정의**
> 근거: `upstream/DEFECT-LEDGER.md` §1.9 · 논문 v0.9 §6.1.3·§6.2f 관측 3
> 우선순위: **P0** — CR-001의 효과가 이것 없이는 기존 등재 문서에만 갇힌다

```
목적      : 온톨로지를 **자유 텍스트에 적용할 수 있게** 만든다.
            현재 하류 파이프라인에는 텍스트에서 개념을 뽑는 단계가 **존재하지 않는다** —
            코퍼스 조립기는 SPARQL 로 **그래프에 이미 있는 링크(realizesProcess 등)를
            읽어올 뿐**이다(`corpus/assemble.py:114 _multi()`).

            귀결 셋:
            ① 링크가 없는 문서는 온톨로지 팔에서 **영구 0점** — 일본어 문서 개념링크
               보유율 0.00 %, 영어 69.64 % 가 방법의 한계가 아니라 **입력 부재**인 이유
            ② **자유 텍스트 질의를 받을 수 없다.** 현 벤치마크가 도는 것은 질의가
               "이미 온톨로지에 등재된 특허"이기 때문이다. 실무 질의 — 소부장 기술문제
               서술, 연구과제 초록, 타 기관 청구항 — 이 들어오면 개념을 붙일 진입로가 없다.
               **제품으로 쓸 수 없는 상태다.**
            ③ CR-001 로 개념 해상도를 올려도 매핑기가 없으면 **신규 문서에는 적용되지 않는다.**
            하류(§0) 전원. SDKB-Match 의 실사용 진입로 그 자체.

입력      : mappings/abox_term_aliases.json (현행 표면형 → 개념 룰 · 한국어 중심)
            data/semiconductor_v0_3.json 의 개념 라벨(@en 632 · @ko 93 · @ja 0)
            promote_korean_aliases.py 등 기존 별칭 생성 스크립트

출력      : (1) **개념 매핑 사전** — 개념 IRI × 표면형(다국어·약어·표기변이·형태소 변형) 테이블을
                릴리스 자산으로 발행. 지금은 스크립트 안에 흩어져 있다
            (2) **매핑 규칙 명세** — 매칭 단위(형태소/문자 n-gram), 대소문자·하이픈·공백 정규화,
                중의성 해소 규칙, 최소 신뢰도. **결정적(deterministic)**이어야 한다
            (3) 매핑 결과에 confidence·규칙 id 부여(D-11 과 같은 규율)
            바뀌는 것: mappings/ + 릴리스 자산. TBox·ABox 불변.

            ⚠ **분업.** 상류는 **규칙과 사전**을 낸다. 텍스트에 적용하는 **적용기(linker)는
              하류가 구현**한다 — 하류마다 텍스트 파이프라인이 다르기 때문이다.
              상류가 적용기까지 만들면 하류의 토큰화·전처리와 이중 관리가 된다.

성공 기준 : ① 개념의 ≥ 95 % 가 사전에 표면형 ≥ 3개 보유(다국어 포함 · CR-003 과 짝)
            ② 같은 입력 → 같은 출력(결정성) 회귀 테스트 통과
            ③ 중의성 있는 표면형(예: "etch" 가 Process 인지 SubProcess 인지)에 대한
               해소 규칙이 명시되고 테스트로 고정
            ④ 하류가 사전만 받아 매핑을 재현할 수 있음 — 상류 코드 의존 없이
            ⑤ make validate · make test 통과

비목표    : 학습 기반 개체 링킹 모델을 만들지 않는다 — 규칙·사전이 먼저다(결정성·감사가능성).
            하류의 적용기를 상류가 구현하지 않는다.
            매핑 정확도를 심사관 인용으로 평가하지 않는다(그건 누출이다 — 아래).

파급      : 신규 릴리스 자산 추가. TBox·IRI 불변이므로 기존 소비자는 깨지지 않는다.
            CHANGELOG 기록 · 하류 통보.
```

## 누출 규율 (하류 §1-4 와 같은 층)

- **질의 텍스트에서 개념을 뽑는 것은 안전하다** — 질의 자체의 기술(記述)이지 정답 파생이 아니다.
  현행 코퍼스 조립기도 같은 근거로 `concepts` 를 oracle-free 로 취급한다(`assemble.py:341`).
- **위험한 것은 사전의 출처다.** 매핑 사전을 심사관 인용 정답 문서에서 학습·추출하면 그 순간
  정답이 피처로 새어 들어온다. 사전은 **도메인 어휘(개념 라벨·표준 용어집·공개 분류체계)에서만**
  만들고, 정답 문서를 사전 구축 입력에서 배제한 사실을 provenance 에 기록한다.
- 하류는 새 매핑을 붙인 코퍼스에 대해 **`make leakage` 를 다시 통과**시켜야 한다.

## 하류가 되돌려줄 측정

일본어·영어 문서의 개념링크 보유율(현재 0.00 % · 69.64 %), 자유 텍스트 질의에 대한 개념 부착률,
그리고 매핑기 산출을 입력으로 한 P 시스템의 R@100·nDCG@20. **매핑기 도입 자체는 새 가설이 아니라
기존 H3 시스템 정의의 변경**으로 사전등록한다(PLAN-029 §3).
