# CR-002 · 개념 계층(taxonomy) 부여 (D-02)

> 제출처: `~/Dev/sdkb` · 양식: 상류 CLAUDE.md §2 **1단계 요구정의**
> 근거: `upstream/DEFECT-LEDGER.md` §1.3 · 논문 v0.9 §6.4 A6 = 0.0000 · `analysis/results_table.py:38`
> 우선순위: **P0**

```
목적      : 온톨로지의 "의미 경로(semantic path)" 신호가 현재 그래프에서
            구조적으로 작동하지 않는 문제를 고친다.
            인스턴스 계층은 ont:hasSubprocess 11 트리플이 전부이고
            (skos:broader·인스턴스 subClassOf 는 0건), 클래스 트리는
            41개·최대 깊이 4다. 그 결과 하류의 PathSim(Wu-Palmer)은
            축 일치 여부로 퇴화했고, 가중치 격자에서 w_h = 0 이 선택되어
            계층 항이 순위 함수에서 완전히 빠졌다 — A6(계층 제거) 손실이
            정확히 0.0000 인 것은 통계가 아니라 구조의 결과다.
            하류(§0) 전원 수혜. 특히 "의미 기반 검색"이라는 제품 주장의 근거.

입력      : data/semiconductor_v0_3.json 의 개념 143개 (CR-001 이후에는 확장 어휘)
            mappings/sdkb_semiconto_alignment.csv (외부 온톨로지 정렬 — 계층 원천 후보)
            도메인 권위 원천(공정 분류 체계·IPC/CPC 계층)과 대조 필요(상류 §1.3)

출력      : (1) 개념 인스턴스 간 skos:broader / skos:narrower 계층 (build_owl.py)
            (2) 필요 시 TBox 클래스 세분화 — 단 클래스 추가는 하류를 움직이므로
                별도 승인(상류 §2 3단계)
            (3) 계층 깊이·분기수 리포트를 data/reports/ 에 산출
            바뀌는 것: ABox(개념 간 관계) + 선택적 TBox

성공 기준 : ① 개념의 ≥ 80 % 가 깊이 ≥ 3 의 경로를 가짐 (현재 인스턴스 계층 사실상 없음)
            ② 축별 계층 최대 깊이 ≥ 4 · 순환 0 (SHACL 로 강제 — CR-005 와 짝)
            ③ 두 무작위 개념 쌍의 Wu-Palmer 값 분포가 {0, 1} 양극이 아니라
               최소 5개 이상의 구간으로 분포 (퇴화 해소의 직접 지표)
            ④ make validate · make test 통과

비목표    : 계층을 자동 생성기로 채우고 검증을 생략하지 않는다.
            근거 없는 상위-하위 관계는 D-03 의 방향성 결함을 재생산한다.
            검색 성능 주장은 하류의 재검정 사항.

파급      : 새 술어(skos:broader)를 ABox 에 도입 → TBox 에 선언되어 있어야 한다
            (상류 §1.2 — ABox 인라인 선언 금지). CHANGELOG · 버전 · 하류 통보.
            ⚠ CR-001 과 함께 **문서집합 불변 링크 델타**로 발행 권장(D-10).
```

## 하류가 되돌려줄 측정

`make ablation`의 **A6 제거손실이 0에서 벗어나는지**, 가중치 격자에서 **w_h > 0이 선택되는지**.
이 둘이 이 CR의 합격선이며, 동시에 "온톨로지의 의미 경로가 검색에 기여한다"는 **판매 논거가
데이터로 성립하는 첫 순간**이 된다.
