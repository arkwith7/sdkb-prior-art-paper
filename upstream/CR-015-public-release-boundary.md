# CR-015 · 공개본 경계 확정 — KIPRIS 원문을 Link-Only 로 내린다 (D-36)

> 제출처: `~/Dev/sdkb` · 양식: 상류 CLAUDE.md §2 **1단계 요구정의**
> 근거: `upstream/DEFECT-LEDGER.md` D-36 · 원고 **§10.1·§10.3** · 상류 `docs/dataset_rejected_patents_card.md` §6
> 우선순위: **P0 — 투고 게이트**
> 작성 2026-08-09 · 상태: **1단계 요구정의 · 🛑 상류 승인 대기**

---

## 0. 한 문장

**공개 예정 리포가 재배포하고 있는 KIPRIS 원문(초록 전문 · 청구항 1항 전문 1,000건)을 메타 +
Link-Only 로 내리고, 그 자리에 재인출 스크립트를 놓는다 — 원고 §10.3 이 이미 "재배포할 수
없다"고 선언했기 때문이다.**

---

## 1. 왜 이것이 투고 게이트인가

이 CR 은 자원 품질을 다루지 않는다. 다른 살아 있는 CR 여섯(CR-001A·002·003·005·006·009)은
**온톨로지를 더 좋게 만드는 일**이고 전부 투고 후다. 이것은 다르다 — **논문이 이미 한 약속을
지키는 일**이다.

| | 원고가 쓴 것 | 리포의 실물 |
|---|---|---|
| §10.1 | 데이터셋은 `github.com/arkwith7/semiconductor-knowledge-base` 에서 **CDLA-Permissive-2.0 으로 공개**된다 | 아직 비공개(HTTP 404) · 릴리스 태그 **0개** |
| §10.3 | **KIPRIS 원문(특허 전문·서지)은 학술이용 조건상 재배포할 수 없다** | `data/patents/raw/semiconductor_industry_rejected_patents.jsonl` 이 **초록 전문 + 청구항 1항 전문 1,000건**을 담고 커밋돼 있다 |

**두 문장이 같은 리포를 가리키는데 서로 반대다.** 심사자가 §10.1 의 URL 을 눌러 확인하면
그대로 드러나고, 그 순간 문제가 되는 것은 라이선스가 아니라 **원고의 신뢰성**이다.

---

## 2. 법무 자문은 이 경로에서 제외한다 (2026-08-09 · 사용자 결정)

상류 `docs/project/dataset_publication_risk_review.md`(2026-05-17)와 `dataset_rejected_patents_card.md`
§6 은 **SKKU 산학협력단·법무팀 자문 후 (A)/(B)/(C) 중 택일**로 적혀 있다. **이 전제를 제거한다** —
그 검토는 다른 사업 맥락에서 진행되던 것이고 지금은 해당 사항이 없다.

**자문이 필요 없는 이유는 결정이 이미 내려져 있기 때문이다.** 원고 §10.3 이 *"재배포할 수 없다"*
고 쓴 시점에 선택지는 (B) 하나로 좁혀졌다. 리포를 그 문장에 맞추는 것은 **법적 판단이 아니라
정합성 작업**이다. 따라서 **(B) 확정**으로 바로 간다.

- ~~(A) 본 형태 그대로 학술 목적 재배포~~ — 원고 §10.3 과 충돌하므로 선택 불가
- **(B) 본문 재배포 불가 → 메타 + `ground_truth_*` + URL Link-Only** ← **확정**
- ~~(C) 초록만 공개, 청구항은 Link-Only~~ — 초록도 KIPRIS 원문이므로 §10.3 과 충돌

---

## 3. 요구정의

```
목적      : 공개 예정 리포에서 KIPRIS 원문을 제거하고, 같은 데이터를 복원할 수 있는
            경로를 그 자리에 남긴다. 재현성을 줄이지 않으면서 재배포만 끊는다.

입력      : data/patents/raw/semiconductor_industry_rejected_patents.jsonl
              — 1,000행 · 9.7 MB · 커밋 2개
              — 각 행: target_patent{application_number, title, abstract(전문),
                ipc, date, claim1(전문)} + 인용/정답 필드
            docs/dataset_rejected_patents_card.md §6
            .gitignore:41 의 자인 주석
            LICENSE.txt (CDLA-Permissive-2.0)

출력      : (1) 공개본 jsonl — abstract·claim1 을 **값 없이** 두고
                (application_number, title, ipc, date, family, ground_truth_*, kipris_url)
                만 남긴다. 스키마는 유지하고 값만 비운다 — 소비자 코드가 안 깨지도록.
            (2) scripts/refetch_rejected_patents.py — KIPRIS 키 보유자가
                공개본 + 키로 원본 jsonl 을 **바이트 단위로 복원**한다.
                복원 성공은 sha256 대조로 확인한다.
                **새로 짜지 않는다** — CR-016 §4 로 흡수되는 paper_data 수집기
                (src/kipris_dataset/kipris.py · expand_dataset_via_api.py)를 호출한다.
                새로 짜면 수집 규칙이 갈려 복원본이 원본과 달라진다.
                → **CR-016 §4 의 이관이 이 항목의 선행조건이다.**
            (3) .gitignore 갱신 — 원본 jsonl 을 ignore 로 옮기고 자인 주석을 교체한다.
            (4) 이력 처리 — 아래 §4. **이것이 이 CR 에서 유일하게 되돌릴 수 없는 작업이다.**
            (5) card §6 개정 — (A)/(B)/(C) 분기를 지우고 **(B) 확정**을 적는다.
                법무 자문 대기 문구를 제거하고 그 자리에 "결정 근거 = 원고 §10.3" 을 쓴다.
            (6) docs/project/dataset_publication_risk_review.md 갱신 — 2026-05-17 이후
                전제가 바뀌었다(단일 AEI·DSR 논문 · 데이터셋 트랙 분리 없음 · 자문 제외).
                체크리스트 9항 중 미체크 7항을 현 시점 기준으로 재판정한다.
                **특히 #2(합성 3-rater 를 "전문가 평점"으로 오기)는 문서 스스로
                retraction 급이라 적어 놓고 미체크다** — 원고 서술과 대조해 닫는다.

            바뀌는 것: 공개 자산의 경계. **T-Box·A-Box 내용·트리플 수는 불변.**

성공 기준 : 게이트 통과 형태로 —
            ① 공개본 전체에서 abstract·claim1 **값 문자열 0건**
               (하류가 깨끗한 체크아웃에서 grep 으로 센다)
            ② 이력 어느 커밋에도 위 두 필드의 값이 남아 있지 않다
               (git log -p --all | grep 으로 센다)
            ③ **공개본 메타 + 재인출만으로 조립한 하류 코퍼스의 sha256 이
               `83eef760…` 과 일치한다** ← 하류 태스크 지표(§0.1 요구)
            ④ 원고 §10.1 의 공개 목록과 리포 실물이 항목 단위로 대응한다
            ⑤ make validate · make test 통과 · G₀ 트리플 119,208 불변

비목표    : 온톨로지 내용 변경 · 어휘 확장 · 성능 개선 주장.
            거절결정서 442건(`rejection_decisions/structured/`)은 **건드리지 않는다** —
            이미 excerpt 스크럽이 끝났고(`_excerpt_removed: true`) 구조화 매핑만 남아 있다.
            전문가 프로필·평점도 범위 밖이다(합성 · PII 제거 완료) —
            다만 서술 정합은 §3 출력 (6) 에서 본다.
```

---

## 4. 이력 처리 — 되돌릴 수 없으므로 방법을 먼저 정한다

**파일을 지우는 커밋만으로는 공개되지 않는다.** 대상 파일은 커밋 2개에 걸쳐 있고, 리포가
공개되는 순간 누구나 과거 커밋에서 원문을 받을 수 있다.

| 방법 | 장점 | 대가 |
|---|---|---|
| **ⓐ history 재작성** (`git filter-repo`) — 두 커밋에서 해당 경로만 제거 | 이력의 연속성 유지 · 커밋 메시지·기여 기록 보존 | **모든 커밋 해시가 바뀐다** — 하류 `PROVENANCE.json` 의 상류 커밋 참조(`212fe62` 등)와 원고 §6·§9 의 상류 해시 인용이 **전부 무효**가 된다 |
| **ⓑ orphan 공개본** — 현 트리에서 원문을 뺀 상태로 새 루트 커밋 1개를 만들어 공개 | 기존 이력·해시 **전부 불변** · 원고 인용이 살아 있다 | 공개본에 개발 이력이 없다 |

**ⓑ 를 권고한다.** 이유는 하나다 — **ⓐ 는 원고를 깨뜨린다.** 원고와 하류 `PROVENANCE.json` 은
상류 커밋 해시를 **판정의 출처**로 인용하고 있고(§6.7 의 `39855bb`, §9.1 의 `2839afb`,
스냅샷의 `212fe62`), 해시가 바뀌면 그 인용이 전부 가리키는 곳을 잃는다. 그것은 §1-3 이 막는
"판정 기록의 소급 훼손"과 실질이 같다.

**ⓑ 를 택하면 개발 이력은 비공개 리포에 남고, 공개본은 "릴리스 vX 시점의 상태"가 된다.**
원고 §10.1 이 이미 *"논문이 명시한 릴리스 태그를 체크아웃해야 한다"* 고 쓰고 있으므로 서술도
그대로 맞는다.

**🛑 이 결정은 상류 3단계(설계)에서 확정한다.** 여기서는 권고까지다.

---

## 5. 하류가 확인할 것 (이 CR 이 닫힌 뒤)

1. 깨끗한 체크아웃 → `grep -r` 로 성공기준 ①② 실측
2. 공개본 메타 + 재인출 → `make corpus` → 코퍼스 sha256 대조 (성공기준 ③)
3. 원고 §10.1·§10.2·§10.3 을 리포 실물과 항목 단위로 대조 → 어긋난 문장 교정
4. 릴리스 태그·DOI 를 §10 에 기입 (CR-016 과 함께 닫힌다)
