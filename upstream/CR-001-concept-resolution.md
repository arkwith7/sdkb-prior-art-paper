# CR-001 · 개념 어휘 해상도 확장 (D-01 · D-04)

> 제출처: `~/Dev/sdkb` · 양식: 상류 CLAUDE.md §2 **1단계 요구정의**
> 근거: `upstream/DEFECT-LEDGER.md` §1.1·§1.2 · 논문 v0.9 §6.2c·§6.3(H3·H4)·§6.4(A2·A3)
> 우선순위: **P0** — 아래 모든 CR의 효과가 이 해상도 위에서만 측정된다

```
목적      : 개념 신호가 문서를 구분하지 못하는 문제를 고친다.
            현재 온톨로지 팔은 40,552개 문서를 3,292개 동치류로만 구분하며,
            중앙값 문서는 개념집합이 완전히 동일한 문서 670건과 한 덩어리다.
            이 해상도에서는 상위 20위 정렬이 원리적으로 불가능하다
            (실측: 재랭크 시 nDCG@20 −0.0395, p=.029 · 유의 악화).
            하류(§0) 중 SDKB-Match(PriorArt)와 sdkb-prior-art-paper 가 직접 수혜.

입력      : data/semiconductor_v0_3.json (개념 원천 · 현재 143개 축 개념)
            data/patents/**, data/external/** (링크 대상 문서 40,552건)
            mappings/abox_term_aliases.json (표면형 → 개념 룰)
            규모: 개념 143 → 목표 10³ · 개념 링크 62,663 → 목표 2×10⁵ 이상

출력      : (1) 개념 어휘 확장 — data/ 원천 큐레이션 + build_owl.py 재생성 (ABox 인스턴스 개념)
            (2) 문서-개념 링크 생성기 커버리지 확대 (build_abox_patents.py ·
                build_abox_prior_art.py) — 문서당 다중 링크 허용
            (3) 릴리스 메타에 개념별 문서빈도(df) 테이블 추가 —
                하류가 특이도(IDF) 가중을 걸 수 있어야 한다. 현재 하류는
                가중 없는 Jaccard 를 쓸 수밖에 없고, 그래서 df=5,146 인
                `eprom` 과 df=12 인 개념이 같은 무게를 갖는다.
            바뀌는 것: ABox(인스턴스·링크) + 릴리스 메타. TBox 클래스는 불변.

성공 기준 : 게이트 통과 형태로 —
            ① 문서당 개념 수 평균 ≥ 5 (현재 1.55)
            ② 개념집합 동점블록 크기의 문서가중 중앙값 ≤ 20 (현재 670)
            ③ 고유 개념집합 수 ≥ 20,000 (현재 3,292)
            ④ 축별 링크 점유율의 최소 축 ≥ 10 % (현재 Skill 3.18 % · FailureMode 4.60 %)
            ⑤ 신규 링크 전량에 provenance·confidence (§1.4 수치 신뢰성)
            ⑥ make validate · make test 통과, 릴리스 서명 변경을 CHANGELOG 에 기록

비목표    : 검색 성능 개선을 이 CR 안에서 주장하지 않는다. 성능은 하류가
            새 사전등록 아래 재검정한다. 링크 수를 채우려고 근거 없는 링크를
            만들지 않는다 — 커버리지 한계는 데이터카드에 명시한다(상류 §1.3·§1.4).
            TBox 클래스 추가·IRI 규칙 변경은 이번 범위 밖.

파급      : 어휘가 커지고 링크 수가 바뀐다 → 릴리스 서명 변경. IRI 규칙은 불변이므로
            기존 IRI 는 유지되어야 한다(하류가 커밋 SHA + sha256 으로 핀한다).
            CHANGELOG · 버전 상향 · 하류 3곳 통보 필요.
            ⚠ 이 델타는 **문서집합을 바꾸지 않는 링크 수준 델타**로 발행할 것 —
              그래야 하류가 H2(갱신 승인 안전성)를 처음으로 검정할 수 있다(CR 없음 · D-10).
```

## 왜 이 수치들인가 (설계 근거, 상류 2단계에서 검증 대상)

- **문서당 5개**: 질의당 개념 수가 평균 2.91인 현재도 질의 쪽은 3개를 들고 있다. 후보 쪽이 1.55개면
  Jaccard의 분모가 질의에 지배되어 값이 뭉갠다. 양쪽이 5개 수준이면 겹침 0/1/2/3의 계조가 생긴다.
- **동점블록 ≤ 20**: 상위 20위를 정렬하려면 상위 20위 안에서 점수가 갈려야 한다. 현재 중앙값 670은
  "상위 100 전체가 한 점수"에 가깝다.
- **df 테이블**: 이것만 있어도 하류는 코드 변경 없이 IDF 가중을 걸 수 있다. 어휘 확장보다 싸고
  즉시 효과를 측정할 수 있으므로, **상류가 이 CR을 분할한다면 df 테이블을 먼저 내는 것을 권한다.**

## 하류가 되돌려줄 측정

새 스냅샷을 vendor 하면 이 저장소는 다음을 재실행해 상류에 회신한다 —
`make eval`(R@100·nDCG@20·MRR), `make ablation`(A1–A8 제거손실), 해상도 통계(대장 §1.1 표).
**A6·A3의 부호 전환과 nDCG의 부호 전환이 이 CR의 진짜 합격선이다.**
