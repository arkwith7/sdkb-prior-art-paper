# upstream/archive — 종료된 CR (인용 가능 · 재송부 금지)

여기 있는 CR 은 **상류 구현이 끝났고, 상류에 더 요구할 것이 없는 것**이다. 삭제하지 않는 이유는
하나다 — 대장(`DEFECT-LEDGER.md`)과 원고가 이 문서의 결정 항목을 참조하고, 재실험의 서사가
"무엇을 어떤 근거로 정했는가"를 되짚을 수 있어야 하기 때문이다.

**여기 있는 것을 상류에 다시 보내지 않는다.** `HANDOFF-QUEUE.md` §3 의 세션 프롬프트에서도
제거돼 있다.

> **"아카이브"를 "결함이 닫혔다"로 읽지 않는다(2026-08-08 명시).** 이 폴더가 뜻하는 것은
> **상류 트랙의 종료**뿐이다. 하류 소비가 남은 CR 이 여기 있을 수 있고 — 지금은 **CR-012**가
> 그렇다(검증기준 ①⑤ 미결 · PLAN-045) — 그 경우 해당 항목의 **하류 소비 행에 미결로 적는다.**
> 대장에서 결함이 닫혔는지는 `DEFECT-LEDGER.md` 가 정하지 이 폴더가 정하지 않는다.

## CR-012 · B층 확증분할의 질의 노드 (D-27) — 상류 종료 2026-08-08 · **하류 미결**

| | |
|---|---|
| 상류 구현 | `732b0d9` (2026-08-08) — B층 질의 200 이 `ontology/sdkb-abox-b-layer-queries.ttl`(신설 · 트리플 4,204)로 섰다. **새 술어 0 · 새 클래스 0 · T-Box 델타 0**(층 구분을 술어가 아니라 **별도 파일**로 냈다 — CR-012 §4 ⓑ 의 선호 형태) · **인용 간선 0** · 봉인 `127a138f…` 불변. 청구항 2,618 → `ClaimFeature` 5,962(A층과 같은 분해기·같은 모델 `ollama qwen3-coder:30b` · 채택 9.7 %) |
| 검증기준 6항 | ② 독립항 확보율 **200/200 = 1.0000** ✅ · ③ A층 질의 1,000 불변 — 산술 증명(`Claim` 594,078 − 2,618 = 591,460 · `ClaimFeature` 1,306,419 − 5,962 = 1,300,457) ✅ · ④ 인용 간선 신설 **0** ✅ · ⑥ T-Box 델타 0 ✅ · **① `is_query` 1,000 → 1,200 = 하류 몫 · 미결** · **⑤ 하류 몫 · 미결** |
| 하류 소비 | **미완 — 이것이 판독 B 개봉을 막는 마지막 것이다.** 경로는 §2.1 재측정이 아니라 **§2 전체**다(`VENDOR_FILES` 추가 + `assemble.py` 층 분리 = 코드 변경 · CR-012 §8 이 예고). 정본 계획 [PLAN-045](../../01.code_spec/plans/PLAN-045-b-layer-query-ingestion-downstream.md) — 🛑 §1 요구정의 승인 대기 |
| 회신 전문 | [HANDOFF-QUEUE §1.16](../HANDOFF-QUEUE.md) |
| 상류가 느슨하게 풀지 않은 것 | `Shape_RejectedPatent` 가 인용 ≥1 을 요구하는데 CR-012 는 인용을 싣지 말라 한다 — **둘 다 옳다.** `minCount` 를 낮추지 않고 검증 범위로 갈랐다(§1.16). 합격선을 결과에 맞춰 고치지 않은 사례로 인용 가능하다 |
| 부수 관측 (개봉 전 기록) | **B층은 A층과 화제가 다르다** — 문서당 개념 **1.105 대 2.909** · `H01L` **0.0 % 대 14.8 %**. 온톨로지 결함이 아니라 표본의 성질이며 확증분할 교환가능성의 열린 질문이다. **이 관측으로 ε·δ·주지표를 조정하지 않는다**(§1-2) — 개봉 전에 적어 둔 이유가 그것이다 |

## CR-011 · B층 청구항 사이드카 발행 (D-26) — 종료 2026-08-07

| | |
|---|---|
| 상류 구현 | `993f5e2` (2026-08-06) — B층 청구항이 A층과 같은 사이드카 형식으로 발행됐다. 분해기·모델은 A층과 동일하며 채택률 **506/4,903 = 10.3 %** 로 A층과 사실상 같다(§1.10) |
| 검증기준 | ①②③ **상류 통과** · ④ **하류 통과 — 독립항 확보율 0.0085 → 1.0000**(§1.11) |
| 하류 소비 | **완료** — 경로는 `make vendor` 가 **아니었다**(§1.10). §2.1 사전등록 [PLAN-042](../../01.code_spec/archive/PLAN-042-cr011-central-axis-reprep.md) 아래 `central_axis build` → `make corpus` 로 재측정 |
| 함께 해소된 것 | **D-24**(미선언 `requests`) — 구현이 실제로 그 경로를 지났다 |
| 남은 비대칭 아님 | **JP 0.0000 은 양층 동일**하므로 CR-011 범위 밖이다 — D-05 이고 분모 제외는 합의됐다(§1.11) |

> **이 CR 이 판독 B 개봉을 막던 차단은 해제됐다** — 그러나 개봉되지 않았다. 다음 차단이
> **D-27(질의 노드 부재)** 이었고, 그것이 위 CR-012 다. **차단 해제를 개봉 가능으로 읽지 않는다.**

## CR-013 · 원소 기호 별칭의 정밀도 (D-20) — 종료 2026-08-08

| | |
|---|---|
| 상류 구현 | `4f3dbfb` (2026-08-08) — 단독 `hf` **제거** · `high k` 를 `material:hfO2` 에서 떼어 상위 부류 `material:dielectric` 로 **재지정**. 어휘 신설 **0** · T-Box 델타 **0** · IRI 변경 **0**. 사전 쌍 653 → 652 · 표면형 636 → 635 · blocked 6 → 8(`R6-SURFACE-SUPPRESS` 신설). A-Box `involvesMaterial → hf_acid` **34 → 15**(제거 19 · 추가 0 · 원문 대소문자로 갈랐다) |
| 검증기준 7항 | ① 단독 `hf` 표면형 **0건** ✅ · ② `high k`→`hfO2` **0건** ✅ · ③ `hf_acid` 문서 1,522 → **412** ✅ · ④ `hfO2` 391 → **298** ✅ · ⑤ 고유 (doc,concept) 쌍 106,496 → **105,347** ✅ · ⑥ T3 em·tf·core 1.000 **하락 0** ✅ · ⑦ **합격선 없음(보고)** — P1 family R@100 0.4556 → **0.4556** |
| **⑤ 가 재산출된 이유** | 상류가 **제거가 아니라 재지정**을 골랐으므로(회신 §2·§4) 순수 제거를 가정한 구 예측 105,293 이 무효가 됐다. **동결 전에** 다시 계산했다(`scripts/plan043_predict.py` · `data/profiles/plan043_prediction.md` · 커밋 `8443f7c`). 같은 스크립트로 순수 제거판을 계산하면 105,293 이 재현되므로 **바뀐 것은 산술이 아니라 상류가 고른 형태**다 |
| **⑦ 을 읽는 법** | **오링크 1,110 문서분을 걷어냈는데 검색은 미동도 하지 않았다**(198질의 전부 동점 · P0★ +0.0025 · B5 −0.0013 · 셋 다 CI 가 0 을 포함). "제거가 무익했다"가 아니라 **그 표면형이 정답 근처의 순위를 결정하는 신호가 아니었다**는 관측이다. A층 test 는 개봉 표본이므로 **탐색적**이고, 이 값으로 H3 를 재확증하지 않는다 |
| 하류 소비 | **완료** — PLAN-043 사전등록 `8443f7c` → vendor(상류 `4f3dbfb`) → G₀ 115,095 → **115,076**(−19) → corpus(41,031행 불변 · sha `1a108009…` → **`55e38161…`**) → index·dense·hybrid → 누출 **PASS**(금지간선 0 · 봉인 `984f8ef3…`·`127a138f…` 불변) → gate(L0–L3 ✅ · T3 ✅) |
| 분리된 후속 | **D-28**(적용기 `.lower()` 가 `Hf`/`HF` 를 뭉친다 · 하류 소관) · **D-29**(G₁/G₂ 가 2026-07-23 동결이라 A-Box 교정이 코퍼스 union 에서 되살아난다) · **D-30**(델타 유형 분류기가 파일명으로 판정해 T1·T2 가 **미검정**) |

> **T1·T2 가 미검정인 것을 "통과"로도 "실패"로도 읽지 않는다.** E7 적격심사가 이 델타를
> `abox` 로 분류해 비교를 거부했다(D-30). 자격 없는 비교로 지지를 만들지 않는 장치가 의도대로
> 작동한 것이고, 다만 **자격 규칙이 실질보다 거칠다** — CR-013 은 문서를 한 건도 늘리지 않았다.
> **`make gate SPLIT=test` 의 `Accept(ΔG)=1` 은 P1 대 B3 의 시스템 판정이지 이 자원 델타의
> 승인이 아니다 — 인용하지 마라.**

## CR-008 · B층 확증분할의 인용 선행기술 A-Box (D-18) — 종료 2026-08-06

| | |
|---|---|
| 상류 구현 | `39855bb` (2026-08-04) — B층 인용 **479 노드** · `CitedPatent` 3,034 → **3,513** · `sdkb-abox-prior-art.ttl` 57,075 → **66,453** 트리플 · 금지 간선 **0** |
| 검증기준 4항 | ① B층 도달성(분모 503 동결) **482/503 = 0.9583** ✅ · ② 관할별 본문 확보율 — KR 청구항 1.0000 ✅ · **US 청구항 0.9608 = 미달 확정** ❌ · JP 초록 0.8226 ✅ · WO·CN·EP 초록 1.0000 ✅ · ③ A층 IRI·트리플 변경 **0** ✅ · ④ 재 vendor 후 `make leakage` **금지 간선 0** ✅ |
| **② 를 미달로 확정한 근거** | 재시도 **1회 수행 · 실패**(2026-08-05 · §1.6a · 증거 [handoff/CR-008-us-retry-evidence.json](../handoff/CR-008-us-retry-evidence.json)). **임계 0.99 는 낮추지 않았다** — 결과를 본 뒤 합격선을 고치는 것이기 때문이다(§1-2·§1-3). 원인은 상류 진단과 **반대**였다: 미해소가 아니라 **본문 부재**(1968 출원 2건 · OCR 전문 없음)이며 원천을 바꿔도 닫히지 않는 D-05 계열 상한이다 → **D-25** 신설 |
| 하류 소비 | **완료** — PLAN-040 `make vendor`(상류 `39855bb`) → G₀ 105,713 → **115,095** · 코퍼스 41,031행(sha `db8132c33ceeb50a`) · **G1 하류 재측정 482/503 = 0.9583**(상류 값과 일치) · G3 누출 **0** |
| 분리된 후속 | **D-25**(`resolved` 플래그가 서지 해소와 본문 확보를 한 칸에 뭉친다 · CR 미발행) · **D-26 → [CR-011](CR-011-b-layer-claim-features.md)** |

> **⚠ 이 CR 은 종료됐지만 후속이 진행 중이다 — "재송부 금지"를 "후속 없음"으로 읽지 않는다.**
> CR-008 이 세운 B층 479 노드를 **대상으로 삼는** [CR-011](CR-011-b-layer-claim-features.md)(D-26)이
> 대기열 1순위로 살아 있다. CR-008 은 노드를 세웠고, 그 노드의 **청구항이 사이드카 형식이 아니라
> 하류 코퍼스에 도달하지 않는다**는 것이 CR-011 의 일이다. 상류에 보낼 것은 CR-008 이 아니라 CR-011 이다.

**이 CR 을 "B층 개봉 가능"으로 읽지 않는다.** 도달성 합격선은 채웠고 **봉인은 열리지 않았다**
(`127a138f…` 불변). 도달성은 정답이 후보 코퍼스에 **있는가**를 재고, 개봉 여부는 그 문서에
**본문이 있는가**가 함께 정한다 — 후자가 B층에서 비어 있었다(KR 청구항 0.0085 대 A층 1.0000).

## CR-004R · 거절이유 조항(RejectionReason) 구조화 (D-06) — 종료 2026-08-04

| | |
|---|---|
| 상류 구현 | `84ea514` (2026-08-03) — `ont:RejectionType` 개체 7 + 술어 5 · `RejectionReason` **2,749건** 실체화 · SHACL 2 shape · `sdkb-abox-claim-features.ttl` +19,240 트리플 |
| 결정 이행 | ⓐ 항(項) 단위 발행 ✅ · ⓑ 기존 개체 5개 IRI·의미 불변 ✅ · ⓒ 회차를 `PriorArtJudgment` IRI 에 넣지 않음 ✅(635 전량 존속) · ⓓ 표 파싱 우선 ✅ |
| 검증기준 6항 | #3 커버 출원 **994/1000** ✅ · #4 조항 2종 이상 **612 출원** ✅ · #5 IRI 635 존속 ✅ · #6 **T3 하락 0** ✅ · #2 조립률 68.9 % → **구조적 미달로 합격선 제외 · CR-010 분리** · #1 → 아래 |
| **#1 의 종결** | "청구항 × 조항 연결률"은 **폐기한다**(2026-08-04 결정). 조항은 `ont:rejectionEvidence` 로 **출원(Patent) 단위**에 붙었고, 그것을 출원 단위로 **확정**했다. 청구항 단위 발행은 T-Box 술어 추가(= H2 자격 유형 ①)를 부르므로 **이번 스냅샷의 변인을 늘리지 않기 위해** 요구하지 않는다 |
| 하류 소비 | **미완** — `sdkb-abox-claim-features.ttl` 은 아직 vendor 되지 않았다(PLAN-040 §2.1 사전등록 뒤) |
| 분리된 후속 | **CR-010**(`opinion_notices` 인용 해소 · 누출 통제 설계가 선행) · **D-24**(미선언 `requests` 2곳) |

**이 CR 을 "성능 개선"으로 읽지 않는다.** 만든 것은 조항 단위 거절근거를 **조회할 수 있는
구조**이고, 그것이 A5 제거손실·거절근거 축 하위집단 분석의 입력이 되는지는 **재측정이 판정한다**
(대장 D-06 검증기준).

## CR-007 · 개념 매핑 자산과 상위 개념 계층 (D-14 · D-15 · D-16) — 종료 2026-08-04

| | |
|---|---|
| 상류 구현 | `2839afb` (2026-08-01) — 상위 개념 7 + 구체 하위 6 · `skos:broader` 18 트리플 · `mappings/concept_mapping.json` 신설(11,727행) · `abox_term_aliases.json` 프로파일화 |
| 결정 이행 | ⓐ 상위 개념 7 ✅ · ⓑ `skos:broader` TBox 선언 ✅(연구 최초의 T-Box 술어 델타 · `owl:ObjectProperty` 97 → 98) · ⓒ 한글 단문 차단 = 규칙 `R4-SHORT-KO-TASK` ✅ · ⓓ `결함`·`수율` = `patent-text: null` 비활성 ✅ |
| 하류 소비 | PLAN-034(적용기 구현 · vendor 대상 추가) → PLAN-035(두 팔 실행) · **D-19 해소** |
| 성공기준 ① | **미달** — "개념의 ≥95 %가 표면형 ≥3개"는 27.0 %(patent-text 74/274). 원인은 다국어 라벨 부재(@en 632 · @ko 93 · @ja 0)이며 **CR-003 의 일이다.** 여기서 채우면 어휘를 지어내는 것이 된다 |
| 성공기준 ⑤⑥ | 하류 재측정으로 이관 — 상세는 대장 D-15 |
| 분리된 후속 | **D-20**(`hf` → 불산 오지정 1,234문서 · `high k` 과대특정) · **D-21**(`patent-text` 프로파일 ja 표면형 0) — 둘 다 대장에 등재됐고 CR 은 아직 없다 |

**이 CR 을 "성공"으로 읽지 않는다.** 자산은 발행됐고 하류가 그것을 소비해 **H2 를 최초로
실검정했으며 결과는 기각**이었다(D-23 · ΔR₁₀₀ = −0.0293). CR-007 이 만든 것은 그 검정을
가능하게 한 입력이지 성능 개선이 아니다.
