# upstream/archive — 종료된 CR (인용 가능 · 재송부 금지)

여기 있는 CR 은 **상류 구현이 끝났고 하류가 그 산출을 소비한 것**이다. 삭제하지 않는 이유는
하나다 — 대장(`DEFECT-LEDGER.md`)과 원고가 이 문서의 결정 항목을 참조하고, 재실험의 서사가
"무엇을 어떤 근거로 정했는가"를 되짚을 수 있어야 하기 때문이다.

**여기 있는 것을 상류에 다시 보내지 않는다.** `HANDOFF-QUEUE.md` §3 의 세션 프롬프트에서도
제거돼 있다.

## CR-004R · 거절이유 조항(RejectionReason) 구조화 (D-06) — 종료 2026-08-04

| | |
|---|---|
| 상류 구현 | `84ea514` (2026-08-03) — `ont:RejectionType` 개체 7 + 술어 5 · `RejectionReason` **2,749건** 실체화 · SHACL 2 shape · `sdkb-abox-claim-features.ttl` +19,240 트리플 |
| 결정 이행 | ⓐ 항(項) 단위 발행 ✅ · ⓑ 기존 개체 5개 IRI·의미 불변 ✅ · ⓒ 회차를 `PriorArtJudgment` IRI 에 넣지 않음 ✅(635 전량 존속) · ⓓ 표 파싱 우선 ✅ |
| 검증기준 6항 | #3 커버 출원 **994/1000** ✅ · #4 조항 2종 이상 **612 출원** ✅ · #5 IRI 635 존속 ✅ · #6 **T3 하락 0** ✅ · #2 조립률 68.9 % → **구조적 미달로 합격선 제외 · CR-010 분리** · #1 → 아래 |
| **#1 의 종결** | "청구항 × 조항 연결률"은 **폐기한다**(2026-08-04 결정). 조항은 `ont:rejectionEvidence` 로 **출원(Patent) 단위**에 붙었고, 그것을 출원 단위로 **확정**했다. 청구항 단위 발행은 T-Box 술어 추가(= H2 자격 유형 ①)를 부르므로 **이번 스냅샷의 변인을 늘리지 않기 위해** 요구하지 않는다 |
| 하류 소비 | **미완** — `sdkb-abox-claim-features.ttl` 은 아직 vendor 되지 않았다(PLAN-040 §2.1 사전등록 뒤) |
| 분리된 후속 | **CR-010**(`opinion_notices` 인용 해소 · 누출 통제 설계가 선행) · **D-24**(미선언 `requests` 2곳) |

**이 CR 을 "성능 개선"으로 읽지 않는다.** 만든 것은 조항 단위 거절근거를 **조회할 수 있는
구조**이고, 그것이 A5 제거손실·거절근거 축 하위집단 분석의 입력이 되는지는 **재측정이 판정한다**
(대장 D-06 검증기준).

## CR-007 · 개념 매핑 자산과 상위 개념 계층 (D-14 · D-15 · D-16) — 종료 2026-08-04

| | |
|---|---|
| 상류 구현 | `2839afb` (2026-08-01) — 상위 개념 7 + 구체 하위 6 · `skos:broader` 18 트리플 · `mappings/concept_mapping.json` 신설(11,727행) · `abox_term_aliases.json` 프로파일화 |
| 결정 이행 | ⓐ 상위 개념 7 ✅ · ⓑ `skos:broader` TBox 선언 ✅(연구 최초의 T-Box 술어 델타 · `owl:ObjectProperty` 97 → 98) · ⓒ 한글 단문 차단 = 규칙 `R4-SHORT-KO-TASK` ✅ · ⓓ `결함`·`수율` = `patent-text: null` 비활성 ✅ |
| 하류 소비 | PLAN-034(적용기 구현 · vendor 대상 추가) → PLAN-035(두 팔 실행) · **D-19 해소** |
| 성공기준 ① | **미달** — "개념의 ≥95 %가 표면형 ≥3개"는 27.0 %(patent-text 74/274). 원인은 다국어 라벨 부재(@en 632 · @ko 93 · @ja 0)이며 **CR-003 의 일이다.** 여기서 채우면 어휘를 지어내는 것이 된다 |
| 성공기준 ⑤⑥ | 하류 재측정으로 이관 — 상세는 대장 D-15 |
| 분리된 후속 | **D-20**(`hf` → 불산 오지정 1,234문서 · `high k` 과대특정) · **D-21**(`patent-text` 프로파일 ja 표면형 0) — 둘 다 대장에 등재됐고 CR 은 아직 없다 |

**이 CR 을 "성공"으로 읽지 않는다.** 자산은 발행됐고 하류가 그것을 소비해 **H2 를 최초로
실검정했으며 결과는 기각**이었다(D-23 · ΔR₁₀₀ = −0.0293). CR-007 이 만든 것은 그 검정을
가능하게 한 입력이지 성능 개선이 아니다.
