# 상류 대기열 세션 로그 — 2026-07~08 (기록 · 인용 금지 아님)

> 이 파일은 [HANDOFF-QUEUE.md](../HANDOFF-QUEUE.md) 상단에 쌓여 있던 **날짜별 인계 블록 11개**를
> 그대로 옮긴 것이다. 옮긴 이유는 하나다 — 대기열 표가 267행에 묻혀 다음 세션이 **무엇이
> 열려 있는지** 읽어내지 못했다. **한 줄도 고치지 않았고 삭제하지 않았다.**

> **`§1.x` 절 참조는 여전히 [HANDOFF-QUEUE.md](../HANDOFF-QUEUE.md) 를 가리킨다** — CR 회신
> 전문(§1.1–§1.16)과 §2·§3·§4 는 옮기지 않았다.

---

> ## 2026-08-08 · 11차 — **판독 B 가 끝났다. 상류에 보낼 것은 이번에도 없다.**
>
> 봉인 개봉·판정 1회 완료([PLAN-047 §16](../../01.code_spec/archive/PLAN-047-readout-b-unsealing-preregistration.md) ·
> 결과 커밋 `ff93456`). **H3 미지지**(R@100 개선 · nDCG@20 두 구성 모두 음의 방향) ·
> **A층 패턴은 독립 표본에서 재현**. 하류 결함 둘 등재 — **D-34**(봉인 qrel 이 수집 형식 ·
> 해소) · **D-35**(세 명령이 각각 봉인을 열어 "1회 개봉"을 셀 수 없다 · P2).
>
> **C0 관점의 결과 해석은 상류 요구로 곧장 이어지지 않는다.** 절제에서 **A7(온톨로지 전체) 만
> 유의**하고 축별(A1–A3·A6)은 전부 n.s. 이므로, *"어느 축이 이득을 내는가"* 는 **가르지 못했다** —
> 이 상태에서 특정 축의 자원 보강을 요구하면 근거 없는 요구가 된다. **CR 을 만들지 않는다.**
>
> **살아 있는 CR 여섯(CR-001A·002·003·005·006·009)은 논문 임계경로 밖 · C0 트랙**이며 투고 후로
> 미룬다. 그중 **CR-009 소비(df 가중)** 만은 판독 B 가 근거를 더했다 — P1 이 깊은 회수에서만
> 이기고 상위 정렬에서 못 이기는 패턴이 **두 표본에서 반복**됐다. 다만 그것은 재측정이 아니라
> **새 방법**이므로 §2 전체를 타는 별도 사전등록이며, 이 논문 밖이다.

> ## 2026-08-08 · 10차 — **상류에 보낼 것은 여전히 없다. 하류가 개봉 사전등록을 세웠다.**
>
> [PLAN-047](../../01.code_spec/archive/PLAN-047-readout-b-unsealing-preregistration.md) 작성 ·
> 🛑 §1 요구정의와 결정 3건 승인 대기. **봉인 둘 불변 · 코드 변경 0 · 지표 산출 0.**
> 판정식(PLAN-031 §4 승계)과 **T4 마진**(ε_T4 = 0.02 · η = 0.01 · §6.8 판독값 미참조)을 동결
> 대상으로 적었고, 개봉 전 게이트에 **G6**(B층 독립항 확보율)·**G7**(배관 검증 · 봉인 열람 0)을
> 더했다. **개봉 경로는 §2.1 이 아니라 §2 전체다** — 배관 넷이 비어 있다(PLAN-047 §6).
>
> **이 세션이 상류에 요구하는 것은 없다.** CR-001A(송부 보류) · CR-005 · CR-006 은 임계경로
> 밖 그대로이고, **CR-009 는 상류 발행 완료 · 하류 미소비**라 대기열에 남긴다(아래 표 불변).

> ## 2026-08-08 · 9차 — **상류에 보낼 것도, 하류에 남은 것도 없다. 다음은 개봉이다.**
>
> **D-33 을 닫았다** — 사용자 결정 ⓐ(코퍼스 재조립) · 사전등록 [PLAN-046](../../01.code_spec/archive/PLAN-046-d33-corpus-rebuild-preregistration.md)
> 동결 커밋 `8bdd843` · CLAUDE.md §2.1 정지 게이트 1개. **코드는 한 줄도 바꾸지 않았다** —
> `assemble.py:86` 이 `ont:publicationDate` 를 이미 OPTIONAL 로 읽고 있었다.
>
> **예측 넷을 실행 전에 적었고 넷 다 맞았다.** 공개일 결측 **6 → 0** · 여섯 값이 G₀ 실측과
> 전부 일치 · 시점 배제 쌍 A층 4,176 + B층 1,200 · 코퍼스 sha `3a5afe22…` → **`83eef760…`**.
> **불변식 여덟도 전부 지켜졌다** — 41,223행 · `is_query` 1,200 · `is_candidate` 41,031 ·
> **`text_main` 41,223건 바이트 동일** · `split.parquet` **바이트 불변** · 봉인 둘 불변 ·
> `make leakage` PASS · 질의밀도 **98.1 %** 불변 · T3 1.000 · **`make gate` Accept=1** ·
> pytest **520 passed**.
>
> **코퍼스 전 컬럼 대조에서 바뀐 칸은 둘뿐이다** — `publication_date` 198 ·
> `publication_number` 200. 전부 B층이고 값→다른값 변경은 **0**(전부 결측→값). 그중 후보인
> 것이 **6건**이다. **여섯은 qrel 정답으로 한 번도 등장하지 않으므로**(examiner 0 · 봉인 둘 0)
> 배제는 **방해문서만** 줄였고 회수가능 정답을 줄이지 않았다.
>
> **부수 관측이 더 중요하다.** 공개일이 채워지자 후보 제외 192건이 전부 공개일을 갖게 됐고,
> A층 질의 출원일보다 앞서는 조합이 **57,728쌍**이다. **시점 필터만 있었다면 B층 192건이
> A층 주분석 후보로 새어 들어갔다.** 배제를 지는 것은 `is_candidate` 배열이며(PLAN-045 D2)
> 회귀 테스트가 그것을 고정한다.
>
> **문서 정리도 끝냈다** — CR-014 → `archive/` · PLAN-045·PLAN-046 → `01.code_spec/archive/` ·
> CR-012 항목의 "하류 미결" 표기 해소 · 링크 전량 재지정(깨진 링크 0).
>
> **다음 행동은 개봉이다 — 그리고 그것은 별도 승인이다.** PLAN-040 §4 · PLAN-038 §16.6-2 의
> 사전등록을 세우고(**T4 마진을 §6.8 판독값을 보지 않은 상태에서 동결**) PLAN-031 아래
> 봉인 `127a138f…` 를 **1회** 연다. **봉인은 이 세션에서 열지 않았다.**
>
> **⚠ 개봉 사전등록이 반드시 인용해야 할 승계 조건 둘.** ① **D-32** — 이 코퍼스는 A층 후보
> 8건의 본문이 구 세대와 다르므로 **A층 §6 확증 수치를 여기서 재산출하지 않는다.**
> ② **B층은 A층과 화제가 다르다** — 문서당 개념 1.105 대 2.909 · `H01L` 0.0 % 대 14.8 %.
> **이 관측으로 ε·δ·주지표를 조정하지 않는다**(§1-2) — 해석의 경계로만 쓴다.


> ## 2026-08-08 · 8차 — **CR-014 가 끝났다. 대기열이 비었고, `make gate` 가 처음으로 완주했다.**
>
> **상류 `212fe62`.** 이 세션이 상류 역할로 구현했다(§0.1 예외 · 사용자 승인). 회신 전문은
> [CR-014 §7](CR-014-b-layer-bibliographic-fields.md), 하류 조치와 판정은 **§8**.
>
> **셋 중 둘만 채웠다 — 그리고 그것이 이 CR 의 결론이다.** `publicationNumber`·
> `publicationDate` **200/200**. 값은 KIPRIS `openNumber`(공개번호)다 — 같은 응답의
> `publicationNumber` 필드는 **공고번호**이고 거절특허는 등록되지 않아 **전량 `null`** 이었다.
> **이름만 보고 골랐으면 칸은 비고 값은 A층과 다른 것을 담았을 것이다**(상류 §1.3).
>
> **`processFamily`·`valueChainStage` 는 채우지 않았다.** CR-014 가 요구한 *"A층 파생 규칙
> 그대로"* 가 **성립하지 않았다** — A층의 두 값은 파생의 산물이 아니라 **SIRP 코호트의 수집
> 출처**이고(`search_strategy='plasma_H01J37' → process_family='etch'`), B층 200 은 IPC 스트림
> 스크리닝으로 뽑혀 그 라벨이 없다(A층 원본과 교집합 **0건** — 조인도 불가).
> **거부의 결정적 이유는 하류에 있다:** T2 하위집단이 "공정군"으로 갈리므로 A층 검색전략 라벨과
> B층 IPC 추론 라벨을 같은 축에 놓으면 **빌드는 성공하고 하위집단 분석만 거짓이 된다.**
>
> **그 회신을 받은 뒤에 shape 를 고쳤다 — 순서가 정당성이다.** `RejectedPatentContentShape` 의
> **두 줄만** `sh:or` 로 열었고 조건은 출처(`prov:wasGeneratedBy …/b_layer_query_ingest`)다.
> `minCount` 는 그대로이고, **공개번호는 B층도 여전히 요구받는다.** 회귀 테스트 셋이 양방향을
> 고정한다. 위반 **600 → 400(충전) → 0(개정)** · G₀ **119,208**(+400 = 200 × 2칸 · 600 이
> 아닌 것이 "추정으로 채우지 않았다"의 증거) · 스냅샷 sha 21개 중 **1개만** 변경 · 봉인 불변 ·
> **`make gate` Accept=1** · pytest 520 passed.
>
> **다음 행동은 상류가 아니다 — [D-33](../DEFECT-LEDGER.md) 결정이다.** 코퍼스는 아직 이 공개일을
> 읽지 않았고, 재조립하면 후보 6건의 시점 필터가 비로소 작동한다. **개봉 전에 정한다.**

> ## 2026-08-08 · 7차 — **대기열이 다시 찼다. CR-014 하나이고, 그것이 임계경로다.**
>
> **하류 PLAN-045 를 5단계까지 관통했다 — S1–S7 전부 통과.** `is_query` 가 실제로
> 1,000 → **1,200** 이 됐고(D-27 검증기준 ①), A층 분할 경계·봉인 둘·누출 0 이 전부 지켜졌다.
>
> **그런데 L1 이 새로 깨졌다** — B층 200 이 `publicationNumber`·`processFamily`·
> `valueChainStage` 를 갖지 않아 SHACL **600 위반**(200 × 3). A층은 셋 다 1,000/1,000 이다.
> **"상류가 발행했는데 하류가 소비하지 못한다"의 네 번째 사례**(D-19 · D-26 · D-27 · **D-31**).
> **하류에서 shape 를 느슨하게 만들지 않았다** — 그것은 결과를 본 뒤 합격선을 고치는 것이다.
>
> **다음 행동은 [CR-014](CR-014-b-layer-bibliographic-fields.md) 송부다.**

> ## 2026-08-08 · 6차 — **CR-012 가 끝났다. 상류 대기열이 비었고, 개봉을 막는 것은 이제 하류 코드다.**
>
> **상류 `732b0d9`.** 이 세션이 상류 역할로 구현했다(§0.1 예외). 회신 전문은 **§1.16**.
> 질의 200 이 `ontology/sdkb-abox-b-layer-queries.ttl`(신설 · 트리플 4,204 · `RejectedPatent`
> 200)로 섰다. **ⓑ 층 구분은 별도 파일이고 T-Box 델타는 0 이다** — 새 술어 0 · 새 클래스 0 ·
> IRI 규칙 변경 0. **인용 간선 `hasPriorArtExaminer`·`hasPriorArt`·`overPriorArt` 전부 0** ·
> **봉인 `127a138f…` 불변.**
>
> | # | 기준 | 실측 | 판정 |
> |---|---|---|---|
> | ② | 독립항 확보율 | **200/200 = 1.0000** | 통과 |
> | ③ | A층 불변 | `Claim` 594,078 − 2,618 = **591,460** · `ClaimFeature` 1,306,419 − 5,962 = **1,300,457** | 통과 |
> | ④ | 인용 간선 신설 | **0** | 통과 |
> | ⑥ | 층 구분 | 파일 분리 · A층 교집합 **0** | 통과 |
> | ①⑤ | `is_query` 1,200 · T3 | — | **하류 몫** |
>
> **`SHACL` 이 CR-012 와 정면 충돌했고, 느슨하게 풀지 않았다.** `Shape_RejectedPatent` 가
> 인용 ≥1 을 요구하는데 CR-012 는 인용을 싣지 말라 한다 — 둘 다 옳다. `minCount` 는 1 로
> 두고 **출처(`prov:wasGeneratedBy activity/b_layer_query_ingest`)를 가진 노드만** `sh:or`
> 로 면제했다. 음성 대조 확인 — **무인용·무출처 거절특허는 여전히 거부된다.**
>
> **공개/비공개 분리를 설계에 넣었다.** `~/Dev/sdkb` 는 공개 기반 온톨로지이고 B층 200 은
> **논문 확증분할의 봉인 질의**다. 산출물 둘(`sdkb-abox-b-layer-queries.ttl` ·
> `b_layer_queries_raw.jsonl`)을 **gitignore** 로 두고 생성기·테스트·집계 리포트만 커밋했다.
> **논문 종료 후 공개본 정리 시 그 두 줄이 경계선이다.**
>
> **⚠ 개봉 전에 반드시 읽을 것 — B층은 A층과 화제가 실제로 다르다.** 문서당 개념
> **1.105**(A층 2.909) · 링크 보유 **117/200**(A층 977/1,000). 구조화 브리지 부재만으로는
> 설명되지 않아 IPC4 를 함께 쟀다 — A층 `C23C` 20.7 % 대 B층 7.3 % · **A층 `H01L` 14.8 %
> 대 B층 0.0 %** · B층에만 `B23K` 10.9 %. **온톨로지 결함이 아니라 표본의 성질이다.**
> 판독 B 가 A층 결과를 재확증할 표본인지가 열린 질문이며, **결과를 본 뒤 꺼내면 사후
> 해석이 되므로 개봉 전에 적어 둔다.** 판정 규칙은 사전등록대로 두고 해석의 경계로만 쓴다.
>
> **다음 행동은 상류 회신 대기가 아니다 — 하류 코드 변경이고, §2.1 이 아니라 §2 전체다.**
> ① `vendor.py::VENDOR_FILES` 에 새 파일 추가 · ② `assemble.py:424` 의 `len(q) == 1000` ·
> ③ 질의밀도 분모(98.1 % → 81.7 % 거짓 하락) · ④ 검색·평가의 층별 분리 실행.
> **🛑 봉인 `127a138f…` 는 그때까지 닫아 둔다.**
> 상류에 남은 것은 임계경로 밖의 CR-001A → CR-005 · CR-006 뿐이다.
>
> **아래는 같은 날 5차 인계(CR-013 종료)이며 기록으로 남긴다.**

> ## (기록) 2026-08-08 · 5차 — **CR-013 이 끝났다. 상류에 남은 것은 CR-012 하나다.**
>
> **상류 `4f3dbfb` 가 두 줄을 고쳤고, 하류가 [PLAN-043](../../01.code_spec/archive/PLAN-043-cr013-hf-precision-remeasure.md)
> 사전등록(`8443f7c`) 아래 전량 재측정했다.** 절차는 §2.1 정지 게이트 1개였고, 동결 뒤 조건을
> 한 글자도 고치지 않았다.
>
> **검증기준 ①–⑥ 은 전부 통과했고 예측과 정확히 일치했다** — 단독 `hf` 표면형 0건 ·
> `high k`→`hfO2` 0건 · `hf_acid` 문서 1,522 → **412** · `hfO2` 391 → **298** ·
> 고유 (doc,concept) 쌍 106,496 → **105,347** · T3 em·tf·core 1.000 **하락 0**.
> G₀ 115,095 → **115,076**(−19) · 코퍼스 41,031행 불변 · sha `1a108009…` → **`55e38161…`** ·
> 누출 **PASS** · 봉인 `984f8ef3…`·`127a138f…` **불변**.
>
> **⑦ 은 합격선 없이 보고한다 — P1 family R@100 0.4556 → 0.4556 (ΔR₁₀₀ = +0.0000 · 198질의
> 전부 동점).** P0★ 0.4543 → 0.4568 · B5 0.2265 → 0.2253(셋 다 CI 가 0 을 포함).
> **41 %가 반대 뜻이던 표면형을 1,110 문서에서 걷어냈는데 검색은 미동도 하지 않았다.**
> "제거가 무익했다"로 읽지 않는다 — 그 표면형이 **정답 근처의 순위를 결정하는 신호가
> 아니었다**는 관측이고, D-23 이 보인 자원↔태스크 어긋남의 **세 번째 사례**다(DP1).
>
> **⑤ 는 동결 전에 재산출했다.** 상류가 제거가 아니라 **재지정**을 골랐으므로(회신 §2·§4)
> 순수 제거를 가정한 구 예측 105,293 이 무효였다. 재산출은 `scripts/plan043_predict.py` ·
> 산출 `data/profiles/plan043_prediction.md` · 커밋 `8443f7c`. 같은 스크립트로 순수 제거판을
> 계산하면 105,293 이 재현되므로 **바뀐 것은 산술이 아니라 상류가 고른 형태**다.
>
> **새 결함 둘 — 둘 다 하류 소관이다.**
> **D-29**: G₁/G₂ 가 2026-07-23 에 얼려져 있어 상류가 지운 A-Box 링크가 코퍼스 union 에서
> 되살아난다(`graph_v0` 15 대 `graph_v1`·`graph_v2` 32). **적용기 층(⑤)에는 영향 없다.**
> **D-30**: 델타 유형 분류기가 파일명 표지만 보고 CR-013 을 `abox` 로 오분류해 **T1·T2 가
> 미검정**이 됐다(E7 실격). **결과를 본 뒤 고치지 않았다** — 개정은 별건의 새 사전등록이다.
>
> **⚠ `make gate SPLIT=test` 의 `Accept(ΔG)=1` 을 CR-013 승인으로 인용하지 마라** — 그것은
> P1 대 B3 의 시스템 판정이다. 자원 델타의 답은 **미검정**이다.
>
> **다음 행동은 CR-012 회신 대기다.** 회신이 오면 순서는 CR-013 때와 같다 — ① `VENDOR_FILES`
> 판정 → ② §2.1 사전등록 커밋 → ③ 무정지 관통 → ④ G1–G6 재판정 → ⑤ 통과 시에만 개봉 1회.
> **판독 B 봉인 `127a138f…` 는 계속 닫아 둔다.**
> 그 뒤가 CR-001A → CR-005 · CR-006 이다.

> **(기록) 2026-08-07 · 4차 — CR-012 를 상류에 송부했다. 상류 회신(2단계 분석 → 3단계 설계 승인 요청) 대기.**

> 송부한 것은 §3 첫 블록의 **정정 반영판** 송부문이고, 상류가 읽을 파일 둘은 커밋된 상태다 —
> 정본 [CR-012](CR-012-b-layer-query-nodes.md)(§3 정정 포함) · 이관
> [handoff/CR-012-b-query-ids.txt](../handoff/CR-012-b-query-ids.txt).
> **송부 직전 재검증(2026-08-07):** 이관 파일 200행 · sha256
> `ef4ad03c2734af4212209516c05064b1550bea478f1f79bc72f3e2f14bac60e5` 일치 · 사전순 정렬 확인 ·
> 중복 0 · 워킹트리 clean.
> **하류는 회신 전까지 판독 B 를 열지 않는다** — 봉인 `127a138f…` 불변.
> **회신 접수 시 첫 행동은 §2.1 사전등록**이며, `make vendor` 여부는 CR-011 때처럼
> **바뀐 파일이 `VENDOR_FILES` 에 속하는지로 판정한다**(§1.10 · 중심축일 수 있다).
>
> **함께 송부 — [CR-013](CR-013-element-symbol-alias-precision.md)(D-20 · 2026-08-07 송부 · §1.15).**
> CR-012 와 **독립이므로 병행 송부했다** — 상류 회신 둘을 각각 기다린다. 요구는 사전 값 변경 둘뿐이다 — 단독 `hf` **제거** ·
> `high k` → `hfO2` **분리**. 어휘 0 · T-Box 0 · IRI 0 · 생성기 재실행 없음.
> **§1.14.5 의 검증기준 초안 둘이 뒤집혔다(§1.15).** ① 재지정은 **기각**됐다 — 단독 `hf` 문서 1,245 의
> 원문 표기가 `Hf` 736 · **`HF` 488** 이라 재지정하면 488 문서에 반대 방향 오링크가 생긴다 ·
> ② `hfO2` 는 **391 → 298 로 감소**한다(초안은 '증가'라고 적었고, 그것은 재지정을 전제한 값이다).
>
> **(기록) 2026-08-07 · 3차 — 송부 준비 완료 시점의 인계.**
> 송부 재료 셋이 준비됐다 — ① 이관 파일 [handoff/CR-012-b-query-ids.txt](../handoff/CR-012-b-query-ids.txt)
> (200행 · sha256 `ef4ad03c…`) · ② §3 첫 블록의 송부문 · ③ **§1.13 정정**.
> **정정이 요구를 줄였다** — 하류 조립기의 질의 식별은 `a ont:RejectedPatent` **타입**이지
> 간선이 아니다(`assemble.py:131-134`·`208`). 규약 충돌은 없었고, ⓑ 에 새 T-Box 술어가 필요 없다.
> **정정 전 송부문을 보내면 상류가 필요 없는 술어를 설계한다.**
> 다음은 CR-001A(§1.4 · **D-20 과 파일이 겹치므로 순서 제약을 먼저 설계**) → CR-005 · CR-006.
>
> **순서 제약 설계 완료 (2026-08-07 · §1.14) — CR-001A 앞에 D-20 이 선다.** 셋이 나왔다 —
> ① **증폭**: 지금 릴리스 A-Box 에 34 건뿐인 `hf`→불산 오링크가 재추출로 **1,245 문서 규모**로
> 들어가고 하류 3곳이 상속한다(하류는 §0.1 로 못 고친다) · ② **하류 한계효과 ≈ 0**: 적용기가
> 2026-08-01 부터 이미 같은 사전을 같은 `text_main` 에 돌린다(`concept_link.py:130`) ·
> ③ **D-20 의 수정안 ①(ambiguous 병기)은 하류에서 오링크를 지우지 못한다** — Q3 규칙이 후보를
> 전부 유지한다. **D-20 은 CR 문서가 없으므로 발행(CR-013)이 선결이다.**

> **2026-08-06 · 2차 — CR-011 이 상류에서 구현됐다(`993f5e2`). 상류 대기열은 지금 비어 있다.**
> 성공기준 **①②③ 통과**(KR 235/235 · US 49/49 · A층 IRI 1,875,867 전량 존속) · **④ 는 하류 몫**이다.
> **다음 행동은 상류 회신 대기가 아니라 하류 `make vendor` 이고, 그 앞에 §2.1 사전등록이 선다.**
> 원인은 CR 본문이 적은 것보다 한 겹 앞이었다 — **형식이 아니라 분해 미실행**이었고, 하류가 관측한
> **B층 KR 0.0085 = 2/235** 가 우연히 분해돼 있던 그 2건이다(D-26 갱신 참조).
> **함께 해소된 것 하나 — D-24**(미선언 `requests`). P2 로 미뤄 뒀으나 CR-011 구현이 실제로 그
> 임포트에서 죽어 임계경로로 올라왔다.
> **보낼 수 있는 것은 이제 CR-001A 다**(§3 · 선행조건 전부 해소). 그 뒤 CR-005·CR-006.

> **2026-08-06 — CR-008 을 종료하고 `archive/` 로 내렸다.** 하류 소비가 끝났기 때문이다
> (PLAN-040 `make vendor` · G₀ 115,095 · 하류 재측정 482/503 = 0.9583 · `make leakage` 0).
> **보낼 것은 CR-011 하나다** — 아래 표의 순서 1이고, 송부문은 §3 첫 블록이다. 그 뒤가
> CR-001A → CR-005 · CR-006 이다(아래 2026-08-04 줄의 "CR-001A 하나다"는 그 시점의 기록이며,
> CR-011 이 2026-08-05 에 신설돼 앞에 섰다).
> **CR-009 는 상류 대기가 아니라 하류 대기다** — 상류 발행은 끝났고 하류가 `concept_meta` 를
> 아직 읽지 않는다. 읽게 만드는 것은 재측정이 아니라 **새 방법**이므로 §2 전체를 탄다.
>
> **2026-08-05 — 임계경로에서 상류가 빠졌다.** US 문헌 2건 재시도를 1회 수행했고 **실패했다**
> (§1.6a). 해소되지 않았으므로 **US 청구항 0.9608 은 미달로 확정**이고, 인용 노드가 늘지 않았으므로
> **스냅샷 서명도 바뀌지 않는다.** 즉 더 기다릴 회신이 없다 — 다음 행동은 `make vendor` 이고,
> 그 뒤는 PLAN-040 사전등록 아래 무정지 관통이다.
> **함께 나온 것 하나 — 상류의 원인 진단이 반대였다**(미해소가 아니라 본문 부재 · 대장 **D-25**).
>
> **2026-08-04 갱신에서 바뀐 것 넷(기록 보존).** ① CR-004R 회신 2항 접수 — #6 T3 하락 0 · #1 은
> 산출 불가가 답이다(§1.1b) · ② CR-008 구현 완료 `39855bb` — 성공기준 ①③ 통과 · ② US 청구항
> 0.9608 미달(§1.6) · ③ CR-009 발행 완료 — 스키마 1.1(§1.7) · ④ CR-001A 선행조건 해소.
> **판단 3건은 전부 결정됐다(§1.8).**
>
> **지금 상류에 보낼 수 있는 것은 CR-001A 하나다**(§3 · 선행조건 전부 해소). 그 뒤 CR-005·CR-006.

> **받는 곳:** `~/Dev/sdkb` · **보내는 곳:** `~/Dev/SKKU/sdkb-prior-art-paper`
> 하류 §0.1 규약상 **CR의 정본은 하류에 있다.** 상류는 이 저장소의 파일을 **읽고** 구현하며
> 복사본을 만들지 않는다. 상류는 자신의 CLAUDE.md §2 게이트를 따로 갖는다 —
> TBox·IRI 변경은 상류에서 다시 승인받는다.
>
> **종료된 CR 은 [archive/](./) 로 내린다 — 인용은 가능하고 재송부는 금지한다.**
> 현재 **일곱**이다 — CR-007(D-14·D-15·D-16) · **CR-004R**(D-06 · 2026-08-04 종료) ·
> **CR-008**(D-18 · 2026-08-06 종료) · **CR-011**(D-26 · 2026-08-07 종료) ·
> **CR-012**(D-27 · 2026-08-08 종료 — **하류 소비도 완료**) ·
> **CR-013**(D-20 · 2026-08-08 종료) · **CR-014**(D-31 · 2026-08-08 종료).
> 2026-08-08 에 CR-011·CR-012·CR-014 를 내렸다.
>
> **아카이브는 상류 트랙의 종료를 뜻하지 결함이 닫혔음을 뜻하지 않는다** — 대장이 정하지 이
> 폴더가 정하지 않는다([archive/README.md](README.md) 서두). **다만 2026-08-08 9차
> 기준으로 미결인 항목은 없다** — CR-012 의 ①⑤ 는 PLAN-045 완주로, 후속 D-31 은 CR-014 로,
> D-33 은 PLAN-046 으로 닫혔다.
>
> **살아 있는 CR 은 여섯이다** — CR-001A(송부 보류) · CR-002 · CR-003 · CR-005 · CR-006(송부 가능) ·
> **CR-009**(상류 발행 완료 · **하류 미소비**라서 내리지 않는다).

