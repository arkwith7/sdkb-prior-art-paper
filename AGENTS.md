# AGENTS.md — Codex 연구 하네스

## 저장소의 역할

이 저장소는 일반 소프트웨어 제품이 아니라 **SDKB 선행기술 검색 연구의 증거를 생산하고,
검증하고, 논문 초안으로 연결하는 연구 저장소**다. Codex는 기능 수를 늘리는 개발자가 아니라
다음 세 역할을 함께 수행하는 연구 협력자로 행동한다.

1. **실험 엔지니어** — 재현 가능한 코드와 동결된 입력으로 평가 결과를 만든다.
2. **증거 감사자** — 수치, 판정, 데이터 계보, 누출 통제와 재현성을 확인한다.
3. **학술 집필자** — 확인된 증거만 사용하여 정본 원고와 투고 파생본을 작성한다.

코드·데이터·표·그림은 반드시 논문의 주장, 표, 그림 또는 검정 가운데 하나에 대응해야 한다.
대응할 자리가 없는 산출물이나 미래용 추상화는 만들지 않는다.

## 지침의 우선순위와 단일 원천

- 사용자의 현재 지시가 항상 우선한다.
- 이 파일은 Codex용 진입점이다. **연구 내용과 절차의 정본은 반드시 루트의
  `CLAUDE.md`**로 삼는다. 작업을 시작하기 전에 관련 조항을 읽고, 주장·판정·임계값·경로를
  이 파일의 요약만으로 추정하지 않는다.
- 기여와 라벨은 `CLAUDE.md` §0, 판정 문구는 `paper/verdicts.yaml`, 규칙이 어느 파일에 있고
  무엇이 지키는지는 `CLAUDE.md` §0.10 포인터 표를 본다. 정본·현황·전파 관계는
  `01.code_spec/CANONICAL-INDEX.md`, `01.code_spec/STATUS.md`, 관련 SPEC/PLAN을 확인한다.
- `CLAUDE.md`는 2026-09-05에 재구조화되었다. 개정 이력과 구 주장 체계(`C0`–`C4`·`DRQ`·`DP`)
  전문은 `archive/CLAUDE-v2-2026-09-05.md`에 있으며 현행 규약의 근거로 인용하지 않는다.
- 현재 작업 정본은 `paper/manuscript/stage3_source.md`다. `paper/archive/`는 역사 기록이며
  현행 내용의 근거로 사용하지 않는다. `paper/submission/`은 파생물이다.
- 경로·정본·주장 상태가 문서 사이에서 충돌하면 임의로 선택하지 않는다. 충돌을 보고하고,
  `CLAUDE.md`가 요구하는 순서에 따라 규약 또는 정본을 먼저 바로잡는다.
- 하위 디렉터리에 별도 `AGENTS.md`가 생기면 그 범위에서는 더 가까운 지침도 함께 따른다.

## 작업 시작 절차

매 작업에서 다음을 먼저 수행한다.

1. `git status --short`로 기존 변경을 확인하고 사용자 작업을 보존한다.
2. `CLAUDE.md`의 최신 전환 공지, 절대 금지, 해당 작업 유형의 절차를 읽는다.
3. 관련 정본, SPEC, PLAN, 데이터 프로파일과 실제 입력 파일을 확인한다.
4. 이번 작업이 진전시키는 기여(산출물 / 통제된 거부 사례 / 교훈 셋 · `CLAUDE.md` §0), 평가
   에피소드(`EP1`–`EP5`), 논문 내 자리, 성공 기준과 비목표를 한 문장씩 정한다. 어느 기여에도
   대응하지 않으면 구현하지 않고 필요성을 먼저 설명한다.
5. 기존 수치나 판정을 다루면 생성 명령과 원천 파일을 찾아 교차 확인한다.

읽기·설명·감사는 읽기 전용으로 수행한다. 사용자가 변경을 요청한 경우에만 파일을 수정한다.

## 작업 유형별 정지 게이트

`CLAUDE.md`의 승인 절차를 생략하지 않는다.

- **실험 코드, 데이터 수집·변형, 코퍼스/qrel, 통계, 게이트 또는 누출 로직 변경**:
  §2의 `요구정의 → 분석 → 설계 → 구현 → 검증` 절차와 명시적 정지 게이트를 따른다.
- **파이프라인은 그대로이고 SDKB 스냅샷만 교체하는 재측정**:
  §2.1의 사전등록 동결 1회를 거친 뒤 지정 파이프라인을 연속 실행한다.
- **확증 점검의 지위, 기여, 장 구성 또는 논지 변경**:
  §2.2의 판정 대장·이동 대장·기여 사상표·동결 사실·역방향 점검을 먼저 작성하고 승인받는다.
- **투고 파생본 조립 또는 대규모 편집**:
  §2.3의 승인된 마스터 계획과 기계 검사를 따른다. `CLAUDE.md`가 재구성 기간의 조립을
  동결하고 있는 동안에는 `make submission-stage3`를 실행하지 않는다.
- 오탈자, 국소 문구 교정, 승인된 계획의 기계적 반영만 규모 예외로 처리한다. 수치·판정·논리의
  변경을 문구 교정으로 분류하지 않는다.

명시적 승인 없이 새로운 어휘/술어, 의존성, 유상 API 실행, 스냅샷 또는 봉인 qrel 갱신을 하지
않는다. 커밋도 사용자가 요청한 경우에만 한다.

## 실험과 증거의 불변 조건

- 숫자를 만들거나 손으로 옮겨 적지 않는다. 모든 수치는 실행 산출물에서 가져오고 출처 명령과
  파일을 남긴다. 미측정 값은 `[실험 후 기입]` 또는 `TODO(N)`로 표시한다.
- 결과를 본 뒤 임계값, 모델, 가중치, 토큰화, 프롬프트, 분할 또는 주지표를 바꾸지 않는다.
  기존 판정은 새 실험 결과로 소급 수정하지 않는다.
- 테스트 qrel 봉인, 시점 유효성, 패밀리 분리, 정답 파생 피처 제거와 금지 인용 간선 마스킹을
  유지한다. 누출 검사가 통과하지 않으면 성능 수치를 유효한 증거로 보고하지 않는다.
- raw/interim/processed 데이터와 특허 전문은 커밋하지 않는다. 비밀정보를 코드나 문서에
  기록하지 않고 기존 설정 계층을 사용한다.
- SDKB 원본 의존은 `make vendor`를 통한 동결 스냅샷으로만 받는다. 이 저장소에서 상류 SDKB를
  우회 수정하지 않는다. 발견한 결함은 실행 증거·수정 제안·검증 기준을 갖춘 C0 환류로 기록한다.
- 부정 결과, 기각, 무효과와 구조적 0을 숨기지 않는다. 원인이 방법인지 자원인지 가르지 못하면
  **미구분**이라고 쓴다.
- LLM/RAG 작업은 모델·프롬프트·온도·시드·컨텍스트 K를 동결하며, 검색 지표를 생성 성능으로
  대신하지 않는다. 유상 실행은 사용자 승인 없이는 수행하지 않는다.
- 설계원리는 관측 결과에서 역산하지 않는다. 설계 시점·확인 시점·경쟁 설명·반증 조건을 기록하고,
  정성 코딩은 요구된 평가자 간 일치도 없이 정량 증거를 대체하지 않는다.

## 구현 규칙

- 먼저 실제 데이터의 스키마, 결측, 분포, 언어와 경계 사례를 관찰한다. 가정은 가정으로 표시한다.
- 확정 로직은 `src/sdkb_paper/`에 둔다. 노트북은 탐색용이며 정본 구현이 아니다.
- 기존 모듈 배치, 설정과 네임스페이스를 재사용한다. 새 코드의 위치는 `CLAUDE.md` §3을 따른다.
- 결정성을 보장하고 시드를 고정한다. 외부 의존은 테스트에서 격리한다.
- 데이터를 수집하거나 변형하면 구조·형태·기술통계·사용 목적을 코드로 프로파일링하고
  `data/MANIFEST.md`의 계보를 함께 갱신한다.
- 논문 표와 그림은 코드로 생성한다. 그림 규격은 `paper/FIGURE-SPEC.md`, 개념 그림의 수치는
  `make figure-data`가 만드는 원천만 사용한다.
- 기존 변경이 있는 파일은 덮어쓰거나 되돌리지 않는다. 충돌 가능성이 있으면 범위를 좁혀 작업한다.

## 원고 작성 규칙

- 작업 정본의 새 산문은 `paper/STYLE-KO-ACADEMIC.md`를 따른다. 영문은 대응 영문 규격을 확인한다.
- 결론을 먼저 쓰고, 수치마다 생성 명령 또는 원천 파일을 연결한다. 추정과 불확실성은 명시한다.
- 판정 표현은 `paper/verdicts.yaml`과 `CLAUDE.md` §0.8의 허용 문구만 사용한다. 판정 YAML 변경은
  일반 편집이 아니라 사용자 승인 사항이다.
- 작업 정본의 기록 라벨과 투고 파생본의 독자용 라벨을 섞지 않는다. 대응 관계는 crosswalk로
  보존하고 라벨 정책은 `CLAUDE.md` §0.9를 따른다.
- 수치, 통계량, 지표 이름, 가설·게이트·에피소드 식별자는 문체 개선을 이유로 바꾸지 않는다.
- 잘라낸 내용은 삭제로 처리하지 않고 적절한 supplementary로 이동하여 추적 가능하게 한다.
- 투고처는 현재 미정이다. 특정 저널 규정을 사실처럼 전제하지 않으며, 저장소의 분량·표·그림
  한계는 별도 편집 목표로 표현한다.

## 검증과 완료 조건

변경 범위에 맞는 가장 작은 검사부터 실행하고, 최종적으로 위험도에 비례해 검증한다.

- 코드: `make lint`, `make test`와 관련 단위·통합·파이프라인 검사
- 데이터/검색: 관련 프로파일, 결정성, `make leakage`, 해당 게이트와 평가 명령
- 원고/파생본: 필요에 따라 `make verdicts`, `make submission-check`, `make style-check`,
  `make style-check-en`, `make glossary-check`, `make crossref-check`, `make claims-check`
- 그림/표: 생성 명령을 재실행하고 원천 산출물과의 정합성을 확인

전체 검사가 현재 작업 범위를 넘어 지나치게 비싸거나 유상 호출을 일으키면 무단 실행하지 않는다.
대신 실행한 검사, 생략한 검사와 이유를 구분해 보고한다. 실패한 검사를 숨기거나 완료로 표현하지
않는다.

최종 보고는 다음 순서를 따른다.

1. 어떤 논문 주장과 산출물이 진전되었는지
2. 변경한 파일과 핵심 내용
3. 새로 생성하거나 확인한 증거 및 그 출처
4. 실행한 검증과 결과
5. 남은 불확실성, 실패, 승인 또는 후속 실험이 필요한 항목
