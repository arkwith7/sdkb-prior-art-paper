# CLAUDE.md — 작업 규약 (v0.9 기조 · **v2.0 전환 중** · **설계과학(DSR) 프레임**)

> **DSR 전환 (2026-08-07 · PLAN-041 승인).** 논문의 중심을 *"가설이 채택됐는가"*에서 **"산출물을
> 어떻게 설계·평가했고 거기서 어떤 이전 가능한 설계지식이 나왔는가"**로 옮긴다. 기존 통계 검정은
> **버리지 않고 산출물 평가의 사전 동결 점검으로 재배치**한다.
> **판정은 하나도 바뀌지 않았고 수치는 한 개도 재산출하지 않았다** — H1–H5·ε·δ·주지표·분할 경계·
> §6의 모든 값이 그대로다(PLAN-041 §2 요건 1·4). 바뀐 것은 **결과를 쓰는 문장과 싣는 자리**다.
> 신설 조항 넷 — **§0 C4(설계지식)** · **§0.6 DP 승격 기준** · **§0.7 DRQ·DP 표** · **§1-12 DSR
> 고유 금지**. 개정 넷 — §0 확증 가설 문단(→ *"사전등록된 평가 점검"*) · §0 C2′ 상태 ·
> §0.2 N·I 표(설계원리 열) · §0.3 적용범위 · §7 산출물 3종.
> **왜 이 순서인가:** §2.2 요건 5 — *"원고가 규약을 앞질러 가면 그다음 세션이 낡은 규약을 근거로
> 되돌린다."* 그래서 **규약을 먼저 고치고 원고를 만진다.**
>
> **v2.0 전환 (2026-08-02).** 논지를 "층마다 지표가 어긋나므로 승인은 다음 층에서 검증한다"로
> 세우고, 주장에 **C2′(전달)** 를 더했다. 신설 조항 일곱 — §0.2 신규성·진보성 명세 · §0.3 "확실"의
> 정의 · §0.4 실무 이익 3등급 · §0.5 한 논문 유지 규칙 · §1-11 LLM 금지 · §2.2 원고 기조 변경 절차 ·
> §5 T4(설계만). **원고 v0.9의 판정과 §6 수치는 이 전환으로 바뀌지 않는다** — 바뀐 것은 앞으로
> 무엇을 재고 무엇을 주장해도 되는가이다.

> **전환 배경(2026-07-25):** 커버리지 H1 패러다임(v0.5/v0.7)에서 **선행기술 검색 주 태스크 + T-gate**
> 기조로 전환. 구본은 [archive/CLAUDE-v05.md]. 상세 [01.code_spec/archive/PLAN-017-v09-ir-benchmark-dataset.md],
> 메모리 `pivot-v09-retrieval-primary-task`·`spike-retrieval-feasibility-passed`.

이 저장소는 **소프트웨어 제품이 아니라 논문의 증거를 만드는 연구 코드**다. 모든 코드·데이터·그림의
유일한 존재 이유는 **정본 원고**의 세 주장을 **검증 가능한 수치로** 뒷받침하는 것이다. 논문에 들어갈
자리가 없는 코드는 쓰지 않는다.

> **정본 회귀(2026-07-29).** v1.0(영문 투고본)·v1.1(국문 정본)은 **`paper/archive/`로 내렸다 — 인용 금지.**
> 작업 정본은 다시 [paper/논문_v0_9_SDKB_통합초안.md](paper/논문_v0_9_SDKB_통합초안.md)이다.
> **이유:** v0.9 실험의 기각·무효과 다수가 방법이 아니라 **온톨로지 자원의 결함**임이 확인됐고
> (`upstream/DEFECT-LEDGER.md`), 상류 교정 후 **가설을 원래 설계대로 재평가**하기로 했다.
> v1.0/v1.1은 "교정 전 상태"의 정직한 기록으로 보존한다 — 재실험의 서사가 "결함을 찾아 고치고
> 다시 쟀다"인 이상 **고치기 전 상태를 인용할 수 있어야** 하기 때문이다.
>
> **v0.9를 쓸 때의 두 규칙.** ① 새 사전등록의 뼈대로 쓰는 것은 **§7.6 "결론 규칙"과 §8 "한계"**다
> — 앞의 것은 개봉 전에 동결한 "허용/금지 결론" 표이고, 뒤의 것은 "무엇을 왜 못 쟀는가"의 목록이라
> 다음 실험이 채워야 할 칸을 그대로 지정한다. (앞선 판이 지목한 §7 "예상 가능한 발견"은 **현
> 원고에 존재하지 않는다** — PLAN-033 재구성에서 §7이 논의로 재편되며 사라졌다. 없는 절을 가리키던
> 참조를 실재하는 절로 교정한다.) ② §6의 수치는 **구 자원(교정 전 스냅샷)에서의 관측**으로만 인용하고,
> **새 실험의 기대치로 인용하지 않는다** — 그 순간 사후 조정이 된다(§1-2·§1-3).
> 재실험 로드맵은 [01.code_spec/archive/PLAN-029-post-remediation-reexperiment.md]. 투고본은
> 재실험 후 **v2.0**으로 새로 쓴다.
>
> **투고 파생 경로 (2026-08-10 · PLAN-048).** 작업 정본(v0.9 통합초안)은 감사 가능한 전체
> 기록으로 유지하고, 투고본은 `paper/submission/` 아래 **파생물**로 만든다. 파생은 PLAN-048
> 이동 대장에 따른 이관·축약·재조준만 허용하며, **수치 재산출과 판정 변경은 포함하지 않는다.**
> 파생물과 정본의 판정 문구 정합은 `make verdicts`(§2.3)가 강제한다. 잘라낸 전문은 전부
> `paper/supplementary/`에 남아 링크로 도달 가능해야 한다.

> **투고 대상: Advanced Engineering Informatics (AEI).** 요구는 셋 — (1) 명시적 지식 표현(온톨로지·
> OWL/SHACL/SPARQL), (2) 지식집약 공학 태스크 적용(선행기술조사가 주), (3) 정성·정량 강한 평가.
> "돌아간다"는 시연은 증거가 아니다 — 검정·효과크기·강건성·누출통제가 증거다.
> **구 원고(v0.5·v0.7)와 커버리지 H1/H2/RQ3 산출물은 `paper/archive/`·`archive/`로 내린다 — 인용 금지.**

---

## 0. 주장 체계 — C0·C1·C2·C2′·C3 (모든 판단의 기준선)

SDKB는 **태스크 확장형 반도체 도메인 온톨로지 데이터셋**이다. 하나의 공유 T-Box가 전문가매칭·
선행기술조사·기술예측 세 뷰를 지탱하며, 이 데이터셋을 **핵심 태스크(선행기술 검색)로 증명하고,
진화 시 다른 태스크를 훼손하지 않음을 보증**한다.

> **v2.0 논지 한 문장 (2026-08-02 확정).**
> **다중 태스크 온톨로지의 진화는 태스크 성능을 승인 조건으로 통제되어야 하며, 그 승인 조건이
> 실제 가치를 대표하는지는 한 층 아래 태스크에서만 확인된다.**
>
> 층은 셋이고 **각 층의 지표는 다음 층으로만 검증된다** — 이것이 이 연구의 뼈대다.
> **자원 층**(어휘·링크·해상도) → **검색 층**(Recall@100·nDCG) → **생성 층**(RAG 근거성).
> 실측으로 확인된 것은 층 사이의 **어긋남**이다: 자원 지표를 2.4배 개선했더니 검색이 나빠졌다
> (D-23 · T1 실패). 같은 어긋남이 검색→생성 사이에도 있는지가 C2′의 질문이다.
> **"RAG"는 이 문장에 등장하지 않는다 — 계측기이지 주제가 아니기 때문이다**(§0.4).

| | 주장 | 논문 | 증거 | 상태 |
|---|---|---|---|---|
| **C0 · 상류 환류** | 실험은 SDKB의 **수정 가능한 결함**을 식별하고, 각 결함은 검증기준을 갖춘 상류 변경 요구로 전환된다 | 논문 밖 (제품 목적) | [upstream/DEFECT-LEDGER.md](upstream/DEFECT-LEDGER.md) · `upstream/CR-*.md` | 상시 |
| **C1 · 자원** | 공유 T-Box가 세 태스크를 표현한다 | §3·§6.1 | CQ 28·SHACL·도달성 사다리 (관측 사실) | 지지 |
| **C2 · 핵심증명** | 선행기술 검색에서 온톨로지 보강이 강한 텍스트 기준선을 (조건부) 개선 = 데이터셋이 핵심 태스크에서 실효 | §5–6 (RQ2·RQ3) | Recall@100·nDCG·ablation·부트스트랩 | **부분 지지 — 깊은 회수 한정** (H3 부분지지: 부차 P1 R@100 +0.0534 · 주 구성 비유의 · nDCG 미개선 / H4·H5 기각) |
| **C2′ · 전달 (신설 2026-08-02)** | 검색 층의 이득이 **생성 층(RAG 근거 답변)으로 전달되는지**를 잰다 — 승인 조건이 실제 가치를 대표하는지의 검증 | §5–6 (RQ5) | 인용 정확도·환각률·근거 지역화·Effort@Recall | **확증 수행 · 전달 미확증** (2026-08-09 갱신) — A층 탐색 1,188 호출 → B층 확증 1,188 호출 · **T4 판정 1회 = 실패**(인용 정확도 `LB₉₅ = −0.0205` ≤ −ε_T4 · 환각률 조건은 통과). **점추정은 P1 이 앞선다**(+0.0236) — *"생성 층에서 나빴다"* 가 아니라 *"비열등을 이 표본으로 보이지 못했다"* 다. PLAN-047 §19 |
| **C3 · 진화안전** | T-gate(T1 검색 비열등 + T2 하위집단 + T3 교차태스크 CQ 비회귀)는 세 태스크가 서로 간섭한다는 **관측을 전제로**, 그 간섭이 릴리스로 새어 나가지 않게 **차단**한다 — L0–L3가 놓치는 회귀를 잡는다 | §4·§6 (RQ1) | 결함주입·McNemar·음성대조군 | **부분 지지** (H1‴ 홀드아웃 범위 내 지지 · **H2 = 본 원고 자원에서는 미검정** · 이후 별도 사전등록(PLAN-035)에서 **1회 실검정 → 기각** — 아래 경계 참조) |
| **C4 · 설계지식 (신설 2026-08-07 · PLAN-041)** | 이 산출물의 설계·평가에서 **다른 공학 온톨로지로 이전 가능한 설계원리**가 도출된다 | §8 (신설) | DP1–DP6 + 근거 매핑 (§0.6 승격 기준) | **DP1–DP5 확립 · DP6 제안** |

> **투고본의 사전등록된 평가 점검은 두 개다(H3·H5) — 2026-08-01 · PLAN-033 · 사용자 승인 ·
> 2026-08-07 지위 개정(PLAN-041 A-2).** 이 둘은 **논문의 중심 주장이 아니라 산출물 평가의
> 사전 동결 점검**이며, **판정은 §2.2 요건 1 대로 불변이다.** 중심 주장은 설계 연구질문
> DRQ1–DRQ4(§0.7)가 지고, H3·H5 는 그 안의 평가 점검으로 **원고 §1.4a 에 그대로 보존**된다.
> **RQ2 검색 유용성 → H3**(하이브리드 효과) · **RQ3 계층 특이성 → H5**(음성 대조군). 기여도
> **2개**(① 세 태스크 표현 자원 + 진화 장치 ② 검색 유용성 실증 — 경계 명시)로 정렬됐다.
> **라벨은 재부여하지 않는다** — H3·H5 는 PLAN-031 이 사전등록에 쓴 이름이고, H1/H2 로 번호를
> 다시 매기면 사전등록 기록과의 추적성이 끊긴다. 개수만 줄이고 이름은 그대로 둔다.
> **이 규칙의 적용 범위는 작업 정본·PLAN·대장이다 — 투고 파생본은 §0.9 를 따른다**(2026-08-15 개정).
>
> **강등된 셋은 삭제가 아니다 — 판정 불변, 자리만 이동.** H1(게이트 판별력) → §4.9 설계 +
> §6.5 홀드아웃 확증 1절(4회차 재판정 전문은 `paper/supplementary/S2`) · H2(승인 안전성) →
> **§9.1**(§9.1.1 본 원고 자원에서의 미검정 · §9.1.2 이후 1회 실검정과 기각 · 아래 경계표) ·
> H4(계층 기여) → §6.4 절제 표의 A4·A5 행.
> **이 강등은 §1-2·§1-3 위반이 아니다** — 위반은 *판정을 바꾸는 것*이고, 바뀐 것은 *싣는 자리*다.
> 아래 C0–C3 표의 상태 열은 **판정 기록이므로 소급 수정하지 않는다**(H1–H5 전량 유지).

**구 5가설 체계(H1–H5)는 판정 기록으로 보존한다.** RQ1 검증 게이트 → H1 게이트 판별력·H2 승인
안전성(=C3) · RQ2 검색 유용성 → H3 하이브리드 효과(=C2) · RQ3 계층 기여와 특이성 → H4 계층 기여·
H5 음성 대조군(=C2) · **RQ4 계층 독립성** → H5의 음성 대조군 실패가 답한다(=C2·C3 경계).
구 기여 4개(검증 아키텍처·SDKB 자원·결함주입 게이트 평가·경계 명시 검색 결과)는 위 2개로 묶였다.
나머지(운용 효율·법적 맥락·의미 도달성)는 **탐색적 분석으로 강등**한다. 교차언어 결과도 v1.1에서
**탐색적 진단으로 강등**됐고, 절제의 "태스크 결합(entanglement)" 인과 표현은 **"경험적 교차 태스크
의존성"으로 완화**됐다.

> **경계 — H2(갱신 승인 안전성)는 미검정이고, 그 이유는 구조적이다.**
> **P1 대 B3 비교는 H2의 증거가 아니다** — 그것은 서로 다른 두 시스템의 검색 유용성 비교이고,
> H2는 **변경 없는 동일 파이프라인에 O와 O′를 넣은** 비교로만 잰다.
> **실측(D-12): G0·G1·G2의 T-Box는 완전히 동일하다**(클래스 103·ObjectProperty 97·DatatypeProperty 81 ·
> 술어 델타 추가 0·제거 0). 세 "세대"는 전부 특허 A-Box 추가다. 즉 자격 있는 델타가 우연히 없었던
> 것이 아니라 **T-Box가 한 번도 바뀐 적이 없어 존재할 수 없었다.**
>
> **델타 유형을 뭉뚱그리지 않는다(재실험 사전등록 필수 항목).**
>
> | 유형 | 예 | T1 | T2 | T3 | H2 자격 |
> |---|---|---|---|---|---|
> | ① T-Box 델타 | 공리·클래스·술어 (CR-005) | 간접 | 간접 | **직접** | 있음 |
> | ② 개념층 델타 | 어휘·계층·라벨·링크 (CR-001·002·003) | **직접** | **직접** | 직접 | 있음 |
> | ③ A-Box 코퍼스 델타 | 문서 추가 (G0→G1→G2) | 비교 불성립 | — | — | **없음** |
>
> **갱신 (2026-08-01) — T-Box 는 이제 한 번 바뀌었다. 위 D-12 기록은 소급 수정하지 않는다.**
> 상류 `2839afb` 스냅샷(CR-007 반영)의 G₀ 실측: 트리플 105,588 → **105,713**(+125) ·
> `owl:ObjectProperty` 97 → **98**(`skos:broader` 선언) · `skos:broader` 11 → **18** ·
> 클래스 103 불변 · DatatypeProperty 81 불변. **연구 최초의 T-Box 술어 델타**이며, 위 표의
> **유형 ①이므로 H2 자격이 있다.** 다만 자격이 있다는 것과 검정됐다는 것은 다르다 —
> 하류에 적용기(linker)가 없어 `ir_corpus_v09.parquet` 서명이 바이트 단위로 불변이었고
> (D-19), 그래서 ΔR₁₀₀ 이 정의상 0 이었다 — 그 시점까지 **H2 는 미검정**이었다.
>
> **갱신 (2026-08-01 · PLAN-035) — H2 는 최초로 실검정됐고, 기각됐다. 위 두 기록은 소급 수정하지 않는다.**
> D-19 가 해소되어(하류 적용기 구현) 문서집합 불변 · 온톨로지만 교체한 두 팔이 처음 성립했다.
> 자원 델타는 문서당 개념 1.545 → **3.779**(2.4배) · 어휘 141 → 199 · 신규 링크 128,875 ·
> 코퍼스 sha `ec5ea51b…` → `9fec15c6…`. 코드·검색설정·분할·봉인 qrel 은 전부 동결(적격심사 E1–E7 7/7).
> test 198질의 family R@100 : **B5 온톨로지 단독 0.1800 → 0.2282(+0.0482)** · P0★ 0.4635 → 0.4543 ·
> **P1 0.4849 → 0.4556 (ΔR₁₀₀ = −0.0293 · 95%CI [−0.0542, −0.0053])**.
> **판정: T1 실패(LB₉₅ ≤ −ε) · T2 통과(max drop +0.0401 < δ) · T3 통과(em·tf·core 1.000 유지) →
> Accept = 0 · 승인 거부.** 원고 반영 위치는 §9.1.2 다.
>
> **이 결과를 읽는 두 규칙.** ⓐ **"온톨로지 보강은 무익하다"로 읽지 않는다** — 온톨로지 단독 팔은
> 27 % 개선됐다. 관측된 것은 **자원 품질 지표와 태스크 성능이 어긋날 수 있다**는 사실이고, 그것이
> T-gate 의 존재 이유 그 자체다. ⓑ **원인은 미구분이다**(D-23) — 자원 결함인지 하류 점수식(비가중
> Jaccard · df 가중 없음 · `w_h`=0)의 결함인지 이 실험은 가르지 못한다. 가르려면 df 가중을 넣고
> 다시 재야 하고, 그것은 재측정이 아니라 **새 방법이므로 §2 전체를 탄다.**
>
> 추가로 P1 점수식의 **w_h = 0**이므로 계층·정렬만 바꾸는 델타는 T1·T2에 원리적으로 비가시임을
> 사전 자인한다(`results_table.py:38`) — 단 이는 D-02가 교정되면 해소될 수 있다.
> 자격 없는 델타로 "지지"를 만드는 것은 §1-2 위반이다.
전문가매칭·기술예측은 C1의 표현범위이자 C3의 감시대상(+절제 음성대조군)이며, **성능은 주장하지
않는다**(별도 논문 AFCP-EM). 구 11개 세부가설(H1a–d·H2a–c·H3a–d)은 이 5개로 축약됐다(매핑:
[01.code_spec/RECONCILIATION-v09.md] §0.1).

> **구 라벨 취급 — S-시리즈 재라벨 규약은 폐기한다(2026-08-02).** v0.9는 RQ1–3/H 라벨을 **재정의**
> 했으므로(RQ1=검증게이트·RQ2=검색유용성·RQ3=계층기여) 구 패러다임 라벨은 **문맥 없이 "H1"이라
> 쓰지 않는다**는 규칙만 남긴다("구 커버리지 H1" 형식으로 쓴다). 구 라벨을 S1·S2·S3로 재라벨해
> 보존하던 규약은 **폐기**한다 — 두 가지 이유다. ① 현 원고는 그 라벨을 **한 번도 쓰지 않는다**(산출물
> 없는 유지 의무). ② **이름이 충돌한다** — 원고가 실제로 쓰는 `S1`·`S2`·`S3`는 supplementary 파일
> (`S1` 부록 A·C–H · `S2` 결함주입 4회차 재판정 · `S3` 미실행 설계)이고, 같은 기호를 구 가설에도
> 붙이면 본문의 링크와 규약이 서로 다른 것을 가리킨다. **`S1`–`S3`는 supplementary 파일만 가리킨다.**
> 구 라벨 대응은 필요할 때 [01.code_spec/RECONCILIATION-v09.md] §1 라벨 사전에서 찾는다.

**동결된 사실 (예고 없이 바뀌면 회귀 테스트가 차단):**
- **다국어 벤치마크**: 질의(거절특허 1,000)는 **전량 한국어**. 후보 코퍼스 40,552는 **다국어**다
  (한국어 39,246 = 96.8% · 영어 1,189 · 일본어 117) — 앞선 판의 "후보 코퍼스 = 한국어"는 부정확한
  요약이었다. 이 구분이 §6.2f 교차언어 진단의 전제다; qrel 정답 2,321 =
  한국어 57%·영어 39%·일본어 2%. 질의밀도(회수가능 정답 ≥1) = 97.6%.
- **분모 규율**: 2,534(인용 엣지) ≠ 2,321(고유 정답) ≠ 2,211(노드도달) ≠ 584(판단연결). 혼용 금지.
- **기술 스택**: Pyserini(BM25 nori + FAISS flat Dense + RRF) · parquet/pandas(메타데이터·후보필터) ·
  Titan Embed v2(임베딩). Bedrock 접근은 `config.get_secret()`.
  FAISS flat = 정확·결정적(ablation 오염 없음), 4만 건엔 근사 인덱스·DB 서버 불필요.
  **질의 번역은 미구현이다(2026-07-29 정정).** `src/`에 번역 모듈이 없다 — 이전 판이 계획을
  구현처럼 적었다. 논문 §6.2f가 "번역 부재"를 교차언어 실패의 세 원인 중 하나로 보고한 그대로다.
  도입은 PLAN-029 §3.2(질의 측만 번역).
- **질의·문서는 장문이다.** 주분석 질의 = `claims_independent`(독립항 전문 · 중앙값 527자),
  문서 = `text_main`(초록+청구항 전체 · 2,255자). 키워드·단문 검색이 아니다. 질의 표현 4종이
  준비돼 있으나 비교 실행은 아직 없다(PLAN-029 §3.3 탐색적).
- **개념 링크는 그래프에서 읽어올 뿐, 텍스트에서 뽑지 않는다**(`corpus/assemble.py:114`). 그래서
  자유 텍스트 질의를 받을 수 없다 — D-14·CR-007. 이것을 "온톨로지의 한계"로 적지 않는다.

작업 전 자문하라: **이 산출물은 C0/C1/C2/C3 중 무엇을 지지하는가?** 답할 수 없으면 코드를 쓰지 말고 물어라.

> **C0가 존재하는 이유.** 이 저장소의 최종 목적은 투고만이 아니다 — SDKB를 상업적으로 쓸 수 있게
> 만드는 것이 실무 목적이고, 논문은 그 근거를 만드는 수단이다. **기각된 가설과 무효과 결과의 다수는
> 방법의 실패가 아니라 온톨로지 자원의 결함이다**(예: `w_h=0`은 계층이 11트리플뿐이라 생긴 구조적 0,
> nDCG 악화는 개념 어휘 143개·문서당 1.55개라는 해상도의 귀결). 이런 관측을 "타당성 위협"으로만
> 적고 끝내면 **논문은 나가지만 제품은 한 걸음도 나아가지 않는다.** C0는 그 손실을 막는다.

---

## 0.1 상류 SDKB 계약 — 코드 의존은 단방향, 결함 정보는 **역방향 의무**

G0·정답지의 원천은 `~/Dev/sdkb`(자체 CLAUDE.md 보유). **코드·데이터 의존은 한 방향뿐이다.**
```
SDKB 원본 ──(사람이 make vendor)──> data/external/sdkb/ (얼린 스냅샷 + sha256) ──> 코퍼스 조립
        <──(결함 대장 · CR 문서 · 재검정 회신)────────────────────────  실험 결과
```
- 런타임에 `~/Dev/sdkb`를 읽지 않는다. `ontology/vendor.py`만, 사람이 `make vendor` 할 때만.
- SDKB 결함은 **상류에서 고친다.** 우회 패치는 스냅샷 출처를 거짓으로 만든다.
- **결함 정보의 역류는 의무다(C0).** 실험이 온톨로지 결함을 드러내면 `upstream/DEFECT-LEDGER.md`에
  등재하고, 우선순위 항목은 상류 CLAUDE.md §2 **1단계 요구정의 양식**의 `upstream/CR-NNN-*.md`로 쓴다.
  등재 요건은 셋 — **증거(실행된 코드의 출력)·수정 제안·검증기준(수정되면 어떤 수치가 어떻게 변해야
  하는가)**. 검증 불가한 "개선하면 좋음"은 등재하지 않는다.
  **검증기준에는 하류 태스크 지표가 최소 1개 들어간다(2026-08-02 · D-23 의 교훈).** 자원 측 지표만
  (동점블록·어휘 수·커버리지) 걸어 두면 **그 기준을 전부 통과하고도 검색이 나빠질 수 있다** —
  실제로 그랬다(문서당 개념 1.545 → 3.779 달성 · P1 ΔR₁₀₀ = −0.0293 · T1 실패). 자원 지표는
  **수정이 됐는지**를 재고, 하류 지표는 **그 수정이 쓸모가 있었는지**를 잰다. 둘은 대체 관계가 아니다.
- **이 저장소는 상류를 직접 수정하지 않는다.** CR을 제출할 뿐이고, 실제 수정은 상류의 5단계 절차를
  탄다. 수정 후에는 `make vendor`로 새 스냅샷을 뜨고, 효과는 **새 사전등록 아래 새로 검정**한다 —
  기존 원고의 판정은 소급 수정하지 않는다(§1-2·§1-3).
- 온톨로지 자산(T-Box·CQ·SHACL·claim-feature sidecar·개념링크)은 C1의 근거이자 온톨로지 검색팔의 입력 —
  **재사용하되 논문이 변경하지 않는다**(코퍼스는 파생 뷰).

---

## 0.2 신규성·진보성 명세 — 무엇이 새롭고 무엇이 자명하지 않은가 (2026-08-02 신설)

**왜 이 조항이 필요한가.** 지금까지 규약은 "무엇을 하면 안 되는가"(§1)만 정하고 **"무엇이 이
논문의 발명인가"** 는 정하지 않았다. 그래서 결과가 쌓여도 기여로 번역되지 않고, 부정적 결과가
전부 §8 한계로 흘러갔다. 각 항목에 **어느 산출물이 증명하는가**를 붙여 추적 가능하게 만든다.

| | 주장 | 증명 산출물 | 상태 | **일반화 → 설계원리** |
|---|---|---|---|---|
| **N1** | 릴리스 **사전 승인 게이트**에 교차 태스크 비회귀를 넣은 설계 | T-gate 명세 · 결함주입 검출 매트릭스 | 확보 | **DP3** |
| **N2** | 언어중립 개념 IRI를 교차언어 검색의 **제3 통로**로 두고 정답 언어별로 분해 | 원고 §6.2f | 확보(탐색적) | — (탐색적이므로 승격 대상 아님) |
| **N3** | 같은 온톨로지를 **검색과 생성 두 층에서 함께 측정**한 벤치마크 | C2′ 전달 실험 | **미확보** | **DP6**(제안) |
| **I1** | **자원 품질을 2.4배 올렸더니 태스크 성능이 나빠졌다** — 결과가 예측 불가 | D-23 두 팔 실측 · 원고 **§7.5**(세 관측 집성) · §6.3a 운용 효율 · 패널 사례 4위→67위(PLAN-036 §12) | **확보 · 진보성의 본체** | **DP1·DP2** |
| **I2** | 검색과 무관하게 설계한 음성 대조군 제거가 검색을 유의하게 악화 | A8 −0.0316 | 확보(경합 설명 D-13·D-15) | **DP3** |
| **I3** | 형식 검증 L0–L3를 전부 통과한 델타를 **성능 게이트가 거부** | PLAN-035 Accept=0 | 확보 | **DP2** |

> **설계원리 열이 이 표의 출구다(2026-08-07 · PLAN-041 A-4).** N·I 는 "이 논문에서 무엇이
> 새로운가"이고, DP 는 "그것이 다른 온톨로지로 무엇을 가져가는가"다. **DP 로 나가지 않는 행은
> 이 논문 안에서만 사는 관측**이며, 그 자체로 문제는 아니지만 이론적 기여로 세지 않는다.
> 승격 문턱은 §0.6, 원리 목록은 §0.7 이다.

**I1을 결론의 중심에 놓는다.** "온톨로지를 좋게 하면 검색이 좋아진다"는 자명한 명제이고, 그것이
**아니라는 것**을 실측으로 보인 것이 이 연구의 자명하지 않음이자 게이트가 필요한 이유의 실증이다.
구 원고는 이 결과를 §8 한계에 두었다 — v2.0에서는 **본론**이다.

**"최초"를 주장하지 않는다.** 기여는 요소의 신규성이 아니라 결합과 실험설계의 차별성에 둔다
(원고 §2.6 기조 유지). N·I 표는 심사자에게 내미는 목록이 아니라 **우리 자신의 점검표**다 —
새 작업이 이 표의 어느 칸을 채우는지 답할 수 없으면 그 작업은 하지 않는다.

---

## 0.3 "확실하다"의 정의 — 결과를 보기 전에 못박는다 (2026-08-02 신설)

> **적용범위 (2026-08-07 · PLAN-041 B-1).** **이 조항은 성능 우월 주장에만 발동한다.**
> 설계원리(DP) 주장의 문턱은 **§0.6** 이다. 두 주장은 종류가 다르므로 같은 자로 재지 않는다 —
> 성능 주장은 *"효과가 있는가"*를 묻고 설계지식 주장은 *"이전 가능한가"*를 묻는다.
> **아래 5조건 자체는 한 글자도 바꾸지 않는다.**

**왜 필요한가.** 새 기조는 "성능 향상 효과가 확실하다"를 말하려 한다. 그런데 **무엇이 충족되면
'확실'인가**가 정의돼 있지 않으면, 결과를 본 뒤 기준을 고르게 되고 그것이 정확히 §1-2 위반이다.
그래서 기준을 **미리** 적는다.

다음 다섯이 **전부** 충족될 때만 "효과가 확실하다"고 쓴다.

1. **두 독립 확증 분할**에서 부호와 유의성이 재현된다 (현재 A층뿐 — B층은 CR-008 선결)
2. **사전 지정 주 구성**에서 유의하다 (현재 P0★ *p*=.181로 **미달**)
3. **효과크기 95% CI 하한 > 0** (현재 P1 충족)
4. **민감도 격자 전반에서 부호가 유지된다** (가중치·K·토큰화)
5. **누출 감사 0 · 봉인 1회 개봉**

**현재 상태는 2/5다.** 그러므로 지금 시점에 원고가 쓸 수 있는 표현은 **"조건부"·"부분 지지"·
"깊은 회수에 한정"** 이며, "확실"은 위 다섯이 채워진 뒤에만 쓴다. 하나라도 미달인 채로 "확실"을
쓰면 §1-2 위반으로 다룬다 — 이 조항은 야심을 꺾는 장치가 아니라 **야심을 검증 가능하게 만드는
장치**다.

---

## 0.4 실무 이익 주장의 3등급 — FTO는 주장이 아니라 명세다 (2026-08-02 신설)

연구개발·기술기획 실무 이익을 쓰되 **세 등급으로 나눠 표로 분리**한다. 등급을 섞으면 전체가
등급 3으로 읽힌다 — **분리 자체가 방어 장치**다.

| 등급 | 성격 | 싣는 곳 | 예 |
|---|---|---|---|
| **1 · 측정된 이익** | 이 논문이 숫자를 산출 | 결과 장 | Effort@Recall · Candidate Reduction · 인용 정확도 · 환각률 · 지연 p95 |
| **2 · 조건부 이익** | 구조는 갖췄으나 미측정 | 논의 장 | 근거 제시로 인한 검토시간 단축 → **사용자 연구 필요** |
| **3 · 인접 응용** | **자원 결손을 명세로 제시** | 논의 말미·한계 | **FTO** · 기술기획 화이트스페이스 |

**FTO는 이 벤치마크로 잴 수 없다.** 특허성 조사(신규성·진보성 · 정답 = 심사관 인용)와 FTO
(침해 위험 · 정답 = **존속 중인 등록 청구항**)는 법적 질문이 다르다. 그러나 **후보 스크리닝
단계는 공유**하므로 거기까지는 정직하게 주장할 수 있다. 쓰는 형태는 **"공유하는 단계 + 빠진 축"**
표이며, 빠진 축 셋은 다음과 같다 — ① 법적 상태(등록·존속·소멸 · A-Box 없음) ② 청구항 범위
(ClaimFeature 1,289,512개 **이미 있음** · D-07 승격이 선결) ③ 제품 매핑(없음). 이 형태는 과장이
아니라 로드맵이고, 그대로 C0(상류 환류)로 연결된다.

**금지 표현 — 원고의 결론 규칙 표에 행으로 넣는다.**

| 관측 | 허용 | 금지 |
|---|---|---|
| Effort@Recall 감소 | 같은 회수를 **더 적은 검토 건수**로 달성 | 조사 시간·비용을 X% 절감 |
| 인용 정확도·환각률 | 제시된 근거의 **신뢰 가능 범위** | 변리사 검토를 대체·자동화 |
| FTO | 후보 스크리닝 단계로의 **이전 가능성**과 결손 축 명세 | **FTO 위험을 줄인다** · 침해 판단을 지원한다 |
| 기술기획 | 동일 T-Box의 재사용 가능성 | 화이트스페이스를 발견한다 |

---

## 0.5 C2′를 넣어도 **한 논문**으로 유지하는 네 규칙 (2026-08-02 신설)

논문이 둘로 갈라지는 지점은 "주제"가 아니라 **평가 아키텍처**다 — 질의 집합이 둘, 분할이 둘,
정답 체계가 둘, 누출 통제가 둘, 관련연구 흐름이 둘이 될 때 심사자는 "두 논문"이라고 읽는다.
C2′는 그중 **어느 것도 늘리지 않도록** 설계한다.

1. **생성기는 상수, 검색팔만 변인.** 모델·프롬프트·컨텍스트 K·온도·시드를 고정하고 바꾸는 것은
   **B3를 넣느냐 P1을 넣느냐** 하나뿐이다. 이러면 **RAG 방법 주장이 원리적으로 불가능해지고**,
   GraphRAG·HyDE 같은 RAG 계열 기준선을 붙일 이유도 사라진다. 늘어나는 관련연구는 문단 하나다.
2. **같은 벤치마크의 두 번째 판독(readout)이지 두 번째 벤치마크가 아니다.** 같은 test 질의 ·
   같은 qrel · 같은 분할 · 같은 누출 통제 · 같은 run을 쓰고, 바뀌는 것은 **읽어내는 방식 하나**다.
   **새 질문셋을 만들지 않는다.** 정답은 심사관 인용 기반 근거성이며 채점은 식별자 대조로 결정적이다.
3. **기여 목록에 올리지 않는다.** 기여는 둘로 유지한다 — ① 세 태스크 표현 자원 + 진화 장치
   ② 검색 유용성 실증. C2′는 ②의 **경계를 검증하는 절**이다. 기여가 셋이 되면 논점이 갈라진다.
4. **"RAG 성능"이 아니라 "전달(transfer)"이라 쓴다.** 용어 하나가 논점을 지킨다.

**양방향을 함께 사전등록하는 것이 "두 논문 아님"의 증명이다.** 전달되면 → 깊은 회수 개선은
실무 가치가 있고 T1의 주지표가 하류 가치를 대표한다. 전달되지 않으면 → 검색 지표가 하류 가치를
대표하지 않으므로 **T4가 필요하다**(§5). **두 결과가 같은 하나의 논지로 수렴한다** — 결과에 따라
논문의 주제가 바뀌지 않는다는 것이 이것이 부록이 아니라 본체라는 뜻이다.

**RQ·가설 번호는 이어 붙인다.** C2′의 연구질문은 **RQ5**다(RQ1 검증게이트 · RQ2 검색유용성 ·
RQ3 계층기여 · RQ4 계층독립성이 이미 쓰였다). **기존 번호를 재사용하지 않는다** — §0의 라벨
재부여 금지와 같은 이유로, 재번호는 사전등록 기록과의 추적성을 끊는다.
**투고 파생본은 예외다 — §0.9**(2026-08-15 개정).

**선제 방어.** 심사자는 반드시 *"왜 GraphRAG 같은 최신 방법과 비교하지 않았나"* 를 묻는다.
한계에 미리 적는다 — *"본 연구는 RAG 방법을 제안하지 않는다. 생성기를 상수로 고정하고 검색팔만
바꾼 전달 실험이며, RAG 방법 간 비교는 별개의 연구 질문이다."*

---

## 0.6 설계원리(DP)의 승격 기준 다섯 — §0.3이 성능에 한 일을 설계지식에 한다 (2026-08-07 신설)

**왜 필요한가.** §0.3은 *"효과가 확실하다"*의 문턱을 결과 보기 전에 못박았다. 그런데 **C4가
신설되면서 새로운 종류의 주장이 생겼다** — *"이 설계원리는 다른 온톨로지에도 이전된다."*
이 주장에는 문턱이 없었고, 문턱 없는 주장은 결과를 본 뒤 만들어진다. 그것이 DSR 판의
p-hacking이다.

**다섯이 전부 충족될 때만 "확립"이라 쓴다. 하나라도 미달이면 "제안(design proposition)"으로
강등한다.**

1. **근거가 실행된 코드의 출력이다** — 서사나 통념이 아니다(§1-1).
2. **경쟁 설명이 배제됐거나, 배제되지 않았음이 명시됐다** — 숨기면 미달이다.
3. **산출물 밖으로 이전 가능한 형태로 진술됐다** — 문장에 "SDKB에서는"이 붙으면 그것은
   설계원리가 아니라 **관측**이다.
4. **반증 가능하다** — **어떤 관측이 이 원리를 깨는지**를 함께 적는다.
5. **사후 역산이 아니다** — 원리의 **설계 시점**과 **확인 시점**을 각각 기록한다(§1-12와 짝).

**"제안"은 실패가 아니라 정직한 등급이다.** DP6이 제안인 것이 §0.7 표의 정직성이고, 승격은
판독 B 확증 후에 **다시 판정**한다 — 지금 올리지 않는다.

---

## 0.7 설계 연구질문(DRQ)과 설계원리(DP) — C4의 내용물 (2026-08-07 신설 · PLAN-041)

**라벨은 별도 이름공간이다.** `RQ1`–`RQ5`·`H1`–`H5`는 **전량 살아 있고 재사용하지 않는다**(§0.5).
DRQ는 설계 질문, RQ/H는 그 안의 **사전 동결된 평가 점검**이다 — 두 층이 문서에서 함께 보여야
사후 재구성으로 읽히지 않는다. **투고 파생본에서는 이 두 층을 라벨로 보이지 않는다 — §0.9.**

| 라벨 | 설계 질문 | 대응 주장 |
|---|---|---|
| **DRQ1** | 세 태스크를 한 T-Box로 표현하면서 태스크별 확장이 가능한 온톨로지를 **어떻게 설계하는가** | C1 |
| **DRQ2** | 형식적으로 유효한 변경이 하류 성능과 **다른 태스크 기능**을 훼손하지 않게 하는 게이트를 **어떻게 설계하는가** | C3 · C0 |
| **DRQ3′** | 산출물 평가에서 **어떤 효용·실패 경계·층간 지표 불일치가 관측되며**, 거기서 어떤 이전 가능한 설계원리가 도출되는가 (구 DRQ3+DRQ4 통합 · 2026-08-10 · PLAN-048) | C2 · C2′ · C4 |
| ~~DRQ4~~ | **결번 보존** — DRQ3′로 통합됨. **재사용 금지**(RQ·H·S 라벨 규칙과 동일) | — |

> **DRQ4를 지운 것이 아니라 합쳤다(2026-08-10 · PLAN-048 개정 2).** 구 DRQ3(어긋남·실패 패턴)과
> 구 DRQ4(기여 조건)는 같은 증거 위에 서 있어 본문에서 두 절로 갈리면 같은 표를 두 번 싣게 된다.
> **라벨은 결번으로 남긴다** — 재사용하면 사전등록 기록과의 추적성이 끊긴다(§0.5와 같은 이유).

| | 설계원리 | 근거(실측) | **설계 시점** | 등급 | **계층** |
|---|---|---|---|---|---|
| **DP1** | **층별 검증** — 온톨로지 내부 품질과 하류 태스크 성능은 **별도 층에서** 평가한다 | D-23 · 원고 §6.7 (자원 2.4배 ↔ 검색 −0.0293) | 확인 2026-08-01 (PLAN-035) | **확립** | **코어** |
| **DP2** | **한 층 아래 승인** — 자원 변경은 자원 지표가 아니라 **다음 사용 층의 비회귀 결과**로 승인한다 | I3 · PLAN-035 Accept=0 (L0–L3 전부 통과 · T1 실패) | **설계 PLAN-018** · 확인 2026-08-01 | **확립** | **코어** |
| **DP3** | **교차 태스크 감시** — 공유 T-Box에서는 **주 태스크 성능만으로** 변경을 승인하지 않는다 | H1 홀드아웃(T3 단독 12/45 · *p*=.0001) · H5 기각(A8 **A층 −0.0316** Holm 통과 ↔ **B층 0.0000 n.s.** — **두 표본에서 갈렸다** · PLAN-047 §16.4) | **설계 PLAN-018**(T3) · 확인 2026-07 | **확립** — 남은 근거는 결함주입 T3 단독 검출 (경쟁 설명 D-13·D-15 명시 · A8 근거는 갈림을 그대로 적는다) | **코어** |
| **DP4** | **통제된 자원 교체** — 문서집합·모델·가중치를 고정하고 **자원만 교체**해야 온톨로지 효과가 판별된다 | D-12(T-Box 불변 → H2 검정 불가) · D-19 해소 후 두 팔 최초 성립 | 확인 2026-08-01 | **확립** | **코어** |
| **DP5** | **후보생성 분리** — 재순위화는 후보 풀 **밖**의 문헌을 복구할 수 없으므로 후보생성과 **구분해** 평가한다 | 원고 §6.3a — 상한 없을 때 세 시스템의 미도달 비율이 **완전히 동일**(26.8 % · 55.1 %) | 확인 2026-08-02 (PLAN-036) | **확립** | **범위** |
| **DP6** | **전달 검증** — 검색 지표의 개선을 검토 효율이나 생성 답변 품질로 **자동 일반화하지 않는다** | 원고 §6.3a(Candidate Reduction 중앙값 0.0 %) · §6.8 — A층 탐색 + **B층 확증 T4 실패**(검색에서 유의 개선된 팔이 생성 층 비열등을 통과하지 못했다) | 설계 PLAN-038 · **확증 판정 2026-08-09**(PLAN-047 §19) | **제안 유지** — 판정은 DP6 을 지지하는 방향이나 전달 부재인지 검정력 부족인지 **가르지 못했다**(LB 가 임계를 0.0005 넘긴 knife-edge) → §0.6 조건 2 미달 | **범위** |

**설계 시점 열이 §1-12의 집행 장치다.** 비어 있거나 확인 시점보다 늦은 원리는 **설계원리가
아니라 후속 설계명제**이며, 그렇게 표기한다.

**계층 열은 등급이 아니라 자리다(2026-08-10 · PLAN-048 개정 3).** DP1–DP4 = **코어 원리**
(승인 게이트의 본체) · DP5–DP6 = **범위 원리**(적용 경계를 정하는 원리). **§0.6 등급(확립/제안)은
전량 불변**이며, 바뀌는 것은 본문 7장에서 **싣는 자리**뿐이다 — 코어는 원리 절, 범위는 경계·후속 절.
이 구분이 없으면 원리 여섯이 동열로 나열되어 서로 겹쳐 읽힌다.

**반증 조건을 함께 적는다(§0.6 조건 4).** 예 — DP1은 *"자원 지표와 태스크 지표가 여러 자원
세대에 걸쳐 단조 동행하면"* 깨지고, DP5는 *"재순위화만으로 미도달 질의 비율이 줄면"* 깨진다.
**깨는 관측이 무엇인지 적을 수 없는 원리는 싣지 않는다.**

---

## 0.8 판정 서술 문구 사전 — 본문·초록·결론이 쓸 수 있는 문장 (2026-08-10 신설 · PLAN-048)

**왜 필요한가.** 판정 기록(§0 표·원고 §1.4a)과 본문 서술 사이의 **표류가 재구성마다 재발했다**
(§2.2 신설 배경 3건). 판정 자체는 §1-2·§1-3이 지키지만, **판정을 문장으로 옮길 때** 강도가
올라가는 것은 아무도 막지 않았다. 아래 표 밖의 판정 서술은 쓰지 않는다.

**정본은 `paper/verdicts.yaml`이고 이 조항은 그 사본이다.** 기계 검사는 `make verdicts`(§2.3)가
하며, **yaml 수정은 §1-2와 동급**으로 다룬다(사용자 승인 커밋으로만).

| 대상 | 허용 | 금지 |
|---|---|---|
| H3 (A/B 합성) | "사전등록된 복합 기준의 동시 충족은 두 분할에서 확인되지 않았다" + "깊은 회수 개선은 두 분할에서 반복 관측됐다" | "H3 지지"·"부분 지지"를 **합성 서술로** 사용 (각 사전등록의 판정 기록을 인용할 때만 허용) |
| P0/P1 | "사전 지정 주 구성은 유의에 이르지 못했고, 부차 구성의 개선이 반복 관측됐다" | 주·부차 구분 없는 "+0.0534 개선" 단독 인용 |
| §6.7 자원 교체 | "자원 지표가 개선되고 형식 검증을 통과한 변경이 성능을 떨어뜨릴 수 있다" · "원인(자원/점수식)은 미구분" | "자원 지표는 태스크 성능을 대표하지 않는다"(무조건 일반형) |
| H2 | "승인된 변경의 안전성은 미검정" (+ PLAN-035 기각은 별도 사전등록의 기록으로 인용) | "게이트가 안전을 보장" |
| T4 · C2′ | "전달을 확증하지 못했다" · "전달 부재인지 검정력 부족인지 미구분" | "전달되지 않는다" |
| A8 · H5 | "설계 진단 — 모듈·매핑 경계 불일치를 드러냈고 두 표본에서 갈렸다" | "동적 태스크 결합의 발견"(PLAN-049 실측이 지지하기 전) |
| **봉인 개봉** (2026-08-13 신설) | "사전등록된 확증 평가를 수행했고 **모든 접근을 열람 원장에 기록했다**" · 원장을 같은 자리에서 밝히는 "1회 개봉" | **원장 없는 "1회 개봉" 단정** — 원장은 5행이다 |
| **구성 명칭** (2026-08-13 신설) | P0★ = "사전 지정 **주 구성**" · P1 = "**부차 구성**" · EP3의 P1 = "**교체 대상 구성**" | P1을 "주 시스템"이라 부르는 것 · "사전 지정 주 **시스템**" |
| bpref | (판정 비적합 집합이 없으므로) 삭제 또는 "proxy score"로 개명 후 인용 | 고전적 bpref로서의 인용 |

**이 표는 판정을 바꾸지 않는다 — 판정을 옮겨 적는 방식만 정한다.** 판정 기록은 §0 C-표와 원고
§1.4a에 그대로 남고, 이 조항은 그 기록을 **본문 문장으로 번역할 때의 유일한 사전**이다.

**언급(mention)은 사용(use)이 아니다.** 위 금지 문구를 *"이 문구는 쓰지 않는다"*의 형태로 인용하는
것은 위반이 아니며, 검사기도 따옴표 안의 언급을 위반으로 세지 않는다.

---

## 0.9 라벨 정책 — 지켜야 하는 것은 **기호 동일성이 아니라 추적성**이다 (2026-08-15 신설 · 사용자 승인)

**왜 고치는가.** §0·§0.5·§0.7은 라벨 문자열(`H3`·`H5`·`RQ5`)을 **어디서든 그대로 쓸 것**으로
읽혔고, 그래서 투고 파생본이 라벨 이름공간을 **여섯 개**나 이고 다니게 됐다(실측 2026-08-15:
`DRQ` · `RQ` · `H` · `T` · `EP` · `A`). 그중 `H1`–`H5`와 `RQ1`–`RQ5`는 **DSR 전환으로 중심에서
내려온 층**인데도 본문에 남아, 심사자에게 *"이전에 다른 틀로 쓰다가 바꿨구나"* 를 노출한다.
`DRQ3′`의 프라임은 그 노출을 한 글자로 압축해 보여준다.

**무엇이 외부 규범이고 무엇이 우리 규약인가 — 이 구분이 이 조항의 근거다.**

| | 성격 | 처리 |
|---|---|---|
| 사전등록 사실과 링크를 밝힌다 | **외부 규범** | 지킨다 |
| 확증 분석과 탐색 분석을 구분한다 | **외부 규범** | 지킨다 |
| 사전등록된 결과를 전량 보고한다(실패 포함) | **외부 규범** | 지킨다 |
| 원고 주장 ↔ 사전등록 결과의 **대응이 확인 가능**하다 | **외부 규범** | 지킨다 — **crosswalk로** |
| 원고가 사전등록의 **라벨 문자열을 그대로 쓴다** | **우리 규약** | **폐기한다** |

외부 규범이 요구하는 것은 **추적 가능성**이지 **기호 동일성**이 아니다. 논문에서 가설을 자체
호칭으로 다시 부르고 부록에 대응표를 두는 것은 통상의 관행이다.

**규칙 넷.**

1. **작업 정본·PLAN·결함대장·`verdicts.yaml`의 record는 원 라벨을 그대로 쓴다.** 세션 간
   추적성이 목적이며, 이 부분은 하나도 바뀌지 않는다(§0·§0.5·§0.7 그대로 유효).
2. **투고 파생본은 자체 호칭을 쓴다.** `H1`–`H5`·`RQ1`–`RQ5`는 파생본 본문에 **쓰지 않는다**.
   가설은 **서술형**으로 부른다(예: "검색 유용성 점검"·"계층 특이성 점검"·"전달 점검").
   설계 질문은 `DRQ` 대신 **`RQ1`–`RQ3`**으로 쓴다 — 파생본 안에서 번호가 이어지고 결번이 없다.
3. **파생본에서 새 가설 기호를 만들지 않는다.** `H1`·`H2`로 재번호하면 같은 기호가 작업 정본의
   다른 것을 가리켜, 이미 두 번 난 사고를 반복한다(§0 말미 S-시리즈 폐기 경위).
4. **crosswalk 1장은 필수다.** `paper/supplementary/`에 *"파생본 호칭 ↔ 사전등록 라벨 ↔
   사전등록 문서"* 표를 두고 파생본 Methods에서 링크한다. **이 표를 빼는 것은 규약 위반이 아니라
   정직성 위반이다** — 위 표의 외부 규범 넷 중 마지막이 이것 하나에 걸려 있다.

**바뀌지 않는 것.** 판정·수치·동결 사실·게이트 이름(`L0`–`L3`·`T1`–`T4`)·절제 식별자(`A1`–`A8`)·
평가 에피소드(`EP1`–`EP4`). `T`는 레거시가 아니라 **산출물 자체의 구성요소 이름**이라 남는다.
**바뀌는 것은 판정을 부르는 이름뿐이다.**

---

## 1. 절대 금지 (예외 없음)

1. **수치를 지어내지 않는다.** 모든 숫자는 실행된 코드의 출력. 미확정은 `[실험 후 기입]`/`TODO(N)`로 남긴다.
2. **가설에 유리하게 만들지 않는다.** C2/C3가 기각되면 기각대로 보고. 임계값·모델·가중치를 결과 보고
   바꾸는 p-hacking 금지. 변경은 사전 동결된 민감도 격자로만.
3. **사전등록을 사후에 고치지 않는다.** 동결 대상: ε(비열등 마진 0.02)·δ(하위집단 0.05)·주지표
   (family-level Recall@100)·시점/패밀리 분할 경계·low-overlap 정의·CQ 스위트 분할·결함주입 유형·
   Dense 모델·토큰화. **테스트 qrel은 최종 비교 시점까지 봉인.** 증거는 커밋 해시.
   → **자원 스냅샷이 바뀐 뒤의 재측정은 이 조항이 막는 대상이 아니다**(새 실험 = 새 사전등록).
   경로는 **§2.1**. 금지되는 것은 *결과를 본 뒤 같은 실험의 조건을 바꾸는 것*이다.
4. **누출을 허용하지 않는다.** 질의 인용 간선(`hasPriorArtExaminer`/`hasPriorArt`/`overPriorArt`) 마스킹·
   시점유효(공개일<cutoff)·패밀리 분리·`NoveltyScore` 등 정답 파생 피처 배제. `leakage_check`가 강제.
5. **원문 데이터를 커밋하지 않는다.** raw/interim/processed·특허 전문(claim/abstract)은 gitignore.
   KIPRIS 학술이용·비재배포. 커밋 가능한 것은 집계·메타데이터·식별자·해시·재구축 절차뿐.
6. **어휘를 발명하지 않는다.** 클래스·프로퍼티는 SDKB 실물(`data/external/sdkb/*.ttl`)에 있는 것만.
   새 술어가 필요하면 **코드 전에 사람에게 물어라.**
7. **수작업 그림·표 금지.** 논문 그림·표는 코드가 생성한다(`viz/figures.py`·분석 CLI).
8. **스냅샷(`data/external/sdkb/`)·봉인 qrel 임의 갱신 금지.** 사용자 지시 시에만, MANIFEST 절차대로.
9. **비밀정보 하드코딩 금지.** `config.get_secret()`로만(`KIPRIS_API_KEY`·`AWS_*`·`BEDROCK_*`).
10. **요청하지 않은 코드 금지.** 리팩터·추상화·"나중에 쓸" 유틸·승인 없는 파일 — 제안하고 멈춘다.
11. **LLM·RAG 실험의 다섯 금지 (2026-08-02 신설 · C2′ 발효 조건).** 생성 층을 넣는 순간 새 부정
    경로가 열린다. 아래가 없으면 C2′는 **원리적으로 검증 불가**이며, RAG 논문이 심사에서 가장
    많이 깨지는 자리다.
    - **프롬프트를 결과 보고 고치지 않는다.** 프롬프트·모델 ID·온도·시드·컨텍스트 K는 사전등록
      동결 대상이다(ε·δ와 동급).
    - **판정자에게 정답을 보여주지 않는다.** LLM 판정자 누출은 qrel 누출과 같은 등급으로 다룬다.
      기본 채점은 **인용 식별자 대조**(결정적)이며, LLM 판정자는 보조로만 쓰고 쓸 때는 사람
      표본 검증을 동반한다.
    - **비결정성을 숨기지 않는다.** 온도 0에서도 반복 실행 분산을 보고한다.
    - **모델 버전을 고정한다.** 모델이 바뀌면 재측정이 아니라 **새 실험**이다.
    - **검색 지표로 생성 성능을 대체하지 않는다.** R@100 개선을 "RAG가 좋아졌다"로 쓰지 않는다.
12. **설계과학(DSR) 고유의 네 금지 (2026-08-07 신설 · C4 발효 조건).** 가설검정에 p-hacking이
    있듯 DSR에는 고유한 부정 경로가 있다. **이 넷이 없으면 §0.7의 DP 표는 결과의 사후 요약일 뿐
    설계지식이 아니다.**
    - **설계원리를 결과에서 역산하지 않는다.** 관측을 본 뒤 만든 원리는 설계원리가 아니라
      **후속 설계명제**다. §0.7 표의 **설계 시점 열을 반드시 채운다** — 비어 있거나 확인 시점보다
      늦으면 그렇게 표기한다.
    - **경쟁 설명을 침묵으로 배제하지 않는다.** 가르지 못했으면 **"미구분"이라 쓴다**(§8 보고 방식).
    - **부정 결과를 설계원리로 세탁하지 않는다.** 기각은 기각으로 남기고(§2.2 요건 1), 원리는
      그 위에 **얹을** 뿐 **대체하지** 않는다. H3·H5의 판정은 원고 §1.4a에 그대로 남는다.
    - **정성 평가를 정량 부족의 대체물로 쓰지 않는다.** 질적 코딩은 **κ와 합의율 없이 싣지 않는다**
      (κ<0.4이면 본문 제외 후 부록).

---

## 2. 개발 절차 — 5단계와 정지 게이트 🛑

`1 요구정의 🛑 → 2 분석(데이터 먼저 관찰) 🛑 → 3 설계(결정성·검정방법 확정) 🛑 → 4 구현 → 5 테스트·검증 🛑`
승인은 명시적 응답("진행"·"OK"·"승인"). 침묵은 승인이 아니다.

- **1 요구정의**: 목적(C1/C2/C3 중)·입력·출력(논문 자리)·성공기준(검정가능)·비목표.
- **2 분석**: 실제 파일/그래프/코퍼스를 열어 스키마·결측·분포·언어·엣지케이스를 보고. 가정은 "가정"으로 라벨.
- **3 설계**: 모듈·시그니처·결정성(시드 고정)·검정(단측/양측·효과크기·표본단위)·신규 의존성 근거.
  통계·평가 방법은 여기서 확정하고 **결과를 본 뒤 바꾸지 않는다.**
- **4 구현**: 승인 범위 안에서만. 경로·네임스페이스는 `config`에서. 하드코딩 금지.
- **5 검증**: `make lint && make test` + 아래 3층. 실패는 실패로 보고. 검증 없이 "완료" 금지.
  (a) 단위(경계·결측·빈입력·중복, 외부의존 격리, 결정성) (b) 통합(모듈 경계 계약 — 특히 게이트 우회
  차단·누출 0·분할 결정성) (c) 파이프라인(§4 프로파일·`make gate`·코퍼스 재현성).

> 규모 예외: 오타·문구·승인된 계획의 기계적 반복은 압축 가능. **데이터 수집·변형, 코퍼스·qrel·통계
> 결과를 바꾸는 변경, 게이트/누출 로직 변경에는 예외가 없다.**

---

## 2.1 자원 버전이 바뀌면 — 정지 게이트 1개로 전량 재측정 (2026-08-01 신설)

**왜 이 조항이 필요한가.** §2는 데이터를 건드리는 모든 작업에 정지 게이트 4개를 요구한다. 그래서
**코드는 그대로 두고 온톨로지 스냅샷만 갈아 끼우는 재측정**까지 매번 4번 멈췄고, 재측정 경로가
사실상 막히자 남은 진행 방향이 결함대장·CR 문서뿐이 되어 **실험 대신 문서만 쌓였다.** 이 조항은
규율을 푸는 것이 아니라 **없던 길을 낸다.**

### 발동 조건 — "T-Box 변경"이 아니라 **스냅샷 서명 변경**

`data/external/sdkb/`의 **파일 sha256이 바뀌면** 발동한다. T-Box 공리가 바뀌었는지는 묻지 않는다.
실측(D-12) T-Box는 한 번도 바뀐 적이 없고, 상류 교정(CR-001~008)이 실제로 움직이는 것은 대부분
**②개념층**(어휘·계층·링크·매핑)인데, 델타 유형표대로 ②는 T1·T2에 **직접** 영향이며 **H2 자격이
있다.** 트리거를 "T-Box 변경"으로 잡으면 자격 있는 델타를 통째로 놓친다.

**커밋 해시로 동결하지 않는다 — 산출물 sha256으로 동결한다.** 상류의 일부 TTL은 gitignore된 빌드
산출물이다(예: `ontology/sdkb-core-data.ttl`, 상류 `.gitignore:6`). 같은 커밋에서도 빌드 결과가
다를 수 있으므로, 사전등록이 붙잡는 것은 `PROVENANCE.json`의 **파일별 sha256**이다.

### 절차 — 🛑는 4개가 아니라 1개, 그러나 0개도 아니다

**1) 사전등록 동결 🛑 (유일한 정지점).** 파일 하나에 아래를 적고 **커밋한 뒤** 실행한다.
스냅샷 파일별 sha256 · 상류 커밋 · ε=0.02 · δ=0.05 · 주지표(family-level Recall@100) · 시점/패밀리
분할 경계 · 봉인 qrel 해시 · 검정할 가설과 판정식 · **확증과 탐색의 구분**(어느 수치가 확증이고
어느 수치가 대장 회신용 참고인지).

**2) 무정지 연속 실행.** 아래를 중간 승인 없이 끝까지 돌린다.

```
make vendor → corpus → index → leakage → retrieve → eval → gate → figures
```

**`leakage`와 `gate`를 "주변 검증"으로 빼지 않는다.** `leakage`는 §1-4이자 T1 판정의 전제이고
(누출이 0이 아니면 Recall 수치 전체가 무효), `gate`의 T1·T2 출력은 **부수 확인이 아니라 H2의
증거 그 자체**다. 이 둘을 건너뛴 재측정은 재측정이 아니다.

**2′) 파이프라인 서명이 움직이지 않았으면 그 실행은 "통과"가 아니라 "비가시"다 (2026-08-15 · D-43).**
스냅샷 sha256 이 바뀌어도 파이프라인 구성요소(`ir_corpus`·`concept_axis`·`graph_v1`)의 서명이
그대로면 **파이프라인은 그 델타를 읽은 적이 없다.** 그때의 ΔR₁₀₀ 은 측정된 0 이 아니라 **구성상
0** 이고, 그 위에서 나온 `Accept = 1` 은 승인의 증거가 아니다. `tgate_report.json` 의
`resource_visibility.note` 가 이것을 `invisible`·`no_evidence`·`unknown` 으로 분류한다 —
**`invisible` 인 실행의 결과는 "게이트로는 보이지 않았다"로 적는다.** `no_evidence` 는 *"비가시라는
증거가 없다"*이지 *"델타가 보였다"*가 아니며, `unknown` 은 근거 없음이지 통과가 아니다.
이 필드는 **승인식에 들어가지 않는다**(§5 개정 아님) — 판정은 그대로 두고 판정의 뜻만 지킨다.

**3) 결과 보고 → 그 다음에 C0.** 결함 등재·CR 작성은 **실험의 선행조건이 아니라 후행 산출물**이다.
실험 1회 완주 후 배치로 처리한다. C0를 임계경로에 올리지 않는다.

### 이 조항은 §1-2·§1-3을 위반하지 않는다

§1-3이 금지하는 것은 **결과를 본 뒤 같은 실험의 조건을 바꾸는 것**이다. 새 스냅샷 위의 재측정은
**새 실험**이며, 구 판정은 소급 수정하지 않고 그대로 보존된다 — 그것이 §1-3의 요구 전부다.
구 결과는 **"교정 전 자원에서의 관측"**으로 남고, 새 결과는 **새 사전등록 아래** 따로 선다.

### 이 조항을 쓸 수 없는 경우 (안전장치)

**코드가 바뀌면 §2 전체가 적용된다.** 이 경로는 **파이프라인 완전 동결 · 자원만 교체**일 때만
유효하다. 검색 설정·가중치·토큰화·Dense 모델·분할 경계 중 하나라도 손대면 그것은 재측정이 아니라
새 방법이고, 요구정의부터 다시 탄다. **"재실험"이라는 이름으로 로직 변경을 무검증 통과시키지 않는다.**

또한 **개봉된 표본으로 우월성을 다시 확증하지 않는다.** 이미 개봉한 test 분할 위의 재측정은
① 사전등록된 승인 규칙의 적용(T1·T2·T3)과 ② 탐색적 재측정까지이며, H3/H4/H5의 **재확증은
신규 확증분할로만** 한다(PLAN-029 A층/B층 구분).

---

## 2.2 원고 기조가 바뀌면 — 정지 게이트 1개, 요건 5개 (2026-08-02 신설)

**왜 이 조항이 필요한가.** 코드 변경에는 §2(정지 게이트 4개), 자원 교체에는 §2.1(1개)이 있는데
**원고 자체의 대규모 재구성에는 절차가 없었다.** §2의 규모 예외가 원고 편집을 "문구 수정"으로
분류하므로, 기조 전환이 아무 점검 없이 통과한다. 실제로 2026-08-02 정합 점검에서 발견된 세 건
(C3 문구가 H5 결과와 반대 · H2 상태 지연 · 존재하지 않는 §7 참조)이 전부 이 공백에서 생겼다.

**적용 대상.** 확증 가설의 개수·지위, 기여 목록, 장 구성, 주장(C-표), 논지 문장 중 하나라도
바뀌는 작업. 절 안의 문장 다듬기·오탈자·이미 승인된 계획의 기계적 반영은 대상이 아니다.

**정지 게이트는 1개다 — 변경 전에 아래 다섯을 한 파일에 적고 커밋한 뒤 편집을 시작한다.**

1. **판정 대장** — H1–H5 각각의 판정이 **변경 전후로 불변임**을 표로 확인한다. 하나라도 바뀌면
   그것은 기조 변경이 아니라 §1-2 위반이다.
2. **이동 대장** — 각 절이 어디로 갔는지(삭제가 아니라 이동). 잘라낸 전문은 `paper/supplementary/`로.
3. **C 사상표** — 새 기조의 각 주장이 C0/C1/C2/C2′/C3 중 어디에 대응하는지. **대응하지 않는
   것이 있으면 멈추고 승인 요청**(§0 말미의 자문 규칙과 같은 문턱).
4. **동결 사실 점검** — 주지표·분할 경계·라벨·게이트 이름·토큰화·Dense 모델을 건드렸는지.
   건드렸으면 이 조항이 아니라 §2 또는 §2.1로 넘어간다.
5. **역방향 점검** — 바뀐 기조가 **이 CLAUDE.md의 문구와 충돌하지 않는지.** 충돌하면 **규약을
   먼저 고친다.** 원고가 규약을 앞질러 가면 그다음 세션이 낡은 규약을 근거로 되돌린다.

**이 조항이 보장하는 것은 하나다 — "판정은 지키면서 서사는 자유롭게".** §0이 이미 그 원칙을
문장으로 갖고 있다(*"위반은 판정을 바꾸는 것이고, 바뀐 것은 싣는 자리다"*). 이 조항은 그 원칙에
**절차를 붙인 것**이다.

---

## 2.3 투고 파생본 절차 — 정지 게이트 1개, 검증은 기계가 (2026-08-10 신설 · PLAN-048)

**왜 필요한가.** §2.2는 원고 기조 변경에 게이트를 놓았지만, 투고본을 **파생물로 만드는 작업**에는
경로가 없었다. 그 결과 축약·이관·재조준이 전부 "문구 수정"으로 분류되어 아무 점검 없이 지나갔다.
이 조항은 규율을 푸는 것이 아니라 **§2.1이 재측정에 했던 일을 투고 준비에 한다** — 멈춤은 한 번,
대신 기계가 매 커밋 검사한다.

**게이트(1개):** PLAN-048 류의 마스터 계획(§2.2 요건 5종 포함)을 커밋·승인받은 뒤 파생을
시작한다. **이후 단계별 재승인은 요구하지 않는다** — 대신 아래 둘이 커밋마다 돈다.

1. **`make verdicts`** — `paper/verdicts.yaml`(판정 SSOT)과 정본·파생본의 판정 문구 정합 검사.
   위반 시 실패. **yaml 수정은 §1-2와 동급으로 다룬다**(사용자 승인 필수).
2. **`make submission-check`** — 데스크 리젝 요인 검사: 플레이스홀더 0 · 영문 제목/초록 존재 ·
   본문 표/그림 상한 · 내부 링크 도달성 · 분량 목표. 규칙은 `scripts/submission_check.py`에 코드로.
   **D7–D9 증설 (2026-08-13).** 외부 검토가 지적한 셋이 **검사기를 전부 통과하고 있었다** —
   영문 초록 303단어(D7 · AEI 상한 250) · 키워드 10개(D8 · 상한 7) · 존재하지 않는 절 참조
   8건(D9 · §4.8·§4.9·§4.9.1). **투고처 규정인 D7·D8은 우리가 조정할 수 있는 목표가 아니다**
   (D6의 −40 %와 성격이 다르다). **D9는 §2.3의 "이관은 삭제가 아니다"를 절 참조로 확장한 것**이다 —
   파생본은 장 구성을 접으며 만들어지므로(11장 → 8장) 산문에 남은 옛 절 번호가 조용히 죽은 링크가
   된다. 파일 링크만 검사하면 놓친다. 참고문헌 이후는 보지 않는다(서지의 `§`는 법령 조항이다).

**이관은 삭제가 아니다** — supplementary로 나간 전문은 파생본에서 **링크로 도달 가능해야** 하며,
링크 단절은 `submission-check`가 실패시킨다.

**차단 승격 시점 (2026-08-10 사용자 승인).** 두 검사는 **경고(non-blocking)로 시작**한다 —
켜는 시점의 정본은 이미 위반을 갖고 있고(제목·서지 플레이스홀더), 그것을 없애는 작업이 PLAN-048
3단계이기 때문이다. **1단계(순수 이관)에서 문장을 고쳐 녹색을 만드는 것은 "수정 0" 원칙 위반**이므로
하지 않는다. 3단계 종료 시 **차단으로 승격**하고, 그 승격 커밋 이후로는 위반이 곧 CI 실패다.

> **승격 완료 (2026-08-12 · PLAN-048 3단계 종료).** 두 검사에서 `--warn` 을 뗐다(Makefile ·
> CI quality-gate). 승격 시점 실측은 **정본·파생본 모두 위반 0**이다 — 정본의 제목은 §0.8
> CLAIM_STRENGTH 를 피하는 형태로 바뀌었고(변경 기록을 제목 아래에 남겼다), 서지 플레이스홀더
> 6건은 원문 대조로 확정됐다. **판정과 수치는 하나도 바뀌지 않았다.**
>
> **파생본은 이제 산문 소스에서 조립한다.** `paper/manuscript/stage3_source.md`(사람이 쓰는 산문)
> + 축약 전 전문(`paper/supplementary/S5-submission-full-v2.md`)에서 **문자 단위로 복사한 표** →
> `make submission-stage3`. **표의 수치를 손으로 옮겨 적지 않는다**(§1-1) — 앵커가 없거나 둘 이상
> 매치되면 스크립트가 실패한다(rc 2). 파생본을 직접 편집하지 말고 **산문 소스를 고친 뒤 다시
> 조립**한다.
>
> **표 안의 문구는 `CELL_FIXES`로 고친다(2026-08-13).** 절 번호가 바뀌거나 §0.8 문구 사전이
> 늘면 표 셀도 따라가야 하는데, 그 이유로 표를 손으로 옮겨 적으면 이 배선의 존재 이유가 사라진다.
> 그래서 셀 치환은 **앵커가 정확히 한 번 매치되는 목록으로만** 허용하고, **수치가 하나라도
> 달라지면 실패**시킨다(절 번호는 수치로 세지 않는다). 수치를 바꿔야 하면 사유를 적어야 하고
> 그 사유는 빌드 로그에 남는다 — **§1-1을 사람의 주의력이 아니라 기계가 지킨다.**

---

## 3. 모듈 배치표 — 새 코드가 갈 곳

| 모듈 | 책임 | 넣지 말 것 |
|---|---|---|
| `collect/` | 외부 원천 수집 (KIPRIS·BigQuery·EPO/PatentsView·DART). raw 저장 | 정제·해석 |
| `corpus/` | sidecar 청구항 조인 · 코퍼스 조립 · 질의 번역(Haiku) · 임베딩(Titan) · 시점/패밀리 분할 · hard negative | 검색 로직 |
| `ontology/` | vendor(스냅샷)·baseline(G0)·mapping·merge(게이트 통과분) — **검색 개념·경로 피처의 원천** | 통계·검색 |
| `retrieval/` | bm25(Pyserini nori)·dense(FAISS flat)·hybrid(RRF)·ontology_rerank — 순위 산출만 | 평가·게이트 |
| `validate/` | L0–L3 게이트 + **T-gate**(t1_noninferiority·t2_subgroup·t3_cross_task_cq) · leakage_check · fault_inject · vocab_coverage | 데이터 수정 |
| `analysis/` | metrics(Recall/nDCG/MRR/bpref)·**effort@recall·candidate reduction**·bootstrap·ablation·subgroup — 검정과 효과크기 | 그림 |
| `rag/` **(신설 · C2′)** | 컨텍스트 조립(동결 run 상위 K) · 생성 호출(모델·프롬프트 동결) · **결정적 채점**(인용 정확도·환각률·근거 지역화) | 검색 로직 · 새 질의셋 · RAG 방법 제안 |
| `viz/` | `figures.py`만. 논문 그림 전량 | 계산 |
| `queries/` | `cq/*.rq`(스위트 pa·em·tf·core) · `shapes/graph`·`shapes/delta` | — |

노트북은 초안, `src/`가 정본. 확정 로직은 반드시 `src/`로 승격.

---

## 4. 데이터 프로파일 의무 (수집·변형 산출물의 필수 동반물)

데이터를 수집·변형한 뒤 예외 없이 산출·보고: **(1) 구조**(컬럼·dtype·단위·키·원천) **(2) 형태**
(행×열·중복·결측률·기간·**언어분포**·dedup 전후) **(3) 기술통계**(수치/범주/연도별/IPC·CPC 분포)
**(4) 사용 목적**(어느 표·그림·검정에, 안 쓰는 컬럼은 왜 남기나). 코드가 생성(`data/profiles/*.md`),
같은 커밋에 `data/MANIFEST.md`(일시·검색식·건수·파일·반영 버전) 기록. **코퍼스 서명이 예고 없이
바뀌면 통합 테스트가 실패시킨다.** 놀라운 점(이상치·결측편중·기간공백)은 다음 단계 전에 알린다.

---

## 5. 검증 게이트 — L0–L3 + T-gate (병합 전 사전승인)

게이트를 통과 못한 델타는 병합되지 않는다. 우회 경로를 만들지 않는다. CI가 매 push 같은 것을 돌린다.

| 층 | 검증 | 실패 예 |
|---|---|---|
| L0 | 신선도·무결성 | 낡은 산출물, sha256 불일치 |
| L1 | SHACL 구조(graph+delta) | 필수속성 누락·잘못된 카디널리티 |
| L2 | 논리 일관성(HermiT) | 모순 타입·순환 계층 |
| L3 | CQ 기능 (스위트 pa·em·tf·core) | 필수 경로 단절·무응답 |
| **T1** | 검색 비열등성 | Recall@100 하락(LB₉₅ ≤ −ε) — 누출 감사 통과 전제 |
| **T2** | 하위집단 안전성 | 거절근거·공정군·**언어(KR/외국)**별 국소 회귀(max drop ≥ δ) |
| **T3** | 교차 태스크 CQ 비회귀 | 타 태스크(em·tf·core) CQ 통과율 하락 |
| **T4** *(승인식 미편입 · 판정 1회)* | **하류 생성 층 비회귀** | 인용 정확도 하락 · 환각률 상승 |

- **승인 규칙**: `Accept = (L0=L1=L2=L3=pass) ∧ (LB₉₅(ΔR₁₀₀)>−ε) ∧ (max_s drop_s<δ) ∧ (∀f∈{em,tf,core}: pass_f(new)≥pass_f(old))`. ε=0.02·δ=0.05, 테스트 개봉 전 동결.
- **T4는 아직 승인식에 들어가지 않는다(2026-08-02 · 2026-08-09 갱신).** C2′ 전달 실험이 수행되기
  전에 승인식에 넣으면 **돌지 않는 게이트를 가졌다고 주장**하게 된다. 발효 조건은 둘 — ① C2′ 1회
  완주 ② 임계(비열등 마진)를 결과 보기 전에 동결.
  **두 조건은 채워졌다** — 마진 `ε_T4=0.02`·`η=0.01` 은 동결 커밋 `67568c8` 에서 결과 보기 전에
  박혔고, B층 확증에서 **실제 판정 1회**가 나왔다(**실패** · PLAN-047 §19). **그럼에도 승인식에
  넣지 않는다** — 편입은 이 표의 개정이므로 **별도 승인 사항**이며 요청된 바 없다. 그때까지 T4 는
  **설계 + 판정 1회의 기록**으로 싣는다.
  **T4가 필요한 이유는 D-23이 이미 보여줬다** — 자원 층 지표가 검색 층 성능을 대표하지 않았듯,
  검색 층 지표가 생성 층 가치를 대표하지 않을 수 있다. 층마다 다음 층의 조건이 필요하다.
- T3는 통계검정이 아니라 **결정론적 통과율 비교**(CQ는 명세). 하락 시 즉시 실패, 예외는 waiver 토큰만(횟수 보고).
- 게이트 대상은 **G0(+교정 스냅샷) · mini_graph(엄격 delta shape) · IR 코퍼스**(누출 0·분할 결정성).
  **G1/G2는 게이트 대상이 아니라 후보 모집단(방해문서 풀)이다(2026-07-29 재명명 · D-12).**
  T-Box가 G0와 동일하므로(클래스 103·술어 97/81 불변 — **구 세대 실측**. G₁/G₂ 는 2026-08-01
  현재 재조립되지 않아 구 G₀ 위에 있다) "세대"가 아니며, G1/G2가 더하는 것은 특허 문서뿐이다. **정답 2,211건은 전부 G0 안에 있고 G1/G2에만 있는 정답은 0건** — 그래서 빼면
  후보 코퍼스가 40,552 → 4,034로 줄어 건초더미가 사라진다. 데이터는 유지하고 **역할 이름만** 고친다.
- **어휘 검증 커버리지·CQ 도출(P1–P5)는 측정으로 운용**(게이트 아님) — 채우려 CQ를 지어내지 않는다.

---

## 6. 명령 레퍼런스 (재구축 중 — 확정 시 갱신)

```bash
make setup / lint / test
make vendor / snapshot / baseline / merge          # 그래프 조립 (온톨로지 자산)
make corpus        # sidecar 조인·번역·임베딩·분할·hard neg → IR 코퍼스
make index         # Pyserini 색인 (BM25 nori + FAISS flat Dense)
make leakage       # 누출 검사 (금지 간선 0)
make retrieve      # B0–B5·P0–P2 순위 산출 (3모드: oracle-free/citation-assisted/gt-assisted)
make eval          # Recall/nDCG/MRR/bpref·부트스트랩·하위집단
make effort        # Effort@Recall·Candidate Reduction·깊이별 회수 곡선 (등급1 실무 지표)
make rag           # 동결 run 상위 K → 컨텍스트 조립 → 생성 (모델·프롬프트 동결)
make rageval       # 인용 정확도·환각률·근거 지역화 (결정적 채점)
make gate          # L0–L3 + T1·T2·T3
make faults        # 결함주입(교차태스크 포함) × 게이트 검출 매트릭스
make ablation      # A1–A8 (A8 음성대조군)
make figures       # 논문 그림 전량
```
커밋은 사용자 요청 시만. 메시지는 **어느 주장(C1/C2/C3)·가설을 진전시켰는지** 밝힌다. `data/` raw 혼입 확인.

> **논문에 자리 없는 산출은 돌리지 않는다(2026-08-02 · 서두 원칙의 집행 조항).** `make retrieve`의
> 3모드 중 **주분석은 oracle-free 하나뿐**이고, citation-assisted·gt-assisted 는 현 원고 §6 에
> **한 줄도 실리지 않는다.** 두 모드를 돌릴 때는 **어느 표·그림·검정에 들어가는지**를 먼저 말하고,
> 답할 수 없으면 돌리지 않는다(§4 프로파일 의무 (4)와 같은 규칙). 같은 잣대를 다른 산출에도 건다 —
> **결과 파일이 늘어나는 것은 진전이 아니다.**

---

## 7. 논문 산출물 대응 — 표·그림은 코드가 만든다 (재구축 후 확정)

C1 자원(도달성 사다리·CQ·SHACL) / C2 검색성능표·ablation·하위집단·nDCG·**Effort@Recall 곡선** /
**C2′ 전달표(인용 정확도·환각률·근거 지역화)** / C3 결함주입 검출 매트릭스·
**게이트 승인 델타의 타 태스크 CQ 비회귀 기록(waiver 로그 포함)**·음성대조군.
**수기 기입 없음.** 대응이 깨지면 결함.

**평가 에피소드 라벨 EP1–EP4를 결과 장의 정식 이름공간으로 등록한다 (2026-08-10 · PLAN-048 개정 7).**
**EP1**(표현 감사) · **EP2**(게이트 판별력) · **EP3**(통제된 자원 교체) · **EP4**(검색 효용과 경계).
`E1`–`E7`(PLAN-035 적격심사)·`S1`–`S3`(supplementary 파일)와 **구분하며, EP 번호는 재사용하지 않는다** —
같은 기호가 두 가지를 가리켜 생긴 사고가 이미 두 번 있었다(§0 말미 S-시리즈 폐기 경위).

**C4 산출물 3종 (2026-08-07 신설 · PLAN-041 A-5).**

| 산출물 | 내용 | 수기 기입 금지의 적용 방식 |
|---|---|---|
| **설계원리 표 (DP1–DP6)** | 원리 · 근거 · **설계 시점** · 등급(§0.6) | 근거 열의 **수치는 기존 산출 파일을 참조**한다 — 손으로 옮겨 적지 않는다 |
| **실패 유형 분류표** | 온톨로지 팔이 **악화시킨** 질의의 유형 · 유형별 빈도 · 대표 사례 process tracing | **κ와 합의율을 함께 싣는다**(§1-12) · 시스템명·순위를 가린 2인 독립 코딩 |
| **경쟁 설명 분석표** | 경쟁 설명 · 예측 · **가를 실험** · 현 상태 | "가르지 못했다"를 한계로 적는 대신 **무엇으로 가를 수 있는지의 명세**로 싣는다(§0.4의 FTO와 같은 형태) |

**세 표는 전부 DRQ3·DRQ4의 답이고 C4로 나간다.** 실패 유형 분류는 AEI 요구 (3)의 **정성 축의
본체**이며, [§5.5](paper/논문_v0_9_SDKB_통합초안.md)가 설계해 둔 프로토콜을 **재활용**한다 —
**새 프로토콜을 만들지 않는다.**

> **"세대별 CQ 통과율 표"는 산출물 목록에서 뺀다(2026-08-02).** D-12 로 G0·G1·G2 의 T-Box 가
> 동일함이 실측됐으므로 그 표는 **온톨로지 세대가 아니라 A-Box 충전량**을 보여줄 뿐이고, 원고
> §6.6 도 "세대를 만들어 표를 채우지 않는다"로 이미 정리했다. **표를 채우려고 세대를 만드는 것은
> 금지한다** — 그것은 증거가 아니라 분량이다. 세대 감시가 다시 의미를 갖는 시점은 **자격 있는
> 델타(유형 ①·②)가 둘 이상 쌓였을 때**이며, 그때 다시 넣는다.

---

## 8. 보고 방식

- **결론 먼저.** 무엇이 나왔는가를 첫 문장에.
- 수치엔 **출처 명령/파일**을 함께(예: `make eval` 출력, `data/profiles/ir_corpus_v09.md`).
- 불확실은 불확실로, 추정은 "추정"으로 라벨.
- **가설과 반대되는 결과·실패한 검정·건너뛴 단계는 반드시 먼저 알린다.**
- **기각·무효과·구조적 0을 보고할 때는 "이 결과가 상류에 요구하는 수정"을 함께 말한다**(C0).
  원인이 방법인지 자원인지 구분되지 않으면 "미구분"이라고 말한다 — 자원 결함을 방법의 한계로
  적어 넘기는 것이 지금까지의 가장 큰 손실이었다.
- 사용자는 연구자다. 코드보다 **그 코드가 만든 증거**를 보여준다.

### 8.1 원고 문체 — 쉬운 한국어를 먼저, 전문용어는 괄호로 (2026-08-01 · PLAN-033)

원고는 **중학생이 읽어도 무슨 말인지 아는 문장**을 목표로 한다. 심사자는 전문가지만, 쉬운 문장이
전문가에게 부정확해지는 것은 아니다 — 부정확해지는 것은 **정의가 고정된 용어를 지웠을 때**다.
그래서 규칙은 "용어를 없앤다"가 아니라 **"뜻을 먼저 쓰고 용어를 괄호에 넣는다"** 이다.

- **첫 등장 1회만 병기**한다: "성능이 떨어지지 않는지 확인한다(비열등성 검정, non-inferiority)".
  이후로는 쉬운 쪽 표현을 쓴다. 표·수식·통계 보고에서는 정식 용어를 그대로 쓴다.
- **한자어 뭉치를 풀어 쓴다**: 비열등성 → "성능이 떨어지지 않음" · 비회귀 → "기능이 뒷걸음치지
  않음" · 정박 → "기준점으로 삼음" · 위양성 → "잘못된 경보" · 도달성 → "닿을 수 있는 정도".
- **약어를 문장 안에서 풀어 준다**: "T3(다른 태스크의 CQ가 뒷걸음치지 않았는지 보는 조건)".
- **바꾸지 않는 것**: 수치·통계량·지표 이름(Recall@100·nDCG@20·McNemar·95% CI)·가설 라벨
  (H3·H5)·게이트 이름(L0–L3·T1·T2·T3). 이것들은 쉬운 말로 바꾸면 **다른 것을 가리키게 된다.**
- **문장을 짧게.** 한 문장에 주장 하나. 접속으로 이어 붙인 3줄짜리 문장은 나눈다.
- **투고 파생본에서는** 사전등록·봉인·개봉·정정의 절차 서술을 Methods **1문단으로 한 번만** 쓰고,
  이력·대장·원장 전문은 supplementary 참조로 대체한다 — **정직성은 반복이 아니라 도달 가능성으로**
  보인다(2026-08-10 · PLAN-048 개정 6).
- **독자 해석 통제형 메타 문장**("섞어 읽지 않는다" 류)은 파생본에서 쓰지 않는다 — **절 구성이 그
  일을 하게 한다.** 작업 정본에는 그대로 둔다(감사 기록이므로).
