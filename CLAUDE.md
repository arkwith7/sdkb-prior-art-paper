# CLAUDE.md — 작업 규약

이 저장소는 **소프트웨어 제품이 아니라 논문의 증거를 만드는 연구 코드**다. 모든 코드·데이터·그림의
유일한 존재 이유는 **정본 원고**의 주장을 **검증 가능한 수치로** 뒷받침하는 것이다. 논문에 들어갈
자리가 없는 코드는 쓰지 않는다.

| 축 | 정본 | 위치 |
|---|---|---|
| **작업 정본** | 현행 논리의 유일한 원본 · **분량 상한 없음** | [paper/manuscript/stage3_source.md](paper/manuscript/stage3_source.md) |
| **투고 파생본** | 정본의 **파생물** — 직접 편집하지 않는다 · 조립 = `make submission-stage3` | [paper/submission/](paper/submission/) |
| **감사 기록** | 구 판정과 이관된 전문 — **현행 내용의 근거로 인용 금지** | [paper/archive/](paper/archive/) · [paper/supplementary/](paper/supplementary/) |
| **정본 인덱스** | 지금 무엇이 정본인가 · 권위 있는 서명 숫자 | [01.code_spec/CANONICAL-INDEX.md](01.code_spec/CANONICAL-INDEX.md) |
| **구 규약 전문** | 2026-09-05 이전 판(1,209행) · 개정 이력과 구 주장 체계 | [archive/CLAUDE-v2-2026-09-05.md](archive/CLAUDE-v2-2026-09-05.md) |

**문서끼리 어긋나면 임의로 고르지 않는다.** 충돌을 보고하고 **규약을 먼저 고친 뒤 원고를 만진다** —
원고가 규약을 앞질러 가면 그다음 세션이 낡은 규약을 근거로 되돌린다(§2.2 요건 5).

**Codex 진입점은 [AGENTS.md](AGENTS.md)이며 같은 규칙을 요약한다.** 둘이 어긋나면 이 문서가 이긴다.

---

## 0. 논지·기여·라벨 (모든 판단의 기준선)

SDKB는 **태스크 확장형 반도체 도메인 온톨로지 데이터셋**이다. 하나의 공유 T-Box가 전문가매칭·
선행기술조사·기술예측 세 뷰를 지탱한다.

> **논지 한 문장.** 하나의 도메인 지식베이스가 서로 다른 **지식 기반 작업의 공통 기반**이 되려면
> **어떤 조건을 갖추어야 하는가**를 `C-1`–`C-7` 로 정식화하고, 그 조건을 부분적으로 충족한
> **사례**(SDKB)와 그것을 **만드는 절차**를 제시한다.
>
> 조건 가운데 **`C-6`(진화 통제)은 형식 검증만으로 충족되지 않는다.** 자원을 고치는 사람이 보는
> 것은 **자원 층 지표**(문서당 개념 수·어휘 수)이고, 가치가 나타나는 곳은 **검색 층**(Recall@100·
> nDCG)이며, 그 위에 **생성 층**이 있다. 실측으로 확인된 것은 층 사이의 **어긋남**이다 — 자원
> 지표를 2.4배 개선했더니 검색이 저하되었다. 이것이 **층간 지표 불일치(cross-layer metric
> misalignment)**이고, **`C-6` 이 필요한 이유의 실증**이다.
>
> **이 관측의 범위는 §0.0.1 이 정한다** — 그 저하가 자원 때문인지 점수식 때문인지 갈리지 않았고,
> 현 스냅샷의 검색 팔은 추론을 쓰지 않는다. 그러므로 `C-6` 의 논지는 *"자원 지표가 태스크 성능을
> 대표하지 않는다"* 가 아니라 **"형식 검증만으로는 승인할 수 없고 승인 조건은 다음 층에서
> 확인되어야 한다"** 이다. 앞의 형태로 쓰면 근거보다 큰 주장이 되고, 뒤의 형태는 거부 사례 1건이
> 그대로 지지한다.
>
> **논지 피벗의 기록 (2026-09-05 · 사용자 승인 · §2.2).** 이 문단 이전의 논지는 위 두 번째 문단
> (승인 조건은 한 층 아래에서 확인된다)이 **전부**였다. 그것은 **방법의 부품 하나**였고 지금은
> `C-6` 의 자리에 있다. **판정·수치는 하나도 바뀌지 않았다** —
> [PLAN-092](01.code_spec/plans/PLAN-092-kb-exemplar-pivot.md) §2.1 판정 대장이 그것을 진다.

**기여는 셋이며 늘리지 않는다.** 넷이 되면 논점이 갈라진다.

1. **조건의 정식화** — 다중 태스크 지식 기반 작업의 베이스가 갖추어야 할 조건 `C-1`–`C-7` 과,
   **각 조건을 무엇으로 확인하는가**의 측정 규약.
2. **사례** — 그 조건을 (부분적으로라도) 실제로 충족한 도메인 지식베이스 **SDKB 와 그 공개**.
   **어느 조건을 어디까지 충족했는지를 표로 밝히는 것이 이 기여의 형태다** — 미충족을 숨기면
   기여가 아니라 과장이 된다.
3. **만드는 방법** — 큐레이션·접지·공리화·**진화 통제**의 절차, 그리고 **그 절차가 필요함의
   실증**: 파이프라인을 동결하고 자원 번들만 교체한 조건에서 형식 검증을 전부 통과한 변경이
   거부된 **통제된 사례 1건**. 자격을 갖춘 자원 변경이 1건이므로 이것은 **빈도의 주장이 아니라
   존재 증명**이다. 여기서 얻은 **교훈 셋**은 근거의 크기에 맞추어 후속 연구가 검정할
   **가설의 형태로** 진술한다.

**전달 판독(생성 층)은 기여가 아니라 기여 ③ 안의 교훈에 대한 경계 검증이다.** 생성기를 상수로 고정하고 검색팔만
바꾸므로 **RAG 방법을 제안하지 않으며**, 같은 벤치마크의 두 번째 판독이지 두 번째 벤치마크가
아니다 — 새 질의셋을 만들지 않는다.

### 0.0 본문 라벨 — 규약과 논문이 같은 이름을 쓴다

| 라벨 | 뜻 | 자리 |
|---|---|---|
| **`C-1`–`C-7`** | **지식베이스 적격 조건** — 공유 T-Box / R-Box / 근거 있는 A-Box / 외부 정답 / 작동 증명 / 진화 통제 / 출처·재현 | 원고 §1 · 상류 `sdkb/01.code_spec/plans/PLAN-004` |
| `EP1`–`EP5` | 평가 에피소드 — 표현 감사 · 게이트 판별력 · 통제된 자원 교체 · 검색 효용과 경계 · 제2 온톨로지 이식 | 단일 원천 [paper/episodes.yaml](paper/episodes.yaml) |
| `교훈 ①②③` | 이전 가능한 설계 교훈 — 한 층 아래 승인 · 교차 태스크 감시 · 후보생성 분리 | 원고 §6.3 |
| `L0`–`L3` | 형식 검증 층 — 신선도·무결성 / SHACL / 논리 일관성 / CQ 기능 | §5 |
| `T1`–`T4` | 태스크 조건 — 검색 비열등 / 하위집단 / 교차 태스크 CQ / 하류 생성 | §5 |
| `G0`–`G2` | 그래프 계보 — `G0` 게이트 대상 · `G1`·`G2` **후보 모집단**(세대가 아니다) | §5 |
| `B0`–`B5` · `P0`–`P2` · `A1`–`A8` | 비교 구성과 절제 | 원고 §4.3 · §5.3.2 |

**라벨을 재부여하지 않으며 번호를 재사용하지 않는다** — 재번호는 사전등록 기록과의 추적성을 끊는다.
`L0`–`L3`은 형식 검증 층 전용이고(그래서 교훈은 원문자를 쓴다), `E`는 본문에서 평가환경 하나만
가리킨다. 구 라벨(`C0`–`C4`·`DRQ`·`DP1`–`DP7`·`H1`–`H5`·`RQ1`–`RQ5`)은 **사전등록 기록의
좌표계로 살아 있으며** 대응은 [S6 crosswalk](paper/supplementary/S6-preregistration-crosswalk.md)가 진다.
**`C-1`–`C-7`(하이픈 있음)과 구 `C0`–`C4`(하이픈 없음)는 다른 것이다** — 구 라벨은 아카이브의
주장 체계이고 본문에 0회 등장한다. 표기를 섞지 않으며, 새 조건 라벨을 재부여하지 않는다.

### 0.0.1 현 스냅샷의 온톨로지 팔은 추론을 쓰지 않는다 — **주장 범위를 정하는 전제이자 `C-2`·`C-5` 의 정의**

**이 조항은 폐지되지 않았고 그대로 유효하다.** 다만 지위가 하나 늘었다 — 이것은 **현 스냅샷의
전제**이면서 동시에 **이 논문이 해소하려는 결손 자체**다(`C-2` R-Box · `C-5` 작동 증명 ·
상류 `sdkb/01.code_spec/plans/PLAN-004`). **해소되기 전까지 아래 허용·금지 표는 한 글자도 완화되지 않으며**, 해소된
뒤에도 **그 전제 아래 난 판정은 조건과 함께 보존한다** — 소급 수정하지 않는다(§2.1).

**이것을 §6 한계에 적지 않고 여기에 적는다.** 뒤에 적으면 매 세션이 결과 문장마다 *"선언 층에
한정하여"* 를 덧붙여 방어하게 되고, 실제로 그렇게 되었다. **전제는 앞에서 한 번 선언하고, 그
다음부터는 주장을 그 크기에 맞춰 쓴다**(§8.0).

**실측 — 검색 팔의 점수식은 다음이 전부다.**
`(1−α)·text + α(w_c·c + w_h·p + w_i·ipc + w_f·fc)` (`analysis/ontology_eval.py:155`),
본문이 인용하는 P1 의 가중은 **`(w_c, w_h, w_i, w_f) = (0.25, 0.0, 0.25, 0.5)`**
(`analysis/results_table.py:44`). 개념 항 `c` 는 **비가중 집합 Jaccard**
(`retrieval/ontology_rerank.py:90`)이고, **추론기는 `analysis/ep5.py`·`analysis/faults.py`에만
있으며 `retrieval/` 어디에도 없다.** 곧 검색이 실제로 쓴 것은 **추론 없는 분류체계의 어휘 겹침과
한정요소 포괄률**이다.

**그러므로 다음 셋이 전제이며, 셋 다 이 자원에서 관측된 사실이다.**

1. **`w_h = 0` 이므로 계층·정렬만 바꾸는 델타는 T1·T2에 원리적으로 비가시다.** 게이트의 성능
   센서가 볼 수 있는 것은 **어휘·매핑 밀도 변경**이며, 의미 구조 변경이 아니다.
2. **R-Box(추론 공리)가 없어 L2 는 공허하게 통과한다** — 모순을 만들 공리가 없으므로 모순이 없다.
   이것은 이미 결함 대장 **D-03**과 **CR-005**에 등재돼 있다.
3. **T3 의 단독 검출 수는 공리 빈곤 조건에서의 값이다.** CR-005 의 검증기준이 *"방향 역전 결함
   검출 0/9 → ≥ 7/9"* 인 것이 그 뜻이다 — **공리가 생기면 그 결함의 상당수는 형식층 귀속이
   된다.** T3 의 값을 인용할 때 이 조건을 함께 적는다.

**쓸 수 있는 것과 쓸 수 없는 것.**

| 허용 | 금지 |
|---|---|
| "분류 어휘 신호가 강한 텍스트 기준선을 깊은 회수에서 개선하였다" | "온톨로지가 검색을 개선하였다"(추론을 함의하는 형태) |
| "형식 검증 네 층을 통과한 변경이 태스크 조건에서 거부되었다" | "논리적으로 검증된 온톨로지" |
| "이 공리 구성 아래에서 T3 이 단독 검출하였다" | 조건 없는 "T3 단독 검출 12/45" |
| "자원 지표 개선이 검색 층으로 전달되지 않았다 — 자원과 점수식은 미구분" | "자원 지표는 태스크 성능을 대표하지 않는다"(무조건 일반형) |

**저하의 원인은 자원인지 점수식인지 갈리지 않았다.** 비가중 Jaccard 는 항이 늘수록 잡음이 커지므로
개념을 2.4배 늘렸을 때의 저하는 **점수식의 귀결일 수 있다.** 그러므로 이 결과를 제목·초록·그림에서
**조건 없는 일반 명제로 쓰지 않는다.** 가르려면 df 가중을 넣고 다시 재야 하며, 그것은 재측정이
아니라 **새 방법이므로 §2 전체를 탄다.**

### 0.0.2 동결된 사실 (예고 없이 바뀌면 회귀 테스트가 차단)

- **다국어 벤치마크**: 질의(거절특허 1,000)는 **전량 한국어**. 후보 코퍼스 40,552는 **다국어**다
  (한국어 39,246 = 96.8% · 영어 1,189 · 일본어 117). qrel 정답 2,321 =
  한국어 57%·영어 39%·일본어 2%. 질의밀도(회수가능 정답 ≥1) = 97.6%.
- **분모 규율**: 2,534(인용 엣지) ≠ 2,321(고유 정답) ≠ 2,211(노드도달) ≠ 584(판단연결). 혼용 금지.
- **기술 스택**: Pyserini(BM25 nori + FAISS flat Dense + RRF) · parquet/pandas(메타데이터·후보필터) ·
  Titan Embed v2(임베딩). Bedrock 접근은 `config.get_secret()`.
  FAISS flat = 정확·결정적(ablation 오염 없음), 4만 건엔 근사 인덱스·DB 서버 불필요.
  **질의 번역은 미구현이다.** `src/`에 번역 모듈이 없다.
- **질의·문서는 장문이다.** 주분석 질의 = `claims_independent`(독립항 전문 · 중앙값 527자),
  문서 = `text_main`(초록+청구항 전체 · 2,255자). 키워드·단문 검색이 아니다.
- **개념 링크의 출처는 자원 세대에 따라 다르다.** **O 조건**에서는 그래프에서만 읽었고
  (`corpus/assemble.py:114`) 그래서 자유 텍스트 질의를 받을 수 없었다 — **원고 §6 의 모든 수치가
  이 조건이다.** **O′ 이후**에는 `corpus/concept_link.py` 가 표면형 사전을 본문에 적용하므로 자유
  텍스트 질의가 원리적으로 가능하다(적용기는 질의/후보를 구분하는 인자를 받지 않는다 — 비대칭이
  T1 을 오염시키므로 구조로 막았다). **세대를 밝히지 않고 둘 중 하나만 인용하지 않는다.**
  어느 세대에서도 이것을 "온톨로지의 한계"로 적지 않는다.
- **T-Box 는 연구 대부분의 기간 동안 불변이었다.** `G0`·`G1`·`G2`의 T-Box는 동일하며 세 "세대"는
  전부 특허 A-Box 추가다. 그러므로 **문서 추가 델타는 승인 안전성의 증거가 될 수 없다.**
- **세 뷰 가운데 외부 정답을 가진 것은 선행기술조사 하나뿐이다** — 심사관 인용이 그것이다.
  **전문가 매칭 뷰의 A-Box 는 전량 합성이다**(`ExpertCase` 163건 전부
  `caseSource "synthetic_ontology_heuristic"` · `sdkb-abox-experts-problems.ttl`).
  **기술예측 뷰에는 태스크 인스턴스가 없다**(`sdkb-foresight.ttl` 은 열거 개체 12건뿐 —
  `STEEPVEDimension` 7 · `OptionType` 5). 그러므로 두 뷰는 **어휘 선언과 CQ 감시 대상**이며,
  **성능을 주장하지 않는 이유는 "별도 논문이 있어서"가 아니라 "정답이 없어서"다**(§0.0.3).

### 0.0.3 정량 평가의 비대칭은 약점이 아니라 설계 결정이다 — 다만 **공개해야 성립한다** (`C-4` 의 결손)

**정량 태스크를 선행기술조사로 고른 이유는 외부 정답(심사관 인용)이 거기에만 있기 때문이다.**
이 비대칭을 밝히지 않으면 공개된 TTL 을 여는 심사자가 `caseSource "synthetic_ontology_heuristic"`
을 먼저 보게 되고, 그 순간 기여 ①(검증 가능한 형태의 공개)과 충돌한다. **밝히면 설계 결정이고,
밝히지 않으면 은폐다** — 수정 비용은 문장 몇 개이며 데스크 리젝 위험은 그보다 크다.

- **합성이라는 사실을 산출물 서술에서 밝힌다.** 두 뷰의 태스크 정확도를 재지 않는 이유가 그것이다.
- **"별도 논문이 있어서 성능을 주장하지 않는다"라고 쓰지 않는다.** 미투고 원고를 근거로 쓰면 그
  근거는 검증 불가능하고, 두 원고가 서로를 인용하면 어느 쪽도 자기 안에서 닫히지 않는다.
  **작성 중인 다른 논문을 이 저장소의 산문·주석·규약에서 근거로 인용하지 않는다.**
- **CQ 통과율은 대리지표다.** 질의 경로가 비어 있지 않다는 것만 보며 그 태스크의 **정확도를 보지
  않는다.** T3 의 결과를 인용할 때 이 성질을 함께 적고, **"교차 태스크 성능 비회귀"라고 쓰지
  않는다** — 잰 것은 통과율이다.

**합성 A-Box 에서 정답을 파생하는 설계는 쓰지 않는다.** `ExpertCase` 링크를 가리고 복구율을 재는
것은 심사관 인용 마스킹과 형태만 같고 성질이 다르다 — **온톨로지가 만든 사례를 온톨로지 경로로
복구하는 순환**이며, 재는 것은 자원 변경의 효과가 아니라 **생성 휴리스틱의 자기일치**다.
교차 태스크 성능을 실측하려면 **정답이 온톨로지 밖에서 와야 한다**(전문가 패널 판정 등 · κ와
합의율을 동반한다 · §1-12). 그 정답이 확보되기 전까지 **초점 태스크를 교체한 승인 실험은 성립하지
않는다** — 정답 없는 T1 은 EP5 에서 이미 비어 있었던 그 방식으로 빈다.

작업 전 자문하라: **이 산출물은 어느 기여, 어느 에피소드, 논문의 어느 자리를 진전시키는가?**
답할 수 없으면 코드를 쓰지 말고 물어라.

---

## 0.1 상류 SDKB 계약 — 코드 의존은 단방향, 결함 정보는 **역방향 의무**

G0·정답지의 원천은 `~/Dev/sdkb`(자체 CLAUDE.md 보유). **코드·데이터 의존은 한 방향뿐이다.**
```
SDKB 원본 ──(사람이 make vendor)──> data/external/sdkb/ (얼린 스냅샷 + sha256) ──> 코퍼스 조립
        <──(결함 대장 · CR 문서 · 재검정 회신)────────────────────────  실험 결과
```
- 런타임에 `~/Dev/sdkb`를 읽지 않는다. `ontology/vendor.py`만, 사람이 `make vendor` 할 때만.
- SDKB 결함은 **상류에서 고친다.** 우회 패치는 스냅샷 출처를 거짓으로 만든다.
- **결함 정보의 역류는 의무다.** 실험이 온톨로지 결함을 드러내면
  [upstream/DEFECT-LEDGER.md](upstream/DEFECT-LEDGER.md)에 등재하고, 우선순위 항목은 상류
  CLAUDE.md §2 **1단계 요구정의 양식**의 `upstream/CR-NNN-*.md`로 쓴다. 등재 요건은 셋 —
  **증거(실행된 코드의 출력)·수정 제안·검증기준.** 검증 불가한 "개선하면 좋음"은 등재하지 않는다.
  **검증기준에는 하류 태스크 지표가 최소 1개 들어간다** — 자원 측 지표만 걸어 두면 그 기준을 전부
  통과하고도 검색이 나빠질 수 있고, 실제로 그랬다. 자원 지표는 **수정이 됐는지**를, 하류 지표는
  **그 수정이 쓸모가 있었는지**를 잰다. 둘은 대체 관계가 아니다.
- **이 저장소는 상류를 직접 수정하지 않는다.** CR을 제출할 뿐이고 실제 수정은 상류 절차를 탄다.
  수정 후에는 `make vendor`로 새 스냅샷을 뜨고, 효과는 **새 사전등록 아래 새로 검정**한다 —
  기존 판정은 소급 수정하지 않는다(§1-2·§1-3).
- 온톨로지 자산(T-Box·CQ·SHACL·claim-feature sidecar·개념링크)은 온톨로지 검색팔의 입력 —
  **재사용하되 논문이 변경하지 않는다**(코퍼스는 파생 뷰).

**왜 역류가 의무인가.** 이 저장소의 최종 목적은 투고만이 아니다. **기각된 가설과 무효과 결과의
다수는 방법의 실패가 아니라 온톨로지 자원의 결함이다.** 이런 관측을 "타당성 위협"으로만 적고
끝내면 **논문은 나가지만 제품은 한 걸음도 나아가지 않는다.**

---

## 0.2 신규성·진보성 명세

**"최초"를 주장하지 않는다.** 기여는 요소의 신규성이 아니라 결합과 실험설계의 차별성에 둔다.
허용되는 형태는 *"우리가 확인한 범위에서는"*이며, 선취·최상급 표현은 `make claims-check`가 막는다.

**진보성의 본체는 하나다** — *"자원 품질을 2.4배 올렸더니 태스크 성능이 나빠졌다."* "온톨로지를
좋게 하면 검색이 좋아진다"는 자명한 명제이고, 그것이 **아니라는 것**을 실측으로 보인 것이 이
연구의 자명하지 않음이자 승인 게이트가 필요한 이유의 실증이다. 이 결과는 한계가 아니라 **본론**이다.

*신규성·진보성 명세표(N1–N3 · I1–I4) 전문은 [구 규약 §0.2](archive/CLAUDE-v2-2026-09-05.md)에 있다.*

---

## 0.3 "확실하다"의 정의 — 결과를 보기 전에 못박는다

**성능 우월 주장에만 발동한다.** 다음 다섯이 **전부** 충족될 때만 "확실"·"입증"류 표현을 쓴다.

1. 두 독립 확증 분할에서 부호와 유의성이 재현된다
2. 사전 지정 **주 구성**에서 유의하다
3. 효과크기 95% CI 하한 > 0
4. 민감도 격자 전반에서 부호가 유지된다 (가중치·K·토큰화)
5. 누출 감사 0 · 봉인 1회 개봉

**현재 충족은 1/5이다.** 그러므로 쓸 수 있는 표현은 **"조건부"·"부분 지지"·"깊은 회수에 한정"**
이다. 하나라도 미달인 채로 "확실"을 쓰면 §1-2 위반으로 다룬다.

---

## 0.4 실무 이익 주장의 3등급

등급을 섞으면 전체가 등급 3으로 읽힌다 — **분리 자체가 방어 장치**다.

| 등급 | 성격 | 싣는 곳 |
|---|---|---|
| **1 · 측정된 이익** | 이 논문이 숫자를 산출 | 결과 장 |
| **2 · 조건부 이익** | 구조는 갖췄으나 미측정 | 논의 장 |
| **3 · 인접 응용** | **자원 결손을 명세로 제시** (FTO · 기술기획) | 논의 말미·한계 |

**FTO는 이 벤치마크로 잴 수 없다** — 특허성 조사와 침해 위험 판단은 법적 질문이 다르다. 쓰는
형태는 **"공유하는 단계 + 빠진 축"** 명세이며, 그것은 과장이 아니라 로드맵이다.
**등급별 허용·금지 문구는 [paper/verdicts.yaml](paper/verdicts.yaml)이 강제한다.**

---

## 0.5 한 논문으로 유지하는 규칙

**조건 장이 늘어도 평가 아키텍처는 하나다.** 피벗으로 결과 절이 셋 늘지만(`C-2`·`C-3`·`C-5`),
**같은 질의·같은 qrel·같은 분할·같은 누출 통제** 위에서 잰다. 이 조항이 막는 것은 장의 수가
아니라 **평가 아키텍처가 둘이 되는 것**이며, 그 규칙은 피벗으로 느슨해지지 않는다.

논문이 둘로 갈라지는 지점은 "주제"가 아니라 **평가 아키텍처**다 — 질의 집합이 둘, 분할이 둘, 정답
체계가 둘, 누출 통제가 둘이 될 때 심사자는 "두 논문"이라고 읽는다.

1. **생성기는 상수, 검색팔만 변인.** 모델·프롬프트·컨텍스트 K·온도·시드를 고정한다.
2. **같은 벤치마크의 두 번째 판독이지 두 번째 벤치마크가 아니다.** 같은 질의·qrel·분할·누출 통제.
3. **기여 목록에 올리지 않는다.** 기여는 §0의 셋으로 유지한다.
4. **"RAG 성능"이 아니라 "전달(transfer)"이라 쓴다.** 용어 하나가 논점을 지킨다.

**선제 방어.** *"본 연구는 RAG 방법을 제안하지 않는다. 생성기를 상수로 고정하고 검색팔만 바꾼
전달 실험이며, RAG 방법 간 비교는 별개의 연구 질문이다."*

---

## 0.6 교훈 승격 기준 다섯 — §0.3이 성능에 한 일을 설계지식에 한다

교훈은 **다섯이 전부 충족될 때만** 지지된 것으로 쓰고, 하나라도 미달이면 **후속 가설**로 내린다.

1. **근거가 실행된 코드의 출력이다** — 서사나 통념이 아니다.
2. **경쟁 설명이 배제됐거나, 배제되지 않았음이 명시됐다** — 숨기면 미달이다.
3. **산출물 밖으로 이전 가능한 형태로 진술됐다** — "SDKB에서는"이 붙으면 그것은 **관측**이다.
4. **반증 가능하다** — **어떤 관측이 이 교훈을 약화시키는지**를 함께 적는다.
5. **사후 역산이 아니다** — **설계 시점**과 **확인 시점**을 각각 기록한다(§1-12와 짝).

**"후속 가설"은 실패가 아니라 정직한 등급이다.** 등급 전문과 승격 이력은
[구 규약 §0.6·§0.7](archive/CLAUDE-v2-2026-09-05.md)에 있다.

---

## 0.7 구 설계 연구질문·설계원리 (DRQ · DP)

**이 좌표계는 이관됐다.** `DRQ1`–`DRQ3′`와 `DP1`–`DP7` 전문은
[구 규약 §0.7](archive/CLAUDE-v2-2026-09-05.md)에 있고, 본문 대응(`DP` ↔ 교훈 ①②③, 그리고
본문에서 내려간 원리가 어디로 갔는지)은
[S6 crosswalk](paper/supplementary/S6-preregistration-crosswalk.md) §4가 진다.

**본문은 `DP`가 아니라 「교훈 ①②③」을 쓴다** — 사례 1건 위에 선 일반 명제로 읽히지 않게, 주장의
문법을 현상 제시에서 존재 증명으로 내렸기 때문이다. **판정과 수치는 하나도 바뀌지 않았다.**

---

## 0.8 판정 서술 문구 — 정본은 `paper/verdicts.yaml`

**허용·금지 문구의 정본은 [paper/verdicts.yaml](paper/verdicts.yaml)이고, 기계 검사는
`make verdicts`다.** 여기에 표를 다시 적지 않는다 — 사본을 두면 원본과 어긋나고, 실제로 그렇게
되었다.

- **`verdicts.yaml` 수정은 §1-2와 동급**으로 다룬다(사용자 승인 커밋으로만).
- **이 사전은 판정을 바꾸지 않는다** — 판정을 옮겨 적는 방식만 정한다.
- **언급(mention)은 사용(use)이 아니다.** 금지 문구를 *"이 문구는 쓰지 않는다"*의 형태로 인용하는
  것은 위반이 아니며, 검사기도 따옴표 안의 언급을 위반으로 세지 않는다.
- **영문 판정 강도**는 [paper/STYLE-EN-ACADEMIC.md](paper/STYLE-EN-ACADEMIC.md) §4가 정본이며
  `make style-check-en`이 강제한다.

---

## 0.9 라벨 정책 — 지켜야 하는 것은 **기호 동일성이 아니라 추적성**이다

외부 규범이 요구하는 것은 **추적 가능성**이지 기호 동일성이 아니다. 사전등록 사실과 링크를 밝히고,
확증과 탐색을 구분하고, 결과를 실패까지 전량 보고하고, 원고 주장 ↔ 사전등록 결과의 대응이 확인
가능하면 된다. **마지막 항목은 crosswalk가 진다.**

1. **작업 정본·PLAN·결함대장·`verdicts.yaml`의 record는 원 라벨을 그대로 쓴다.**
2. **투고 파생본은 자체 호칭을 쓴다.** `H1`–`H5`·`RQ1`–`RQ5`는 파생본 본문에 쓰지 않으며 가설은
   **서술형**으로 부른다(예: "검색 유용성 점검"·"전달 점검"). 설계 질문은 `RQ1`–`RQ3`로 쓴다.
3. **파생본에서 새 가설 기호를 만들지 않는다** — 같은 기호가 두 가지를 가리켜 난 사고가 이미 둘 있다.
4. **crosswalk 1장은 필수다** —
   [S6](paper/supplementary/S6-preregistration-crosswalk.md). **이 표를 빼는 것은 규약 위반이
   아니라 정직성 위반이다.** `DP` ↔ 교훈 행과 **라벨이 사라진 자리**를 반드시 포함한다.
5. **`E`는 본문에서 평가환경 하나만 가리킨다.** 적격심사 항을 `E1`–`E7`로 부르지 않고 서술형으로
   쓴다(사전등록 문서와 결함 대장에서는 그대로 둔다 — 규칙 1). 기계 검사는 `style-check`의 V7.

**바뀌지 않는 것**: 판정·수치·동결 사실·게이트 이름(`L0`–`L3`·`T1`–`T4`)·절제 식별자(`A1`–`A8`)·
평가 에피소드(`EP1`–`EP5`). **바뀌는 것은 판정을 부르는 이름뿐이다.**

---

## 0.10 정본 포인터 — 규칙이 어디에 있고 무엇이 지키는가

**아래 파일이 정본이다. 이 규약에 그 내용을 옮겨 적지 않는다** — 사본을 두면 원본과 어긋난다.

| 관심사 | 정본 | 검사 |
|---|---|---|
| 판정 문구 강도 | [paper/verdicts.yaml](paper/verdicts.yaml) | `make verdicts` |
| 데스크 리젝 요인 · 분량 · 표·그림 상한 · §참조 도달성 | `scripts/submission_check.py`(D2–D10) · [paper/word-budget.yaml](paper/word-budget.yaml)(D13 · 경고) | `make submission-check` |
| 한국어 학술 문체 | [paper/STYLE-KO-ACADEMIC.md](paper/STYLE-KO-ACADEMIC.md) | `make style-check` |
| 영문 문체 · 영문 판정 강도 | [paper/STYLE-EN-ACADEMIC.md](paper/STYLE-EN-ACADEMIC.md) | `make style-check-en` |
| 용어 첫 등장 규율 | [paper/glossary-terms.yaml](paper/glossary-terms.yaml) | `make glossary-check` |
| EP ↔ 절 대응 · 상호참조 · 국·영문 대조 | [paper/episodes.yaml](paper/episodes.yaml) | `make crossref-check` |
| 주장 강도 (선취·최상급 · 미실행 이력) | `scripts/check_claims.py` | `make claims-check` |
| 그림 규격 · 개념 그림 수치 | [paper/FIGURE-SPEC.md](paper/FIGURE-SPEC.md) · `paper/figures/data/concept_values.json` | `make figure-data` |
| 서명 숫자 표류 | [01.code_spec/CANONICAL-INDEX.md](01.code_spec/CANONICAL-INDEX.md) §1 | `make sig-check` |
| 국·영문 보충자료 수치·의미 정합 | `scripts/check_supplementary_en.py` | `make supplementary-check-en` |

**층이 겹치지 않는다.** 판정 강도 · 데스크 리젝 요인 · 어체 · 용어 · 상호참조 · 주장 크기 ·
수치 표류가 각각 다른 검사에 걸려 있다.

- **경고 모드의 규칙은 통과를 근거로 인용하지 않는다.** 설계상 경고로 남는 것은
  `glossary-check`의 G4(식별자 산문 사용 — 라벨 체계를 설명하는 자리가 정당하게 있다) ·
  `submission-check`의 D6 권고 구간과 D13 분량 예산이다.
- **검사 기준선은 코드에 동결한다** — 검사기가 현 상태를 따라 움직이면 "움직이는 골대"가 된다.
- **구 규약의 `§2.3-1`~`-7`(검사기 항 번호)은 이 표의 행으로 대체되었다.** 옛 참조는 여기로 온다.

---

## 1. 절대 금지 (예외 없음)

1. **수치를 지어내지 않는다.** 모든 숫자는 실행된 코드의 출력. 미확정은 `[실험 후 기입]`/`TODO(N)`로 남긴다.
2. **가설에 유리하게 만들지 않는다.** 기각되면 기각대로 보고. 임계값·모델·가중치를 결과 보고
   바꾸는 p-hacking 금지. 변경은 사전 동결된 민감도 격자로만.
3. **사전등록을 사후에 고치지 않는다.** 동결 대상: ε(비열등 마진 0.02)·δ(하위집단 0.05)·주지표
   (family-level Recall@100)·시점/패밀리 분할 경계·low-overlap 정의·CQ 스위트 분할·결함주입 유형·
   Dense 모델·토큰화. **테스트 qrel은 최종 비교 시점까지 봉인.** 증거는 커밋 해시.
   → **자원 스냅샷이 바뀐 뒤의 재측정은 이 조항이 막는 대상이 아니다**(새 실험 = 새 사전등록).
   경로는 **§2.1**. 금지되는 것은 *결과를 본 뒤 같은 실험의 조건을 바꾸는 것*이다.
4. **누출을 허용하지 않는다.** 질의 인용 간선(`hasPriorArtExaminer`/`hasPriorArt`/`overPriorArt`) 마스킹·
   시점유효(공개일<cutoff)·패밀리 분리·`NoveltyScore` 등 정답 파생 피처 배제. `leakage_check`가 강제.
5. **원문 데이터를 커밋하지 않는다.** raw/interim/processed·특허 전문(claim/abstract)은 gitignore.
   KIPRIS 학술이용·비재배포. 커밋 가능한 것은 집계·메타데이터·식별자·해시·재구축 절차뿐.
6. **어휘를 발명하지 않는다.** 클래스·프로퍼티는 SDKB 실물(`data/external/sdkb/*.ttl`)에 있는 것만.
   새 술어가 필요하면 **코드 전에 사람에게 물어라.**
7. **수작업 그림·표 금지.** 논문 그림·표는 코드가 생성한다(`viz/figures.py`·분석 CLI).
8. **스냅샷(`data/external/sdkb/`)·봉인 qrel 임의 갱신 금지.** 사용자 지시 시에만, MANIFEST 절차대로.
9. **비밀정보 하드코딩 금지.** `config.get_secret()`로만(`KIPRIS_API_KEY`·`AWS_*`·`BEDROCK_*`).
10. **요청하지 않은 코드 금지.** 리팩터·추상화·"나중에 쓸" 유틸·승인 없는 파일 — 제안하고 멈춘다.
11. **LLM·RAG 실험의 다섯 금지.** 생성 층을 넣는 순간 새 부정 경로가 열린다.
    - **프롬프트를 결과 보고 고치지 않는다.** 프롬프트·모델 ID·온도·시드·컨텍스트 K는 사전등록
      동결 대상이다(ε·δ와 동급).
    - **판정자에게 정답을 보여주지 않는다.** LLM 판정자 누출은 qrel 누출과 같은 등급으로 다룬다.
      기본 채점은 **인용 식별자 대조**(결정적)이며, LLM 판정자는 보조로만 쓰고 쓸 때는 사람
      표본 검증을 동반한다.
    - **비결정성을 숨기지 않는다.** 온도 0에서도 반복 실행 분산을 보고한다.
    - **모델 버전을 고정한다.** 모델이 바뀌면 재측정이 아니라 **새 실험**이다.
    - **검색 지표로 생성 성능을 대체하지 않는다.** R@100 개선을 "RAG가 좋아졌다"로 쓰지 않는다.
12. **설계지식 고유의 네 금지.** 가설검정에 p-hacking이 있듯 설계지식 주장에는 고유한 부정 경로가
    있다. **이 넷이 없으면 교훈은 결과의 사후 요약일 뿐 설계지식이 아니다.**
    - **교훈을 결과에서 역산하지 않는다.** 관측을 본 뒤 만든 것은 교훈이 아니라 **후속 가설**이다.
      **설계 시점과 확인 시점을 반드시 적는다** — 확인이 선행하면 그렇게 표기한다.
    - **경쟁 설명을 침묵으로 배제하지 않는다.** 가르지 못했으면 **그렇게 쓴다**(§8 보고 방식).
    - **부정 결과를 교훈으로 세탁하지 않는다.** 기각은 기각으로 남기고(§2.2 요건 1), 교훈은
      그 위에 **얹을** 뿐 **대체하지** 않는다.
    - **정성 평가를 정량 부족의 대체물로 쓰지 않는다.** 질적 코딩은 **κ와 합의율 없이 싣지 않는다**
      (κ<0.4이면 본문 제외 후 부록).

---

## 2. 개발 절차 — 5단계와 정지 게이트 🛑

`1 요구정의 🛑 → 2 분석(데이터 먼저 관찰) 🛑 → 3 설계(결정성·검정방법 확정) 🛑 → 4 구현 → 5 테스트·검증 🛑`
승인은 명시적 응답("진행"·"OK"·"승인"). 침묵은 승인이 아니다.

- **1 요구정의**: 목적(어느 기여·어느 에피소드)·입력·출력(논문 자리)·성공기준(검정가능)·비목표.
- **2 분석**: 실제 파일/그래프/코퍼스를 열어 스키마·결측·분포·언어·엣지케이스를 보고. 가정은 "가정"으로 라벨.
- **3 설계**: 모듈·시그니처·결정성(시드 고정)·검정(단측/양측·효과크기·표본단위)·신규 의존성 근거.
  통계·평가 방법은 여기서 확정하고 **결과를 본 뒤 바꾸지 않는다.**
- **4 구현**: 승인 범위 안에서만. 경로·네임스페이스는 `config`에서. 하드코딩 금지.
- **5 검증**: `make lint && make test` + 아래 3층. 실패는 실패로 보고. 검증 없이 "완료" 금지.
  (a) 단위(경계·결측·빈입력·중복, 외부의존 격리, 결정성) (b) 통합(모듈 경계 계약 — 특히 게이트 우회
  차단·누출 0·분할 결정성) (c) 파이프라인(§4 프로파일·`make gate`·코퍼스 재현성).

> 규모 예외: 오타·문구·승인된 계획의 기계적 반복은 압축 가능. **데이터 수집·변형, 코퍼스·qrel·통계
> 결과를 바꾸는 변경, 게이트/누출 로직 변경에는 예외가 없다.**

**게이트가 넷인 것은 §2뿐이다.** 아래 세 경로는 정지점이 **하나**이며, 규율을 푸는 것이 아니라
없던 길을 낸다 — 재측정마다 네 번 멈추면 실험 대신 문서만 쌓인다.

| 경로 | 발동 조건 | 정지점 |
|---|---|---|
| **§2** | 코드·데이터·게이트·누출 로직 변경 | 🛑 4 |
| **§2.1** | 파이프라인 동결 · **자원 스냅샷 sha256만 변경** | 🛑 1 (사전등록 동결) |
| **§2.2** | 확증 점검의 개수·지위, 기여, 장 구성, 논지 변경 | 🛑 1 (요건 다섯) |
| **§2.3** | 투고 파생본 조립·대규모 편집 | 🛑 1 (마스터 계획) + 기계 검사 |

---

## 2.1 자원 버전이 바뀌면 — 정지 게이트 1개로 전량 재측정

**발동 조건은 "T-Box 변경"이 아니라 스냅샷 서명 변경이다.** `data/external/sdkb/`의 **파일
sha256이 바뀌면** 발동한다 — 상류 교정이 실제로 움직이는 것은 대부분 **개념층**(어휘·계층·링크)
인데 그것은 T1·T2에 직접 영향이므로, 트리거를 "T-Box 변경"으로 잡으면 자격 있는 델타를 놓친다.
**커밋 해시가 아니라 `PROVENANCE.json`의 파일별 sha256으로 동결한다** — 상류의 일부 TTL은
gitignore된 빌드 산출물이라 같은 커밋에서도 결과가 다를 수 있다.

**1) 사전등록 동결 🛑 (유일한 정지점).** 파일 하나에 아래를 적고 **커밋한 뒤** 실행한다.
스냅샷 파일별 sha256 · 상류 커밋 · ε=0.02 · δ=0.05 · 주지표(family-level Recall@100) · 시점/패밀리
분할 경계 · 봉인 qrel 해시 · 검정할 가설과 판정식 · **확증과 탐색의 구분**.

**2) 무정지 연속 실행.**
```
make vendor → corpus → index → leakage → retrieve → eval → gate → figures
```
**`leakage`와 `gate`를 "주변 검증"으로 빼지 않는다.** `leakage`는 §1-4이자 T1 판정의 전제이고,
`gate`의 T1·T2 출력은 **부수 확인이 아니라 증거 그 자체**다.

**2′) 파이프라인 서명이 움직이지 않았으면 그 실행은 "통과"가 아니라 "비가시"다.** 스냅샷 sha256이
바뀌어도 구성요소(`ir_corpus`·`concept_axis`·`graph_v1`) 서명이 그대로면 파이프라인은 그 델타를
읽은 적이 없다. 그때의 ΔR₁₀₀은 측정된 0이 아니라 **구성상 0**이고 그 위의 `Accept = 1`은 승인의
증거가 아니다. `tgate_report.json`의 `resource_visibility.note`가 분류하며, `no_evidence`는
*"비가시라는 증거가 없다"*이지 *"델타가 보였다"*가 아니다. **이 필드는 승인식에 들어가지 않는다.**

**3) 결과 보고 → 그 다음에 상류 환류.** 결함 등재·CR 작성은 **후행 산출물**이며 임계경로에 올리지 않는다.

**이 조항을 쓸 수 없는 경우.** **코드가 바뀌면 §2 전체가 적용된다.** 검색 설정·가중치·토큰화·
Dense 모델·분할 경계 중 하나라도 손대면 그것은 재측정이 아니라 새 방법이다. **"재실험"이라는
이름으로 로직 변경을 무검증 통과시키지 않는다.** 또한 **개봉된 표본으로 우월성을 다시 확증하지
않는다** — 재확증은 신규 확증분할로만 한다. 새 스냅샷 위의 재측정은 **새 실험**이므로 §1-3 위반이
아니며, 구 판정은 소급 수정하지 않고 보존된다.

---

## 2.2 원고 기조가 바뀌면 — 정지 게이트 1개, 요건 5개

**적용 대상.** 확증 점검의 개수·지위, 기여 목록, 장 구성, 주장, 논지 문장 중 하나라도 바뀌는 작업.
절 안의 문장 다듬기·오탈자·이미 승인된 계획의 기계적 반영은 대상이 아니다.

**변경 전에 아래 다섯을 한 파일에 적고 커밋한 뒤 편집을 시작한다.**

1. **판정 대장** — 각 판정이 **변경 전후로 불변임**을 표로 확인한다. 하나라도 바뀌면 그것은 기조
   변경이 아니라 §1-2 위반이다.
2. **이동 대장** — 각 절이 어디로 갔는지(삭제가 아니라 이동). 잘라낸 전문은 `paper/supplementary/`로.
3. **기여 사상표** — 새 기조의 각 주장이 §0의 기여 셋 중 어디에 대응하는지. **대응하지 않는 것이
   있으면 멈추고 승인 요청.**
4. **동결 사실 점검** — 주지표·분할 경계·라벨·게이트 이름·토큰화·Dense 모델을 건드렸는지.
   건드렸으면 이 조항이 아니라 §2 또는 §2.1로 넘어간다.
5. **역방향 점검** — 바뀐 기조가 **이 CLAUDE.md의 문구와 충돌하지 않는지.** 충돌하면 **규약을
   먼저 고친다.**

**선례.** 이 조항이 다룬 가장 큰 변경은
[PLAN-092](01.code_spec/plans/PLAN-092-kb-exemplar-pivot.md)(2026-09-05 · 논지 피벗)이며,
요건 다섯을 어떻게 채우는지의 본보기로 쓴다 — 특히 **요건 1 판정 대장**과 **요건 5 역방향 점검**.

**이 조항이 보장하는 것은 하나다 — "판정은 지키면서 서사는 자유롭게".** 위반은 *판정을 바꾸는
것*이고, 자리를 옮기는 것은 위반이 아니다.

---

## 2.3 투고 파생본 절차 — 정지 게이트 1개, 검증은 기계가

**게이트(1개):** §2.2 요건 다섯을 포함한 마스터 계획을 커밋·승인받은 뒤 파생을 시작한다.
**이후 단계별 재승인은 요구하지 않는다** — 대신 §0.10의 검사가 커밋마다 돈다.

- **파생본을 직접 편집하지 않는다.** 산문 소스(`paper/manuscript/stage3_source.md`)를 고친 뒤
  `make submission-stage3`로 다시 조립한다.
- **표의 수치를 손으로 옮겨 적지 않는다**(§1-1). 표는 동결본에서 **문자 단위로 복사**하며, 앵커가
  없거나 둘 이상 매치되면 스크립트가 실패한다.
- **표 안의 문구는 `CELL_FIXES`로 고친다.** 앵커가 정확히 한 번 매치되는 목록으로만 허용하고,
  **수치가 하나라도 달라지면 실패**시킨다. §1-1을 사람의 주의력이 아니라 기계가 지킨다.
- **이관은 삭제가 아니다** — supplementary로 나간 전문은 파생본에서 **링크로 도달 가능해야** 하며,
  링크 단절은 `submission-check`가 실패시킨다.
- **분량·표·그림·초록 길이·키워드 수의 상한(D5–D9)은 전부 우리가 정한 편집 목표다.**
  **투고처는 미정이며, 투고처를 근거로 하는 문장을 원고·보충자료·검사기·커버레터에 쓰지 않는다.**
  투고처가 정해지면 그 규정으로 대체하고 출처를 `scripts/submission_check.py` 주석에 남긴다.
- **경고 구간(D6 권고 초과 · D13)은 편집 중에만 쓰는 작업 여유다.** 경고를 상시 켜 둔 채 투고하지
  않으며, **경고 모드의 규칙은 통과를 근거로 인용하지 않는다.**

**품질 기준 셋은 투고처가 정해지지 않았다고 느슨해지지 않는다** — (1) 명시적 지식 표현
(온톨로지·OWL/SHACL/SPARQL), (2) 지식집약 공학 태스크 적용, (3) 정성·정량 강한 평가.
"돌아간다"는 시연은 증거가 아니다 — 검정·효과크기·강건성·누출통제가 증거다.

---

## 3. 모듈 배치표 — 새 코드가 갈 곳

| 모듈 | 책임 | 넣지 말 것 |
|---|---|---|
| `collect/` | 외부 원천 수집 (KIPRIS·BigQuery·EPO/PatentsView·DART). raw 저장 | 정제·해석 |
| `corpus/` | sidecar 청구항 조인 · 코퍼스 조립 · 질의 번역 · 임베딩 · 시점/패밀리 분할 · hard negative | 검색 로직 |
| `ontology/` | vendor(스냅샷)·baseline(G0)·mapping·merge(게이트 통과분) — **검색 개념·경로 피처의 원천** | 통계·검색 |
| `retrieval/` | bm25(Pyserini nori)·dense(FAISS flat)·hybrid(RRF)·ontology_rerank — 순위 산출만 | 평가·게이트 |
| `validate/` | L0–L3 게이트 + **T-gate**(t1_noninferiority·t2_subgroup·t3_cross_task_cq) · leakage_check · fault_inject · vocab_coverage | 데이터 수정 |
| `analysis/` | metrics(Recall/nDCG/MRR/bpref)·**effort@recall·candidate reduction**·bootstrap·ablation·subgroup — 검정과 효과크기 | 그림 |
| `rag/` | 컨텍스트 조립(동결 run 상위 K) · 생성 호출(모델·프롬프트 동결) · **결정적 채점** | 검색 로직 · 새 질의셋 · RAG 방법 제안 |
| `viz/` | `figures.py`만. 논문 그림 전량 | 계산 |
| `queries/` | `cq/*.rq`(스위트 pa·em·tf·core) · `shapes/graph`·`shapes/delta` | — |

노트북은 초안, `src/`가 정본. 확정 로직은 반드시 `src/`로 승격.

---

## 4. 데이터 프로파일 의무 (수집·변형 산출물의 필수 동반물)

데이터를 수집·변형한 뒤 예외 없이 산출·보고: **(1) 구조**(컬럼·dtype·단위·키·원천) **(2) 형태**
(행×열·중복·결측률·기간·**언어분포**·dedup 전후) **(3) 기술통계**(수치/범주/연도별/IPC·CPC 분포)
**(4) 사용 목적**(어느 표·그림·검정에, 안 쓰는 컬럼은 왜 남기나). 코드가 생성(`data/profiles/*.md`),
같은 커밋에 `data/MANIFEST.md`(일시·검색식·건수·파일·반영 버전) 기록. **코퍼스 서명이 예고 없이
바뀌면 통합 테스트가 실패시킨다.** 놀라운 점(이상치·결측편중·기간공백)은 다음 단계 전에 알린다.

---

## 5. 검증 게이트 — L0–L3 + T-gate (병합 전 사전승인)

게이트를 통과 못한 델타는 병합되지 않는다. 우회 경로를 만들지 않는다. CI가 매 push 같은 것을 돌린다.

| 층 | 검증 | 실패 예 |
|---|---|---|
| L0 | 신선도·무결성 | 낡은 산출물, sha256 불일치 |
| L1 | SHACL 구조(graph+delta) | 필수속성 누락·잘못된 카디널리티 |
| L2 | 논리 일관성(HermiT) | 모순 타입·순환 계층 |
| L3 | CQ 기능 (스위트 pa·em·tf·core) | 필수 경로 단절·무응답 |
| **T1** | 검색 비열등성 | Recall@100 하락(LB₉₅ ≤ −ε) — 누출 감사 통과 전제 |
| **T2** | 하위집단 안전성 | 거절근거·공정군·**언어(KR/외국)**별 국소 회귀(max drop ≥ δ) |
| **T3** | 교차 태스크 CQ 비회귀 | 타 태스크(em·tf·core) CQ 통과율 하락 |
| **T4** *(승인식 미편입 · 판정 1회)* | **하류 생성 층 비회귀** | 인용 정확도 하락 · 환각률 상승 |

- **승인 규칙**: `Accept = (L0=L1=L2=L3=pass) ∧ (LB₉₅(ΔR₁₀₀)>−ε) ∧ (max_s drop_s<δ) ∧ (∀f∈{em,tf,core}: pass_f(new)≥pass_f(old))`. ε=0.02·δ=0.05, 테스트 개봉 전 동결.
- **T4는 승인식에 들어가지 않는다.** 마진은 결과 보기 전에 동결됐고 판정도 1회 나왔으나, **편입은
  이 표의 개정이므로 별도 승인 사항**이며 요청된 바 없다. 그때까지 T4는 **설계 + 판정 1회의
  기록**으로 싣는다. T4가 필요한 이유는 이미 관측됐다 — 자원 층 지표가 검색 층 성능을 대표하지
  않았듯, 검색 층 지표가 생성 층 가치를 대표하지 않을 수 있다.
- **L2 는 현 자원에서 공허하게 통과하며, 그 사실이 게이트 결과를 읽는 조건이다**(§0.0.1). 검증
  대상은 T-Box·A-Box 이고 **R-Box(추론 공리)는 검증 대상이 아니라 부재한다** — D-03·CR-005.
  그러므로 **"논리적으로 검증된 온톨로지"라고 쓰지 않으며**, T3 단독 검출 수를 인용할 때는
  **공리 구성을 함께 적는다.** 공리가 채워지면 T3 의 분담은 줄어드는 것이 정상이고, 그것은
  게이트의 실패가 아니라 **층 사이의 귀속 이동**이다.
- **추론 회귀 층(가칭 L2′ · 변경 전후 함의 집합의 회귀 검사)은 설계로만 싣고 이 표에 넣지 않는다.**
  근거는 T4 와 같다 — **돌지 않는 게이트를 가졌다고 주장하지 않는다.** 현 스냅샷에서는 물질화될
  함의가 거의 없어 이 층은 아무것도 관측하지 못한다. 편입 조건은 둘이다: ① 함의가 실제로 움직이는
  자원에서 1회 완주 ② 임계를 결과 보기 전에 동결. **정의할 때는 추론기 중립으로**(물질화된 트리플
  집합 비교) 한다 — OWL 문법에 묶으면 표현 관습이 다른 자원에서 델타 규칙과 같은 방식으로
  "미충족"이 반복된다(EP5 가 실측한 그 실패다).
- T3는 통계검정이 아니라 **결정론적 통과율 비교**(CQ는 명세). 하락 시 즉시 실패, 예외는 waiver 토큰만(횟수 보고).
- 게이트 대상은 **G0(+교정 스냅샷) · mini_graph(엄격 delta shape) · IR 코퍼스**(누출 0·분할 결정성).
  **G1/G2는 게이트 대상이 아니라 후보 모집단(방해문서 풀)이다.** 정답 2,211건은 전부 G0 안에 있고
  G1/G2에만 있는 정답은 0건 — 그래서 빼면 후보 코퍼스가 40,552 → 4,034로 줄어 건초더미가 사라진다.
- **어휘 검증 커버리지·CQ 도출은 측정으로 운용**(게이트 아님) — 채우려 CQ를 지어내지 않는다.

---

## 6. 명령 레퍼런스

```bash
make setup / lint / test
make vendor / snapshot / baseline / merge          # 그래프 조립 (온톨로지 자산)
make corpus        # sidecar 조인·번역·임베딩·분할·hard neg → IR 코퍼스
make index         # Pyserini 색인 (BM25 nori + FAISS flat Dense)
make leakage       # 누출 검사 (금지 간선 0)
make retrieve      # B0–B5·P0–P2 순위 산출 (3모드: oracle-free/citation-assisted/gt-assisted)
make eval          # Recall/nDCG/MRR/bpref·부트스트랩·하위집단 (bpref = 현행 분석에서 제외 · §0.8)
make effort        # Effort@Recall·Candidate Reduction·깊이별 회수 곡선
make rag / rageval # 생성 층 — 컨텍스트 조립·생성 / 인용 정확도·환각률 (결정적 채점)
make gate          # L0–L3 + T1·T2·T3
make concept-status # 개념·공리 계기판 (선언·실체화·작동 3층 · 읽기 전용)
make faults        # 결함주입(교차태스크 포함) × 게이트 검출 매트릭스
make ablation      # A1–A8 (A8 음성대조군)
make figures / figure-data   # 논문 그림 전량 / 개념 그림 수치 추출
make score-spec    # 점수 함수 확정표 — 항 정의·최종 가중치·코드 위치를 코드에서 추출
```

**문서 검사군은 §0.10의 표에 있다.** 커밋은 사용자 요청 시만. 메시지는 **어느 기여·어느
에피소드를 진전시켰는지** 밝힌다. `data/` raw 혼입 확인.

> **논문에 자리 없는 산출은 돌리지 않는다.** `make retrieve`의 3모드 중 **주분석은 oracle-free
> 하나뿐**이다. 다른 모드를 돌릴 때는 **어느 표·그림·검정에 들어가는지**를 먼저 말하고, 답할 수
> 없으면 돌리지 않는다. **결과 파일이 늘어나는 것은 진전이 아니다.**

---

## 7. 논문 산출물 대응 — 표·그림은 코드가 만든다

산출물 / 검색성능표·ablation·하위집단·**Effort@Recall 곡선** / 전달표(인용 정확도·환각률) /
결함주입 검출 매트릭스·**타 태스크 CQ 비회귀 기록(waiver 로그 포함)**·음성대조군.
**수기 기입 없음.** 대응이 깨지면 결함.

- **EP ↔ 절의 단일 원천은 [paper/episodes.yaml](paper/episodes.yaml)이다.** 절을 옮기면 산문·표·
  캡션·보충자료 네 자리가 따라 움직여야 하며 `make crossref-check`가 대조한다. **여기서 EP를
  지우거나 다시 매기지 않는다.**
- **점수 함수 확정표는 코드가 만든다** — `make score-spec`. 항의 정의와 최종 가중치를 산문에만
  두면 어긋난다(실제로 코드는 **비가중** Jaccard인데 보충자료가 *"가중 Jaccard"*라 적은 채 여러 판을
  살아남았다). **손으로 옮겨 적지 않는다.**
- **실패 유형 분류를 실을 때는 κ와 합의율을 함께 싣는다**(§1-12). 시스템명·순위를 가린 2인 독립 코딩.
- **경쟁 설명은 "가르지 못했다"를 한계로 적는 대신 무엇으로 가를 수 있는지의 명세로 싣는다.**
- **표를 채우려고 세대를 만들지 않는다** — 그것은 증거가 아니라 분량이다.

### 7.1 그림 규격

**정본 규격은 [paper/FIGURE-SPEC.md](paper/FIGURE-SPEC.md)이다.** 문체 규격이 산문에 하는 일을
그것이 그림에 한다. 여기에 규칙을 옮겨 적지 않는다.

- **개념 그림의 수치는 `paper/figures/data/concept_values.json`에서만 읽는다.** 그 파일은
  `make figure-data`가 산출물에서 기계로 추출하며 **손으로 고치지 않는다.** 어긋나면 그리지 않고
  멈춘다. 통제된 자원 교체(EP3)의 값은 디스크에 한쪽 팔만 남아 있어 **사전등록 문서가 원천**이며,
  같은 숫자 문자열이 다른 뜻으로 다른 표에 있으므로 추출 패턴은 **행 전체를 앵커로** 잡는다.
- **그림 안의 글자도 §0.8 판정 문구와 §8.1 문체 규격의 대상이다.**
- **도구는 matplotlib 하나다.**

---

## 8. 보고 방식

- **결론 먼저.** 무엇이 나왔는가를 첫 문장에.
- 수치엔 **출처 명령/파일**을 함께(예: `make eval` 출력, `data/profiles/ir_corpus_v09.md`).
- 불확실은 불확실로, 추정은 "추정"으로 라벨.
- **가설과 반대되는 결과·실패한 검정·건너뛴 단계는 반드시 먼저 알린다.**
- **기각·무효과·구조적 0을 보고할 때는 "이 결과가 상류에 요구하는 수정"을 함께 말한다.**
  원인이 방법인지 자원인지 구분되지 않으면 그렇게 말한다 — 자원 결함을 방법의 한계로 적어 넘기는
  것이 지금까지의 가장 큰 손실이었다.
- 사용자는 연구자다. 코드보다 **그 코드가 만든 증거**를 보여준다.

### 8.0 유보는 관측당 한 번

**무엇을 주장하면 안 되는가**는 §0.3·§0.8이 정한다. 이 조항이 정하는 것은 **같은 유보를 몇 번
적는가**이다. 그 규칙이 없어서 하나의 경계가 서론·결과·논의·결론에 되풀이됐고, 헤드라인 수치가
매번 즉시 상쇄되어 **읽는 사람이 결과를 붙잡지 못하게** 되었다.

- **하나의 유보는 그 관측이 처음 제시되는 자리에 한 번 적고, 이후에는 그 자리를 참조한다.**
- **경계는 결과 문장에 붙이지 않고 한계 절에 모은다.** 결과 문장 옆에 남길 유보는 **그것이 없으면
  그 결과가 오독되는 것**에 한한다.
- **독자 해석 통제형 메타 문장은 절 구성이 대신한다.** *"섞어 읽지 않는다"*·*"같은 무게로 읽지
  않는다"* 류를 결과 앞에 쌓지 않는다 — 그 일을 하라고 절과 표가 있다.
- **기여 진술 안에 그 기여를 축소하는 문장을 넣지 않는다.** 범위 한정은 기여 문단이 아니라 해당
  결과 절과 한계 절에서 한다.
- **금지 문구 사전은 강도를 낮추라는 뜻이지 문장을 늘리라는 뜻이 아니다.** 허용 문구로 한 번 쓰고
  멈추는 것이 규약을 지키는 방식이다.

### 8.1 원고 문체

**정본 규격은 [paper/STYLE-KO-ACADEMIC.md](paper/STYLE-KO-ACADEMIC.md)(한국어) ·
[paper/STYLE-EN-ACADEMIC.md](paper/STYLE-EN-ACADEMIC.md)(영문)이고, 기계 검사는 §0.10의 표에
있다.** 규칙을 여기에 옮겨 적지 않는다 — 규격 두 파일이 검사기가 보는 것과 보지 못하는 것을
스스로 밝히고 있다.

**저장소 고유의 예외 넷만 여기 남긴다.**

- **바꾸지 않는 것**: 수치·통계량·지표 이름(Recall@100·nDCG@20·McNemar·95% CI)·게이트 이름
  (L0–L3·T1–T4)·에피소드 라벨(EP1–EP5). 쉬운 말로 바꾸면 **다른 것을 가리키게 된다.**
- **판정 서술은 §0.8과 `verdicts.yaml`이 문체 규격보다 우선한다.** 충돌하면 판정 문구를 남기고
  주변 문장을 고친다. 문체 개정으로 판정이 흔들리면 그것은 개선이 아니라 §1-2 위반이다.
- **적용 범위는 "내용 보존 + 어체 변환"이다.** 기조를 바꾸려면 그것은 문체 작업이 아니라 §2.2다.
- **강제 대상은 투고 파생본 계열**(산문 소스 + `paper/submission/`)이다. 작업 정본과 supplementary는
  **감사 기록**이므로 소급 적용하지 않는다 — 다만 **정본에 새로 쓰는 문단은 이 규격을 따른다.**
  파생본에서는 사전등록·봉인·개봉·정정의 절차 서술을 Methods **1문단으로 한 번만** 쓰고 전문은
  supplementary 참조로 대체한다 — **정직성은 반복이 아니라 도달 가능성으로 보인다.**

---

## 부록 — 옮긴 것과 간 곳 (2026-09-05 재구조화)

**규약을 1,209행에서 이 분량으로 줄였다. 판정·수치·동결 사실은 하나도 바뀌지 않았고, 절 번호는
전부 보존했다** — 아카이브된 PLAN 227개가 `CLAUDE.md §0.8`·`§1-2` 형태로 이 번호를 참조한다.

| 옮긴 것 | 간 곳 | 왜 |
|---|---|---|
| 개정 이력 전량 (날짜별 신설·개정·갱신 서술) | [archive/CLAUDE-v2-2026-09-05.md](archive/CLAUDE-v2-2026-09-05.md) | 규칙을 읽을 때 이력을 함께 읽지 않아도 되게 |
| 구 주장 체계 `C0`–`C4` · `DRQ1`–`DRQ3′` · `DP1`–`DP7` 전문 | 〃 (대응은 [S6](paper/supplementary/S6-preregistration-crosswalk.md) §4) | 원고·정본에 **0회** 등장하는 어휘였다 |
| §0.8 판정 문구 표 | [paper/verdicts.yaml](paper/verdicts.yaml) | 사본이 원본과 어긋나고 있었다 |
| §2.3 검사기 일곱의 개별 서술 | Makefile · 스크립트 docstring · §0.10 포인터 표 | 3중 유지가 실제로 표류를 만들었다 |
| §7.1 그림 규격 세부 | [paper/FIGURE-SPEC.md](paper/FIGURE-SPEC.md) | 규약이 `F-v1`에 멈춰 있었다 |
| §8.1 문체 규칙 열여섯 | STYLE-KO / STYLE-EN | 열둘이 이미 기계 검사였다 |

**신설은 셋이다** — §0.0 본문 라벨 표(규약과 논문이 같은 이름을 쓴다) · §0.10 정본 포인터 표 ·
**§8.0 유보는 관측당 한 번**.
